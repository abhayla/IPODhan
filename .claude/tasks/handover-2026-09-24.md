# Handover 2026-09-24: finish the pending pull-model build items

You are starting a fresh IPODhan session. The previous session ran long and was compacted several times.

**What to do:**
1. Read the contract `docs/contracts/2026-09-24-finish-pending-build-items.md` in full. Also read the
   memory index entries it names under "Context to read first".
2. Tell the owner, in one line, to type the `/goal` line from the contract's header into this window.
   It must be the full line, never the bare path: a bare-path goal gets ruled "met" within hours.
3. Start with Stage 0:
   - Merge PR #952 after re-running its gates.
   - Ask the owner the one #949 question (Spec basis: OD-87, §2.1; recommend option (a)).
4. Then work the stages in order. The spec's dependency chain is 9←6, 10←6,9, 17←6,7,10 and 11←10.
   Everything in Stage 1 is unblocked now.

**Owner direction (2026-09-24):** finish every pending item first. The IPO data fine-tuning issues
are deferred, and the contract lists them out of scope: #928, #932, #933, #936, #938, #947 and #951.
Never deploy to production.

**State at handover (verify each before relying on it):**
- **PR #952:** open (findings F-151 and F-152). Its worktree is `D:/Abhay/Ventures/IPODhan-IPODhan-staging-check-935`.
- **PR #949:** a draft (item 7 S4 opening-day check, parked after 4 Tier A rounds; read its last comment).
- **Merged today:**
  - #945 (source record keys; the staging proof is still owed and needs the tunnel)
  - #948 (F-150)
  - #950 (OD-87)
- **Board:** https://claude.ai/artifact/NohBg52m7AUjS8kTDMxxKM (version 80).
- **Items:** 13 are PARTIAL: 3, 6, 7, 9, 10, 11, 12, 14, 17, 19, 21, 22 and 32. The rest are BUILT.
- **Staging** serves 084928e0. Its 2026-09-24 check failed: the data slot never closes because of the
  document retry timer (F-151, #943). Fixing that is Stage 1, item 7 (1).
- **Mechanism task T-568** is claimed, not built: builder briefs must count calls and assert columns.
  Build it when convenient; it's a fleet mechanism, not an app item.

**Stop rule:** if the owner says stop or "no new work", finish only what is in flight, list what is
open, and wait. Don't read it as a question.
