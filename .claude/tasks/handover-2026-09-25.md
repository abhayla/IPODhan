# Handover 2026-09-25: close the remaining pull-model items (new goal session)

You are a fresh IPODhan session. The previous goal session (ipodhan-e6) ran for about 16 hours and was
compacted many times. The owner asked for a new window with a new contract.

**Do this:**
1. Read `docs/contracts/2026-09-25-finish-remaining-items.md` in full. For any mechanic it does not
   restate, read `docs/contracts/2026-09-24-finish-pending-build-items.md`. Also read the MEMORY.md
   index entries it names.
2. Check the old session stopped: the last entry of
   `docs/contracts/.run/finish-pending-items-PROGRESS.md` must be its run-end SUMMARY. If it isn't,
   ask the owner to close that window before you build anything.
3. Ask the owner, in one line, to type the `/goal` line from the contract header in this window. It
   must be the full line, never the bare path.
4. Work the order:
   - 3, 7, 9, 11, 19, 21, 22;
   - then 6 and 10;
   - then 17 and 32.
   PARK anything blocked or not progressing (the contract's PARK rule), track it, and move on.

**State at handover (updated 2026-09-25 ~01:45 IST, after the old session's wind-down; verify before relying on it):**
- **The old session has stopped.** Its run-end SUMMARY is the last entry of
  `docs/contracts/.run/finish-pending-items-PROGRESS.md`. No worktrees are open. The DB tunnel is
  closed: a leftover ssh forward was found still running and stopped at ~01:45.
- **Items:** 19 BUILT, 10 PARTIAL (3, 6, 7, 9, 10, 17, 19, 21, 22, 32). Items 11 and 12 are now
  BUILT (board v86). Item 11 in the contract's list is therefore a verify-and-skip.
- **Two DRAFT PRs, each with a "what is left" comment. They CLASH over OD-96: merge one, then rebase
  and renumber the other.**
  - **#1010** (#993: `ipos` rows name their document, item 6). CI is red: the plan-board parser fails
    after the OD-96 row. Needs a rebase. After merge, a 156-row staging repair is owed.
  - **#1012** (item 22: per-value OCR mark and the OCR-loses rule, OD-96 / F-167, migration 0062).
    Its Tier A review has NOT run.
- **Staging:** serves a2833963. The admin panel is enabled on staging.
- **#943 / #957 (the data slot never closes): PARKED by the owner.** Tracker line landed via #1013.
  Label it `parked`; don't work it.
- **Production:** never. Six admin routes are unauthenticated on production until the next release;
  it's fixed on main (#990). This is known and accepted by the owner.
- **The GitHub label `parked` exists** (created 2026-09-25).
- **Staging merge backups** from the old run are archived at
  `D:/Abhay/Ventures/IPODhan-state-archive/2026-09-24-ops-0924/`.

**Stop rule:** if the owner says stop or "no new work", finish what is in flight, list what is open,
and wait.
