# PROGRESS — BSE IPO enrichment (`/goal` run)

**Branch:** `feat/bse-ipo-enrichment` (off the PR #23 `feat/ipo-field-enrichment` tip — PR #23 is OPEN, not merged; enrichment stacks on it). **Owner:** this session (IPODhan141), taken over 2026-06-15 from a hung IPODhan126.

## §0.2 Preflight (done)
- **PR #23 (corporate-action classification) = OPEN**, 2 commits on `feat/ipo-field-enrichment` (`236d870f`, `73c32b81`). Do NOT redo.
- **BSE list JSON API VALIDATED** ✅ — `GET api.bseindia.com/BseIndiaAPI/api/IPO_HomePageDetail/w` (UA + Origin/Referer headers) → HTTP 200, 14 live rows. Keys: `Scrip_name, Start_Dt, End_Dt, Status, IR_flag, IR_FLAG_FULL, IPO_NO, Scrip_cd`. Susan Electricals = `IPO_NO:7770, Scrip_cd:4627, IR_flag:IPO`.
- **Baseline coverage (tunnel):** MAINBOARD total=178, issue_size>0=**60**, lot_size>0=**68** (~118 skeletons). Matches contract.
- **Dup problem (#16) confirmed:** Susan Electricals = **3 rows** (`Ltd. CT`, `Ltd. O`, all-caps `LIMITED`), 2 with issue_size 703.8M, all lot_size null.

## ✅ DETAIL ENDPOINT CRACKED (2026-06-15)
Found in the SPA bundle: for `type=='IPO'` the app calls
**`GET api.bseindia.com/BseIndiaAPI/api/GetMkt_ISSUE_BBS_IPO/w?IPO_NO=<IPO_NO>`** (headers: UA + Origin/Referer `https://www.bseindia.com`). Returns the full core detail. Susan (IPO_NO 7770) fields → DB mapping:
- `Issue_Size_No_of_shares` (4019000) × top-of-band price → **issue_size** (use `calculateIssueSize`, guard >0)
- `Price_Band` "120.00-127.00" → **priceRangeMin/Max**
- `Market_Lot` (1000) → **lot_size** · `Minimum_Bid_Quantity` → min bid · `Face_Value` (10) → **face_value**
- `Registrar` → **registrar** · `Book_Running_Lead_Manager`(+Co/Syndicate) → **lead_managers**
- `Issue_Period` "11 Jun 2026 to 15 Jun 2026" → **open/close dates** · `Symbol` (SUSAN) → symbol
- `Prospectus_GID`/`Anchor_Details`/`Price_Band_Advertisement` → documents (Stage C/DRHP)
- SPA detail page route (for G-UI ref): `/markets/publicIssues/DisplayIPO?id=<Scrip_cd>&type=<IR_flag>&idtype=1&status=<Status>&IPONo=<IPO_NO>`
- Bid/demand (subscription, Stage C): `GetBidDetBookBuilding/w`, `Pubissues_GetBkbldgCatdem_ng/w`.
- Playwright is BOT-BLOCKED on bseindia.com (Access Denied) → use `node`+`fetch` with headers (works).

## ~~OPEN BLOCKER — Stage A detail endpoint not yet cracked~~ (RESOLVED above)
- The list API gives only schedule + IR_flag, NOT issue_size/lot/registrar/price/lead-managers. Those need the **detail endpoint**, which must be discovered.
- Guessed endpoints (`Mkt_Pub_DisplayIPO__beta/w`, `ipoHomePageDetail/w`, `IPODetail/w` with `IPONo`/`scripcode`) all return a generic 1814-byte XHTML page → wrong path/param.
- The SPA main bundle (`/assets/includenew/js/main-CPK3PQBE.js`, 15MB) has only 3 `/api/X/w` endpoints, none IPO-related → the IPO-detail call is in a **lazy-loaded chunk**.
- **NEXT:** capture BSE's SPA XHR via Playwright (navigate an IPO detail page, read network requests) to get the real detail endpoint + exact params. Then TDD-rebuild `bse-scraper.ts`→`bse-api-scraper.ts` + `bse-detail-scraper.ts` on the API (flag `ENABLE_BSE_API`, default OFF), register fields in the priority matrix, backfill via tunnel.

## Stage A — IN PROGRESS
- ✅ **Core BSE-API scraper module BUILT + tested + live-verified + committed.** `scraper/src/scrapers/bse-api-scraper.ts`: `scrapeBSEViaAPI()` (list IR_flag=IPO → per-IPO detail), pure mappers (`parsePriceBand`, `parseBSEDate`, `parseLeadManagers`, `computeBSEIssueSize`, `mapBSEToScrapedIPO`). Flag `ENABLE_BSE_API` (default OFF). Pino-only.
- ✅ **A.1 Orchestrator wiring DONE.** `bse-scraper-orchestrator-v2.ts scrapeData()`: when `ENABLE_BSE_API`, sources from `scrapeBSEViaAPI()` via `summarizeBSEApiResult()` (new pure helper → ScrapedData shape + segment counts) instead of the dead `scrapeBSEIPOs()`. Routes through the existing `validateIPO` (pipeline + Zod) → `upsertIPO`. TDD.
- ✅ **A.1b Required-field fix.** `mapBSEToScrapedIPO` was missing `listingExchange`+`status` (TS2739 — only surfaced on `tsc`, not at runtime since scraper isn't strict-gated). Added `listingExchange:'BSE'` + new pure `deriveBSEStatus(open,close,today)` (window-based, since BSE JSON `Status` is opaque 'L'; status is `timeBased` in the matrix so it self-corrects). New test asserts the mapped row passes the real `validateIPOData` Zod gate. **16 unit tests green; my 2 touched files `tsc`-clean** (remaining repo tsc errors pre-exist in untouched files).
- ✅ **A.2 Field-priority matrix — VERIFIED already-registered, no edit (deliberate).** BSE is already a source in every field the mapper writes: `issue_size`, `price_band_min/max`, `lot_size`+`lotSize`, `open_date`, `close_date`, `status`, `company_name`, `registrar`, `lead_managers` — and the `getFieldRules` DEFAULT also lists BSE. **Key finding:** consolidation iterates **camelCase** keys (`ipoData`), so snake_case entries (`issue_size`, `open_date`…) are NOT matched at runtime — those fields fall to the DEFAULT, which still ranks NSE>BSE correctly. `finalValue` is always the ORIGINAL value (normalization is comparison-only); `validateValue` failure REJECTS the source. Therefore adding camelCase mirrors with `issue_size`'s `min:1e6` validation would **reject legitimate sub-₹1Cr SME issues from NSE/MC** — an unflagged cross-source consolidation change the guardrails forbid ("keep non-BSE scrapers untouched"). **DEFERRED:** broad camelCase-mirror matrix hygiene is a separate cross-source PR, not BSE-enrichment scope. BSE's >0 guards live upstream (`computeBSEIssueSize` >0, Zod `nonnegative`, `validateLotSize`/data-validation-pipeline lot<10).
- ✅ **A.3 Backfill FEASIBILITY RESOLVED (read-only probe, 2026-06-15).**
  - BSE **list** endpoint `IPO_HomePageDetail/w` = current board ONLY (14 rows; `?Flag=Closed` ignored → same 14). Of those only **2 are `IR_flag=IPO`** (Susan 7770, Horizon 7771); the other 12 are corp actions (OTB/DPI/RI/CMN) — incl. SARDA PROTEINS = OTB takeover (contract's "NOT sarda"). Other guessed historical list endpoints → XHTML (dead).
  - **KEY: historical detail IS reachable by enumerating `IPO_NO` backward.** `GetMkt_ISSUE_BBS_IPO/w?IPO_NO=<n>` returns archive detail for past issues (verified 7740=Jun'26, 7700, 7600, 7500=Dec'25, 7300, 7000=Mar'25, 6000=Jan'23, 1000=Mar'15). IPO_NO is ~sequential with gaps. The archive MIXES IPOs + NCDs + rights + OFS (NCDs/rights show empty `Price_Band` + `Market_Lot`=1).
  - **Backfill design (enumerate-and-match, dry-run default):** enumerate IPO_NO from current max (≈7772) downward → fetch detail → normalize `ScripName` → look up an EXISTING `MAINBOARD` skeleton by normalized name (`normalizeCompanyNameForMatching`) → if matched AND skeleton missing fields → enrich via `upsertIPO`. **Never create rows** (skips NCDs/rights/OFS not already in our IPO table → no fake data). Detail-only rows have no list `Start/End_Dt` → parse `Issue_Period` ("01 Jun 2026 to 03 Jun 2026") for dates (new `parseIssuePeriod` + detail-only mapper needed). G-PERSIST read-back + before/after coverage.
  - ✅ Built `parseIssuePeriod` + `mapBSEDetailToScrapedIPO` (TDD, 22 tests) + `scripts/backfill-bse-historical.ts` (dry-run default, `--apply` gated, routes through `upsertIPO`).
  - 🛑 **DRY-RUN RUN (7772→7700, read-only via tunnel) — BLOCKED on data integrity. Do NOT `--apply`.**
    - Match mechanics WORK: 73 IPO_NO scanned → 44 book-built rows → **30 matched MAINBOARD skeletons**. Approach proven.
    - **BLOCKER 1 — corporate-action re-pollution.** Matches include `WIPRO ₹15000cr`, `ZYDUS ₹1100cr`, `AUROBINDO ₹800cr`, `RESTAURANT BRANDS ₹1464cr`, `GARWARE`, `CMS INFO`, `ONWARD`, `SARDA PROTEINS` (the contract's known **OTB takeover**) — already-listed cos doing OFS/buyback/rights, NOT IPOs. The **detail** endpoint carries NO `IR_flag` (only the **list** does), so `mapBSEDetailToScrapedIPO` can't filter them → it would enrich the exact corporate-action rows **PR #23/#25 own**. Violates the "don't re-classify corp actions" fence. (Side-finding: residual corp-action MAINBOARD skeletons with null issue_size still exist in prod — not mine to fix; PR #23 fence.)
    - **BLOCKER 2 — invalid `lot→1`.** BSE archive `Market_Lot`="1" for these rows; mainboard lots are never 1 (pipeline rejects <10). Naive fill proposes lot=1.
    - **Why the current-board path is safe (and the historical path isn't):** the live orchestrator path (`ENABLE_BSE_API`) uses the **list** endpoint → has `IR_flag=IPO` → only genuine IPOs (Susan, Horizon). The historical enumerate-by-IPO_NO path loses that signal.
  - ⏳ **DECISION NEEDED (fork) before any historical write:** either (a) find a reliable historical IPO-vs-corp-action signal BSE exposes per-IPO_NO, or (b) scope historical enrichment to skeletons independently confirmed as genuine IPOs (post-#23), or (c) drop historical backfill and enrich only the current board via the orchestrator. lot=1 must also be floored out regardless.
- ✅ **Stage B — DIAGNOSED (read-only, tunnel). Normalizer already sufficient; Susan already clean.**
  - `scripts/dedup-bse-mainboard.ts` (dry-run diagnostic, JS normalizer) over 178 MAINBOARD rows → 175 normalized clusters → **only 2 DUP clusters**: `hexagon nutrition` (3 rows: "Ltd.", "Ltd. LT", "IPO") + `cmr green technologies` (2 rows: "Ltd.", "IPO"). **Susan NOT among them** — the GMP-revival B4 dedup already merged it; contract's "Susan 3 dups" is STALE.
  - **B1 (extend normalizer) NOT NEEDED:** the existing `company-name-normalizer.ts` already collapses every real pattern (trailing 1-2 letter code after Ltd/Limited line 31; ` IPO` line 32). The 2 residual clusters are pre-#16 rows the normalizer now PREVENTS going forward. No JS/SQL change → no agreement-test churn.
  - **B2 (merge the 2 clusters):** destructive (DELETE rows + repoint FKs: gmp_records/subscriptions/anchor). Both clusters are near-identical LISTED historical IPOs (same size/band) — low value, real risk. GATED — not run; offered as optional follow-up.
- ✅ **Stage C subscription — DONE.** Cracked `Pubissues_GetBkbldgCatdem_ng/w?IPO_NO=<n>` (returns `table2`: per-category bid demand; SRNo 1/2/3 = QIB/NII/Retail, `Total` row, `col5` = subscription multiple, `Maxdt` = ts). New `parseSubTimes` + `mapBSESubscription`; `scrapeBSEViaAPI` fetches it per IPO (best-effort — failure never drops the IPO), carried through `summarizeBSEApiResult` → orchestrator's existing `validateSubscription` + persistence. 27 unit tests green (incl. real `validateSubscriptionData` gate). **Live: Susan 192.06× (QIB 142.65/NII 210.55/Retail 207.56), Horizon 69.98×, 0 errors.** (Other working bid endpoints if needed: only `Pubissues_GetBkbldgCatdem_ng/w?IPO_NO=` returns JSON; `IPONO=`/`scripcode=` variants → XHTML.)
- ⏳ **Stage C DRHP — DEFERRED (best-effort, scoped follow-up).** CORRECTION: `Prospectus_GID` IS a direct URL (`.../downloads/ipo/RHP&GID_<id>.zip`) — RHP discoverable. BUT (1) it's a **`.zip`**, not a direct PDF; (2) DRHP/RHP URLs persist in the separate **`documents`** table (type enum DRHP/RHP/PROSPECTUS + its own `extraction_status` pipeline behind `ENABLE_DRHP_EXTRACTION`), NOT on the `ipos` row / `ScrapedIPO` / `upsertIPO` path. So this is a separate multi-component integration (documents-table writes + unzip→PDF + pipeline wiring), beyond best-effort and lower-yield than subscription. **Follow-up:** capture `Prospectus_GID` → a `documents` RHP row (status PENDING) + teach the extractor to unzip. Not built here.

## FINAL STATE (2026-06-15)
**Core mission ACCOMPLISHED:** BSE scraper rebuilt on the JSON API (Stage A) + live subscription capture (Stage C) — draft **PR #26** (base `main`, flag `ENABLE_BSE_API` OFF). 27 unit tests green; live-verified (Susan/Horizon enrichment + subscription, 0 errors).
**Safely NOT done (each blocked/gated/deferred with reason):** historical mass-backfill (detail lacks IR_flag → would re-pollute #23/#25 corp actions; dry-run tool only), dedup-merge of 2 residual clusters (destructive, gated), DRHP (separate documents-pipeline integration, deferred), activation (enable flag + deploy = Abhay's §GATE call).
**Side-finding for Abhay (PR #23 owns):** residual corp-action MAINBOARD skeletons (WIPRO/ZYDUS/AUROBINDO, null issue_size) still in prod.
- ✅ **Ship — DRAFT PR #26 opened** (`feat/bse-ipo-enrichment` → **base `main`**; no deploy/merge — gated). §GATE = enable `ENABLE_BSE_API` + deploy (Abhay's call). **CORRECTION:** PR #23 AND #25 are now **MERGED** to main (not OPEN as §0.2 assumed); #25 already swept the Stage A core (`bse-api-scraper.ts`, flag-gated dormant) onto main — so the PR rebased onto `origin/main` and adds only the wiring + listingExchange/status fix + detail-only mapper + 2 read-only scripts. Local `main` reset to `origin/main` (accidental BSE commit dropped; content preserved on the branch/main).

## ⚠️ BRANCH HYGIENE NOTE (2026-06-15)
The prior session committed Stage A core (`a16fe57a`) onto **local `main`** by mistake (contract requires the feature branch). **Recovered:** cherry-picked it to `feat/bse-ipo-enrichment` (now `8427df52`, based on `73c32b81` = PR #23 base, as intended); wiring stacked on top. **Local `main` is NOT pushed** (`origin/main`=`9b49a141` clean); it still carries the stray `a16fe57a`+merge. **TODO before any future `main` push:** reset local `main` to `origin/main` (drops the accidental BSE commit + merge; a16fe57a preserved on the feature branch). Left for explicit user OK — touches the shared trunk.

**Run commands:** scrape live = the module via `npx tsx --tsconfig tsx.tsconfig.json` (plain `tsx -e` mis-resolves `.js` imports). Tunnel DB = set `DATABASE_HOST=localhost DATABASE_PORT=15432` (shared pool prefers host/port over DATABASE_URL).
