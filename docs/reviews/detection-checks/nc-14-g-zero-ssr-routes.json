{
  "id": "nc-14-g-zero-ssr-routes",
  "section": "notCoveredByThisManifest",
  "note": "g_zero_ssr_routes (#201, T-473) — a page whose initial HTML is chrome-only ('Loading...' and no data rows) because its data is fetched client-side; HTTP-only probe inside scripts/audit-prod.mjs (ZERO_SSR_ROUTES: fetch the route with no JS and require the real-content marker, e.g. <table><tbody><tr> for /history, or a resolved (non-loading, non-disabled) control for /dashboard's sector filter), not audit-detection-floor.mjs, so it cannot be a checks[] entry here — this manifest's wire-or-retire test requires every declared id to be record()-ed by audit-detection-floor.mjs. Guarded by web/tests/e2e/history-ssr.spec.ts and web/tests/e2e/dashboard-ssr.spec.ts (both JS disabled) and extended per page as each gets the server-service pattern."
}
