{
  "id": "reread_verdict",
  "designId": "REREAD-VERDICT",
  "findingClass": "pull-model verification check REREAD-VERDICT: share of re-reads ending verified_against_document over 7 days",
  "whatItAsks": "share of re-reads ending verified_against_document over 7 days",
  "healthy": "below 0.95",
  "alarmsWhen": "at or above 0.95 — a source that is never wrong is a source never actually consulted",
  "scope": "phase-1 IPOs, MAINBOARD + SME, prod + staging slots (docs/design/data-sourcing-pull-model.md §2.1)",
  "consumer": "nightly floor-delta (Notifier post + issue on NEW)",
  "consumerReason": "explicitly a 7-day rolling share — needs a nightly cadence to accumulate and diff, not a single cycle’s data",
  "status": "designed, not yet built — specified by docs/design/data-sourcing-pull-model.md; build item 10 (owner decision OD-42: \"a check nobody reads is no detection\")",
  "sourceDesign": "docs/design/data-sourcing-pull-model.md §4 (\"How we would know it worked\")",
  "section": "notCoveredByThisManifest",
  "note": "Retired 2026-09-21: OD-65 (owner, 2026-09-21) withdrew the re-read loop this check measures: \"everything should be done on day one itself, and it should not be repeated for the same IPO... one IPO, one round of document read.\" The loop was never built (zero `reread` hits in scraper/src), and it is now explicitly not wanted, so this check measures a population that can never become non-empty — the same shape ratios_extraction_yield was retired for on 2026-09-16. It was also mis-sized from the start: the loop was scoped from \"578 leadManagers conflicts\" and the real count, after removing same-source rows, null-on-one-side rows and byte-identical values, is 1 on staging and 2 on prod. Its §3.2 premise additionally contradicts OD-32, which deletes the PDF bytes the check's receipt requires (#853).",
  "retiredBy": "OD-65 (#853) — no replacement; the behaviour is withdrawn, not moved",
  "retiredReason": "OD-65 (owner, 2026-09-21) withdrew the re-read loop this check measures: \"everything should be done on day one itself, and it should not be repeated for the same IPO... one IPO, one round of document read.\" The loop was never built (zero `reread` hits in scraper/src), and it is now explicitly not wanted, so this check measures a population that can never become non-empty — the same shape ratios_extraction_yield was retired for on 2026-09-16. It was also mis-sized from the start: the loop was scoped from \"578 leadManagers conflicts\" and the real count, after removing same-source rows, null-on-one-side rows and byte-identical values, is 1 on staging and 2 on prod. Its §3.2 premise additionally contradicts OD-32, which deletes the PDF bytes the check's receipt requires (#853)."
}
