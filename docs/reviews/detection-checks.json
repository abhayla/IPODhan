{
  "$schema": "T-335 detection-checks manifest — read by the round-8 review contract to classify a fresh finding as SHOULD-HAVE-BEEN-CAUGHT (a listed check already covers this finding's class) vs a genuinely new gap.",
  "generatedBy": "T-335",
  "sourceRca": "docs/reviews/round-7-detection-rca.md",
  "auditScript": "scripts/audit-detection-floor.mjs",
  "selfTest": "scripts/tests/audit-detection-floor.test.mjs",
  "wiredInto": "scripts/vps-data-audit-cron.sh (cron 45 3 * * * on 72.61.240.224)",
  "checks": [
    {
      "id": "a_b_live_conflict",
      "findingClass": "live IPO publishes an open/close date, price band, lot size, or issue size that disagrees with an independent non-ingested oracle",
      "round7Findings": [
        "P1-1",
        "P1-2"
      ],
      "githubIssue": "https://github.com/abhayla/IPODhan/issues/199 (G-N, T-472, 2026-09-07): extended from 2 fields (dates only, Chittorgarh oracle) to 6 fields against ipowatch.in, a source the scraper does NOT ingest -- Chittorgarh is ingested (scraper/src/scrapers/chittorgarh-scraper.ts), so comparing against it proved nothing about a scraper defect. The 4 P1s originally cited (subscription understated, band collapsed, Fascinate Textiles priced low) were invisible to a dates-only compare.",
      "failThreshold": "any OPEN/UPCOMING IPO whose published openDate, closeDate, priceRangeMin, priceRangeMax, or issueSize disagrees with the value this audit fetches LIVE from ipowatch.in (PRIMARY signal, owned by nothing in our pipeline) - dates compared exactly, priceRangeMin/priceRangeMax/issueSize tolerate a 1% relative difference (ipowatch rounds, e.g. 'Approx Rs 40.88 Crores'). lotSize is ALSO folded into this check's violations, but (T-506, #415, 2026-09-08) no longer compared directly against ipowatch's figure: ipowatch's 'minimum bid is N Shares' line is the MINIMUM RETAIL APPLICATION, not the exchange lot -- since SEBI's 2025 SME rule change that figure is 2x the exchange lot for SME issues (Qualiance International: lot_size=1000, ipowatch=2000, both correct). The lot comparison divides ipowatch's figure by the segment's minimum-application-lot multiplier (SME=2, MAINBOARD=1, findLotDisagreements() in scripts/lib/detection-floor-checks.mjs) before comparing to our exchange lot. See the sibling check a_b_min_application for the direct minimum-retail-application comparison. An unresolved data_conflicts row on a HIGH_VALUE field is a SECONDARY signal. The primary comparison is deliberately independent because the cross-source monitor resolves and re-inserts every row each 30-minute cycle (T-335C), and field_sources.previous_source is populated on 1 of 5746 prod rows so it cannot serve as the oracle.",
      "severity": "P1",
      "unverifiableWhen": "the ipowatch list-index fetch fails or returns no rows (the check is BLIND, and pages P2); a matched company whose detail page cannot be parsed is excluded from comparison, not silently passed"
    },
    {
      "id": "a_b_min_application",
      "findingClass": "live IPO's derived minimum retail application (exchange lot x segment multiplier) disagrees with the independent non-ingested oracle's minimum-bid-shares figure",
      "round7Findings": [],
      "githubIssue": "https://github.com/abhayla/IPODhan/issues/415 (T-506, 2026-09-08, owner decision 4): the nightly a_b_live_conflict FAILed on Qualiance International (SME, OPEN) because ipowatch.in's 'minimum bid is 2000 Shares' line was compared directly against ipos.lot_size=1000. Both figures were correct -- 1000 is the exchange lot, 2000 is the SME retail minimum RETAIL APPLICATION (2 lots, SEBI ICDR rule since 2025). Split out as its own check so the two concepts each get a distinct, correctly-derived comparison and a distinct PASS/FAIL, instead of one check silently comparing the wrong pair.",
      "failThreshold": "any OPEN/UPCOMING IPO where our derived minimum retail application (ipos.lot_size x minApplicationLots(segment), where the multiplier is 2 for SME and 1 for MAINBOARD/unknown -- see scripts/lib/detection-floor-checks.mjs minApplicationLots()) disagrees with the minimum-bid-shares figure this audit fetches LIVE from ipowatch.in, via findMinApplicationDisagreements(). Compared exactly (no rupee tolerance -- share counts, not rupee amounts). Shares the same ipowatch fetch as a_b_live_conflict (checkA_B() in scripts/audit-detection-floor.mjs) -- no second HTTP round.",
      "severity": "P1",
      "unverifiableWhen": "the ipowatch list-index fetch fails or returns no rows (the check is BLIND, and pages P2, in lockstep with a_b_live_conflict since they share the same fetch); a matched company whose detail page cannot be parsed is excluded from comparison, not silently passed"
    },
    {
      "id": "c_issue_size_consistency",
      "findingClass": "issue_size (total incl. OFS) falls outside a one-sided plausibility band against shares_offered x price_range_max",
      "round7Findings": [
        "P1-3"
      ],
      "failThreshold": "issue_size < 0.75x OR issue_size > 3.0x of shares_offered x price_range_max, when both are present (T-452, 2026-09-07: redefined from a symmetric +/-25% band)",
      "severity": "P1",
      "unverifiableWhen": "shares_offered not yet scraped for the row (skipped, not a violation)",
      "note": "T-452 owner decision 2026-09-07: ipos.issue_size is the TOTAL issue size INCLUDING OFS (schema.ts documents fresh_issue + ofs_issue summing to issue_size); subscriptions.shares_offered is the NET public offer (anchor/market-maker excluded). Under this definition, issue_size / (shares_offered x price_range_max) legitimately runs 1.0-1.9x on real rows (Meesho 1.76x, Wakefit 1.82x, Aequs 1.77x) - the prior symmetric +/-25% band false-positived every one of them. The band is now one-sided: below 0.75x still flags a share-count/wrong-unit-down value; above 3.0x flags a wrong-unit-up value. This closed the design item the prior note left open."
    },
    {
      "id": "c_issue_size_floor",
      "findingClass": "issue_size holds a magnitude implausible for its segment (share count stored as rupees)",
      "round7Findings": [
        "P1-3"
      ],
      "failThreshold": "issue_size > 0 AND issue_size < Rs10,00,00,000 for MAINBOARD, or < Rs1,00,00,000 for SME",
      "severity": "P1",
      "unverifiableWhen": "never — always runs against the ipos table"
    },
    {
      "id": "c_issue_size_noncapable_source",
      "findingClass": "ipos.issue_size current provenance (field_sources, field_name issueSize) is a source field-manifest.json does NOT rank as capable for this field, on any status/segment including NULL segment or no price cap (item 14, #728 class)",
      "round7Findings": [],
      "failThreshold": "any offering_type=IPO row whose current issueSize provenance source is not capability:true in field-manifest.json's fields['ipos.issue_size'].capability map (ADMIN exempt as a manual override; rows with no provenance row at all are a different check's job)",
      "severity": "P1",
      "unverifiableWhen": "scraper/config/field-manifest.json is missing or has no fields['ipos.issue_size'].capability entry",
      "note": "Item 14 (2026-09-24, staging measurement): 8 LISTED + 7 CLOSED IPO rows currently source issueSize from BSE, which the manifest marks capable:false (BSE measured 41-76% below the printed total offer on 6/6 live mainboard IPOs, review round 5 item C). Reads the capable-source set from field-manifest.json rather than hard-coding BSE, so the check tracks the manifest, not one incident."
    },
    {
      "id": "check_roster",
      "designId": "CHECK-ROSTER",
      "findingClass": "pull-model verification check CHECK-ROSTER: every id above appears in tonight’s report",
      "whatItAsks": "every id above appears in tonight’s report",
      "healthy": "all present",
      "alarmsWhen": "any missing — a check that crashed must not read as \"no findings\"",
      "scope": "phase-1 IPOs, MAINBOARD + SME, prod + staging slots (docs/design/data-sourcing-pull-model.md §2.1)",
      "consumer": "nightly floor-delta (Notifier post + issue on NEW)",
      "consumerReason": "explicitly “tonight’s report” in the design text — a nightly roster-completeness meta-check over the other 16 checks’ own nightly report",
      "status": "built 2026-09-20 (item 10): record() call lands in scripts/audit-detection-floor.mjs, and runs LAST so a crashed check cannot read as no-findings",
      "sourceDesign": "docs/design/data-sourcing-pull-model.md §4 (\"How we would know it worked\")"
    },
    {
      "id": "closed_ipo_done_without_walk",
      "findingClass": "the closed-IPO job records an IPO DONE while it has no plan rows or ANY plan row is still unsettled (PENDING / NOT_AVAILABLE_YET / CHECK_FAILED), walked or not -- DONE is never re-picked, so the IPO leaves the backlog unfinished (#717, OD-76, OD-79)",
      "round7Findings": [],
      "failThreshold": "any closed_ipo_resourcing row with outcome = 'DONE' whose IPO has zero ipo_field_plan rows, or at least one ipo_field_plan row whose state is not SUPPLIED / NOT_PRINTED / EXHAUSTED (OD-79: whatever the walk asked); reported by IPO name with its plan, unsettled and walked row counts",
      "severity": "P2",
      "unverifiableWhen": "closed_ipo_resourcing does not exist on the slot (migration 0050 not applied)",
      "spec_ref": [
        "OD-22",
        "OD-19",
        "OD-76",
        "OD-79"
      ],
      "note": "Measured read-only on ipodhan_staging 2026-09-23 (repair-closed-ipo-false-done.ts dry run): 10 of 10 DONE rows in this shape after the job's first real run, each plan_rows=0 walked_rows=0 (Advit Jewels, Anubhav Plast, Avience Biomedicals, Clay Craft India, Diksha Polymers, Liotech Industries, Riyaasat Lifestyle, Shreedhar Spinners, Turtlemint Fintech Solutions, Waterways Leisure Tourism). Discriminates: a DONE row whose IPO was walked (ipodhan_test proof: 189 plan rows, 189 with last_attempt_at) passes. Repaired by scraper/scripts/repair-closed-ipo-false-done.ts. Spec: section 6.1 as rewritten by OD-76 (PR #918, not yet on main -- add OD-76 to spec_ref once it lands). Widened by OD-79 (review round 3, 2026-09-23): a walked IPO (5 asked, 3 answered) recorded DONE with 37 rows open passed the never-asked-only predicate; the check and the repair tool now flag DONE with any unsettled row. Discriminates on ipodhan_test: walked + rows open -> flagged/reopened; walked + every row settled -> passes."
    },
    {
      "id": "corpus_shape",
      "designId": "CORPUS-SHAPE",
      "findingClass": "pull-model verification check CORPUS-SHAPE: per source, does the live page behind each fixture still match the fixture’s SHAPE (labels + structure the extractor depends on, never the values)",
      "whatItAsks": "per source, does the live page behind each fixture still match the fixture’s SHAPE (labels + structure the extractor depends on, never the values)",
      "healthy": "no shape difference from the fixture",
      "alarmsWhen": "a label or structure moves — files an issue naming the source, the fixture and the label that moved",
      "scope": "phase-1 IPOs, MAINBOARD + SME, prod + staging slots (docs/design/data-sourcing-pull-model.md §2.1)",
      "consumer": "nightly floor-delta (Notifier post + issue on NEW)",
      "consumerReason": "the design specifies a weekly scheduled job (§4.5); the floor-delta consumer is the only one of the two named consumers that already runs on a schedule independent of the pull cycle, so the weekly run rides that same NEW/GONE/SAME diffing consumer",
      "status": "built — wired at scripts/audit-detection-floor.mjs (checkS_corpusShape); predicates in scripts/lib/corpus-shape-checks.mjs, self-tested by scripts/tests/corpus-shape-checks.test.mjs",
      "sourceDesign": "docs/design/data-sourcing-pull-model.md §4.5 (\"How we would know it worked\")",
      "note": "CORPUS-SHAPE: per source, does the live page behind each HTML fixture still carry the labels its extractor keys on (never the values). Measured 2026-09-22: the corpus holds 11 HTML fixtures and 0 carry a fetchable sourceUrl (all 11 grandfathered in config/fixture-provenance-baseline.json), so this check records UNVERIFIABLE until a fixture is attributed via scripts/create-fixture-from-capture.mjs --source-url — never PASS, because it has measured nothing. Network is opt-in: the nightly floor fetches, CI sets CORPUS_SHAPE_FETCH=0 so the PR gate never depends on a third-party site."
    },
    {
      "id": "d_corporate_action_shape",
      "findingClass": "a row typed offering_type=IPO matches the shape of a corporate action (demerger/scheme-of-arrangement)",
      "round7Findings": [
        "P1-4"
      ],
      "failThreshold": "offering_type=IPO AND price_range_min==price_range_max AND lot_size==100 AND close_date-open_date in [10..14] days",
      "severity": "P1",
      "unverifiableWhen": "never — always runs against the ipos table"
    },
    {
      "id": "d_lot_band_window",
      "findingClass": "lot_size x price_range_max implies a retail application size SEBI would not permit",
      "round7Findings": [
        "P1-4"
      ],
      "failThreshold": "lot value outside [Rs8,000..Rs20,000] MAINBOARD or [Rs90,000..Rs3,00,000] SME, for offering_type=IPO",
      "severity": "P1",
      "unverifiableWhen": "never — always runs against the ipos table"
    },
    {
      "id": "d_segment_provenance",
      "findingClass": "ipos.segment is non-NULL with NO field_sources row for the segment field — a value with no record of who said it (the binary-test write bug, lane C item 2 slice 3b: 'not detected as SME' was asserted as MAINBOARD with no source)",
      "round7Findings": [],
      "failThreshold": "any ipos row with segment IS NOT NULL and no field_sources row (table_name='ipos', field_name='segment') for that ipo_id",
      "severity": "P2",
      "unverifiableWhen": "never — always runs against the ipos and field_sources tables",
      "note": "Closes the detection gap left after scripts/repair-segment-provenance.ts repaired the existing 41-62 rows: that tool clears segment ONLY for non-IPO offering types (segment is meaningless there) and REPORTS-only for unsourced IPO rows (clearing a probably-correct label would destroy real information the missing provenance row already expresses). This check is what stops the gap from reopening on the next write path that skips field_sources — measured missing on 2026-09-10: no existing check asserts provenance PRESENCE (checkSegmentPopulatedForIpo measures SHARE; g_provenance_lineage in audit-ipo-coverage.mjs measures a declining-ceiling ratchet on lineage generally, not this specific field/table pair)."
    },
    {
      "id": "design_traceability",
      "designId": "OD-52",
      "findingClass": "a design rule has no build item, a build item's rule has no test, a test names a rule that does not exist, or a rule's wording changes without its card and test being touched in the same change",
      "whatItAsks": "docs/design/build-cards/item-20-design-traceability-check.md's four failure modes, run per pull request against the diff introduced by that pull request",
      "healthy": "every live rule id in docs/design/rules.json is claimed by a build card or declared unclaimed with a reason; every card-claimed id (once mode 2 flips out of reporting mode at build item 6) is declared by a test; every test-declared id is live; no rule's hash drifted at HEAD without its owning card or a declaring test also in the diff",
      "alarmsWhen": "check-design-traceability.mjs exits 1 (a broken link in the chain, naming the offending rule/card/test id) or exits 2 (the check itself failed to run, including the self-guard for zero live rules read as a PASS)",
      "scope": "every pull request into main touching docs/design/build-cards/**, docs/design/rules.json, docs/design/rules-unclaimed.json, or any test file under scraper/tests/**, web/tests/**, packages/*/tests/**, scripts/tests/** — enforced on ALL pull requests via the pr-gate.yml gate step, not a filtered subset",
      "consumer": "the pr-gate.yml CI job itself (the 'Design traceability (OD-52)' step) — a pull-request gate, not a scheduled nightly consumer; it reports by identity (which rule, which card, which test), never as a bare count, per .claude/rules/signal-ownership.md R1",
      "consumerReason": "item-20-design-traceability-check.md's Detection section: \"The check IS the detection for its own class\" — it runs synchronously in CI on every PR rather than as a nightly batch job",
      "status": "built and wired — scripts/ci/check-design-traceability.mjs runs as the \"Design traceability (OD-52)\" step in pr-gate.yml against the PR base sha; registry entry corrected 2026-09-20 (item 10)",
      "sourceDesign": "docs/design/build-cards/item-20-design-traceability-check.md",
      "auditScript": "scripts/ci/check-design-traceability.mjs"
    },
    {
      "id": "e1_source",
      "designId": "E1-SOURCE",
      "findingClass": "pull-model verification check E1-SOURCE: for the ten E-1 fields, field_sources.source is never DRHP",
      "whatItAsks": "for the ten E-1 fields, field_sources.source is never DRHP",
      "healthy": "true",
      "alarmsWhen": "any E-1 field written by the document path — asserts the outcome, not the declared intent",
      "scope": "phase-1 IPOs, MAINBOARD + SME, prod + staging slots (docs/design/data-sourcing-pull-model.md §2.1)",
      "consumer": "nightly floor-delta (Notifier post + issue on NEW)",
      "consumerReason": "a snapshot assertion over the whole ipos/field_sources table, the same shape as the existing nightly floor checks (e.g. c_issue_size_floor) — not a per-cycle write-path fact but a standing invariant read once a night",
      "status": "built 2026-09-20 (item 10): record() call lands in scripts/audit-detection-floor.mjs; FAILING on first run — 79 E-1 writes from DRHP (#862)",
      "sourceDesign": "docs/design/data-sourcing-pull-model.md §4 (\"How we would know it worked\")"
    },
    {
      "id": "e_route_sweep",
      "findingClass": "a public API route 5xxs, or leaks SQL/stack-trace text in its response body",
      "round7Findings": [
        "P2-1",
        "P2-2",
        "P2-3",
        "P2-4",
        "P2-5"
      ],
      "failThreshold": "any enumerated web/app/api/** non-admin route returns >=500, or its body matches a SQL/bound-param/stack-trace pattern",
      "severity": "P1",
      "unverifiableWhen": "any enumerated public route is unreachable (the sweep did not cover its whole population)"
    },
    {
      "id": "e_unknown_slug_404",
      "findingClass": "/api/ipos/<slug> (or the /ipos/<slug> page, same resolution path) resolves an unknown slug to a DIFFERENT IPO instead of 404ing",
      "round7Findings": [],
      "issue": 350,
      "failThreshold": "any of 3 nonsense slugs (never a real row) does not return 404, OR a real currently-live slug does not resolve to itself",
      "severity": "P1",
      "unverifiableWhen": "a request is unreachable (timeout/network), or there is no LISTED row to run the positive case against",
      "note": "T-485 (2026-09-07): findBySlugWithFallback used to accept any best-scoring Fuse.js fuzzy match with no floor, so 'karamtara-engineering-ltd' (no row on prod) resolved to 'Sumax Engineering Ltd.' purely on the shared word 'engineering' (measured score 0.44, 56% similarity). Fixed by resolving only via the slug-redirect table, an exact normalized company-name match, or a fuzzy match at or above an 85% similarity floor (measured: a genuine one-typo pair scores 15%, comfortably inside; the karamtara/sumax collision at 44% is rejected by more than double the margin) — everything else 404s, with the miss negative-cached per #344."
    },
    {
      "id": "e_verdict_leak_sweep",
      "findingClass": "a public API route (web/app/api/**, excluding /api/admin/**) serves a \"verdict\" or \"witnesses\" JSON key -- OD-61: consensus disagreement must stay admin-only, never visible to a reader",
      "failThreshold": "any enumerated public web/app/api/** route's response body contains a quoted \"verdict\" or \"witnesses\" key",
      "severity": "P1",
      "unverifiableWhen": "any enumerated public route is unreachable (shares the e_route_sweep loop's coverage -- a sweep with holes and no failures is BLIND, not green)",
      "sourceTask": "S7 (docs/design/s7-consensus-check-plan.md) half 2, OD-61 (\"keep everything admin only... no user should not see any disagreement\")",
      "scope": "every web/app/api/** route NOT under /api/admin/** (admin routes excluded by design -- disputes live on the admin surface)",
      "consumer": "nightly detection-floor gate (scripts/audit-detection-floor.mjs --gate)",
      "consumerReason": "reuses the existing e_route_sweep enumeration+fetch loop in the SAME script -- a second sweep script would drift out of sync with the route list and double outbound traffic against a box that serves production",
      "auditScript": "scripts/audit-detection-floor.mjs --gate (checkE, inside the e_route_sweep loop)",
      "selfTest": "scripts/tests/audit-detection-floor.test.mjs (OD-61 verdict-leak cases)"
    },
    {
      "id": "f_conflict_noise_ratio",
      "findingClass": "the data_conflicts table is dominated by non-actionable noise, burying genuine P1 disagreements",
      "round7Findings": [
        "P2-6"
      ],
      "failThreshold": "(empty value2 OR value1==value2) / total unresolved >= 5% OR total unresolved > 500 (#191 F2, unbounded-growth ceiling, T-285 11,493-row incident)",
      "severity": "P2",
      "unverifiableWhen": "data_conflicts table not present"
    },
    {
      "id": "fixture_provenance",
      "findingClass": "a committed test fixture (HTML/JSON/TXT/PNG under scraper/tests/fixtures/** or scraper/tests/unit/pipeline-stages/fixtures/**) has no recorded source URL/capture date, or an HTML fixture's own embedded company name (its <title>/<h1>) disagrees with the company its filename claims — so the fixture silently backs a test with the WRONG page and the test still passes",
      "githubIssue": "T-518, 2026-09-08: PR #425 shipped scraper/tests/fixtures/historical/vikran-engineering-cg-detail.html whose own <title> read \"Neochem Bio IPO...\"; a test asserting \"Vikran Engineering -> Specialty Chemicals\" against it PASSED. Nothing in CI could catch it — a human caught it by opening the file.",
      "failThreshold": "any fixture data file under the two fixture roots with no sibling <file>.meta.json (sourceUrl + capturedAt + company|pageType:true), OR an HTML fixture whose filename claims a company (per the whitelist-strip rule in scripts/lib/fixture-provenance-checks.mjs) and whose extractable <title>/<h1> names a DIFFERENT company, that is not grandfathered in the shrink-only config/fixture-provenance-baseline.json",
      "severity": "P2",
      "unverifiableWhen": "never — this is a static, deterministic file-content check with no external dependency (no network call, no DB)",
      "auditScript": "scripts/ci/require-fixture-provenance.mjs",
      "wiredInto": "pr-gate.yml step \"Fixture provenance gate (T-518)\" — node scripts/ci/require-fixture-provenance.mjs — runs on every PR (NOT the nightly scripts/audit-detection-floor.mjs — this is a static repo-content gate, not a live-data audit)",
      "selfTest": "scripts/ci/tests/require-fixture-provenance.test.mjs (predicates), scripts/tests/normalize-company-name-parity.test.mjs (normalizer parity)"
    },
    {
      "id": "g_freshness_per_type",
      "findingClass": "an offering_type's calendar has gone stale with no per-type freshness check to catch it",
      "round7Findings": [
        "P2-10"
      ],
      "failThreshold": "newest row age exceeds 3 days (IPO/SME) or 21 days (OFS/NCD/RIGHTS)",
      "severity": "P2",
      "unverifiableWhen": "no rows exist yet for that offering_type"
    },
    {
      "id": "g_inert_detector",
      "findingClass": "the conflict detector reports zero new conflicts while an independent invariant (checkPriceBand) is demonstrably violated on live IPO rows — the detector is inert, not the data clean",
      "round7Findings": [],
      "failThreshold": "checkPriceBand violations on offering_type=IPO rows > 0 AND data_conflicts rows inserted (detected_at) in the last 24h == 0",
      "severity": "P1",
      "unverifiableWhen": "data_conflicts or ipos table not present",
      "note": "#191 F3 (T-297 D6), the important one — a detector reporting zero looks like health unless cross-checked against an invariant independently known to be violated: conflictsDetected read 0 every cycle across 3,760 fields consolidated from 6 sources while T-272 P1-1 simultaneously proved real disagreements existed."
    },
    {
      "id": "g_repeated_warn",
      "findingClass": "a scraper_logs FAILURE/PARTIAL message repeats so often it stops functioning as a signal while the underlying rows still write through",
      "round7Findings": [],
      "failThreshold": "any single (truncated) error_message occurs more than 20 times in scraper_logs (status FAILURE/PARTIAL) in the last 24h",
      "severity": "P2",
      "unverifiableWhen": "scraper_logs table not present",
      "note": "#191 F1 (T-297 D6): the InvIT/REIT unit-price WARN was logged 20+ times per cycle every cycle (T-272 P2-2) while the mis-typed rows were written anyway and rendered as ₹0.00."
    },
    {
      "id": "g_reverse_sweep",
      "findingClass": "an IPO the external market lists (chittorgarh.com's public dashboard) has no matching row on our site, or has one mistyped as offering_type != 'IPO' (the Mopshop shape), or its detail page 404s / renders a different company (the T-460 e_unknown_slug_404 shape) -- every OTHER check iterates OUR OWN rows and cannot find a row we never created",
      "round7Findings": [],
      "sourceTask": "#187 (T-461), filed by T-297 discovery-coverage meta-analysis (D2 reverse sweep); real-world instance issue #351 (prod missing Karamtara Engineering/Infrax Renewable/Rentomojo/Manipal Payment/Steamhouse India/Vinod Texworld while staging had discovered several days earlier)",
      "registryClass": "reverse-sweep-missing-or-mistyped-external-ipo",
      "failThreshold": "any chittorgarh.com dashboard IPO (mainboard + SME, OPEN/UPCOMING) fails any of: (a) a matching row exists on the target (slug, then normalized-name search, both compared with the trailing legal-entity suffix stripped), (b) that row's offeringType == 'IPO', (c) GET {BASE_URL}/ipos/<slug> returns 200 and the body mentions the company name",
      "severity": "P1",
      "unverifiableWhen": "the external calendar (both chittorgarh dashboard fetches) could not be reached at all -- BLIND, not a pass. NOTE: does not cover recently-CLOSED (30-day) IPOs -- chittorgarh's listing-date report renders client-side and ipowatch.in was unreachable from the environment this check was authored in; see scripts/lib/chittorgarh-oracle-parser.mjs 'KNOWN GAP'.",
      "auditScript": "scripts/audit-reverse-sweep.mjs --gate",
      "selfTest": "scripts/tests/audit-reverse-sweep.test.mjs"
    },
    {
      "id": "g_served_stored_delta",
      "findingClass": "an API and its backing table each look correct in isolation while a stale cache key or wrong filter silently serves a different row count than the DB holds (T-272 P1-2: /api/registrars served 41 rows while the DB held 15 -- 26 unit-test fixture rows stranded in a 7-day Redis key after an integration suite was pointed at production)",
      "round7Findings": [],
      "sourceTask": "#186 (T-460), filed by T-297 discovery-coverage meta-analysis (D5 serving-vs-storage delta)",
      "registryClass": "serving-vs-storage-drift",
      "failThreshold": "for any of /api/registrars, /api/ipos?status=OPEN&limit=100, /api/ipos?status=UPCOMING&limit=100, /api/market-holidays?year=<current>: served row count (live HTTP JSON body) != stored row count (matching filtered DB query) -- exact equality, not a tolerance",
      "severity": "P1",
      "unverifiableWhen": "the live HTTP fetch or the matching DB query fails (marked unverifiable, not a false PASS)",
      "auditScript": "scripts/audit-ipo-coverage.mjs --gate (evaluateServedVsStoredDelta)",
      "selfTest": "scripts/tests/audit-ipo-coverage-served-stored.test.mjs"
    },
    {
      "id": "h_pm2_env_tz",
      "findingClass": "a pm2 process runs without TZ in its environment, exposing new Date() parsing to the box's local time",
      "round7Findings": [
        "P2-7",
        "P1-1 (enabling gap)"
      ],
      "failThreshold": "any pm2 process's env lacks a TZ key",
      "severity": "P1",
      "unverifiableWhen": "pm2 not reachable on the host running the audit (expected off the production box)"
    },
    {
      "id": "h_pm2_log_size",
      "findingClass": "an unrotated pm2 log file is growing unbounded, risking the 2026-06-13 disk-full class",
      "round7Findings": [
        "P2-11"
      ],
      "failThreshold": "any pm2 out/err log file exceeds 100 MB",
      "severity": "P2",
      "unverifiableWhen": "pm2 not reachable on the host running the audit"
    },
    {
      "id": "i_company_two_live_rows",
      "findingClass": "the same normalised company holds two or more rows simultaneously live (UPCOMING/OPEN/CLOSED), any offering_type — listed for review, never auto-merged",
      "round7Findings": [],
      "failThreshold": "any group of 2+ rows, any offering_type, in status UPCOMING/OPEN/CLOSED, sharing a normalised company name",
      "severity": "P2",
      "unverifiableWhen": "never — always runs against the ipos table",
      "spec_ref": [
        "2.3.3.1",
        "2.3.3.2"
      ],
      "note": "Owner #903 S4. LISTED excluded deliberately — a LISTED row plus a fresh filing is the legitimate new-offering-after-withdrawal shape (OD-35 lapsed-draft rule), not a duplicate."
    },
    {
      "id": "i_identity_held",
      "findingClass": "an incoming IPO record held for review by the OD-68 create-time hold (a same-name, same-segment live row with a differing KNOWN open date or price band) that no human has decided — invisible anywhere else, so the offering it describes never gets a row or a correction",
      "round7Findings": [],
      "failThreshold": "any audit_logs IDENTITY_HELD_FOR_REVIEW row in the last 2 days whose incoming slug has no later IDENTITY_HOLD_OVERRIDDEN row",
      "severity": "P2",
      "unverifiableWhen": "never — always runs against the audit_logs table",
      "spec_ref": [
        "2.3.3.2"
      ],
      "note": "PR #910 review round 1, MAJOR-2: a warn log alone is not detection (signal-ownership.md R3). IPORepository.create writes one audit_logs row per held record per candidate per day; this check lists every undecided one by name. A human decides by correcting the existing row, or by creating the record through /admin (the web admin route is not held) or IPORepository.create(..., { identityHoldOverride })."
    },
    {
      "id": "i_ipo_title_in_name",
      "findingClass": "page title or page-status text stored in company_name or slug instead of the company's real name",
      "round7Findings": [],
      "failThreshold": "any offering_type=IPO row whose company_name contains \"(...IPO)\" or ends in the word IPO, or whose slug matches -ipo(-|$) or still carries a page-status suffix (-o/-p/-lt/-ct)",
      "severity": "P2",
      "unverifiableWhen": "never — always runs against the ipos table",
      "spec_ref": [
        "2.3.3.1"
      ],
      "note": "Owner #903 S3."
    },
    {
      "id": "i_name_bound_live",
      "findingClass": "a live IPO row bound on nothing stronger than its normalised name (no CIN, no exchange symbol, no ISIN) — the OD-34 name-bound flag",
      "round7Findings": [],
      "failThreshold": "any offering_type=IPO row in status UPCOMING/OPEN with no cin, no symbol, and no isin",
      "severity": "P3",
      "unverifiableWhen": "never — always runs against the ipos table",
      "spec_ref": [
        "2.3.3.2"
      ],
      "note": "Implements OD-34: 'the nightly audit reports the name-bound rows by name — never as a count.' A name-bound row may not be auto-merged with another name-bound row."
    },
    {
      "id": "i_same_ipo_two_rows",
      "findingClass": "one IPO stored as two rows — the same offering sharing an identifier (CIN/ISIN/symbol/BSE scrip code), a suffix-stripped slug, or a normalised name + open date",
      "round7Findings": [],
      "failThreshold": "any group of 2+ offering_type=IPO rows sharing CIN, ISIN, symbol, BSE scrip code, a status-suffix-stripped slug, or normalised company name + same open_date",
      "severity": "P1",
      "unverifiableWhen": "never — always runs against the ipos table",
      "spec_ref": [
        "2.3.3.1"
      ],
      "note": "Implements the §2.3.3.1 F-103 standing sweep (owner #903, S1/S2/S7); flags the live Rays of Belief pair on prod/staging."
    },
    {
      "id": "i_source_key_conflict",
      "findingClass": "a source record bound to the wrong IPO row or left un-superseded (OD-85): one IPO holding two ACTIVE record numbers of the same source (a relaunch whose OD-83 supersede never happened, or a merge that joined two relaunch numbers), or a key the CIN/ISIN re-check marked DISPUTED - a wrong bind whose written values need recomputing",
      "round7Findings": [],
      "failThreshold": "any ipos row with more than one ACTIVE ipo_source_keys row of the same (source, key_type), or any ipo_source_keys row whose state became DISPUTED in the last 30 days",
      "severity": "P2",
      "unverifiableWhen": "ipo_source_keys unreadable for a reason other than the table not existing yet (42P01 before migration 0053 = PASS)",
      "spec_ref": [
        "2.3.3.2"
      ],
      "note": "OD-85 / OD-86, F-144..F-149. Keys hitting two different rows cannot be stored (plain UNIQUE(source, key_type, binding_value)); a record whose keys hit two rows is refused at read time and logged as source_key_duplicate. This check reads what the table CAN hold wrongly."
    },
    {
      "id": "i_wire_or_retire",
      "findingClass": "a scheduler/cron/job definition exists in the repo but is never imported by the prod entrypoint",
      "round7Findings": [
        "P2-8"
      ],
      "failThreshold": "any scraper/src/scheduler/*.ts file not `import`-ed by scraper/src/index.ts, ecosystem.config.js, or scripts/deploy-linux.sh",
      "severity": "P2",
      "unverifiableWhen": "never — static analysis of the checked-out repo"
    },
    {
      "id": "issuer_ratio_yield",
      "findingClass": "a completed prospectus-family extraction yields no issuer ratio and names no cause - the reader stopped being called (or never was) and nothing anywhere said so",
      "round7Findings": [],
      "failThreshold": "any COMPLETED RHP/DRHP/PROSPECTUS document extracted on or after the wiring date whose IPO has no financial_data.current_ratio AND whose E9 step evidence records none of ratio_note_not_in_document / ratio_row_not_in_note / balance_sheet_inputs_absent",
      "severity": "P2",
      "unverifiableWhen": "no prospectus-family document has completed extraction since the wiring date - the population is empty, which is a statement about the corpus and not about the pipeline",
      "why": "scraper/scripts/financial_ratios.py merged in #638 with ZERO importers: correct, unit-tested, and never invoked, so every document produced null ratios in silence. A unit test proves the reader reads; only an audit over real rows proves it is still being CALLED. This is the same class as item 8a's peer locator, which was merged into TypeScript where nothing would ever call it. The check deliberately accepts a RECORDED REASON as a pass, because a document with no ratio note in it is a fact about the document, not a defect - what is never acceptable is an absence with no cause (signal-ownership.md R1: a number is not a reading).",
      "locatorNote": "Scoped to documents extracted on or after the wiring date (RATIO_WIRING_MERGED_AT, default 2026-09-16): documents completed before it could not carry a ratio however healthy the pipeline was, so including them would report a permanent FAIL that no night can clear. Their repair is item 8 slice 3b."
    },
    {
      "id": "j_cron_executable",
      "findingClass": "a cron-invoked script is committed without the executable bit, so a direct cron invocation fails silently",
      "round7Findings": [
        "P3-5"
      ],
      "failThreshold": "git-tracked mode of a cron entrypoint script lacks any execute bit",
      "severity": "P2",
      "unverifiableWhen": "git ls-files fails (corrupt checkout)"
    },
    {
      "id": "j_dead_source_retire_by",
      "findingClass": "a scraper source has been degraded/dead for many cycles with no owner decision to fix or retire it",
      "round7Findings": [
        "P3-6"
      ],
      "failThreshold": "a source has >=7 consecutive non-SUCCESS-or-zero-record cycles and is not listed in docs/reviews/dead-source-retirement.json",
      "severity": "P2",
      "unverifiableWhen": "scraper_logs table not present"
    },
    {
      "id": "j_sector_populated",
      "findingClass": "the sector field/facet is populated for too few real IPOs to be usable",
      "round7Findings": [
        "P3-1"
      ],
      "failThreshold": "populated sector fraction < 50% of real IPOs",
      "severity": "P2",
      "unverifiableWhen": "never — always runs against the ipos table"
    },
    {
      "id": "j_segment_not_null",
      "findingClass": "an offering_type=IPO row has a NULL segment",
      "round7Findings": [
        "P3-7"
      ],
      "failThreshold": "any offering_type=IPO row with segment IS NULL — an empty/whitespace segment counts as blank, not only SQL NULL.",
      "severity": "P2",
      "unverifiableWhen": "never — always runs against the ipos table"
    },
    {
      "id": "k_step_consecutive_failures",
      "findingClass": "a post-scrape step fails every cycle, hidden by its non-fatal catch, while the cycle keeps exiting 0",
      "round7Findings": [
        "P2-8 (runtime twin)"
      ],
      "failThreshold": "any step in the derived STEP_NAMES whose most recent scraper_steps rows show status=failed for >= 3 consecutive cycles (leading streak, newest-first). 2 is a blip and PASSes; a non-failed row ends the streak.",
      "severity": "P1",
      "unverifiableWhen": "same as k_step_ledger_silence — unparseable STEP_NAMES, missing scraper_steps table, or an empty 24h window"
    },
    {
      "id": "k_step_ledger_silence",
      "findingClass": "a post-scrape step is wired and runs every cycle but produces no successful work — skipped for an unread reason, or failing inside its non-fatal catch",
      "round7Findings": [
        "P2-8 (runtime twin)"
      ],
      "failThreshold": "any step in the exported STEP_NAMES of scraper/src/index.ts with ZERO rows of status=ok in scraper_steps over the last 24h. The expected-step list is DERIVED from that constant at audit time (parseStepNames), never hand-typed here — a hand-typed copy is the i_wire_or_retire class itself. skipped rows do not count as ok: a step skipped every cycle (ADMIN_API_TOKEN unset) is still a step doing nothing.",
      "severity": "P1",
      "unverifiableWhen": "STEP_NAMES cannot be parsed from scraper/src/index.ts, the scraper_steps table is absent (migration 0033 not applied), or the table has zero rows in the window (the ledger writer has not run). Zero rows is deliberately UNVERIFIABLE rather than 12 simultaneous FAILs — a genuinely dead scraper is caught independently by g_freshness_per_type."
    },
    {
      "id": "l_nse_status_crosscheck",
      "findingClass": "our published OPEN/UPCOMING set disagrees with the exchange that actually runs the issue",
      "round7Findings": [
        "T-340 (new coverage — no round-7 finding; our live status set had NO independent oracle at all)"
      ],
      "failThreshold": "any MAINBOARD row naming NSE among its listing exchanges where (a) we publish OPEN but NSE current-issue does not list it, (b) NSE current-issue lists it as open while we publish UPCOMING or no OPEN row, or (c) an NSE current/upcoming issue has no OPEN/UPCOMING row of ours at all. Scope excludes SME (NSE Emerge is not on these endpoints), BSE-only rows, and rows with unknown listing exchanges — FAILing those would be noise, and a muted channel is a dead mechanism.",
      "severity": "P1",
      "unverifiableWhen": "the NSE cookie handshake or either feed fetch fails, or both feeds return zero usable rows (indistinguishable from a broken feed, so never read as \"nothing is open today\"). UNVERIFIABLE pages and exits 3 — never a silent PASS (T-321 class)."
    },
    {
      "id": "listed_rotation_stall",
      "findingClass": "a LISTED IPO inside the 10-day window is stuck at the front of the document rotation, either with document rows and zero fetch-state rows (shape 1), or with fetch-state rows that exist but are stale (shape 2)",
      "round7Findings": [],
      "sourceTask": "rotation-stall-null-fetch-state (2026-09-06); stale-max-last-attempt shape added 2026-09-06 round 2 (W-136, runDocumentCycle budget-trip LISTED reservation)",
      "registryRow": "docs/reviews/failure-classes.md - 'listed rotation stall' row",
      "failThreshold": "shape 1: no LISTED IPO inside the LISTED_ROTATION_WINDOW_DAYS-day live window has documents on file but 0 document_fetch_state rows - MAX(last_attempt_at) sorts a 0-row IPO first forever (NULLS FIRST), so it occupies the front of the LISTED rotation queue every cycle and starves every LISTED row behind it. shape 2: no LISTED IPO inside the live window has >=1 DUE document_fetch_state row (state not in FOUND/NOT_APPLICABLE/SUPERSEDED, AND next_retry_at IS NULL OR next_retry_at <= now() - a row in a deliberate future backoff is NOT due and must not count) whose MAX(last_attempt_at) across all its rows is more than STALE_ROTATION_HOURS (24h) old, or null - a full live (OPEN/CLOSED/UPCOMING) backlog can trip the discovery budget before the LISTED tier every cycle, so rows exist but `last_attempt_at` never advances even though the cycle runs every 30 minutes",
      "severity": "P2",
      "unverifiableWhen": "document_fetch_state or documents table not present"
    },
    {
      "id": "m_absence_without_evidence",
      "findingClass": "a row settles NOT_YET_FILED - 'the company has not filed it', no retry, no alert - on a rung chain in which not one source ANSWERED. T-403 Class 1, found in four consecutive rounds through four different doors (SEBI failures returning null; a 503 written as not-filed; escalation-budget refusals returning 'absent'; an unsettled EXCHANGES:no_link inherited through an escalation whose every rung was skipped). Each code fix closed the door it could see; this check watches the room",
      "round7Findings": [],
      "sourceTask": "T-403",
      "failThreshold": "state = NOT_YET_FILED AND doc_type is one the exchanges cannot serve (DRHP) AND the row's own last_attempt chain for that type has zero answered rungs (every non-EXCHANGES rung is skipped: or failed:). A row with no chain recorded is skipped, never guessed at",
      "severity": "P2",
      "unverifiableWhen": "document_fetch_state table not present"
    },
    {
      "id": "m_blocked_all_age",
      "findingClass": "a filing has been unreachable on every source for a day or more and nobody noticed, because the P2 alert fires once on entry and then scrolls away",
      "round7Findings": [],
      "sourceTask": "T-403",
      "failThreshold": "any document_fetch_state row in BLOCKED_ALL with blocked_since_at older than 24h (aged from blocked_since_at, NOT last_attempt_at: a row retried every 30 min has a fresh last-attempt forever)",
      "severity": "P2",
      "unverifiableWhen": "document_fetch_state table not present"
    },
    {
      "id": "m_brlm_count",
      "findingClass": "the BSE payload lists more lead managers than we stored (the co-BRLM parser drops entries after the first \"#\"-separated one)",
      "round7Findings": [
        "F17"
      ],
      "sourceTask": "T-403",
      "failThreshold": "array_length(lead_managers) < ipos.bse_payload_lead_manager_count for any IPO. Storing MORE is not flagged: other sources legitimately add managers BSE omits",
      "severity": "P2",
      "unverifiableWhen": "no IPO has a recorded bse_payload_lead_manager_count yet"
    },
    {
      "id": "m_cycle_overrun",
      "findingClass": "the document cycle (scraper/src/services/document-cycle.ts, primarySourceDiscovery step) runs longer than its wake budget, or two wakes' cycles overlap — the class fixed one write path in August (w177-detection-rca.md) and recurred on this path in September because no independent check re-verified cycle duration",
      "round7Findings": [
        "cycle-overrun RCA (2026-09-06, observed 1,210-1,278s document cycles / 105-115 network calls per cycle on a Sunday)"
      ],
      "failThreshold": "any primarySourceDiscovery row in scraper_steps (created_at, duration_ms) over the last 24h with duration_ms > 25 minutes, OR two rows for different cycleIds whose [start, end) wall-clock windows overlap (checkCycleOverrun in scripts/lib/document-state-checks.mjs, unit-tested in scripts/tests/document-state-checks.test.mjs).",
      "severity": "P1",
      "unverifiableWhen": "the scraper_steps table is absent (migration 0033 not applied), or it has zero primarySourceDiscovery rows in the last 24h (ENABLE_DOCUMENT_STATE_MACHINE off, or the writer has not run yet)."
    },
    {
      "id": "m_deploy_failure_open",
      "findingClass": "the deploy-failure STATUS row (scripts/deploy-status.mjs) is write-only - written on failure, cleared on the next success, but never read back - so a row can sit open on disk indefinitely with nothing surfacing it",
      "round7Findings": [],
      "sourceTask": "T-425",
      "registryClass": "merged-fix-never-deployed-while-bug-live",
      "failThreshold": "any slot's deploy-failure STATUS row (deploy-failure-status.json) has been open for more than 24h",
      "warnThreshold": "a slot's row is open but under 24h old - a normal in-progress-fix window, not yet an outage",
      "severity": "P1",
      "unverifiableWhen": "the deploy-failure-status.json file exists but could not be read/parsed (an ABSENT file is a legitimate PASS - no deploy has ever failed)"
    },
    {
      "id": "m_document_state",
      "findingClass": "the document_fetch_state table is absent, so the whole document pipeline is invisible to the audit tonight",
      "round7Findings": [],
      "sourceTask": "T-403",
      "failThreshold": "never FAILs — reports UNVERIFIABLE, because \"the audit could not see\" is not \"documents are healthy\"",
      "severity": "P2",
      "unverifiableWhen": "document_fetch_state table not present (migration 0035 not applied)"
    },
    {
      "id": "m_document_type_classifier",
      "findingClass": "a stored documents.type disagrees with what the classifier says the URL/title is — the classifier fix was forward-only, so rows stored under the old wrong type stayed wrong and invisible",
      "round7Findings": [
        "E14"
      ],
      "sourceTask": "T-403",
      "failThreshold": "any documents row whose stored type has a permitted REFINEMENT to what the classifier returns (RHP->PROSPECTUS, ADDENDUM->CORRIGENDUM/PRICE_BAND_AD, BASIS_OF_ALLOTMENT->BASIS_OF_ALLOTMENT_AD). An UNRELATED reclassification is not a nightly FAIL — that is a source/classifier change for a human, surfaced by scraper/src/scripts/retype-misclassified-documents.ts",
      "severity": "P2",
      "unverifiableWhen": "documents table not present"
    },
    {
      "id": "m_extract_failed",
      "findingClass": "a document repeatedly fails extraction, which is a parse bug needing a human rather than a data outage",
      "round7Findings": [],
      "sourceTask": "T-403",
      "failThreshold": "WARN-level only: any row in EXTRACT_FAILED",
      "severity": "P3",
      "unverifiableWhen": "document_fetch_state table not present"
    },
    {
      "id": "m_extraction_stuck",
      "findingClass": "a required document type (DRHP/RHP/PROSPECTUS) on a live-window IPO is stuck in extraction (MANUAL_REVIEW, EXTRACT_FAILED, FAILED behind a HARD_FAILURE marker, or FAILED repeatedly WITHOUT ever escalating to a HARD_FAILURE marker or MANUAL_REVIEW) with nothing FAIL-level watching it",
      "round7Findings": [],
      "sourceTask": "round-5 review-of-the-reviews (#333 follow-up); 4th shape added T-494 (#396, spawnSync ETIMEDOUT never escalating)",
      "registryClass": "silent-terminal-state-invisible-to-audit",
      "failThreshold": "any UPCOMING/OPEN/CLOSED/LISTED IPO's DRHP/RHP/PROSPECTUS document has extraction_status=MANUAL_REVIEW, a sibling document_fetch_state.state=EXTRACT_FAILED, extraction_status=FAILED with an extraction_error starting HARD_FAILURE:, OR extraction_status=FAILED with retry_count between 3 and MAX_EXTRACTION_ATTEMPTS(10) exclusive and NO HARD_FAILURE: marker, for more than 48h",
      "severity": "P1",
      "unverifiableWhen": "never - always runs against documents/document_fetch_state joined to ipos"
    },
    {
      "id": "m_fix_merged_not_served",
      "findingClass": "a merged PR labelled fixes-live-bug is not yet being served in production — the bug it fixed is still live",
      "round7Findings": [],
      "sourceTask": "T-425",
      "registryClass": "merged-fix-never-deployed-while-bug-live",
      "failThreshold": "for every merged PR carrying the `fixes-live-bug` label, the served SHA's commit time (read via git log against the SHA from GET {BASE_URL}/api/version — the same way the T-324 SHA-drift monitor reads it) is older than the PR's mergedAt for more than 24h",
      "warnThreshold": "same comparison, but the merge is under 24h old — a normal deploy-lag window, not yet an outage",
      "severity": "P1",
      "unverifiableWhen": "gh could not list merged fixes-live-bug PRs, OR the served SHA could not be read (unreachable / returned \"unknown\"), OR the served SHA has no locally-known commit time"
    },
    {
      "id": "m_found_not_extracted",
      "findingClass": "a document was downloaded and then never read, so the fields it carries stay empty while the pipeline looks healthy",
      "round7Findings": [],
      "sourceTask": "T-403",
      "failThreshold": "any row FOUND for more than 48h once the extractor is wired; deliberately INERT while ENABLE_DRHP_EXTRACTION is off, since FOUND is terminal by design until WP C",
      "severity": "P2",
      "unverifiableWhen": "document_fetch_state table not present"
    },
    {
      "id": "m_incomplete_pages_unretried",
      "findingClass": "a document whose read was STOPPED before every page was read (extraction envelope status INCOMPLETE_PAGES, D6 ledger row NOT_AVAILABLE_YET with unreadPages) is never re-read, so the filing is permanently missing the pages the ceiling cut — and unlike m_extraction_stuck this document does NOT look stuck: it completed, its fields persisted, and every existing terminal-state check reads it as healthy",
      "round7Findings": [],
      "sourceTask": "OD-55 (owner, 2026-09-11) ceiling-and-records slice, lane B — the per-document extraction budget was removed and replaced by a 2-hour hung-process ceiling that names its unread pages; nothing yet watches whether those named pages are ever read",
      "registryClass": "silent-terminal-state-invisible-to-audit",
      "failThreshold": "any document with a D6 ledger row whose evidence carries a non-empty unreadPages array, where the document's retained PDF still exists (OD-32's seven-day window) and no later extraction at the same or higher EXTRACTOR_VERSION has produced an envelope without unreadPages for that sha256, for more than 48h",
      "severity": "P1",
      "unverifiableWhen": "the retained PDF has already been purged (past OD-32's seven-day window) — the pages can no longer be re-read from it, so the finding is a permanent data gap to report rather than a retry to schedule; the check must say WHICH of the two it found, never fold them together",
      "status": "built 2026-09-20 (item 10): record() call lands in scripts/audit-detection-floor.mjs",
      "consumer": "nightly floor-delta (Notifier post + issue on NEW)",
      "consumerReason": "brief-assigned 2026-09-20: a P1 silent-terminal-state class needs a same-day diffing consumer; signal-ownership R3"
    },
    {
      "id": "m_live_ipo_has_state",
      "findingClass": "the document job silently stopped: a live IPO has no state rows at all, so nothing was even attempted for it",
      "round7Findings": [],
      "sourceTask": "T-403",
      "failThreshold": "any UPCOMING/OPEN/CLOSED IPO with zero document_fetch_state rows. This is the only check that notices the machine wrote NOTHING — every other check reads rows it wrote",
      "severity": "P2",
      "unverifiableWhen": "document_fetch_state table not present"
    },
    {
      "id": "m_not_yet_filed_age",
      "findingClass": "a document the pipeline can never reach settles as NOT_YET_FILED and stays there silently forever — NOT_YET_FILED is deliberately not a failure, which is exactly what makes an unreachable document invisible (T-403 B-1: the SEBI rung could never fire for a DRHP)",
      "round7Findings": [],
      "sourceTask": "T-403",
      "failThreshold": "NOT_YET_FILED past its filing calendar: DRHP >14d from first sight, RHP >2d after open, PROSPECTUS >3d after close (listing is T+3 and a company cannot list before the Prospectus is filed), anchor report >1d after open. A row whose governing date is missing is skipped, never guessed at",
      "severity": "P2",
      "unverifiableWhen": "document_fetch_state table not present"
    },
    {
      "id": "m_upcoming_missing_price_band_tracking",
      "findingClass": "a live IPO walks to its open date with a blank price band because no PRICE_BAND_AD fetch-state row was ever created — an ABSENCE no existing check can see, since every other document check reads FROM document_fetch_state and can only grade rows that exist",
      "round7Findings": [],
      "sourceTask": "#796 (detection gap for the #795 stage-gate deadlock)",
      "failThreshold": "any offering_type='IPO' row with status UPCOMING or OPEN, open_date within 7 days, and NO document_fetch_state row for doc_type='PRICE_BAND_AD'. Anchored on ipos with NOT EXISTS, never on document_fetch_state — the defect is the missing row. Measured on staging 2026-09-19: flagged exactly 2 of 20 IPOs opening within 7 days and passed the other 18, so a PASS carries information",
      "severity": "P1",
      "unverifiableWhen": "document_fetch_state table not present"
    },
    {
      "id": "not_applicable_documents_named",
      "findingClass": "a PENDING document of a type with no extractor is misread as a stuck/backlogged queue instead of a design fact",
      "round7Findings": [],
      "failThreshold": "never fails by design — a document type having no extractor is a fact about the pipeline's scope, not a defect. The check's job is naming, not grading: it reports PASS with the count and type breakdown of PENDING documents outside AUTO_PERSIST_DOC_TYPES (scraper/src/services/filing-auto-persist.ts) so nobody has to re-derive that number by hand and misread it as backlog again.",
      "severity": "P3",
      "unverifiableWhen": "the documents table cannot be read (query error)",
      "why": "Replaces ratios_extraction_yield (retired here — see its entry), which counted RATIOS_BASIS_ISSUE_PRICE documents extracted-with-no-ratio on a type that has no extractor at all, so it reported UNVERIFIABLE/P1 every night for a population that could never move. On 2026-09-15 that read as '38 stuck documents' and cost an hour. This check states the honest fact once, in words that cannot be misread: N documents of never-extracted types (named per type) sit PENDING by design; this is not a queue."
    },
    {
      "id": "p_document_provenance_share",
      "findingClass": "the offer-document path stalls and the site falls back to republishing websites' transcriptions of the filing — the document's share of RECENT provenance writes on printed offer-term fields collapses",
      "round7Findings": [],
      "failThreshold": "document-sourced (source='DRHP') rows are under 5% of all non-ADMIN field_sources rows WRITTEN IN THE LAST 14 DAYS across issueSize, faceValue, priceRangeMin/Max, lotSize, min_investment, issue_price, fresh_issue_size, offer_for_sale_size, registrar, leadManagers, companyName (healthy today, measured on staging 2026-09-08 over the 14-day window: 10.2% — 39 document-sourced vs 342 website-sourced)",
      "severity": "P1",
      "unverifiableWhen": "field_sources is unreadable, or fewer than 20 provenance rows were written on the printed offer-term fields in the 14-day window"
    },
    {
      "id": "pull_admin",
      "designId": "PULL-ADMIN",
      "findingClass": "pull-model verification check PULL-ADMIN: fields skipped for admin reasons with no live protection row",
      "whatItAsks": "fields skipped for admin reasons with no live protection row",
      "healthy": "0",
      "alarmsWhen": "any non-zero — the guard on §2.7",
      "scope": "phase-1 IPOs, MAINBOARD + SME, prod + staging slots (docs/design/data-sourcing-pull-model.md §2.1)",
      "consumer": "per-cycle failure reading (identities)",
      "consumerReason": "same 0/alarms-on-any-nonzero shape as PULL-WRITE, PULL-FROZEN and PULL-NOBLANK — an admin-override guard evaluated against the current cycle’s field state, not an overnight trend",
      "status": "built 2026-09-20 (item 10): record() call lands in scripts/audit-detection-floor.mjs",
      "sourceDesign": "docs/design/data-sourcing-pull-model.md §4 (\"How we would know it worked\")"
    },
    {
      "id": "pull_doc_nay_with_offer_doc",
      "designId": "PULL-DOC-NAY-WITH-OFFER-DOC",
      "findingClass": "a DOC-ranked ipo_field_plan row answers rank1:DOC:NOT_AVAILABLE_YET ('no document yet') although its IPO already holds a COMPLETED offer document — the DOC fetcher's documentType family was narrower than spec §1's 'best available type' (PRICE_BAND_AD family = PRICE_BAND_AD only), so every IPO without a price-band ad (all SME) waited forever. F-161: 984 staging rows on 23 IPOs, 2026-09-24",
      "whatItAsks": "no plan row with rank1_source = DOC has a cause starting rank1:DOC:NOT_AVAILABLE_YET whose last_attempt_at is later than the extracted_at of a COMPLETED, active RHP / DRHP / PROSPECTUS / PRICE_BAND_AD of the same IPO",
      "healthy": "0",
      "alarmsWhen": "any non-zero; one notify per IPO (top 8) naming the fields",
      "scope": "every IPO, every status and segment, prod + staging slots; keyed on the recorded rank-1 cause, not the state, so a row a lower rank supplied provisionally still counts",
      "consumer": "nightly floor-delta",
      "consumerReason": "a structural fact about the DOC fetcher's document matching, read by IPO identity each run; it goes GONE only after the fixed fetcher re-walks the rows",
      "status": "built (item 6, F-161)",
      "sourceDesign": "docs/design/data-sourcing-pull-model.md §1 (DOC = best available type; document type order), §2.4"
    },
    {
      "id": "pull_excused",
      "designId": "PULL-EXCUSED",
      "findingClass": "pull-model verification check PULL-EXCUSED: NOT_PRINTED count per (IPO, document type), NEW vs yesterday by name",
      "whatItAsks": "NOT_PRINTED count per (IPO, document type), NEW vs yesterday by name",
      "healthy": "stable",
      "alarmsWhen": "a document’s excused set grows at all — this is what catches a mis-resolved type",
      "scope": "phase-1 IPOs, MAINBOARD + SME, prod + staging slots (docs/design/data-sourcing-pull-model.md §2.1)",
      "consumer": "nightly floor-delta (Notifier post + issue on NEW)",
      "consumerReason": "the check is explicitly a day-over-day diff (“NEW vs yesterday”), the same NEW/GONE/SAME shape the floor-delta consumer already reads",
      "status": "built 2026-09-20 (item 10): record() call lands in scripts/audit-detection-floor.mjs",
      "sourceDesign": "docs/design/data-sourcing-pull-model.md §4 (\"How we would know it worked\")"
    },
    {
      "id": "pull_exhaust",
      "designId": "PULL-EXHAUST",
      "findingClass": "pull-model verification check PULL-EXHAUST: EXHAUSTED rows by (IPO, field, reason), NEW vs GONE vs SAME",
      "whatItAsks": "EXHAUSTED rows by (IPO, field, reason), NEW vs GONE vs SAME",
      "healthy": "shrinking",
      "alarmsWhen": "any NEW one on a phase-1 IPO",
      "scope": "phase-1 IPOs, MAINBOARD + SME, prod + staging slots (docs/design/data-sourcing-pull-model.md §2.1)",
      "consumer": "nightly floor-delta (Notifier post + issue on NEW)",
      "consumerReason": "explicitly NEW vs GONE vs SAME and paired in the design’s own worked proof with PULL-YIELD as “the same night”’s evidence — a nightly diff, not a per-slot count",
      "status": "built 2026-09-20 (item 10): record() call lands in scripts/audit-detection-floor.mjs",
      "sourceDesign": "docs/design/data-sourcing-pull-model.md §4 (\"How we would know it worked\")"
    },
    {
      "id": "pull_noblank",
      "designId": "PULL-NOBLANK",
      "findingClass": "pull-model verification check PULL-NOBLANK: fields that went from a value to absent this slot",
      "whatItAsks": "fields that went from a value to absent this slot",
      "healthy": "0",
      "alarmsWhen": "any non-zero — this is the guard on §2.6",
      "scope": "phase-1 IPOs, MAINBOARD + SME, prod + staging slots (docs/design/data-sourcing-pull-model.md §2.1); reads field_sources rows with row_key='' (singleton parent tables) whose previous_value was non-blank in the last 24h",
      "consumer": "per-cycle failure reading (identities)",
      "consumerReason": "brief-assigned: “this slot” is a per-cycle fact and the guard exists to catch a single cycle’s bad write before the next one compounds it",
      "status": "built (item 10, OD-42): record('pull_noblank', ...) lands in scripts/audit-detection-floor.mjs (checkS_pullNoblank), invoked from main(); column resolution via scripts/lib/pull-noblank-checks.mjs, unit-tested in scripts/tests/audit-detection-floor.test.mjs",
      "sourceDesign": "docs/design/data-sourcing-pull-model.md §4 (\"How we would know it worked\")"
    },
    {
      "id": "pull_noop",
      "designId": "PULL-NOOP",
      "findingClass": "pull-model verification check PULL-NOOP: writes per cycle ÷ fields re-asked per cycle",
      "whatItAsks": "writes per cycle ÷ fields re-asked per cycle",
      "healthy": "near zero on a day with no filings",
      "alarmsWhen": "rises without a matching document arrival — verification is rewriting unchanged values",
      "scope": "phase-1 IPOs, MAINBOARD + SME, prod + staging slots (docs/design/data-sourcing-pull-model.md §2.1)",
      "consumer": "per-cycle failure reading (identities)",
      "consumerReason": "brief-assigned: the ratio is defined per cycle (“writes per cycle ÷ fields re-asked per cycle”) and needs the cycle’s own write identities to explain a rise",
      "status": "built 2026-09-20 (item 10): record() call lands in scripts/audit-detection-floor.mjs; the 5% ceiling is a RECOMMENDATION per OD-18, to be replaced by a measured number once the check has weeks of output",
      "sourceDesign": "docs/design/data-sourcing-pull-model.md §4 (\"How we would know it worked\")"
    },
    {
      "id": "pull_overrides",
      "designId": "PULL-OVERRIDES",
      "findingClass": "an active field_source_overrides row is past its expiry with no expired_at flag, or is no longer valid against the current manifest (an incapable source, or S-05 document-on-timetable-field, after a manifest drift since the row was set)",
      "whatItAsks": "every field_source_overrides row with expired_at IS NULL is either genuinely still time-active AND valid against the current manifest, or is flagged expired",
      "healthy": "0 violations across every non-administratively-expired override row",
      "alarmsWhen": "a row's expires_at is in the past with expired_at still null, or a still-time-active row ranks a source the current manifest marks incapable, or ranks a document source on an E-1/class-T timetable field",
      "scope": "every row in field_source_overrides with expired_at IS NULL, on whichever database the audit runs against",
      "consumer": "nightly floor-delta (docs/reviews/build-cards/item-03-s4-override-layer.md)",
      "consumerReason": "an override is a same-day production knob with no PR in front of it (Tier A, per the card) — the nightly floor is the only recurring check that a manifest drift since a row was set has not silently invalidated it",
      "status": "built (item 3 slice S4)",
      "sourceDesign": "docs/design/build-cards/item-03-s4-override-layer.md",
      "note": "a missing field_source_overrides table (prod, before this slice's migration ships there) is reported PASS, not UNVERIFIABLE or FAIL -- layer 2 is simply absent on that database, matching the resolver's own table-absent tolerance."
    },
    {
      "id": "pull_plan",
      "designId": "PULL-PLAN",
      "findingClass": "pull-model verification check PULL-PLAN: plan rows per IPO = the committed manifest, and the manifest hash is unchanged",
      "whatItAsks": "plan rows per IPO = the committed manifest, and the manifest hash is unchanged",
      "healthy": "equal",
      "alarmsWhen": "any mismatch, or a manifest hash that moved without a commit",
      "scope": "phase-1 IPOs, MAINBOARD + SME, prod + staging slots (docs/design/data-sourcing-pull-model.md §2.1)",
      "consumer": "per-cycle failure reading (identities)",
      "consumerReason": "the plan is rebuilt every pull cycle (§2.1) — a manifest drift is a per-cycle structural fact, read by identity (IPO, plan row) each slot, not a trend accumulated overnight",
      "status": "built 2026-09-20 (item 10): record() call lands in scripts/audit-detection-floor.mjs",
      "sourceDesign": "docs/design/data-sourcing-pull-model.md §4 (\"How we would know it worked\")"
    },
    {
      "id": "pull_plan_config_gap_at_cap",
      "designId": "PULL-PLAN-CONFIG-GAP-AT-CAP",
      "findingClass": "an ipo_field_plan row retired at the attempts cap (FIELD_PLAN_RECLAIM_MAX_ATTEMPTS) by a GAP — configuration (a manifest rank naming a source with no field mapping, a DOC rank with no documentType, a source with no registered fetcher, a DOC column read not implemented) or extractor (no document provenance on a COMPLETED document) — rather than by a real failure (#884: 6,220 staging rows across 72 IPOs on 2026-09-23)",
      "whatItAsks": "no CHECK_FAILED plan row at attempts >= the cap has a gap cause: a gap-key-stamped cause ([gap-key:...]) or a legacy free-text gap marker (isConfigGapAtCapRow, the pinned mirror of FIELD_PLAN_CONFIG_GAP_CAUSE_MARKERS / FIELD_PLAN_GAP_KEY_PREFIX)",
      "healthy": "0",
      "alarmsWhen": "any non-zero, named by IPO slug and table.field",
      "scope": "every IPO, every status and segment, prod + staging slots; state = CHECK_FAILED, attempts >= cap",
      "consumer": "nightly floor-delta",
      "consumerReason": "a structural fact about the attempts accounting, read by row identity each run — sibling of pull_plan_stuck_reclaim",
      "status": "built (#884)",
      "sourceDesign": "docs/design/data-sourcing-pull-model.md §2.3 (manifest_version reconciliation), OD-62 COVERAGE_GAP",
      "note": "Companion CI half: scraper/tests/unit/config/manifest-rank-coverage-gaps.test.ts lists every manifest rank the walk cannot ask, read through resolveFieldSourcePolicy per IPO type (311 on 2026-09-23: 201 no-mapping, 96 DOC column read not implemented, 7 DOC no-documentType, 7 no-fetcher) against a shrink-only baseline, so a NEW gap fails CI. Review round 1 widened the class to extractor gaps (MAJOR-2)."
    },
    {
      "id": "pull_plan_gap_stalled",
      "designId": "PULL-PLAN-GAP-STALLED",
      "findingClass": "an ipo_field_plan GAP row (cause stamped [gap-key:...]) is re-asked only when its field's gap key changes, so a gap nobody fixes waits indefinitely and is neither capped nor slot-stuck — invisible to pull_plan_config_gap_at_cap and pull_plan_stuck_reclaim (#884 review round 2 MINOR)",
      "whatItAsks": "no CHECK_FAILED gap-stamped plan row has last_attempt_at older than FIELD_PLAN_GAP_STALLED_DAYS (14) days (isStalledGapRow, scripts/lib/field-plan-slot.mjs)",
      "healthy": "0 (WARN, not FAIL: the known gaps are the shrink-only baseline in manifest-rank-coverage-gaps.test.ts)",
      "alarmsWhen": "any non-zero, grouped by table.field with the IPO slugs each field holds (one notify per field)",
      "scope": "every IPO, every status and segment, prod + staging slots; state = CHECK_FAILED, cause gap-stamped",
      "consumer": "nightly floor-delta",
      "consumerReason": "names the fields whose gap has had no manifest edit, adapter change, extractor bump or new document for two weekly cycles — the fix queue for coverage gaps",
      "status": "built (#884 review round 2)",
      "sourceDesign": "docs/design/data-sourcing-pull-model.md §2.3 (manifest_version reconciliation), OD-62 COVERAGE_GAP, OD-78"
    },
    {
      "id": "pull_plan_origin",
      "designId": "PULL-PLAN-ORIGIN",
      "findingClass": "pull-model verification check PULL-PLAN-ORIGIN: a plan row at the current manifest version with no recorded policy_origin",
      "whatItAsks": "every non-terminal ipo_field_plan row whose manifest_version equals the manifest's current version carries a non-null policy_origin",
      "healthy": "0",
      "alarmsWhen": "any non-zero — a row this cycle's generator/walk should have stamped, that carries no record of which configuration produced its ranks",
      "scope": "phase-1 IPOs, MAINBOARD + SME, prod + staging slots (docs/design/build-cards/item-03-s1a-resolver.md)",
      "consumer": "per-cycle failure reading (identities)",
      "consumerReason": "same 0/alarms-on-any-nonzero shape as PULL-ADMIN/PULL-WRITE/PULL-FROZEN/PULL-NOBLANK — policy_origin is the record of WHICH resolver configuration (registry version or override) produced a row's ranks; a current-version row with none means the generator or walk wrote the row without going through resolveFieldSourcePolicy, which is exactly the reused-vs-rewritten class item 3 S1a exists to prevent",
      "status": "built 2026-09-20 (item 10): record() call lands in scripts/audit-detection-floor.mjs",
      "sourceDesign": "docs/design/build-cards/item-03-s1a-resolver.md"
    },
    {
      "id": "pull_plan_rank",
      "designId": "PULL-PLAN-RANK",
      "findingClass": "a non-terminal ipo_field_plan row whose stored ranks disagree with the manifest policy for its own manifest_version",
      "whatItAsks": "every non-terminal plan row's rank1/rank2/rank3 match the manifest's ranked set for that field, IPO type, at the row's own manifest_version",
      "healthy": "0",
      "alarmsWhen": "any non-zero",
      "scope": "phase-1 IPOs, MAINBOARD + SME, prod + staging slots; non-terminal (state <> SUPPLIED) rows only — a SUPPLIED row's ranks are frozen audit trail, not a live mis-plan",
      "consumer": "nightly floor-delta (docs/reviews/build-cards/item-03-s6-churn-stop-and-detection.md)",
      "consumerReason": "the plan is reconciled to the manifest on generation, not every cycle — a rank disagreement is a per-run structural fact, read by identity (plan row) each run",
      "status": "built (item 3 slice S6)",
      "sourceDesign": "docs/design/build-cards/item-03-s6-churn-stop-and-detection.md"
    },
    {
      "id": "pull_plan_stuck_reclaim",
      "designId": "PULL-PLAN-STUCK-RECLAIM",
      "findingClass": "a non-terminal ipo_field_plan row (NOT_AVAILABLE_YET or CHECK_FAILED) that is reclaimable in principle but has gone 2+ discovery slots unclaimed — the class #762 found: claimNextDueField shipped as state='PENDING' only and left 12,480 such rows permanently unasked with zero PENDING rows anywhere",
      "whatItAsks": "every non-terminal, below-attempts-ceiling, currently-unclaimed plan row has a last_attempt_at within the last ~2 discovery slots (7h) — a longer gap means the claim query has stopped reclaiming that state",
      "healthy": "0",
      "alarmsWhen": "any non-zero",
      "scope": "phase-1 IPOs, MAINBOARD + SME, prod + staging slots; state IN (NOT_AVAILABLE_YET, CHECK_FAILED), attempts < FIELD_PLAN_RECLAIM_MAX_ATTEMPTS, claimed_at IS NULL",
      "consumer": "nightly floor-delta (docs/reviews/build-cards/item-03-s6-churn-stop-and-detection.md)",
      "consumerReason": "a stuck reclaim is a per-cycle structural fact about the claim query, read by row identity each run — the same consumer as pull_plan_rank, which this check is a sibling of",
      "status": "built (#762 S8)",
      "sourceDesign": "docs/design/build-cards/item-05-ipo-field-plan-table.md"
    },
    {
      "id": "pull_policy",
      "designId": "PULL-POLICY",
      "findingClass": "committed field-manifest.json drifts from what generate-field-manifest.mjs produces from the spec",
      "whatItAsks": "committed field-manifest.json equals what generate-field-manifest.mjs produces from docs/design/field-source-resolution.spec.mjs",
      "healthy": "exit 0 (byte-identical, EOL-normalized)",
      "alarmsWhen": "generate-field-manifest.mjs --check exits non-zero",
      "scope": "the single committed scraper/config/field-manifest.json (all classes, all three IPO types, version 2)",
      "consumer": "nightly floor-delta (docs/reviews/build-cards/item-03-s6-churn-stop-and-detection.md)",
      "consumerReason": "the manifest is regenerated by hand-triggered --write, never automatically; a drift is a structural fact read once per run, not a trend",
      "status": "built (item 3 slice S6)",
      "sourceDesign": "docs/design/build-cards/item-03-s6-churn-stop-and-detection.md"
    },
    {
      "id": "pull_type",
      "designId": "PULL-TYPE",
      "findingClass": "pull-model verification check PULL-TYPE: plan rows whose resolved ranks do not match the IPO’s current type; IPOs with a null segment",
      "whatItAsks": "plan rows whose resolved ranks do not match the IPO’s current type; IPOs with a null segment",
      "healthy": "0 / 0",
      "alarmsWhen": "any non-zero",
      "scope": "phase-1 IPOs, MAINBOARD + SME, prod + staging slots (docs/design/data-sourcing-pull-model.md §2.1)",
      "consumer": "per-cycle failure reading (identities)",
      "consumerReason": "ranks are resolved at plan-build time each cycle (§2.3.5) — a rank/type mismatch is a fact about this cycle’s plan, read by identity (IPO, plan row)",
      "status": "built 2026-09-20 (item 10): record() call lands in scripts/audit-detection-floor.mjs",
      "sourceDesign": "docs/design/data-sourcing-pull-model.md §4 (\"How we would know it worked\")",
      "note": "The RANK half of PULL-TYPE is covered by pull_plan_rank; this check owns the null-segment half — an IPO with no segment has every rank resolved for a guessed type."
    },
    {
      "id": "pull_walk",
      "designId": "PULL-WALK",
      "findingClass": "pull-model verification check PULL-WALK: every phase-1 IPO walked this slot",
      "whatItAsks": "every phase-1 IPO walked this slot",
      "healthy": "19 of 19",
      "alarmsWhen": "fewer than all, twice running — and separately, phase-1 count ≥ 1",
      "scope": "phase-1 IPOs, MAINBOARD + SME, prod + staging slots (docs/design/data-sourcing-pull-model.md §2.1)",
      "consumer": "per-cycle failure reading (identities)",
      "consumerReason": "brief-assigned: a per-slot walk count is read each cycle, by which IPO was skipped, not accumulated as a nightly trend",
      "status": "built 2026-09-20 (item 10): record() call lands in scripts/audit-detection-floor.mjs",
      "sourceDesign": "docs/design/data-sourcing-pull-model.md §4 (\"How we would know it worked\")"
    },
    {
      "id": "pull_write",
      "designId": "PULL-WRITE",
      "findingClass": "pull-model verification check PULL-WRITE: plan rows marked SUPPLIED whose write returned skipped",
      "whatItAsks": "plan rows marked SUPPLIED whose write returned skipped",
      "healthy": "0",
      "alarmsWhen": "any non-zero",
      "scope": "phase-1 IPOs, MAINBOARD + SME, prod + staging slots (docs/design/data-sourcing-pull-model.md §2.1)",
      "consumer": "per-cycle failure reading (identities)",
      "consumerReason": "brief-assigned: a SUPPLIED-but-skipped write is a per-cycle plan/write mismatch, resolved to the plan row that produced it",
      "status": "built 2026-09-20 (item 10): record() call lands in scripts/audit-detection-floor.mjs",
      "sourceDesign": "docs/design/data-sourcing-pull-model.md §4 (\"How we would know it worked\")"
    },
    {
      "id": "pull_write_policy",
      "designId": "PULL-WRITE-POLICY",
      "findingClass": "a field_sources row written by a source the manifest policy does not rank for that field/IPO-type",
      "whatItAsks": "the last 24h of field_sources writes were made by a source in the manifest's ranked set for that field and IPO type (ADMIN exempt)",
      "healthy": "0",
      "alarmsWhen": "any non-zero, sampled over the last 24h of field_sources.updated_at",
      "scope": "phase-1 IPOs, MAINBOARD + SME, prod + staging slots",
      "consumer": "nightly floor-delta (docs/reviews/build-cards/item-03-s6-churn-stop-and-detection.md)",
      "consumerReason": "a write from an unranked source is a per-cycle write-path fact, resolved to the field_sources row that produced it",
      "status": "built (item 3 slice S6)",
      "sourceDesign": "docs/design/build-cards/item-03-s6-churn-stop-and-detection.md"
    },
    {
      "id": "pull_yield",
      "designId": "PULL-YIELD",
      "findingClass": "pull-model verification check PULL-YIELD: of the fields the offer document should print for a mainboard/SME IPO, how many round 1 supplied",
      "whatItAsks": "of the fields the offer document should print for a mainboard/SME IPO, how many round 1 supplied",
      "healthy": "rising toward 100%",
      "alarmsWhen": "falls, or the denominator moves more than 5% overnight",
      "scope": "phase-1 IPOs, MAINBOARD + SME, prod + staging slots (docs/design/data-sourcing-pull-model.md §2.1)",
      "consumer": "nightly floor-delta (Notifier post + issue on NEW)",
      "consumerReason": "the design’s own worked proof reads this as an overnight trend (“PULL-YIELD rising ... on the same night”, §4) and the alarm condition is explicitly framed as an overnight denominator move",
      "status": "built 2026-09-20 (item 10): record() call lands in scripts/audit-detection-floor.mjs",
      "sourceDesign": "docs/design/data-sourcing-pull-model.md §4 (\"How we would know it worked\")"
    },
    {
      "id": "q_field_sources_row_key_coverage",
      "findingClass": "a child row in a multi-row table (financial_statements / promoters / ipo_intermediaries / peer_companies) carries no field_sources entry for its own row_key — provenance was written under the '' catch-all default, so a fact from FY2023 is recorded against the same key as FY2024 and the second write silently overwrites the first. Nothing in the schema stops it ('' is a legal row_key), so only an independent read can see it.",
      "round7Findings": [],
      "sourceTask": "item 1 slice s8 (docs/design/build-cards/item-01-child-table-consolidated-writer.md)",
      "failThreshold": "any (ipo_id, table_name) pair with MORE THAN ONE row in one of the four child tables, where that pair already has at least one NON-EMPTY row_key in field_sources (i.e. a row-keyed writer is live for it) and at least one derived child row key has no field_sources row",
      "severity": "P1",
      "unverifiableWhen": "every multi-row (ipo, table) pair carries provenance ONLY under the '' catch-all key — the row-keyed writer is not live yet, so per-row provenance cannot be judged. This was the state until item 1 slice s7a: `promoters`, `ipo_risk_factors`, `ipo_intermediaries` and `peer_companies` now route through `consolidatedUpsertChildRows` with their own row keys whenever ENABLE_CHILD_TABLE_CONSOLIDATION is on for the slot, so each of those pairs starts being judged for real on the first cycle after that flag reaches the slot; a slot with the flag still off stays UNVERIFIABLE, correctly. It reports UNVERIFIABLE rather than PASS on purpose: a PASS here would be permanently, meaninglessly green through exactly the transition the check exists to police, and indistinguishable from a broken check. Also UNVERIFIABLE when the child tables or field_sources are unreadable.",
      "note": "The enforcement switch is automatic and PER PAIR — the moment field_sources holds one non-empty row_key for a pair, that pair is judged for real. There is no flag to flip and no way for the check to go permanently green by accident. Row keys are derived from each child row's OWN identity columns (fiscal_year:basis; normalized promoter name; role:normalized name; normalized peer company name — the item-01 card's row-key table), never from the denormalized normalized_name column the path under audit wrote. Distinct from d_segment_provenance (one field on the singleton ipos row) and p_document_provenance_share (a source-mix ratio): this is the only check that asserts provenance exists PER CHILD ROW. NAMED HOLE (F-101, s7a review 2026-09-11, open): the not-yet-keyed guard now has a SECOND producer. When filing-persister's child consolidator is absent or throws, every row of that (ipo, table) pair is written with no per-row provenance, the pair's field_sources rows are all '', and this check excuses it as 'writer not live' - UNVERIFIABLE, not FAIL. The PARTIAL case is caught; the total case is not, and it is the likelier one because the fallback is per-table. A single-row pair is likewise out of the class. Named rather than patched; the interim surface is filing-persister's unresolved_child_rows line. Prior residual limit: a writer that lands and keys NOTHING leaves every pair NOT-YET-KEYED, so this check pages P2 nightly rather than FAILs — loud, not silent, but the writer slice's own row-key round-trip test is the hard gate for that case."
    },
    {
      "id": "r_provenance_parent_not_null",
      "findingClass": "a field_sources row names a field whose value on the parent row is NULL — a false claim that a source supplied a value which does not exist, so every audit reading field_sources reports the field as sourced and healthy while the page shows nothing",
      "round7Findings": [],
      "sourceTask": "#654 (2026-09-16): two IPOs that were OPEN and UPCOMING got no NSE documents because their NSE symbol was missing, while field_sources said CHITTORGARH had supplied it; 650 rows across 14 fields on staging, 453 across 13 on prod",
      "registryClass": "absence-that-looks-like-a-value",
      "failThreshold": "any field_sources row whose named field resolves to a column on its parent table (ipos / ipo_details) and whose value in that column IS NULL; generic over columns (read from information_schema) so a field a future scraper starts writing is covered without being added here; NULL only, never falsiness, because 0/false/'' are values a source genuinely supplied",
      "severity": "P1",
      "unverifiableWhen": "field_sources or a parent table is not readable — reported as UNVERIFIABLE with the error, never as a PASS. A field_name that matches no column on the parent table is reported as a NOTE rather than counted: that is a different defect (provenance for a field the table does not have, e.g. ipos.listingExchange) and folding it in would hide it inside this check's zero."
    },
    {
      "id": "s7_consensus_verdict",
      "findingClass": "a field_sources.verdict does not match what CONFIRMED/DISPUTED/UNCONFIRMED/SINGLE_SOURCE/NO_WITNESS the row's own stored witnesses actually support -- the verdict writer (S3b-2, dormant behind ENABLE_VERDICT_WRITER) computed the wrong state, or a row carries no verdict at all despite having 2+ real witness answers on a non-ABSTAIN field",
      "failThreshold": "any field_sources row (non-ABSTAIN field, manifest-mapped) whose stored verdict differs from the verdict re-derived by the SAME computeVerdict function the writer calls, given the row's own witnesses and its IPO's segment-scoped capable-source count",
      "severity": "P1",
      "unverifiableWhen": "the target database is unreachable (hard FATAL, exit 1 -- never a silent skip, matching assert-row-key-constraints.ts's philosophy)",
      "sourceTask": "S7 (docs/design/s7-consensus-check-plan.md) half 1 -- the justification for ever flipping ENABLE_VERDICT_WRITER on",
      "scope": "every field_sources row across all three segments (MAINBOARD, SME_BSE, SME_NSE), all IPO statuses, both slots (prod/staging), rows written before AND after this check exists. ABSTAIN fields (14 in the manifest) are excluded by design -- a null verdict there is correct.",
      "consumer": "operator-run pre-flip gate (this check is the reason ENABLE_VERDICT_WRITER is safe to turn on); not yet wired into the nightly cron -- same posture as assert-row-key-constraints.ts (a hand-run verification script, not (yet) a scheduled gate)",
      "consumerReason": "the verdict column is empty on every slot today (ENABLE_VERDICT_WRITER is set nowhere) -- there is nothing for a nightly cadence to accumulate yet; this check exists so that WHEN the flag flips, its first real-data run is not the first time anyone looked",
      "status": "built and self-tested this session (8/8 unit tests, RED-then-GREEN proof on ipodhan_test, mutation-tested); not yet wired into scripts/audit-detection-floor.mjs's nightly run -- follow-up once ENABLE_VERDICT_WRITER actually flips",
      "auditScript": "scripts/audit-consensus-verdict.ts",
      "selfTest": "scripts/tests/audit-consensus-verdict.test.ts"
    },
    {
      "id": "s_settled_field_rewritten",
      "findingClass": "a settled ipos field (price band, lot, issue size, face value, open/close/listing/allotment date, registrar, lead managers) re-stamped by an identical value, rewritten by a source the writer ranks equal or lower, or changed by the source that set it without a refresh right, after the best-ranked source has set it (OD-73 / OD-65 / OD-75)",
      "round7Findings": [],
      "failThreshold": "any field_sources row for a settled ipos field written in the last 24 h whose previous_value equals the stored value (IDENTICAL_RESTAMP), whose new source the writer's getSourcePriority ranks equal or lower than the previous source (EQUAL_RANK_REWRITE / LOWER_RANK_REWRITE), or whose source changed its own value where allowsSameSourceRefresh grants it no refresh (SELF_CHANGE_REWRITE, OD-75); an exchange postponing a date it stated (OD-35) and a later offer document are allowed because the writer allows them",
      "severity": "P2",
      "unverifiableWhen": "never — always runs against field_sources joined to ipos",
      "spec_ref": [
        "3.2"
      ],
      "note": "#908. Ranked through the WRITER's own answers (review round 1 on PR #914, MAJOR-2): scraper/config/writer-source-ranking.json is generated from getSourcePriority/allowsSameSourceRefresh per field, source, IPO type, venue and ENABLE_POLICY_WRITER state (scraper/scripts/build-writer-ranking-snapshot.ts; tests/unit/config/writer-source-ranking.test.ts fails on a stale file); the audit applies the slot's flag the way slotAwareFlagDefault does. First staging run 2026-09-23 (before the fix deploys, manifest-ranked): 103 IDENTICAL_RESTAMP rows across 24 IPOs in 24 h (Adroit Industries 126 -> 126 among them), 40 settled-field writes passed."
    }
  ],
  "notCoveredByThisManifest": [
    "SPEC ONLY - this check is NOT implemented. It has no record('anchor_report_yield') call in scripts/audit-detection-floor.mjs, so it stays in notCoveredByThisManifest rather than being counted as floor coverage; counting it would read as wider coverage than the floor actually has (scripts/tests/audit-detection-floor.test.mjs case 79). Why it is owed: #437 slices 1 and 2 each fixed a mechanism that turned scanned anchor letters into zero published rows (the table geometry, then the scanner's separator damage), and in BOTH cases the only reason anyone knew was a human reading a MANUAL_REVIEW queue. No nightly signal exists that says 'this IPO has an anchor letter on file and publishes no anchor investors', so the next member of the class will again be found by hand, weeks late. Slice 2 deliberately did not implement it: merging an audit check without a real-data run against staging is the exact failure the defect-fix contract names (an audit check merged unrun crashed the nightly audit for three nights), and this task was barred from every live database.",
    "Design-time, and deliberately NOT in `checks` until the audit script can actually record it. Item 8b slice 1 (this change) teaches the extractor to READ the three lines; slice 2 derives and persists the ratios. Until a row can carry a ratio there is nothing for a nightly audit to look at, and an entry in `checks` with no record() call is a paper check that reads as wider coverage than the floor really has. It moves to `checks` with its record('balance_sheet_located') call when 8b-2 lands.",
    "c1_hard_date_trust_on_update (#180 F2, T-459) — a single non-authoritative source asserting open_date/close_date on UPDATE onto a previously-null field with zero field_sources corroboration is not a separately-recorded check: guarded at rest by the existing g_provenance_lineage C1 lineage-ceiling gate (scripts/audit-ipo-coverage.mjs:501 — a hard date with zero field_sources lineage is the same signature) and at write-time by scraper/tests/unit/services/data-persister-180-sme-fpo-hard-date.test.ts.",
    "Item 13 shipped the WRITE-PATH gate only (scraper/src/services/filing-persister.ts, reconcileFreshAndOfs): a filing extraction can no longer write a non-reconciling pair. This audit check is the independent read-side half and is NOT implemented yet - it has no record('c_fresh_ofs_reconciliation') call in scripts/audit-detection-floor.mjs, so it stays in notCoveredByThisManifest rather than being counted as floor coverage. It is owed because the gate only guards ONE write path: rows written before the gate, and any future writer of ipo_details, are invisible to it. Merging the audit check without a real-data run against staging is the exact failure the defect-fix contract names (an audit check merged unrun crashed the nightly audit for three nights), and this task was barred from every live database.",
    "specified by item 21 slice 4 (the provenance line shipped 2026-09-11); no record('c_provenance_line_present') call exists in scripts/audit-detection-floor.mjs yet, because the check needs a staging fetch of a rendered detail page and lane B paused before that was built. Listing it under checks while nothing records it would read as wider coverage than the floor actually has (audit-detection-floor.test.mjs case 79).",
    "the closed-IPO job stops making progress silently — the same (ipo_id, cause_class) pair fails night after night and reads as steady state rather than as a stuck backlog",
    "There is no `record('document_download_refusals'` call in scripts/audit-detection-floor.mjs yet, so this is a DESIGN-TIME check, not a live one. Per .claude/rules/recurrence-detection-gate.md it stays in notCoveredByThisManifest until that call lands — listing it under `checks` before the audit implements it would be a paper check, would fail scripts/tests/audit-detection-floor.test.mjs case 79, and would read as wider coverage than the floor actually has. What EXISTS today is the per-refusal log line (`OD-37 download refusal: ...`, carrying host, url, ipoKey and reason) plus the `refused:resolved_private_address` attempt outcome; what is MISSING is a nightly consumer that counts them and diffs against the previous run.",
    "This check runs in its own script, scripts/audit-field-extraction-failures.mjs, not in scripts/audit-detection-floor.mjs — there is no record('field_extraction_failure_rate') call in the floor audit, so listing it under 'checks' would read as wider floor coverage than the floor actually has (T-487 paper-check rule). It moves to 'checks' when the floor audit absorbs it and a threshold exists.",
    "IMPLEMENTED, not a spec-only placeholder: scripts/ci/check-field-manifest-current.mjs is wired into .github/workflows/pr-gate.yml's `gate` job (one step, item 3 slice S0b) and has a mutation-proof self-test (scripts/tests/check-field-manifest-current.test.mjs) that reproduces the 10 pre-existing hand-written rows exactly (minus two documented spec corrections — Moneycontrol's retirement and an SME_NSE/BSE exclusion, both already true on origin/main before this slice) and asserts a one-source mutation is caught. This runs in CI on every PR, not the nightly floor, so it stays in notCoveredByThisManifest per docs/reviews/detection-checks/_meta.json's schema note (that manifest classifies nightly-audit coverage; a PR-gate check is a different consumer, scripts/ci/require-detection-change.mjs's own registry, not scripts/audit-detection-floor.mjs).",
    "P2-9 / P3-4 (empty content tables, fieldCompleteness thresholds) — owned by T-331's threshold work on audit-ipo-coverage.mjs, not duplicated here",
    "P3-3 (always-red Production Verification GitHub Actions workflow) — owned by T-326",
    "P2-7 ecosystem.config.js dead-config class beyond TZ — owned by T-327's assert-env-keys.sh work; this manifest's h_pm2_env_tz check is the audit-side companion",
    "g_provenance_lineage (#188 C1: hard date/band asserted with zero field_sources lineage rows) — owned by scripts/audit-ipo-coverage.mjs --gate (declining-ceiling WARNING against a committed baseline, config/provenance-lineage-baseline.json, not a hard 0-gate, until the T-292 legacy-row drain completes)",
    "g_duplicate_identity (#188 C2: two different genuine IPOs holding byte-identical date/band/lot/issue_size) — owned by scripts/audit-ipo-coverage.mjs --gate (HARD, MUST be 0, except entries on config/duplicate-identity-allowlist.json which WARN)",
    "g_deployed_sha_lineage (T-297 discovery-coverage sweep, #194) — the sha deploy-linux.sh is about to flip to must be an ancestor of origin/main (T-264 P2-4: prod once served a commit that existed only on an unmerged branch while missing main's merged fix). This is a deploy-time gate (assert_deployed_sha_lineage() in scripts/deploy-linux.sh, run right after $SHA is resolved and again, idempotently, right before the flip), not a nightly DB-row audit — it cannot be a checks[] entry here since this manifest's own wire-or-retire test (audit-detection-floor.test.mjs) requires every declared id to be record()-ed by audit-detection-floor.mjs, and this check never runs inside that script. ALLOW_UNMERGED_DEPLOY=1 is the deliberate escape hatch for a genuine pre-merge hotfix; it fires a Notifier P2 naming the sha+branch. Guarded instead by scripts/tests/deploy-linux.test.sh case 32 (a-e) (see docs/reviews/failure-classes.md, 'deployed sha not on origin/main').",
    "g_test_coverage_negative_grep (T-470, issue #197) — CI-time (pr-gate.yml, every PR) negative grep for untested web data components / scraper validation rules / priority-matrix field keys, enforced by web/tests/unit/meta/coverage-negative-grep.test.ts + scraper/tests/unit/meta/coverage-negative-grep.test.ts against config/coverage-negative-grep-baseline.json (shrink-only ratchet) and config/coverage-negative-grep-allowlist.json (reasoned permanent exemptions). Not a DB audit check, so it is not recorded by audit-detection-floor.mjs / this manifest's checks[] array.",
    "g_rollout_flag_liveness (T-297 D9, issue #193) — a *_PERCENTAGE/ENABLE_* rollout flag silently zeroed/disabled in the prod env file (the CONSOLIDATION_PERCENTAGE=0 / T-282 class). This is a deploy-time gate, not a nightly DB-row audit: `scripts/assert-env-keys.sh`'s `assert_rollout_flags_live()` reads `// LIVE-GATE` / `// PROD-REQUIRED-TRUE` markers off `scraper/src/config/feature-flags.ts` and refuses the deploy before build, on the prod slot only. It cannot be a `checks[]` entry here — this manifest's own wire-or-retire test (`audit-detection-floor.test.mjs`) requires every declared id to be `record()`-ed by `audit-detection-floor.mjs`, and this check never runs inside that nightly audit. Guarded instead by `scripts/tests/assert-env-keys.test.sh` (see `docs/reviews/failure-classes.md`, 'rollout-flag zeroed silently voids live logic').",
    "g_seo_surface_completeness / g_seo_surface (#190, T-464) — sitemap completeness vs the ROUTES SSOT + live IPO slugs, duplicate IPO slug groups, and page-type <title> collisions. HTTP-only, run inside scripts/audit-prod.mjs (three record() calls prefixed g_seo_surface:), not audit-detection-floor.mjs, so it cannot be a checks[] entry here — this manifest's own wire-or-retire test requires every declared id to be record()-ed by audit-detection-floor.mjs. Predicates live in scripts/lib/seo-surface-checks.mjs (self-tested by scripts/tests/seo-surface-checks.test.mjs); auditScript for this class is scripts/audit-prod.mjs.",
    "g_cache_poison_bisect (T-463, issue #189) — a paginated list API's cache poisoned exactly at the page's default `limit` parameter value (limit=20 once served 240 stale rows with blank issuePrice for 19h while limit=19/21 served the correct set). This is an HTTP-only prod probe, not a nightly DB-row audit: `scripts/audit-prod.mjs` runs `bisectDefaultParameter()` (from `scripts/lib/cache-poison-bisect.mjs`) against GET /api/ipos/history at limit=19/20/21. It cannot be a `checks[]` entry here — this manifest's own wire-or-retire test (`audit-detection-floor.test.mjs`) requires every declared id to be `record()`-ed by `audit-detection-floor.mjs`, and this check never runs inside that nightly DB audit. Guarded instead by `scripts/tests/cache-poison-bisect.test.mjs`.",
    "g_repair_held (#192, T-466) — a repair is not done until it survives a real scraper cycle: scripts/assert-repair-held.mjs <invariant> --cycles N records the violation count (must be 0), records a cycle marker (max(last_scraped_at/updated_at) over ipos + latest scraper_logs cycle id), polls until N distinct real cycles have passed, and re-runs the invariant after each. This is a PER-TASK last-mile tool invoked by a fix task at merge time, not a nightly DB-row audit — it cannot be a checks[] entry here since this manifest's own wire-or-retire test (audit-detection-floor.test.mjs) requires every declared id to be record()-ed by audit-detection-floor.mjs, and this tool never runs inside that script. auditScript for this class is scripts/assert-repair-held.mjs (self-test scripts/tests/assert-repair-held.test.mjs); the shipped worked-example invariant is scripts/lib/repair-invariants/issue-size-t451.mjs. Wired into defect-fix-contract.md item 5 and docs/ops/prod-ops-recipes.md section 8 as the mandatory last step of every data-repair fix task.",
    "T-477 / #224 (ipo_financials fill-rate, DASHBOARD row 'ipo_financials (of financial_data pop.)') — owned by audit-ipo-coverage.mjs's own checks[] array (informational threshold, not a hard gate), not duplicated here",
    "g_zero_ssr_routes (#201, T-473) — a page whose initial HTML is chrome-only ('Loading...' and no data rows) because its data is fetched client-side; HTTP-only probe inside scripts/audit-prod.mjs (ZERO_SSR_ROUTES: fetch the route with no JS and require the real-content marker, e.g. <table><tbody><tr> for /history, or a resolved (non-loading, non-disabled) control for /dashboard's sector filter), not audit-detection-floor.mjs, so it cannot be a checks[] entry here — this manifest's wire-or-retire test requires every declared id to be record()-ed by audit-detection-floor.mjs. Guarded by web/tests/e2e/history-ssr.spec.ts and web/tests/e2e/dashboard-ssr.spec.ts (both JS disabled) and extended per page as each gets the server-service pattern.",
    "status or a price band sourced from an exchange that does not list the IPO — a non-listing venue has no standing on an issue it does not carry (OD-64)",
    "a write path resolves a field its extractor never supplied — context passed as a claim, re-stamping provenance and auto-resolving conflicts about fields the document never read (OD-66)",
    "PULL-FROZEN (pull-model verification check PULL-FROZEN: SUPPLIED rows whose chosen_document_id has been superseded) - specified by docs/design/data-sourcing-pull-model.md (\"How we would know it worked\"), not yet implemented in scripts/audit-detection-floor.mjs; owner decision OD-42 build item 10, consumer already named below. Move back to \"section\": \"checks\" once a record('pull_frozen' call lands in the audit script.",
    "Retired 2026-09-24 (item 10 review round): the design's own §4 table lists one check for this ratio, PULL-NOOP. This entry duplicated it under a second id with a weaker gate; removed rather than kept alongside pull_noop.",
    "Retired 2026-09-16: RATIOS_BASIS_ISSUE_PRICE has no extractor in scraper/src/services/filing-auto-persist.ts (AUTO_PERSIST_DOC_TYPES) by design, so this check measured a population that can never move and paged UNVERIFIABLE/P1 every night (cost an hour on 2026-09-15, misread as 38 stuck documents); issuer_ratio_yield reads the real ratio population (RHP/DRHP/PROSPECTUS financial_data rows), and not_applicable_documents_named now names this PENDING count honestly instead of grading it. Replaced by issuer_ratio_yield (#670); the never-extractable population is now named by not_applicable_documents_named.",
    "Retired 2026-09-21: OD-65 (owner, 2026-09-21) withdrew the re-read loop this check measures: \"everything should be done on day one itself, and it should not be repeated for the same IPO... one IPO, one round of document read.\" The loop was never built (zero `reread` hits in scraper/src), and it is now explicitly not wanted, so this check measures a population that can never become non-empty — the same shape ratios_extraction_yield was retired for on 2026-09-16. It was also mis-sized from the start: the loop was scoped from \"578 leadManagers conflicts\" and the real count, after removing same-source rows, null-on-one-side rows and byte-identical values, is 1 on staging and 2 on prod. Its §3.2 premise additionally contradicts OD-32, which deletes the PDF bytes the check's receipt requires (#853).",
    "Retired 2026-09-21: OD-65 (owner, 2026-09-21) withdrew the re-read loop this check measures: \"everything should be done on day one itself, and it should not be repeated for the same IPO... one IPO, one round of document read.\" The loop was never built (zero `reread` hits in scraper/src), and it is now explicitly not wanted, so this check measures a population that can never become non-empty — the same shape ratios_extraction_yield was retired for on 2026-09-16. It was also mis-sized from the start: the loop was scoped from \"578 leadManagers conflicts\" and the real count, after removing same-source rows, null-on-one-side rows and byte-identical values, is 1 on staging and 2 on prod. Its §3.2 premise additionally contradicts OD-32, which deletes the PDF bytes the check's receipt requires (#853).",
    "Retired 2026-09-21: OD-65 (owner, 2026-09-21) withdrew the re-read loop this check measures: \"everything should be done on day one itself, and it should not be repeated for the same IPO... one IPO, one round of document read.\" The loop was never built (zero `reread` hits in scraper/src), and it is now explicitly not wanted, so this check measures a population that can never become non-empty — the same shape ratios_extraction_yield was retired for on 2026-09-16. It was also mis-sized from the start: the loop was scoped from \"578 leadManagers conflicts\" and the real count, after removing same-source rows, null-on-one-side rows and byte-identical values, is 1 on staging and 2 on prod. Its §3.2 premise additionally contradicts OD-32, which deletes the PDF bytes the check's receipt requires (#853).",
    "zip_member_rows: SPEC ONLY - not implemented in scripts/audit-detection-floor.mjs, so it stays in notCoveredByThisManifest (counting it would read as wider coverage than the floor has; scripts/tests/audit-detection-floor.test.mjs case 79). Why not implemented here: the check needs the zip's member list, which lives only in the archive on the exchange's host; the nightly floor is a database read, and adding network reads of 41+ exchange archives to it, merged without a real-data run against staging, is the exact failure the defect-fix contract names. What guards the class today: the CI unit test scraper/tests/unit/services/zip-member-documents.test.ts drives the real runner over a zip built from real F-154 member names and asserts the exact rows written (count, type, part_number, GID writes nothing), and fails when the one-member unwrap is restored (mutation run in the item 22 zip-members PR)."
  ],
  "exitCodes": {
    "0": "every check PASSed",
    "1": "at least one check FAILed — a defect is live",
    "2": "the audit itself crashed or could not start",
    "3": "no FAIL, but at least one check is UNVERIFIABLE — the audit was blind tonight, not green (FAIL dominates when both occur)"
  },
  "paging": {
    "model": "one DIGEST page per check per night (title, failing-row count, first 10 rows, pointer to the full report on the box) — never one page per row",
    "severityRule": "P1 when the check has rows that were NOT present in the previous run (state persisted in the audit state dir); P2 for an unchanged backlog",
    "dedupeKey": "detection-floor-<checkId>-<YYYY-MM-DD> for FAIL digests; detection-floor-unverifiable-<checkId>-<YYYY-MM-DD> for UNVERIFIABLE pages — date-scoped so the Notifier 30-minute cooldown cannot swallow the nightly digest",
    "unverifiablePages": "every UNVERIFIABLE check pages P2; an UNVERIFIABLE check is never treated as a pass"
  }
}
