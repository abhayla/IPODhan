{
  "class_id": "a container is unwrapped to one member, and the other members are silently dropped",
  "feature": "any download that arrives as an archive or bundle (zip, multi-attachment mail, multi-file API response) where the reader picks ONE member to keep",
  "symptom": "a bundle is treated as a wrapper around one file, so the unwrap step chooses a member (by name, else the largest) and discards the rest without a trace. When the bundle actually carries several distinct documents, every member except the chosen one is lost, and anything the lost members would have changed never happens. Nothing fails: the chosen member is valid, the store has a row, and the missing documents are absent rows, which no per-row check can see. Universal shape: an unwrap step that returns one thing from a container of N answers a narrower question (which member is the one we wanted?) than the one the data poses (what did the container hold?).",
  "first_seen": "2026-09-24, IPODhan F-154: all 41 distinct NSE RHP/DRHP/PROSPECTUS zips on staging hold 2 or more PDFs; 5 hold a corrigendum or price-band notice; selectZipMemberForType keeps the RHP and drops them, and 4 of those 5 IPOs (hy-tech-engineers-ltd, abh-healthcare-ltd, madhur-knit-crafts-ltd, rays-of-belief-ltd) have no CORRIGENDUM/PRICE_BAND_AD document at all, so OD-30 corrigendum precedence cannot fire for them.",
  "fix_prs": "item 22 zip-members PR #969 (branch feat/item22-zip-members): the unwrap reports every PDF member; each non-GID member typed as a type other than the main document's is stored as its own document row keyed by a STABLE member identity, url '<zip>#member=<url-encoded member path>' (never by position: HTEL's RHP is part 5, so a reorder would swap bytes), part_number = its zip position; a member whose sha256 is already stored for the IPO in any run (DB lookup, OD-33) writes no row; each type a member supplies is marked FOUND in document_fetch_state so its fallback chain stops. Existing rows: repair tool scraper/scripts/repair-zip-member-documents.ts (dry run default, --expect-db, --apply, ledger; the same storeZipMemberDocuments path).",
  "detection_check": "write path: CI unit test scraper/tests/unit/services/zip-member-documents.test.ts (exact rows per member; a reorder test red when members are keyed by position; a cross-run duplicate test red when the sha256 DB lookup is dropped; FOUND-marking test). Data level: zip_member_rows, registered as notCoveredByThisManifest (spec only, not in audit-detection-floor.mjs).",
  "status": "unguarded: the write path is covered by a CI test, but the rows stored before this change are missing until repair-zip-member-documents.ts is applied on staging (owed, supervisor) and the data-level audit check zip_member_rows exists (spec only, owner UNASSIGNED)",
  "spec_ref": [
    "§2.2.1",
    "§2.5.5"
  ]
}
