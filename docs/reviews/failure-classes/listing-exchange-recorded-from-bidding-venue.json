{
  "class_id": "listing exchange recorded from bidding venue",
  "feature": "data-consolidation (ipos.listing_exchanges write path)",
  "symptom": "a row's `listing_exchanges` includes an exchange that only ran the BIDDING for the issue, not an exchange the issue will actually list on. The exchange that runs bidding for an issue is not necessarily a listing venue for it — mainboard issues sometimes bid through NSE while the offer document and RHP name only BSE as the designated stock exchange.",
  "first_seen": "2026-09-23, NSE (National Stock Exchange of India Ltd) IPO itself: price band ad says \"(To be listed on the main board of BSE)\", \"BSE LIMITED SHALL BE THE DESIGNATED STOCK EXCHANGE\"; RHP p.3 \"proposed to be listed on BSE\"; Chittorgarh \"Listing at: BSE\" — yet staging `listing_exchanges` holds [\"BSE\",\"NSE\"] because NSE ran the bidding (symbol NSE, series EQ). Issue #938. Bears on OD-64.",
  "fix_prs": "branch fix/951-consolidated-save-unclaimed-fields (#938, 2026-09-26): `toListingExchangesForSource` takes the segment and returns a listing claim for an NSE/BSE feed payload ONLY when the issue is SME (an SME platform is exclusive; a mainboard book runs on both exchanges whatever the listing). Mechanism proven on the real consolidation service: an NSE ['NSE'] reaching the set-merge of a DRHP ['BSE'] is recorded as {source DRHP, value ['BSE','NSE'], previous_value '[\"BSE\"]', previous_source DRHP} -- the staging row byte for byte (data-consolidation-service Case 2b stamps a union with `existingSource || incomingSource`). The filing persister's echo of the stored boards is now context under both key spellings (`listingExchange` / `listingExchanges`). Existing wrong rows are NOT repaired (data contract).",
  "detection_check": "write-time regression tests only, no nightly check: `scraper/tests/unit/services/consolidated-save-claimed-fields-951-938.test.ts` (#938 describe blocks + mechanism test) and `filing-echo-listing-exchange-context-938.test.ts`. A table-level check needs the offer document's 'to be listed on' line per IPO, which is not extracted yet.",
  "status": "unguarded",
  "spec_ref": []
}
