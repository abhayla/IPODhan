# Failure-class registry (recurrence loop, part 1)

Machine-readable registry of data/scraper/persister failure classes that have recurred or
could recur. Seeded 2026-09-06 from `scripts/lib/substance-checks.mjs`,
`scripts/audit-ipo-coverage.mjs`, `scripts/audit-detection-floor.mjs`,
`docs/reviews/round-7-detection-rca.md`, `docs/reviews/w177-detection-rca.md`, and the last two
weeks of `docs/walks/2026-09-02-deepa-pipeline-walk.md`.

**`detection_check` must be a real, verifiable id** — a `key` from the `SUBSTANCE_CHECKS` array in
`scripts/lib/substance-checks.mjs`, a `checkId` string passed to `record(...)` in
`scripts/audit-detection-floor.mjs`, or an invariant name in `scripts/audit-ipo-coverage.mjs`. Where
no such check exists, the row says `unguarded` honestly rather than naming something that isn't a
real, running check. A write-time guard inside the scraper/consolidation code (rejects/collapses a
bad value before it is stored) is NOT the same as a `detection_check` — it stops the write but does
not surface a signal if a new write path bypasses it, which is exactly how this class recurs (see
Why, above the table). Guarded rows below are guarded because an *audit* check exists that would
independently re-flag the bad value in the DB even if a write path let it through.

<!-- BEGIN GENERATED TABLE (scripts/build-detection-registry.mjs) -->
| class_id | feature | symptom (user-visible) | first_seen | fix_prs | detection_check | status | spec_ref |
|---|---|---|---|---|---|---|---|
| DRHP-emitted price band | field-priority-matrix (price band) | a DRHP-sourced price band (pre-final, often `[•]` placeholder-derived) is shown instead of the live RHP/Price-Band-Ad/exchange value | flagged in `docs/reviews/skyways-field-audit.md` (2026-08-27) as a live-matrix ordering question, not confirmed as a shipped bug | none — matrix order (`ADMIN>NSE>BSE>DRHP`) was reviewed and left as-is for Skyways | none found in `substance-checks.mjs` or the audit scripts (no plausibility/source check ties price-band value to its `field_sources` origin) | unguarded |  |
| NUL rupee glyph | extract_filing.py / PDF text extraction | the rupee glyph renders as a backtick or NUL/garbage character in extracted amounts (e.g. `` `0.50 million` ``) | walk W-92 | `aca495f0` (glyph normaliser scoped to bid-window text) | none in `substance-checks.mjs` or the audit scripts (round-7 RCA P3 batch names "name glyph" as a still-open audit gap, not shipped) | unguarded |  |
| a 200 response with zero rows read as "no data" rather than "broken call" | scraper/src/scrapers/chittorgarh-scraper.ts (report-82 list endpoint) and any list-report fetch whose page size is a caller-supplied number | Chittorgarh report 82 accepts EXACTLY ONE page size. Measured read-only against the live endpoint 2026-09-11: perPage=10 returns HTTP 200 with all 231 rows (the report ignores the page size), while perPage=20/50/100/300 each return HTTP 200 with zero rows and error "Invalid API Call<year>-<perPage>-01". The failure carries a success status, so an empty list reads as "no IPOs today" instead of "this call is malformed". fetchChittorgarhAPI's default perPage was 100 - a call that returns nothing - and never fired only because its single caller passed 10 by hand; the code comment beside it stated "API accepts perPage: 10, 20, 30...", which is false and actively invites the change that breaks it. | Sep 2026, found while wiring item 2 slice 7 (issue_type from report 82). Never fired in production: latent behind one hand-written argument. | #569 (page size named REPORT82_PAGE_SIZE with the measurement in its comment, made the default, misleading comment corrected) | scraper/tests/unit/scrapers/chittorgarh-report82-page-size.test.ts - pins the constant to 10 and asserts it is none of the four values measured to return zero rows. Mutation-checked: restoring the old default of 100 turns both tests red. NOT a runtime check - a cycle that receives zero rows already raises via the existing `error` branch in scrapeChittorgarhIPOs, so the gap this closes is the source-level one, where the value looks like a harmless tuning knob. | guarded |  |
| a CI step that runs before `npm ci` imports an npm package, so it passes on every laptop and fails in CI | any step in a workflow job that runs a repo script or test (node --test, node scripts/...) placed before the job's `npm ci` / Install dependencies step, and any later edit that adds an npm import to a script such a step already runs | the script or its self-test imports a package that is only present after `npm ci` hoists it into node_modules (e.g. typescript, reachable only via the web/scraper/packages workspaces). Every local run passes, because a developer checkout or worktree already has node_modules (worktrees even junction them in). In CI the step runs first, the import fails with a module-resolution error, and the whole gate job goes red on every PR -- including the gate that guards the class the change was fixing. The builder's own 'red before, green after' proof cannot see it, because it never reproduces CI's step order. | 2026-09-25 ~19:30 IST, IPODhan, PR #1087 (#694): scripts/ci/require-repair-tool-module.mjs switched from a hand-written stripper to `import ts from 'typescript'`; its two T-490 steps sat at pr-gate.yml ~264/273, before `npm ci` at ~450 in the single `gate` job. Local self-test 19/19 pass; pr-gate run 36141849851 failed at 'Repair-tool module lint self-test (T-490)'. Found by the Tier B reviewer reading step order, confirmed by the red run. Fixed in the same PR by moving both steps after npm ci (merged 2026-09-25). Two later PRs in the same run (#1096, #1099, #1101) placed their new steps pre-install only after checking their imports were Node builtins -- the check this class needs. | #1087 (steps moved after npm ci) | none automated. Builder and reviewer briefs in the easy-wins-2 run carried the line 'if the step runs before npm ci, the test must import only Node builtins'. A static gate (every pre-install node step's import graph resolves without node_modules) would guard it. | unguarded |  |
| a backlog count is read as demand, when most of it is the writer's own historical bugs | any accumulating table nothing consumes — conflicts, failures, queues, alerts, dead letters — whose ROW COUNT is quoted in specs, issues and plans as the size of the problem | a table that records problems keeps growing because nothing drains it. Its count gets quoted as the size of the work ('2,398 unresolved conflicts, 578 on leadManagers') and features are designed, sized and prioritised from that number. But a table nothing consumes is also a table nobody INSPECTS, so writer bugs that produce meaningless rows accumulate invisibly: a source compared against itself, a comparison where one side is NULL, two values that are byte-identical, or values differing only in punctuation. When the guards for those bugs later land, they stop NEW junk but never remove the OLD rows, so every count taken afterwards still reads the pre-fix world. Universal shape: the count measures how long the writer has been running and how buggy it was, not how much real work exists. The danger is that it is quoted with precision and never re-derived, so a real feature gets designed for a demand that is two orders of magnitude smaller than stated, and its stated first customer may not exist at all. | 2026-09-20 -- `data_conflicts` in IPODhan, measured read-only through the sanctioned tunnel on both slots. STAGING: 14,141 unresolved rows, of which 12,774 are a source disagreeing with ITSELF, 1,175 have a NULL on one side, 115 hold byte-identical values and 1 differs only in punctuation -- leaving 76 genuine disagreements, 0.5% of the table. PROD: 2,220 unresolved, 0 same-source (prod is 13 days behind and never ran the code that wrote them), 2,042 null-side, 117 identical, 2 punctuation -- leaving 59 genuine, 2.7%. The spec's §3.1 quoted '31,014 rows, 2,398 unresolved, including all 578 leadManagers conflicts' and the re-read loop (build item 9) was designed and sized around it; measured, leadManagers is 1 genuine row on staging and 2 on prod. The junk classes trace to two writer bugs whose guards landed 2026-09-03 and 2026-09-16: same-source rows stop 2026-08-23 and null-side rows stop 2026-08-24, with zero written after either date, so the guards work and only the historical rows remained. The two slots also disagree in SHAPE, not just size: staging's genuine rows are 38/76 CHITTORGARH-vs-MONEYCONTROL (a retired source, OD-3) while prod's are 43/59 exchange-vs-exchange (BSE-vs-CHITTORGARH 22, CHITTORGARH-vs-NSE 21) with only 5 involving MONEYCONTROL -- so a measurement on one slot does not describe the other | no code fix -- the remedy is a reading discipline plus a cleanup. (1) Before a backlog count is used to size, prioritise or justify a feature, it is DECOMPOSED by the ways the writer can produce a meaningless row, and only the residue is quoted. The decomposition here is four filters and ~40 lines of read-only JavaScript. (2) When a guard lands that stops a junk class, the SAME change plans the removal of the rows already written, or records that it did not -- a guard without a cleanup leaves every future count wrong. (3) A count is re-derived on the slot being discussed; staging and prod differed here by 4x in total and in shape | none yet, and one is worth building because this table is the input to a planned feature: a check that reports `data_conflicts` unresolved rows as a DECOMPOSITION (same-source / null-side / identical / normalises-equal / genuine) rather than a single count, run nightly and diffed NEW vs GONE vs SAME per class. That turns the residue into something visible and shrinking rather than a number people quote. The same shape generalises to any nothing-consumes-it table. Note what made this measurable at all: `data_conflicts` stores BOTH values (`value1`, `value2`) plus `resolution_reason`, so the junk classes are derivable from the rows themselves -- a table that stored only 'these disagreed' could not have been decomposed without re-running the writer | unguarded | §3.1, §3.2 |
| a batch job that records a unit as DONE when its worker found nothing to do, while the reason the unit was selected is still unresolved | any selector + worker + completion-ledger job where the SELECTOR chooses units by one signal (a stuck document, a backlog count) and the WORKER acts through a different mechanism that may have no work for that unit, and the ledger excludes DONE units from future runs | the worker returns 'nothing due' (no rows to walk, no fields to fill) and the job maps that to DONE. The unit is then excluded from every later run, although the condition that selected it (the stuck item) is untouched. The run summary reads all green (attempted=N done=N) while the backlog it exists to drain does not shrink. It is invisible to counts of failures because nothing failed; it shows only by comparing the selection signal before and after. | 2026-09-23 -- IPODhan closed-IPO job (scraper/src/scheduler/closed-ipo-job.ts), issue #717, first real cycle on staging after owner decision A. Selected 10 closed IPOs by stuck extractable documents; resourceClosedIpo ran walkFieldPlanForIPO; the IPOs had 0 ipo_field_plan rows; every one returned fieldsWritten 0, stoppedReason NO_DUE_FIELDS and was written to closed_ipo_resourcing as DONE (10 of 10). PROSPECTUS PENDING on staging: 74 before, 74 after. The candidate query re-picks only PARTIAL/FAILED, so the 10 are excluded for good. Flag turned off again the same cycle. | fix/closed-ipo-job-plan-then-walk (part of #717, OD-76): the job's worker now plants the IPO's field plan with the EXISTING generator when it has none (plantFieldPlanForIpo, the same function the document cycle's PASS 2.5 uses), then runs the same section 2.4 walk; resourceClosedIpo refuses DONE when generation produced 0 rows or threw (FAILED), when the walk asked nothing, or when the walk stopped on budget/superseded claim (PARTIAL). Wrongly-DONE rows are reopened by scraper/scripts/repair-closed-ipo-false-done.ts (DONE, never walked, an extractable document still unread). The earlier attempt (PR #912, a separate document-extraction path) was closed: section 6.1 forbids a second path. | closed_ipo_done_without_walk (scripts/audit-detection-floor.mjs, predicate findClosedIpoDoneWithoutWalk in scripts/lib/detection-floor-checks.mjs): flags every DONE row whose IPO has no ipo_field_plan row with last_attempt_at set, by IPO name. Staging 2026-09-23: flags 10 of 10; a walked IPO (189/189 rows asked) passes. Generalisable: for every selector+ledger job, read 'did the worker act' from the worker's own durable trace (here last_attempt_at on the plan), never from the worker's 'nothing due' return. | guarded | §6.1, §6.2 |
| a better document arrives but the fields already chosen from an older document stay on the older one | any per-field plan that records the document a value was chosen from (chosen_document_id) and settles the field as SUPPLIED, where a later filing of the same IPO can outrank that document (a higher-precedence type, or the same type with a newer filing_date) | the plan row is SUPPLIED, so the walk does not ask for the field again. Nothing compares its chosen document with the documents that arrived after it, so a PROSPECTUS extracted after an RHP (or an RHP after a DRHP) leaves every field chosen from the older document where it was. Readers see a settled value with a document behind it, which looks correct. The generic test: count SUPPLIED plan rows whose chosen document is outranked by a COMPLETED document of the same IPO stored after the row last changed. Any row found proves the reopen step is missing, whatever the field. | 2026-09-24, ipodhan_staging, measured read-only (F-164). 3 of 210 SUPPLIED plan rows with a chosen document: jindal-supreme-india-ltd, glass-wall-systems-india-ltd, lcc-projects-ltd, all ipos.company_description chosen from an RHP, each with a COMPLETED PROSPECTUS stored 2026-09-18 to 2026-09-22 while the plan rows last changed 2026-09-17. ipo_field_plan has no superseded_by column and decideSupersession (scraper/src/services/document-state-machine.ts) has no non-test caller. 48 of 345 documents carry filing_date, and none of the three prospectuses does. Corrected by the item 6 dry run: 28 rows on the same 3 IPOs (20 ipo_details on a PRICE_BAND_AD, 8 ipos). | item 6 PR (feat/item6-supersession): per-document receipts written at extraction, reopen in the COMPLETED transaction, receipt-ranked DOC fetcher (OD-91). Rows chosen before receipts existed (the 28 staging rows, F-164) are kept by OD-91. | pull_frozen (scripts/lib/pull-frozen-checks.mjs, recorded by scripts/audit-detection-floor.mjs): SUPPLIED rows whose chosen document is outranked by a COMPLETED document whose receipt has the field (OD-91). | guarded | §2.5, §2.5.1 |
| a capped worker's ORDER BY points away from the backlog it was built to drain, and every row it does process is individually valid | any batch worker that selects a capped slice of a queue each run — drainers, backfills, re-processors, catch-up jobs, retry sweeps — where the selection has a WHERE clause everyone reviews and an ORDER BY nobody does | the worker is built because a specific backlog exists. Its WHERE clause correctly describes the eligible POPULATION, and its cap correctly limits blast radius per run. The ORDER BY is then chosen on a plausible-sounding general principle — newest first, oldest first, alphabetical, by id — rather than by which rows carry the work. Because the backlog is almost always the tail of whatever the natural order is (that is WHY it is a backlog: the normal path already handled the head), the worker spends its first many runs on rows needing nothing. Two things then hide it. First, every row it processes is individually valid, so no row-level check, no data audit and no invariant fires — the output is correct, it is just about the wrong rows. Second, workers of this shape usually write a per-attempt ledger and exclude completed rows, so the wasted runs CONSUME the caps and mark rows done, and the log reads `attempted=N done=N` every run: a green signal over an empty set. Whoever reads week one concludes it is working. The generic test is to run the real selection query against real data BEFORE first execution and count how many of the rows it returns actually carry the work the job exists for. If the answer is zero, the ORDER BY is the defect. | 2026-09-21 — IPODhan item 17, the closed-IPO job, caught the same night it merged (#868) by running its real selection SQL read-only against staging before the job had ever executed. `CLOSED_IPO_CANDIDATES_SQL` ordered `close_date DESC` with a cap of 10. Measured: eligible population 343; the 74 IPOs holding a stuck PENDING PROSPECTUS — the entire reason the job exists (#717) — sit at ranks 156-294, mean 231, because they are OLD, which is precisely why nothing had re-visited them. Night 1 would have reached 0 of them; nights 1-10, still 0; the first on night 16 and the last on night 30. Meanwhile the job writes a `closed_ipo_resourcing` row per attempt and excludes DONE ones, so fifteen nights of slots would have gone to IPOs needing nothing, each marked DONE, logging `attempted=10 done=10` nightly. The unit tests passed throughout: they pinned the four WHERE rules and the cap, and never the ORDER BY. Fixed in #874 by ordering on the count of PENDING documents whose type an extractor can actually read, with close_date surviving as the tie-break — proven on the same real data: 10 of the 10 rows night 1 now selects carry a stuck document, versus 0 of 10 before. | #874. Three parts worth copying. (1) Order by NEED — a subquery counting the rows that represent the work — with the old ordering demoted to a tie-break rather than deleted, so the original intent survives where it does no harm. (2) Restrict that count to work the job can actually DO: counting all pending documents would have ranked an IPO by rows whose types have no extractor at all (#869), which no number of visits can resolve. (3) Assert the ORDER BY as an ORDERED PAIR in the test (a regex that requires the need clause, then DESC and a comma, then the tie-break, in that order), because asserting that both clauses are merely PRESENT says nothing about which one decides — that weaker assertion would have passed against the defect. | none built. Two candidates, both cheap. The pre-merge one is the stronger: for any capped selection query, run it against real data and assert that a non-trivial share of the returned rows carry the work the job exists for — an ordering test that can only pass for the right reason. The runtime one, for jobs already live: alert when a drainer's completion counter rises while the BACKLOG counter it is supposed to reduce does not move, over some number of consecutive runs. That is the signal that distinguishes 'working' from 'working on the wrong rows', and nothing in this repository currently computes it. Note the enabling condition here: the backlog was independently countable (`documents` with `extraction_status = 'PENDING'`), so 'did the job touch the work' was answerable without instrumenting the job. A drainer whose backlog is only knowable from its own ledger cannot be checked this way. | unguarded |  |
| a check whose premise is true in only ONE lifecycle state, applied to all of them | scripts/lib/substance-checks.mjs (checkDegenerateBookbuildingBand); any predicate encoding a domain rule that holds during part of an entity's life | checkDegenerateBookbuildingBand encoded 'a book-built issue ALWAYS has floor < cap (SEBI ICDR)'. True while the book is OPEN; false afterwards, because after price discovery a book-built issue HAS one price and every source publishes it that way. Measured at the source (Chittorgarh report 82, FY2026-27): all 183 CLOSED book-built rows carry a single price, only the 18 STILL-OPEN ones carry a range. On staging the check fired on 239 real IPOs and exempted none, of which the large majority were correct data. THE COST IS NOT THE NOISE, IT IS THE SIGNAL BURIED: 21 genuine defects sat inside that output unseen - rows whose band collapsed to its FLOOR, all 21 storing a value LOWER than the authoritative issue price, none higher, average gap 8.63%, against a control where real-band issues price at the CAP in 46 of 46. Users saw a per-share price up to 12 rupees low on 21 listed IPO pages. | Check added round-6 (T-308) for a real defect - a price-band collapse at close/listing. Premise over-generalised from the start; measured and rewritten 2026-09-11. | this PR - the check now flags three populations that are genuinely wrong (the stored price equals face_value; an authoritative listing_performance.issue_price disagrees with it; the band is degenerate while the book is still open) and stays silent on a closed issue priced at a plausible value. Real-data proof on staging: the SAME script goes from 239 violations to 28, and the 28 are named. | scripts/tests/degenerate-band-check.test.mjs - drives the real predicate over all four outcomes; five mutations each proved applied and each turning it red, including restoring the OLD naive rule (fails 5 tests) and reading the COALESCE alias instead of the raw oracle. scripts/tests/audit-substance-plausibility.test.mjs statically proves the two new columns are SELECTed and goes red if either is dropped. | guarded |  |
| a claim-time filter on a counter that only ever increases is a TERMINAL state that no code can reverse, while the row's own state field still reads transient | any work-queue table whose rows are selected by a predicate over a monotonically-increasing attempt/retry/failure counter, where the counter is never reset and the row's `state` column is deliberately left non-terminal | a queue bounds churn by refusing to hand out rows past an attempt cap — `WHERE state = '<retryable>' AND attempts < MAX` — and documents this as a claim-time FILTER, explicitly not a state transition, so that the row is not retired. The intent is that the condition is TRANSIENT and the row becomes workable again once the blocker is removed. But if nothing ever RESETS the counter, the filter is terminal in every way that matters: the row can never be selected again, by any code path, forever. The state column still says retryable, so no completion report counts it as done; the filter excludes it, so no pending report counts it as work. The population falls into the gap between the two and is invisible to both. The failure is worst precisely where the design is most careful: a row blocked by a DEPLOYMENT gap (a missing adapter, an unconfigured credential, a feature flag off) burns its attempt budget on a condition that has nothing to do with the row, and when the gap is closed the row does not come back. Universal shape: transience must be enforced at the layer the SELECT reads. A transient `state` plus a permanent counter, where the query reads the counter, is a terminal state with a transient label. Any 'registering X must be enough to make this askable again' guarantee is false unless closing the gap also clears whatever the claim predicate reads. | 2026-09-22 -- `ipo_field_plan` in IPODhan, measured read-only on `ipodhan_staging` through the sanctioned tunnel. 154 rows carry `cause LIKE '%NO_FETCHER_REGISTERED%'` (reason_code SOURCE_UNREACHABLE), every one source = NSE, across 17 distinct IPOs; 144 of the 154 sit at exactly `attempts = 5`, which is `FIELD_PLAN_RECLAIM_MAX_ATTEMPTS` (ipo-field-plan-repository.ts:118). All 154 are past due (`due_now` 154 of 154 at utc_now 2026-09-22 12:40:00) and none has been attempted since 2026-09-20 21:16:01 UTC. The claim query filters `state = 'CHECK_FAILED' AND attempts < FIELD_PLAN_RECLAIM_MAX_ATTEMPTS` at :809 and :815, and its own comment states it is 'a claim-time FILTER, never a state transition -- the row is not moved to EXHAUSTED'. Every write to `attempts` in that repository was grepped: there is exactly ONE, the increment at :967, and the plan-regeneration upsert's `ON CONFLICT ... DO UPDATE` set-list (:453-461) updates ranks, manifest_version, policy_origin and updated_at but touches neither `attempts` nor `state`, so not even a manifest-version bump recovers them. The timeline is the finding: last attempt 2026-09-20 21:16 UTC, and PR #867 registered the NSE fetcher at 2026-09-21 00:22 IST -- the rows stopped being askable hours BEFORE the thing that would have answered them arrived. `field-plan-walk.ts:700-711` had named this exact risk in advance: 'TRANSIENT: a missing adapter is a deployment/config state ... Registering the adapter must be enough to make the field askable again -- retiring it terminally would mean a config gap silently outlived its own fix.' The guard was placed on the STATE (sawTransientFailure keeps the row out of EXHAUSTED) while the SELECT reads the COUNTER, so the config gap outlived its own fix exactly as warned. Affected fields are live reader-facing ones: listing_performance.current_price_nse (17), ipo_demand_graph.{cumulative_quantity,exchange,is_cut_off,price_point} (7 each), subscriptions.{b_nii,s_nii,employee,shares_offered,total_shares_bid} (7 each), ipo_details.* (6 each), ipos.lot_size (6), ipos.face_value (2). Filed as #884 | not yet fixed -- filed as #884 with the measurement. Three parts, in order: (1) a transient failure must not consume a permanent budget -- either do not increment `attempts` when the only failure was NO_FETCHER_REGISTERED (the walk already distinguishes this; it sets `sawTransientFailure`), or count transient and real failures in separate columns so the claim predicate reads only the real ones; (2) the 144 existing rows cannot recover on their own and need a productized, re-runnable repair tool (dry-run default, prod guard), never hand SQL; (3) the detection below. Note for whoever builds it: #759 proposed the OPPOSITE fix for the same condition -- a terminal `NO_FETCHER` enum member -- and that would deepen this defect rather than fix it, for the reason field-plan-walk.ts's own comment gives | none, and its absence is why this survived two days on an actively-watched staging box. The gap is structural, not an oversight: `pull_exhaust` reads rows in state EXHAUSTED, and these rows are deliberately NOT in EXHAUSTED; nothing at all reads 'non-terminal state AND past the claim predicate'. The check to build is exactly that population -- rows whose `state` is retryable but which the claim query can never select again -- reported by reason_code and by source, diffed NEW/GONE/SAME nightly. It generalises to any queue with a monotonic cap: the query is 'rows the SELECT excludes that the STATE says are still pending', and a healthy queue holds zero of them. A check that counts only terminal-state rows is structurally blind to this class, the same way a population-by-JOIN can never detect a missing member of that population | unguarded |  |
| a completion condition that a timer keeps re-arming never completes | any job, slot, batch or queue that declares itself done when its due set is empty, where membership of the due set is decided by elapsed time (a retry-after, backoff, next-due or TTL stamp) rather than by an event that happens once | a run is 'complete' only when nothing is due, but an item becomes due again merely because time passed. When the re-arm interval is shorter than or comparable to the time the run needs to drain its due set (wakes 30 minutes apart, a 30/60-minute retry, a per-wake cap), every continuation finds the same items due again, spends its budget on them, and ends 'incomplete'. Nothing throws and every single attempt is individually correct; the aggregate never converges. The same shape appears when a fix removes the timer but keeps an item permanently eligible under a different rule (for example 'every open row once per slot' with a per-wake cap smaller than the backlog): the completion condition is still unreachable, only more slowly. The generic test is a convergence test on the REAL due-computation: N items, cap C per wake, no new events -> after ceil(N/C) wakes the due set is empty and the run logs complete, and each item was attempted exactly once. A timer-based or always-eligible rule fails it. | 2026-09-23/24 -- IPODhan #943. F-151: on staging the 00:00 IST data slot never logged complete; the document state machine re-armed every open document_fetch_state row on a 30/60-minute or 6-hour timer while wakes are 30 minutes apart, so each continuation wake re-found the same documents due and exhausted the 60 s discovery budget. F-152: the field-plan walk wrote a doubling 15 min - 6 h backoff and logged structural gaps as TRANSIENT. PR #957 round 1 removed the timer but made LISTED rows 'due once per slot' and dropped LISTED deferral from the completion condition; the Tier A review measured staging (LISTED document_fetch_state: BLOCKED_ALL 202 rows / 52 IPOs) and showed that at cap 2 per wake one pass needs 26 wakes while a slot has 16, so 'every LISTED row once per slot' could never complete a slot either. | PR #957 round 2: due-ness is event-based, never elapsed-time. An open row is attempted at most once per OD-19 data slot (next_retry_at holds only a slot start); a LISTED IPO's open row is attempted ONCE after the IPO entered LISTED (new column document_fetch_state.attempted_at_stage, migration 0054, written only by a concluded chain) and again only when a new document row or a document first seen after its last attempt appears (OD-56, OD-81 event 2). Due LISTED work is back in the slot completion condition. Convergence test (52 IPOs cap 2, 7 cap 2, 10 cap 3) asserts complete at wake ceil(N/C)-1 and exactly one attempt per IPO; mutation 'LISTED rows due every slot' turns 5 of 7 tests red. | the staging wake log's per-slot complete/incomplete line (document cycle summary `slotComplete` / `incompletePasses`) read by the proof: a slot that never logs complete across its wakes is this class. No nightly audit check yet; the general property is a convergence unit test on any due-computation that gates a completion flag. | unguarded | §2.1, §2.5, §2.5.1, §6.1 |
| a computation's 'I could not' sentinel is written to the database as if it were the answer | any derive-then-persist path where the deriving function returns an in-band sentinel (0, -1, '', 'UNKNOWN', epoch) for missing or unparseable inputs, and the caller persists the return value unconditionally | a pure function returns a sentinel rather than throwing or returning undefined, because a sentinel is convenient for its OWN callers (comparisons, arithmetic, tests that pin it). The persisting caller does not distinguish sentinel from value and writes it straight through. The column now holds a value that means the opposite of what it says: not 'the quantity is zero' but 'nobody knows the quantity'. It is invisible to the usual guards — a 0 satisfies a non-negative constraint, a NOT NULL constraint and an optional-field schema; it is not a null, so null-coverage counts report the field as populated; and it sorts and aggregates as a real number, so averages and minimums are quietly wrong. The generic test is: for each source that writes a field, count how many of its rows hold the deriving function's sentinel. If ONE source accounts for all of them, the sentinel is leaking from that source's derive step rather than being a real observed value. | 2026-09-20 — IPODhan `ipos.issue_size`, measured read-only on ipodhan_staging through the sanctioned tunnel while working item 14 (#728). 20 rows hold issue_size = 0. Partitioned by the writing source in `field_sources`, ALL 20 are BSE-sourced — 20 of 20, and no other source (CHITTORGARH 231 rows, ADMIN 31, NSE 14, MONEYCONTROL 11, DRHP 2) has ever written a zero. The mechanism: `computeBSEIssueSize` (scraper/src/scrapers/bse-api-scraper.ts) returns 0 when the share count or price floor is missing or unparseable, and `buildScrapedIPO` passed that return straight into `issueSize`. The sentinel is deliberate and its own unit tests pin it (`expect(computeBSEIssueSize(0, 120)).toBe(0)`), so the defect is not the function — it is the boundary where a scraped record is built. `issueSize` was ALREADY `.optional()` in the validator, so undefined was legal the whole time and no schema change was needed. Same measurement also found 17 non-zero BSE-sourced rows carrying the separate wrong-quantity defect #728 describes; the two are distinct classes sharing one source. | the guard: `issueSize: computeBSEIssueSize(shares, band.min) || undefined` at the single call site both BSE mappers share, so the sentinel stops at the persist boundary while the function keeps it for its other callers. Failing tests first (3 red: +0 received where undefined expected, on both the detail-only and list+detail mapper paths), then green, then mutation-checked by reverting the guard (2 failed). Writing the test also corrected a wrong assumption in the test itself: an unparseable PRICE BAND makes the mapper return null for the whole row, so only a missing SHARE COUNT reaches the sentinel path — pinned rather than left implicit. What the guard does NOT do: it does not repair the 20 existing zero rows, which need a source-backed repair, and 19 of the 20 have no completed document to re-derive from. | none yet. The check that generalises: for every (field, source) pair, report the count of rows holding that field's sentinel value, and FAIL when one source accounts for effectively all of them — that is the leak signature and it fires on the second field to develop it. Cheaper and more specific than auditing each deriving function. Note what made this findable at all: `field_sources` records WHICH source wrote each field, so the population could be partitioned by writer. A table recording only the value could not have been decomposed this way, and the 20 zeros would read as 20 genuinely tiny issues. | unguarded |  |
| a config key written in a different naming form than the callers use, so its rules silently never apply | the field priority matrix (scraper/src/config/field-priority-matrix.ts) and any other per-field config table looked up by a field name that callers spell in another form (snake_case vs camelCase, table column vs ORM property) | the lookup normalises the caller's name (toCamelKey) and falls back to the raw string. An entry keyed in the OTHER form is reachable only by a caller that passes that form -- in practice, only a test. Production callers pass the real property name, miss the entry, and get the DEFAULT rules, with no error and no log. A test written with the entry's own spelling passes, so it pins the dead entry and hides the miss. Wider shape: any second key space (alias, CLI name, snake/camel) that is not validated against the real set of names turns configuration into dead text. | 2026-09-25 -- IPODhan, found by the tests-only builder for #754 on branch test/tests-only-839-754-727 (probe via npx tsx against the real matrix): min_investment / minInvestment, issue_price / issuePrice, fresh_issue_size / freshIssue, offer_for_sale_size / ofsIssue. For each pair the snake_case (configured) form gives DRHP sameSourceRefresh true, confidenceThreshold 85-95, normalization currency/number; the camelCase form production passes gives sameSourceRefresh false, confidenceThreshold 75, normalization none (the defaults). Not yet measured: listing_price, listing_gain_percentage, expected_listing_price, peer_companies. Issue #754 (comment 5828983851). | none yet -- deferred from the 2026-09-25 low-hanging run because correcting the keys changes the winning source, threshold and normalisation of four live fields and needs real-data proof (data-testing session) | none yet; the natural one is a CI test that every FIELD_PRIORITY_MATRIX key resolves to a real schema property (packages/shared/src/db/schema.ts) in the form callers pass, so a key in the wrong form fails the build | unguarded | §1.11 |
| a container is unwrapped to one member, and the other members are silently dropped | any download that arrives as an archive or bundle (zip, multi-attachment mail, multi-file API response) where the reader picks ONE member to keep | a bundle is treated as a wrapper around one file, so the unwrap step chooses a member (by name, else the largest) and discards the rest without a trace. When the bundle actually carries several distinct documents, every member except the chosen one is lost, and anything the lost members would have changed never happens. Nothing fails: the chosen member is valid, the store has a row, and the missing documents are absent rows, which no per-row check can see. Universal shape: an unwrap step that returns one thing from a container of N answers a narrower question (which member is the one we wanted?) than the one the data poses (what did the container hold?). | 2026-09-24, IPODhan F-154: all 41 distinct NSE RHP/DRHP/PROSPECTUS zips on staging hold 2 or more PDFs; 5 hold a corrigendum or price-band notice; selectZipMemberForType keeps the RHP and drops them, and 4 of those 5 IPOs (hy-tech-engineers-ltd, abh-healthcare-ltd, madhur-knit-crafts-ltd, rays-of-belief-ltd) have no CORRIGENDUM/PRICE_BAND_AD document at all, so OD-30 corrigendum precedence cannot fire for them. | item 22 zip-members PR #969 (branch feat/item22-zip-members). Round 1-2: the unwrap reports every PDF member; each non-GID member typed as a type other than the main document's is stored as its own document row keyed by a STABLE member identity, url '<zip>#member=<url-encoded member path>' (never by position: HTEL's RHP is part 5, so a reorder would swap bytes), part_number = its zip position; a member whose sha256 is already stored for the IPO in any run (DB lookup, OD-33) writes no row; each type a member supplies is marked FOUND in document_fetch_state so its fallback chain stops, ONLY when every consulted exchange answered (round 2 MAJOR 1, round 3 MAJOR 1: an exchange that could not be read must never let an older zip copy close a type the exchange might still supply a newer link for). Round 3: a stored zip whose members were never examined (pre-fix rows) is expanded by the SAME code path, in the pipeline (a bounded per-wake data-slot pass, `stored-zip-expansion-pass.ts`) and by the repair CLI (`scraper/scripts/repair-zip-member-documents.ts`, dry run default, `--expect-db`, `--apply`, ledger); the durable marker `documents.zip_members_checked_at` (migration 0055) records that a zip was examined so it is fetched once. Round 4 (Tier A MAJOR, this class's own recurrence): round 3 marked a zip checked ONLY on HTTP 200, so a permanently dead zip (404, timeout, refused) was left unmarked forever and re-downloaded every wake, and the selection was ordered by `uploaded_at`, so that one dead zip sorted first on every wake and starved the rest of the backlog behind it. Fixed by counting failed re-fetch attempts per zip, once per DISTINCT data slot (`documents.zip_expand_attempts`, `zip_last_attempt_slot`; `DocumentRepository.markZipExpandAttemptFailed`); after 3 distinct-slot failures the zip is marked checked with `documents.zip_unresolved_reason = 'zip_unreachable'` (closed, distinguishable from an expanded zip); the selection is now ordered by last-attempt (`zip_last_attempt_slot ASC NULLS FIRST`), never `uploaded_at`, so a zip that just failed rotates to the back instead of blocking the rest of the backlog. | write path: CI unit tests scraper/tests/unit/services/zip-member-documents.test.ts (exact rows per member; a reorder test red when members are keyed by position; a cross-run duplicate test red when the sha256 DB lookup is dropped; FOUND-marking test; round 4: a test that a stored zip member does NOT close its type when an exchange TIMED OUT, red under the reviewer's round-3 mutation M2 that drops `exchangesAnswered` from either guard) and scraper/tests/unit/scripts/repair-zip-member-documents.test.ts (round 4: a dead zip is not marked after 1-2 distinct-slot failures and IS marked `zip_unreachable` after the 3rd; repeated failures within one slot count once; selection ordering by last-attempt so one dead zip cannot starve the rest of the backlog). Data level: zip_member_rows, registered as notCoveredByThisManifest (spec only, not in audit-detection-floor.mjs). | unguarded: the write path (including the round 4 dead-zip close and reordering) is covered by CI tests, but the rows stored before this change are missing until repair-zip-member-documents.ts is applied on staging (owed, supervisor), the round 4 fix itself is owed a staging proof (no real dead zip has been observed cycling 3 distinct-slot failures yet), and the data-level audit check zip_member_rows exists (spec only, owner UNASSIGNED) | §2.2.1, §2.5.5 |
| a cross-check that scopes one side of a comparison but not the other reports every out-of-scope member as missing | any two-directional cross-check between OUR data and an external oracle feed, where one direction filters its own side to a narrower population (a segment, a source, a status) but the other direction walks the oracle's FULL feed unfiltered | direction A (what we publish, checked against the oracle) is deliberately scoped to a subset of our rows -- reasonable, since only that subset is expected to appear on the oracle's feed. Direction B (what the oracle publishes, checked against us) reuses THAT SAME scoped lookup set to decide whether we already have a row for an oracle entry, instead of building its lookup from every row we publish. Any oracle entry belonging to the population direction A excluded is then structurally invisible to direction B's lookup and is reported as 'we have no row for it at all', even though the row exists -- because the check never looked outside the narrower set to find it. The false premise compounds the bug: the narrowing is often justified by a claim that the excluded population 'never appears' on the oracle feed, a claim nobody re-verified after the oracle's feed changed or was never true to begin with. | 2026-09-23 -- IPODhan `crossCheckNseStatuses` (scripts/lib/detection-floor-checks.mjs), issue #895. Direction 2 built `openByKey`/`knownByKey` from `scoped = ourRows.filter(inScope)`, where `inScope` keeps MAINBOARD rows only, then walked EVERY row of NSE's live current-issue and upcoming feeds. The scope comment claimed 'NSE SME (Emerge) issues ... legitimately never appear on these endpoints' -- false: the prod floor run of 2026-09-23 03:46 IST saw 3 SME issues on NSE's feeds (Himalayan Solar Limited, Pooja Logistics Limited, Coreintegra Consulting Services Limited), all three existing in our data with the correct status, all three reported as 'we have no row for it at all' every night this ran. | the class-level fix is to build the oracle-matching direction's lookup set from EVERY row on our side that could plausibly appear on the oracle feed, never from the other direction's narrower scoped set -- the two directions have different, independently-justified populations, and one must not silently inherit the other's filter. Direction 1 (ours-checked-against-oracle) may legitimately stay narrow when its own noise argument holds; direction 2 (oracle-checked-against-ours) must not reuse that narrowing as its own lookup population. IPODhan fix: scripts/lib/detection-floor-checks.mjs `crossCheckNseStatuses` direction 2 now builds `openByKey`/`knownByKey` from `ourRows` (all OPEN/UPCOMING rows the caller passes in), not `scoped`. Direction 1 stays MAINBOARD-scoped by choice, pinned by a regression test | scripts/tests/audit-detection-floor.test.mjs '(l) #895 ...' tests (4 cases: SME UPCOMING row matches NSE upcoming feed with zero mismatches; SME OPEN row matches NSE current-issue feed with zero mismatches; a genuinely missing company is still discriminated as a FAIL; direction 1 stays MAINBOARD-scoped and does not also fire its own message for the same row) exercise the REAL exported `crossCheckNseStatuses`, red before the fix, green after, mutation-proof (reverting the one-line fix flips them red again). Generalisable check for any two-directional cross-check: assert that the oracle-matching direction's lookup population is built from the SAME unfiltered set the write path actually publishes, not from the other direction's scoped subset | guarded |  |
| a documentation edit breaks a generator that parses that document, and the author runs every test suite except the one that would have caught it | any human-written document that a script parses with a strict pattern — a status table, a CHANGELOG, a decision log, a manifest in Markdown — where the format contract lives only inside the parser's regex and the editor is a person writing prose | the document reads perfectly to a human. A cell that must be exactly `**PARTIAL**` becomes `**PARTIAL - blocked on X**`; a heading gains a clarifying suffix; a row gains a column. Every one of those edits improves the document AS PROSE, which is why they are made and why review waves them through: the reviewer reads meaning, and the meaning is better. The parser then matches fewer rows than it expects. A good parser refuses to emit and fails loudly; a bad one emits a silently short artefact. Either way the break surfaces on the NEXT unrelated PR's gate, minutes to hours later and attributed to whoever opened it, not to the docs change. The deeper trap is which tests the author runs: a documentation change reads as low-risk, so the author runs the suites for the code they were working on and not the one covering the document — and in this repository that test exists, reads the real file, and asserts the exact count. | 2026-09-21 — IPODhan. Updating `docs/design/pull-model-completion-state.md` in #872 I wrote two verdict cells as `**PARTIAL - BLOCKED on an owner decision (#854)**` and `**PARTIAL - existing rows only**`, putting a qualifier inside the bold. `scripts/ops/build-plan-board.mjs`'s ITEM_ROW regex requires the third cell to be exactly `**BUILT**` / `**PARTIAL**` / `**NOT BUILT**`, so both rows stopped matching: the parser saw 27 items where it expects 29 and refused to emit, which failed the Detection-Change Gate on every PR opened afterwards — surfacing on #874, an unrelated scheduler fix, and initially read as #874's problem. Two compounding misses of mine. First, before merging #872 I ran the scraper unit suite and the web unit suite and never `scripts/tests/build-plan-board.test.mjs`, which reads the real document and asserts `EXPECT.items === 29` — it would have gone red immediately. Second, when the gate failed I assumed a stale run and rebased twice before reading the log; the log named the cause on its first line. SECOND AND THIRD INSTANCES, same night, same author: (2) adding four owner-decision rows (OD-64..67) to `data-sourcing-pull-model.md` tripped the board's decision counter, which is pinned in THREE places — the generator's EXPECT, the test's name, and an assertion string — so one deliberate bump read as a broken test; fixed by deriving all three from EXPECT. (3) the same edit plus a CORRECTION blockquote in §5.2 went stale in `rules.json` (a generated rule index) and then surfaced D19: the blockquote became rule R-189, and the design requires every live rule to be claimed by a build card or declared unclaimed — a correction NOTE can be neither built nor claimed, so it needed an explicit declaration in `rules-unclaimed.json`. Both were caught by CI, not by me: I ran the board generator and the detection registry and not the rule index. The count that matters: ONE design document is parsed by FOUR independent generators (plan board, rule index, consistency checker, detection registry), and nothing lists them in one place. | #874 — the qualifiers move to the FRONT OF THE EVIDENCE CELL, where they read identically to a human and parse cleanly, and both generators re-run (`build-plan-board.mjs` then `render-board.mjs`; running only the first leaves a stale `index.html` that its own detection check catches). The general remedy is not 'be careful with tables': it is that a document under a parser names its format contract IN the document, at the top, in one line a prose editor will read — and that the test covering a file is chosen by WHICH FILE CHANGED, not by which area the author believes they were working in. | partially guarded already, and the guard is why this was findable: `build-plan-board.mjs` refuses to emit when the parsed count differs from its expectation, rather than publishing a short table — a loud failure on an unrelated PR beats a quiet 27-item board nobody notices. Unguarded is the pre-merge half. The check worth building is a path-to-suite map: a changed file names the test files that must run before its PR opens, so editing `pull-model-completion-state.md` demands `build-plan-board.test.mjs` the way editing a scraper source demands its tier tests. Without that, 'which suite covers this' is a judgement call made by whoever is most confident the change is trivial. Sharpened by the second and third instances: the check should be a PATH-TO-GENERATOR map, not just path-to-test — a changed file names every generator that reads it AND every test that covers it, so 'which of the four do I run' stops being a memory exercise. A cheaper interim: one `npm run docs:verify` that runs all four, so the answer is always 'all of them'. | unguarded |  |
| a documents row is admitted NOT_EXTRACTABLE, its type later joins the extractable list, and nothing re-admits it or ever revisits it | scraper/src/config/document-admission-status.ts (AUTO_PERSIST_DOC_TYPES, isExtractableDocType), scraper/src/services/filing-auto-persist.ts (selectPendingFilings), scraper/src/services/document-cycle.ts (loadCandidateIpos / isInLiveWindow) | any documents row, on any IPO status/segment/slot, whose extraction_status was set to NOT_EXTRACTABLE while its type had no extractor, when that type is LATER added to AUTO_PERSIST_DOC_TYPES. The admission-time stamp is correct for the day it was written but becomes stale the moment the extractable list changes, and nothing flips it: selectPendingFilings would in fact pick the row up if the document cycle ever re-examined it (NOT_EXTRACTABLE does not trip documentExtractionBlocked's MANUAL_REVIEW/FAILED/IN_PROGRESS gates), but loadCandidateIpos only visits an IPO within its live window (isInLiveWindow) or on WITHDRAWN/POSTPONED — a closed or long-listed IPO whose only outstanding document was this row is simply never visited again, so the stale status is never noticed. | 2026-09-25, measured on ipodhan_staging: documents id 3b77bc3b-e44a-4d3d-9622-edb71763e9e8 (skyways-air-services-ltd, CORRIGENDUM), stamped NOT_EXTRACTABLE before #989 (OD-90) put CORRIGENDUM on AUTO_PERSIST_DOC_TYPES, updated_at 2026-09-22, never re-admitted. The one post-#989 corrigendum (veegaland, 038eaf50) discovered fresh and completed normally. | readmit-stranded-corrigenda (this branch): scraper/scripts/repair-readmit-stranded-documents.ts (productized, re-runnable repair — reads the class from isExtractableDocType, sets extraction_status=PENDING and clears extraction_error/retry_count via buildExtractionStatePatch, dry-run default, --expect-db required, prod refused without --allow-prod) | d_stranded_readmit (docs/reviews/detection-checks/d_stranded_readmit.json) — nightly, scripts/lib/document-state-checks.mjs#checkStrandedNotExtractable, unit-tested in scripts/tests/document-state-checks.test.mjs (FAILs a NOT_EXTRACTABLE CORRIGENDUM, PASSes ADDENDUM/COMPLETED/PENDING). Discriminates on real staging data: flags Skyways, passes veegaland's completed corrigendum and every ADDENDUM row. | guarded | §2.5.5 |
| a fix's regression test is added to a test file that no CI workflow runs, so the guard exists only on the laptop | any node --test / bash test file under scripts/tests, scripts/lib or scripts/ci/tests: pr-gate.yml names those files one by one, so a file absent from it is never executed by CI, while vitest suites run by include pattern | the builder writes the failing-test-first case, shows red then green locally, and even mutation-proves it -- in a file pr-gate.yml does not name. CI stays green whatever happens to the guarded code, because the file is never run. The PR reads as 'guarded' and the regression can return silently. A test file that already existed unwired inherits the gap: adding a case to it looks like adding coverage. | 2026-09-25 ~20:00 IST, IPODhan easy-wins-2 run: PR #1096 (#426, #429) put its mutation-proved cases in scripts/tests/failure-classifier.test.mjs, failure-tick-state.test.mjs and failure-delta-cli.test.mjs; PR #1099 (#599) in scripts/lib/signal-health-checks.test.mjs. `grep` of .github/workflows/pr-gate.yml returned zero hits for all four (Tier B review). The builder had disclosed it as the pre-existing '28 of 51 scripts/tests files unwired' gap. Fixed in the same PRs by adding one pr-gate step per file (Node-builtins-only imports, placed before npm ci). | #1096, #1099 (fix round 1: files named in pr-gate.yml) | none for scripts/ tests (scripts/ci/require-integration-test-coverage.mjs covers scraper integration tests only). Reviewer briefs carried 'list each test file WIRED/UNWIRED with the workflow line'. A gate that fails when a *.test.mjs / *.test.sh under scripts/ is named by no workflow (with an explicit exemption list for the known backlog) would guard it. | unguarded |  |
| a heading match that is really a prose cross-reference | any extractor that locates a section of a filing by matching its heading text (scraper/scripts/peer_table_section.py and every future document-section locator) | a document mentions a section by name inside a sentence - typically a footnote reading 'the peer set provided below under "Comparison with listed industry peers"' - and a matcher anchored on the phrase alone binds to that MENTION instead of the heading. The extractor then parses the wrong region: the notes above the table rather than the table. It does not crash and it does not warn; it returns a confident, empty or wrong result. | 2026-09-11, item 8a. Twice, both mine, and the second is the instructive one. First I searched four RHPs for 'comparison with listed industry peers', got zero hits because the documents word it differently, and filed issue #565 claiming the table does not exist at all - reading zero hits as a fact about the document rather than about my pattern. Then, correcting that, I recorded Glasswall's heading AS 'Comparison with listed industry peers' - which is the text of its footnote's cross-reference, not its heading. Its real heading, twelve lines below, reads 'VI. Comparison of accounting ratios with listed industry peers'. The wrong wording was merged onto the build card in PR #579 before the locator's own tests caught it. | #587 - the locator requires the heading PHRASE TO START THE LINE after an optional section marker, so a mid-sentence mention cannot match; the card's wording corrected in the same PR. RE-HOMED in the following slice: #587 put the locator in TypeScript, but the peer extraction lives in Python over page_texts, so the TS version had no caller and no prospect of one. It was retired and ported to scraper/scripts/peer_table_section.py rather than left as a correct-but-uncalled helper - the very class item 20's wiring gate exists to surface. | scraper/scripts/test_extract_filing_peer_section.py - test_glasswall_heading_is_not_the_cross_reference_above_it asserts the located heading line comes AFTER the cross-reference line, on real committed fixture text; plus test_a_prose_mention_inside_a_sentence_is_not_a_heading. Mutation-checked with an assertion that each mutation applied: dropping the line-start requirement, the KPI guard, or the note-terminator shape each turns its own named case red. | guarded for this locator only; any future section locator must re-establish the same property, since the guard lives in this locator's tests rather than in a shared rule |  |
| a narrow writer routed through a whole-row save writes columns it never claimed | any writer that owns only some columns of a table (the opening-day check owning name/status/open/close on `ipos`, the field-plan walk owning one field) but writes through a shared save path that rebuilds the whole row from its payload | the caller narrows its CLAIM (only these fields enter the priority decision), but the save that follows builds the full column set: every column the decision did not produce falls back to the raw single-source payload or a constant, and the repository applies it with a plain whole-object SET. A source that says less than the stored row therefore overwrites the stored row with what it said: one exchange's list turns a stored two-exchange listing into one exchange, a source with no segment writes a segment, every save stamps lastScrapedAt. Universal shape: narrowing what a write CLAIMS is not narrowing what it WRITES; only a SET clause built from the claimed fields is. | 2026-09-24 -- IPODhan item 7 S4 round 3 (head 3434fa9c). The opening-day writer passed `onlyFields` = companyName/status/openDate/closeDate to `DataConsolidationOrchestrator.consolidatedUpsertIPO`; `extractConsolidatedData` / `extractListingExchanges` fell back to the raw scrape (`toListingExchangesForSource`) and `IPORepository.update` (`packages/shared/src/repositories/ipo-repository.ts` ~1187) ran `.set({...data})`. Probe: a stored row with listingExchanges ['NSE','BSE'] and segment 'MAINBOARD', written by the NSE list then the BSE list, ended as ['BSE']; the same save also wrote segment and lastScrapedAt. Reproduced red by `scraper/tests/unit/scheduler/opening-day-strict-write.test.ts` (CORE case: expected ['BSE'] to deeply equal ['NSE','BSE']) on 3434fa9c, green after the strict writer. Shared path tracked in issue #951. | item 7 S4 round 4 (branch feat/item7-s4-opening-day-check): the opening-day check no longer calls the whole-row save; it asks the field-priority matrix (`DataConsolidationService.consolidateIPOData`) for the four fields and SETs only the decided values that differ from the stored row. The shared consolidated save was fixed separately on branch fix/951-consolidated-save-unclaimed-fields (2026-09-26): `extractConsolidatedData` drops every key outside the write's claim (the keys of the consolidated incomingData) on an UPDATE, and `extractListingExchanges` returns nothing for an unclaimed listingExchanges; lastScrapedAt stays as bookkeeping the write owns. | write-time regression test only: `scraper/tests/unit/services/consolidated-save-claimed-fields-951-938.test.ts` (#951 block: the issue probe, NSE-only then BSE-only 4-field writes on a stored ['NSE','BSE'], exact SET key set asserted). No table-level nightly check. | unguarded | §0.0.1, §2.1 |
| a nightly audit that aborts on one check's error, read by a delta consumer that counts not-run checks as resolved | any nightly multi-check audit run against a database whose migrations lag the audit code (production between releases), together with the consumer that diffs its output day over day | one check queries a column or table that the target database does not have yet; the error is not caught per check, so the whole run aborts and every later check silently never runs. The day-over-day consumer compares check ids present yesterday with ids present today and reports the missing ones as GONE, the same word it uses for a failure that was fixed, and prints 'no new findings'. The audit therefore goes blind and announces an improvement at the same moment. Wider shape: a diff that cannot tell 'absent' from 'passed' turns any crash of its producer into good news. | 2026-09-25 -- IPODhan production nightly floor (rfp-vps, /root/data-audit-ipodhan, audit checkout auto-pulled from main d888d8d3). Checks printed: 63 on 2026-09-23 (25 FAIL / 23 PASS / 15 UNVERIFIABLE), 23 on 2026-09-24, 4 on 2026-09-25. run-2026-09-25.log line 201: `error: column fs.row_key does not exist` at checkC_issueSizeSourceCapability (scripts/audit-detection-floor.mjs:507) -> `GATE FAILED: audit-detection-floor exited 2`. The SessionStart floor delta for 09-24 -> 09-25 printed `GONE (14)` and `VERDICT: no new findings`. Issue #1055; earlier narrower member #947 (UNVERIFIABLE instead of not-applicable), #945 (one check fixed). | #1056 MERGED 790c6ce2 (closes #1055, #947): runCheck() isolates every check in main() (an uncaught error prints UNVERIFIABLE and the run continues), isMigrationApplied(tag) gates the fs.row_key reader, and scripts/ops/floor-delta.mjs reports MISSING separately from GONE with a non-clean verdict. Verified on ipodhan_test: 81 declared checks all reported. The VPS audit checkout pulls main, so the first production run with the fix is 2026-09-26 03:45 IST. | scripts/ops/floor-delta.mjs MISSING list and verdict (tests scripts/tests/floor-delta.test.mjs; yesterday {a,b,c} today {a} -> MISSING 2, GONE 0, verdict not clean) plus the check_roster line in the floor itself | guarded |  |
| a precedence rule ranks an input type that no reader ever produces, so the rule is dead | any priority, override or precedence table (source ranks, document-type ranks, config layers) whose entries name an input the pipeline stores but never parses | a precedence table says type X outranks type Y, and X is fetched and stored, so every part of the chain looks present. But the reader that turns X into field values does not exist or does not list X, so X never produces a value and Y always wins. Nothing fails: X rows exist with a terminal status such as NOT_EXTRACTABLE, the table is valid, and every test of the table passes because it feeds values directly. Universal shape: a ranking over producers is only as live as the set of producers that actually emit; a rank for a silent producer is documentation, not behaviour. | 2026-09-24, IPODhan F-158: CORRIGENDUM ranked above RHP (OD-30, field-priority-matrix CORRIGENDUM: 0) but absent from AUTO_PERSIST_DOC_TYPES, so every stored corrigendum is NOT_EXTRACTABLE (staging: skyways-air-services-ltd; 2 CORRIGENDUM + 1 ADDENDUM rows). | feat/item9-corrigendum-reader (item 9, OD-90): CORRIGENDUM added to the one extractable list (document-admission-status.ts); its reader is the suggestion step (read_corrigendum_pages.py -> corrigendum-suggestions.ts), which records admin-reviewed suggestions in data_conflicts and writes no field until an admin accepts one as an ADMIN value. Fixes the CORRIGENDUM instance only; ADDENDUM is still ranked (75) and still NOT_EXTRACTABLE. | instance only: scraper/tests/unit/services/filing-auto-persist.test.ts 'CORRIGENDUM is on the one extractable list' + tests/integration/corrigendum-suggestions.integration.test.ts (suggestion row, accept = ADMIN write, dismiss = no write, no duplicate; mutation-tested). The CLASS check is still not built: a test that every document type ranked in the field-priority matrix is either in AUTO_PERSIST_DOC_TYPES or a named non-extractable exception with a reason (ADDENDUM would fail it today). | unguarded | §2.5.5, §2.2.1 |
| a provenance write drops the lineage its caller holds | any write path that holds a document's lineage ({method, docType, documentId, sourceSha, ...}) and writes some of its values through a shared door (upsertIPO -> DataConsolidationService -> FieldSourcesRepository.trackFieldUpdate) that has no parameter for it | The caller builds one lineage object and passes it to the provenance rows it writes itself, but a value it hands to a shared writer is recorded with whatever lineage that writer builds on its own (for ipos: only { policyOrigin }, or null). The row's source says a document supplied the value while the row cannot say WHICH document, so any reader that needs the document (the item 6 DOC fetcher, OD-91 receipts comparison, supersession) either guesses (the fetcher falls back to the first COMPLETED document of the family) or cannot act. Nothing fails: the row exists, the source is right, the value is right, and the gap is invisible until someone asks which document. | 2026-09-25 -- #993: scraper/src/services/filing-persister.ts persistFilingExtraction passed `lineage` (with documentId) to every child-table and ipo_details field_sources row but called upsertIPO(deps.ipoRepository, scraped, source, existing, contextFields) without it. Measured on ipodhan_staging 2026-09-25 (read-only): 331 of 331 field_sources rows with table_name='ipos' and source='DRHP' had no data_lineage.documentId (251 with null lineage, 80 with only policyOrigin), across 58 IPOs. Other tables are NOT uniformly affected (ipo_details 281 with / 37 without, financial_data 284 with / 0 without), which localises the drop to the ipos door. Note, measured the same day: the item 6 probe's NO_DOCUMENT_PROVENANCE count (264 of 360 class rows) is NOT caused by this class -- those 264 are 213 plan rows with no field_sources row at all and 51 owned by CHITTORGARH/NSE; the effect of this class is on SUPPLIED answers, whose documentId falls back to a guess. | #993 fix: upsertIPO gains a lineage argument that the filing persister fills with its existing lineage object; DataConsolidationService merges it (ConsolidateIPODataInput.incomingLineage, provenanceLineage()) into a provenance row ONLY when that row is written for the incoming source, never onto a row kept under another owner; OD-73's no-restamp rule is untouched. Existing rows: scraper/scripts/repair-ipos-lineage-document-id.ts (dry-run default, --expect-db required, prod refused without --allow-prod; stamps documentId only where exactly one COMPLETED filing of the IPO completed within 10 s after the row's write, otherwise leaves the row and lists it). | d_ipos_doc_lineage_document_id (docs/reviews/detection-checks/d_ipos_doc_lineage_document_id.json) -- any DRHP-source ipos provenance row written in the last 24h with no documentId. Covers the ipos door only; a child table whose writer drops the id is not covered by this predicate (ipo_details 37, ipo_risk_factors 3402, ipo_intermediaries 676 rows without documentId on staging 2026-09-25, cause unmeasured). | guarded | §2.5 |
| a regex corrupted by invisible control characters in the source | any pattern written into a source file through a shell heredoc or generated edit (scraper/scripts/*.py, scripts/lib/*.mjs, and every future generated matcher) | an escape sequence is interpreted BEFORE the file is written, so the source holds the literal control byte instead of the two-character escape. A pattern written as backslash-b-EPS-backslash-b becomes BACKSPACE-EPS-BACKSPACE and can never match anything. Nothing reports an error: the file parses, the module imports, the function runs and returns a clean negative. Editors, terminals, diffs and file-reading tools all render the byte invisibly, so the code READS correctly on screen. The failure therefore presents as a LOGIC gap - the matcher looks not-clever-enough - and the search for it goes to the algorithm rather than the bytes. | 2026-09-11, item 8a slice 3, and it was the THIRD occurrence in one night of the same underlying trap. Earlier the same night it made a mutation test silently no-op (the needle contained a backspace, matched nothing, and the suite stayed green - nearly recorded as 'the mutation survived, so the guard is hollow', a false conclusion about working code). Here it cost about thirty minutes: I checked the logic, then cleared __pycache__ suspecting stale bytecode, then compared module identity and globals, before finally printing the compiled code object's constants and seeing the backspace bytes. | #596 - the corrupted pattern repaired, and a test added that scans this slice's modules for control characters. Building such strings with chr(92) rather than writing a backslash through a heredoc is the prevention; the test is the detection for when prevention is forgotten, which it has been three times. | scraper/scripts/test_peer_table_columns.py - test_no_source_file_contains_a_control_character scans every peer_table_* module for bytes below 0x09 or between 0x0b and 0x1f. Mutation-checked: injecting one real backspace into a regex turns it red by name. Scoped to this slice's own files deliberately - a check that reports on files its owner cannot fix is a check that gets switched off. | guarded for the item 8 modules only. Every other generated matcher in the repo is unguarded, and a repo-wide scan is the obvious extension - not taken here because it belongs to whoever owns the lint gate rather than to an item 8 slice. |  |
| a repair rule that forbids arithmetic and mandates re-reading from source is unrunnable when most rows have no source left to re-read | any data-repair or backfill policy written as 'never compute the corrected value, always re-derive it from the original source' — unit migrations, provenance repairs, re-extractions, currency or precision changes on historical rows | a safety rule is written after a real incident where a value was transformed blindly and became nonsense. The rule is correct about the failure it prevents, and it is stated absolutely: never arithmetic, always re-read from source. It is written while looking at the CODE PATH, where a source always exists because the row is being created from one. Nobody measures the EXISTING rows, where the source is a document that was purged under a retention policy, an endpoint that no longer serves a delisted entity, or a page that has changed. When the repair is finally built, it runs on the minority of rows that still have a source and, obeying the rule's own 'leave it alone and report it' clause, skips the rest. The column is then left holding BOTH the old and the new shape — strictly worse than before the repair, and precisely the mixed state the migration existed to avoid. The rule was not wrong; it was untested against the population it would have to run on. The generic test, cheap and available before any tool is written: join the rows the repair must touch against the source it names, and count how many actually have one. | 2026-09-21 — IPODhan item 11 (OD-20 crore conversion), measured read-only on ipodhan_staging. The spec §5.2 says, in bold, 'Never by arithmetic on the stored number... The repair is a re-runnable tool that, per row, re-reads the value from its source (the offer document via the extractor, or the exchange payload); a row whose source cannot be re-read is left alone and reported, not guessed.' The rule is well-founded: it was written after a share count was once stored as a rupee amount. Measured against the 307 rows a conversion would touch (`issue_size > 0 AND segment IS NOT NULL`), only 46 have a COMPLETED extractable document (PRICE_BAND_AD / RHP / DRHP / PROSPECTUS) — 261 have none, 240 of those LISTED. A conforming tool would convert 15% of the column and report 85%. The source shortage has a documented cause that the rule's author also wrote: OD-32 purges document FILES after 7 days, so for older IPOs the document the repair would re-read is deliberately gone. Two rules, each sound alone, that cannot both be satisfied on the same rows. Separately measured and relevant to the resolution: no row in any of the five target columns holds a crore-shaped value, so the unit for this population is MEASURED rather than unknown — the condition the 'never' was protecting against does not currently hold. MEASURED FURTHER 2026-09-21: OD-32's retention table names the kept page text as the thing that rescues this -- 'this is what every later re-read reads' -- so the obvious resolution is 'a re-read reads the WORDS, not the PDF bytes'. Measured, that rescues nothing for existing rows: of the 261 sourceless rows, ZERO have retained page text. The zero is real rather than a broken query -- `document_pages` holds 8,497 pages across 26 documents on 17 IPOs, and all 17 are already inside the 46 that have a completed document, so page text adds 0 new coverage over 366 IPOs. The retention rule is sound in principle and almost entirely unpopulated in practice, because the page-text writer went live recently (#560, #628) while the rows needing repair are mostly older LISTED IPOs. This is the sharper form of the class: the policy kept the RIGHT thing, and it covers 5% of the population that needs it. | none — escalated to the owner on #854 rather than resolved by a builder, because every path either relaxes a documented safety rule or commits operational cost. The three options recorded there: widen the source to the exchange payload (an operational call about re-fetching 261 mostly-LISTED IPOs); allow arithmetic where the unit is PROVABLY known, with the proof re-run as a precondition inside the tool (refuse to convert unless 100% of target rows are rupee-shaped in that run, so it fails closed the moment the data stops matching); or convert nothing and fix the readers instead, which is what #871 does for the two scorers. The general lesson is about ORDER, not about which option wins: measure the source's availability on the real population BEFORE writing a repair policy as an absolute, and state the measured coverage in the policy itself. | none yet, and the right one is a policy-time check rather than a runtime one: any repair or backfill spec that names a source must carry the measured percentage of target rows for which that source is currently available, re-measured when the spec is used. A retention policy that deletes a source SHOULD name every repair path that reads it — OD-32 and this repair rule were written for the same system and never checked against each other. A cheap proxy for the runtime half: the tool reports converted / skipped counts and FAILS rather than exits 0 when skipped is non-zero, so a partial migration can never be mistaken for a completed one. Sharper still, and the version worth building: a retention policy that names a downstream reader ('this is what every later re-read reads') is checked against what that reader will ACTUALLY FIND -- the measured share of target rows the kept artefact covers -- not against what the policy intends to keep. A kept-thing that is correct and 5%-populated reads as solved on every review that only checks the policy text. | unguarded | §5.2 |
| a retry budget charged for outcomes that could not have succeeded, so a configuration gap retires the work item as if it had genuinely failed | any bounded-retry queue (attempts cap, backoff, dead-letter) whose failure outcomes mix 'we asked and it failed' with 'we could not ask at all because of how we are configured' | a work item is retried until an attempts ceiling stops it, and every attempt is counted the same way — but some failures are facts about CONFIGURATION (no adapter registered, no mapping for this field, no document type declared), not about the item. Those attempts cannot succeed until the configuration changes, so the budget burns in the first few cycles and the item is retired. When the configuration is later fixed, nothing reopens the retired items: the ceiling is a claim-time filter, and the event that would have made them askable (a manifest version bump, a new adapter) does not touch the counter. Generic test: for each outcome class, ask 'could re-asking with the same configuration ever change the answer?' — if not, it must not spend retry budget, and the configuration-change event must be wired to reopen what it retired. Corollary seen in the same data: a counter shared between an uncapped state (NOT_AVAILABLE_YET) and a capped one (CHECK_FAILED) lets the uncapped state pre-spend the capped state's budget, so a row's FIRST real failure can arrive already past the ceiling. | 2026-09-23 -- IPODhan #884. recordOutcome charged attempts + 1 on every CHECK_FAILED, including COVERAGE_GAP ('<SOURCE> has no mapped field for …', 'no documentType in manifest for this field') and SOURCE_UNREACHABLE via NO_FETCHER_REGISTERED; the claim query stops at FIELD_PLAN_RECLAIM_MAX_ATTEMPTS = 5. Measured read-only on ipodhan_staging: 6,624 CHECK_FAILED rows at attempts >= 5; the repair dry run resets 4,811 across 72 IPOs (CHITTORGARH no-mapping 3,250; BSE 898; DOC no-documentType 288; NSE 227; NSE no-fetcher 144; DOC column read 4) and holds 1,813 real ones (1,780 'no document provenance … (extractor gap or field absent)', 33 'no field result returned'). The above-cap rows (101 per IPO at attempts 6/8/9/13 on himalayan-solar, coreintegra-consulting, adroit-industries, bench-mark-infotech) came from the uncapped NOT_AVAILABLE_YET leg: each row was asked N times as NOT_AVAILABLE_YET (attempts + 1 each) and flipped to CHECK_FAILED once the document arrived, landing past the cap on its first real failure. | #884 (PR #923). Round 0: recordOutcome skipped the increment for a config-gap cause and gap rows re-asked at claim priority 2. Review round 1 found that priority is per-IPO only (claims filter by ipo_id; document-cycle walks IPOs sequentially), so ~6,220 uncharged rows would be re-asked every slot forever. Round 1: fetchers DECLARE a gap on the answer from a closed list (FIELD_PLAN_GAP_CODES: NO_FETCHER, NO_MAPPING, NO_DOCUMENT_TYPE, COLUMN_READ_NOT_IMPLEMENTED, NO_DOCUMENT_PROVENANCE); the walk records the row under the cycle's GAP KEY (manifest version + fetcher-coverage fingerprint + EXTRACTOR_VERSION, buildFieldPlanGapKey) only when EVERY rank failed with a gap; recordOutcome does not charge it and stamps the key into the existing cause column; claimNextDueField offers a stamped row again ONLY when the current key differs (§2.3 reconciliation, OD-78 same cause no retry). No schema change. The repair tool now also resets capped extractor-gap rows and its --undo touches exactly the rows it changed. The NOT_AVAILABLE_YET-pre-spends-the-cap corollary is NOT fixed here: it needs a separate failure counter (a migration, owner-gated). | pull_plan_config_gap_at_cap (nightly floor) + scraper/tests/unit/config/manifest-rank-coverage-gaps.test.ts (CI, shrink-only baseline of 311 manifest ranks the walk cannot ask, via resolveFieldSourcePolicy) | guarded | §2.3, §2.5 |
| a source's zone-less local clock time is parsed with new Date() in a UTC process | any scraper field whose SOURCE stamps its own observation time as a wall-clock string with no numeric offset and no timezone name (e.g. BSE `Maxdt`: "9/24/2026 5:00:00 PM", NSE demand-graph timestamps: "02-Sep-2026 17:00:13") and the scraper turns that string into a stored instant with `new Date(rawString)` / `new Date(rawString).toISOString()` | `new Date(<local-time-string-with-no-offset>)` is parsed by the JS engine in the PROCESS's own timezone, then `.toISOString()` reads it back in UTC. The prod/staging scraper runs with TZ=UTC (PM2 does not propagate a host TZ to the process), so a source string that is actually Asia/Kolkata (IST, UTC+5:30) wall-clock time is stored 5h30m AHEAD of the true instant. Downstream, any freshness/future guard comparing the stored value against `now()` then either (a) skips the write outright as "more than N minutes in the future" — losing the observation entirely, unrecoverable after the fact — or (b) accepts it when the write happens to land late enough in the next cycle that the false-future delta has shrunk inside the guard's tolerance, silently persisting a value stamped 5h30m later than it really was. `mapBSESubscription` (scraper/src/scrapers/bse-api-scraper.ts) hit case (b): BSE's day-end subscription snapshot Maxdt ("<M>/<D>/<YYYY> 5:00:00 PM") was parsed with `const parsed = maxdt ? new Date(maxdt) : new Date();` then `.toISOString()`, storing 17:00:00 UTC (= 22:30 IST) for an observation that actually happened at 11:30:00 UTC (5:00 PM IST). W-38's resolveSubscriptionSnapshotTimestamp (data-persister.ts) skipped most of the resulting future-dated intraday reads (pm2 logs: prod 465, staging 11 skips observed) and stored the day-end figure wrong whenever persistence happened late enough. The date-tz-parse-ratchet.test.ts static grep (T-327) did NOT catch this site because its RISKY_CHAIN/INLINE_RISKY_CHAIN patterns require `new Date(<bareIdentifier>)` to appear immediately after `=` or be chained directly into `.toISOString()`/`.getTime()`; the actual code was `const parsed = maxdt ? new Date(maxdt) : new Date();` — a ternary — which neither regex shape matches, so a real member of the T-327 class shipped past a gate built specifically to catch it. | 2026-09-24 -- BSE `mapBSESubscription`, verified live against BSE's own Pubissues_GetBkbldgCatdem_ng API (IPO_NO 7992/7989/7991/7988/7987/7984, fetched 2026-09-24: every live IPO's Maxdt was exactly "9/24/2026 5:00:00 PM", the day-end IST figure). Rows already written wrong on ipodhan_staging: 273 subscriptions rows with scope=BSE_ONLY stamped exactly 17:00:00.000000 (dry-run of scripts/repair-bse-subscription-ist-shift.ts against ipodhan_staging, 2026-09-24), spanning slugs including liqvd-digital-india-ltd, unitec-fibres-ltd, s-k-offset-ltd, himalaya-nutravedics-india-ltd, injecto-polymers-ltd, century-business-media-ltd, raksan-transformers-ltd, maharaja-and-speedex-india-ltd, om-galaxy-ltd, shakti-polytarp-ltd, vama-wovenfab-ltd, vivekanand-cotspin-ltd, robokidz-eduventures-ltd, fx-multitech-ltd, dated 2026-09-12 through 2026-09-23. Intraday (non-day-end) skips are not recoverable: they were never persisted (W-38 rejected them as future-dated), so only the day-end row survives to repair. NSE's own equivalent parser (`parseNseObservedTimestampIST` in scraper/src/scrapers/nse-api-client.ts) was already fixed correctly with the same class in mind (Date.UTC(...) minus the IST offset, inlined so the T-327 ratchet recognizes it as safe) -- the class sweep for this defect found no other zone-less-IST-string site among scraper/src/scrapers (`timestamp:` writers elsewhere all use `new Date()` = scrape time, not a source-supplied observation string, which is out of this class). | packages/shared un-affected; scraper/src/utils/date-string-parsing.ts gained `parseIstMdyToUtcIso()` (M/D/YYYY h:mm:ss [AM/PM] -> UTC ISO, TZ-invariant by construction, same Date.UTC(...)-minus-offset inlined shape as parseNseObservedTimestampIST so the T-327 ratchet recognizes it as safe); scraper/src/scrapers/bse-api-scraper.ts `mapBSESubscription` now calls it instead of `new Date(maxdt)`. Existing wrong rows repaired by scraper/scripts/repair-bse-subscription-ist-shift.ts (dry-run default, --expect-db required, prod refused without --allow-prod, de-duplicates a shifted row against an already-correct identical row for the same ipo). | t_source_local_time_shift (docs/reviews/detection-checks/t_source_local_time_shift.json) — flags any subscriptions row with scope=BSE_ONLY whose stored time-of-day is exactly 17:00:00, the signature this specific site's Maxdt-as-UTC bug leaves; a future member of the class (a different source or field parsed the same wrong way) needs its own predicate added to the same check rather than a new one, since the generic signature (a source-local wall-clock string with no offset, parsed by `new Date(str)` in a TZ=UTC process) differs per source's own reporting convention and cannot be checked generically without knowing each source's own wall-clock stamping habits. | guarded |  |
| a spec restates a schema constraint from memory, the schema later changes, and a whole workstream is justified by the stale copy | any design document that quotes a limit the code or database actually owns — a column precision, a rate cap, a timeout, a size ceiling, a retention window — and then reasons from the quoted number rather than from the source | a spec needs to argue about a limit, so it writes the limit into a table. The number is right on the day it is written. Later the schema is widened, or was never what the author remembered, and nothing re-checks the copy because a document is not code and no test reads it. Work is then SIZED and JUSTIFIED from the stale number: a migration is planned, a repair tool is scoped, an owner is asked to approve a not-cleanly-reversible change. Nobody queries the live column, because the spec looks authoritative and quoting it feels like evidence. The tell is that the justification chain never touches the database: 'the spec says the column is numeric(15,2), therefore it overflows at X, therefore we must convert.' Each arrow is valid and the first premise was never measured. The generic test costs one query: before any work justified by a stated limit, read the limit from the thing that enforces it. | 2026-09-21 — IPODhan item 11 (OD-20 crore conversion). The spec's §5.2 capacity table states rupee columns are `numeric(15,2)` (ceiling about 10 lakh crore) and uses that to argue Saudi Aramco at Rs 2.5 lakh crore would sit at a quarter of the ceiling — tight enough to motivate converting five columns to crore, a data migration the spec itself calls its own release and not cleanly reversible. Measured on the live database: all five columns are `numeric(18,2)`, ceiling Rs 1,00,00,00,000 crore. Aramco uses 0.025% of it — one four-thousandth, not one quarter. India's largest (Hyundai, Rs 27,870 cr) uses 0.0028%; the biggest row we hold (NSE, Rs 26,579.64 cr) uses 0.0027%. Postgres accepts the Aramco value in `numeric(18,2)` directly, verified. The owner had asked this exact question earlier — 'will the current field length hold the world's largest IPO? if yes there is no need to change it' — and the answer, from the schema rather than the spec, is yes with a factor of 4,000 to spare. I repeated the spec's 15,2 to the owner before reading `packages/shared/src/db/schema.ts`; the owner caught it by re-asking. | the immediate correction is to the SPEC, not the schema: §5.2's precision table now states numeric(18,2) with the measured headroom, and the conversion's capacity justification is withdrawn. The durable remedy is narrower than 'keep docs in sync', which never happens: a spec that quotes a limit names the OWNER of that limit next to it (`packages/shared/src/db/schema.ts:287`), so a reader can check in one step, and any work justified by the limit re-reads it from the owner as its first action rather than citing the document. | none built. The cheap one is a test that parses capacity claims of the shape `numeric(P,S)` out of the design docs and asserts each against `information_schema.columns` for the named table.column — it is a small regex plus one query, and it would have failed the day the spec and schema diverged. Worth noting what made this findable at all: the owner asked the same question twice. The first time I answered from the spec; the second time I measured. A question re-asked is a signal that the first answer was thin, and is cheaper to act on than any check listed here. | unguarded | §5.2 |
| a step that skips silently is indistinguishable from a step that never ran | scraper cycle steps returning { status: 'skipped' } (scraper/src/services/**, dispatched from src/index.ts STEP_NAMES) | a cycle step decides it has no work and returns skipped WITHOUT logging, so the log contains the same absence whether the step ran and found nothing, was never registered, threw before its first statement, or was skipped by the dispatcher. Any proof read of the form 'grep the log for this step's line' then produces no evidence in either direction, and the natural misreading is 'the step is not wired'. | 2026-09-10 staging cycle 20:45:02Z on release 20260910-202813-2ad49a4e: triggerPageRevalidation drained an empty touched-slug list and returned skipped with no log line. ADMIN_API_TOKEN was present, so the token skip was excluded; the absence still could not be read, and it cost the item 21 slice 3 staging proof for that cycle. | item 21 slice 3b (this PR) - the empty-drain path emits one line, 'Page revalidation skipped', carrying sent:0 and the reason | scraper/tests/unit/services/page-revalidation-trigger.test.ts, case 'SAYS it sent nothing, so a quiet cycle is not mistaken for a step that never ran' - spies on the logger and asserts the empty-drain path emits exactly one line with sent:0 and the reason string. Mutation-checked: deleting the logger.info call turns the case red (1 failed | 10 passed) and restoring it green (11 passed). | guarded for this step only; the other STEP_NAMES members returning skipped have not been swept for the same silence |  |
| a test asserts a rule the codebase deliberately removed | any validation threshold, guard or business rule that was removed on purpose, and the tests written against it while it existed | a test fails because the code no longer enforces something it once did, and the removal was CORRECT. The failure looks identical to a regression, so the obvious repair — change the code until the test passes — REINSTATES the removed bug. The tell is a test whose docstring or comment cites a specific constant or rule as ground truth ("min=10 per field-priority-matrix") while the code carries a dated comment explaining why that constant was deleted. Universal shape: when a rule is removed, its tests are a second, independent copy of the rule, and nothing links the two, so the removal is complete in the code and silent in the tests. The danger is asymmetric: a stale test read as a regression produces a real product defect, while a stale test read correctly costs one edit. | 2026-09-20 — issue #832 / PR #836. `web/tests/integration/data-flow/lot-size-validation.integration.test.ts` (2 tests) asserted that a lot size below 10 is rejected, citing "min=10 per field-priority-matrix" in its own docstring. The matrix carries `lot_size: validation: { min: 1, max: 100000 }` with a dated comment at `field-priority-matrix.ts:566` recording the deletion as deliberate: "`min: 10` here was a SECOND, silently-enforced copy of the 'legal minimum lot' rule (SEBI sets no universal floor — a high-priced issue can legally have a lot under 10 shares, e.g. lot=8 at a band of ~1700-1785). `validateValue` only ever sees the bare number, never the price band, so it cannot tell a legitimate high-price lot from garbage." Reinstating the floor to satisfy the test would have rejected legitimate high-price IPOs. Found only because PR #834 fixed a harness gap that had been masking these two failures among twelve | #836 corrects the TEST to the matrix's real floor (rejection case `lot_size=0`, boundary case `lot_size=1`), leaving the production rule as the deliberate removal left it. The general remedy is the classification step, not this edit: every failing test is triaged as (a) product bug, (b) test-setup gap, or (c) stale test BEFORE any code is changed, and a (c) is fixed test-side only | none yet, and a mechanical one is hard: nothing links a removed constant to the tests that asserted it. What is cheap and would have caught this: when a validation threshold is deleted, grep the test tree for the literal value and the field name in the same change, and treat any hit as part of the removal. A review question that generalises: 'if this test is failing, would fixing the CODE to satisfy it re-introduce something a dated comment says was removed on purpose?' Note that `config/web-integration-baseline.json` recorded this file as failing for ten days without anyone asking which of the three classes it was — a shrink-only list with no triage behind its entries hides exactly this | unguarded |  |
| a test's git fixture runs in the host repository because an inherited GIT_DIR (or a failed cd) points git back at it | any test (shell or node) that builds a throwaway git repository with git init / config / add / commit, especially one run by a git hook (pre-commit, pre-push) inside a real checkout | the fixture creates a temp directory and runs git init, git config user.*, writes files and commits there. Two ways git ends up operating on the CALLER's repository instead: (1) the test runs under a git hook, and git exports GIT_DIR to hooks (in a push from a linked worktree GIT_DIR=.git/worktrees/<name>); an exported GIT_DIR wins over both `cd <tmp>` and `git -C <tmp>`, so every fixture command targets the real repo, and `git init` under a foreign GIT_DIR rewrites its config (core.bare=true) -- this was the measured mechanism; (2) a `( cd "$tmp"; git ... )` whose cd fails without stopping. Its shared .git/config gains the fixture's identity (and core.bare when the fixture initialises a bare repo), fixture files overwrite real files, and fixture commits land on the real branch. In a linked worktree the damage spreads, because every worktree shares the main checkout's config: one bad run broke git for every checkout of the repo at once. A test run by a hook makes it worse: the developer never chose to run it. | 2026-09-25 ~13:50 IST, IPODhan, owner's laptop: pushing branch chore/pre-push-local-gate (PR #1037, a local pre-push gate) ran the new hook; afterwards D:/Abhay/Ventures/IPODhan/.git/config held `bare = true` and `[user] email = test@example.com name = Test` (session start showed the user as Abhay), every git command in the main checkout failed with 'this operation must be run in a work tree', and the pre-push worktree held three commits authored Test (4f78e4bd chore: init, edb61352 fix(scraper): tweak foo, 59ef23d8 test(checks): add check) plus fixture overwrites of README.md, scraper/src/services/foo.ts and scripts/lib/substance-checks.mjs (900-line diff). Same unguarded shape on origin/main: scripts/tests/deploy-config.test.sh build_fixture_repo `( cd "$repo"; git init -q; git config user.email ...; git add -A ... )` with no `|| exit`, and the Tier A review of #1050 measured that its fresh_dir paths fail on this laptop without a Windows-style TMPDIR. | #1037 fix round 7475b234 (RCA reproduced in a probe repo: the hook's env showed GIT_DIR=.../.git/worktrees/envprobe-wt): the gate clears git's repo-local env vars before any check; hermetic-git.sh/.mjs helpers wired into 9 fixture tests. NOT merged: the fresh Tier A re-check found the helper guard passes an empty or repo-root dir (cd '' succeeds) and 2 fixture tests unwired -> deferred as #1063 under the contract size rule. The first theory (Windows TMPDIR) was measured wrong: the temp dirs resolved fine. | scripts/tests/pre-push-local.test.sh case 11 on the #1037 branch (3 FAIL on the old gate, 45/45 on the new) -- not on main while #1037/#1063 are deferred | unguarded |  |
| a threshold constant is written in one unit and compared against a column stored in another, so every row lands in the same band | any scoring, tiering, banding, alerting or gating rule whose literal thresholds were typed from how a human SAYS the quantity, while the column holds it in the unit the system STORES | a rule compares a stored value against literal thresholds a person wrote while thinking in display units — crore, thousands, MB, minutes, percent-as-0-to-100. The code type-checks, the arithmetic is valid, nothing throws, and no test fails if the fixtures were typed in the same wrong unit as the thresholds. But because real data is uniformly in the other unit and the gap is a fixed multiplier, EVERY row falls on the same side of every boundary. The output is a constant dressed as a calculation: a score that contributes no ranking, an alert that never fires or always fires, a tier everyone is in. It is not a wrong answer for some inputs, which is what testing usually looks for; it is the same answer for all of them, which reads as 'that component just isn't very discriminating'. The generic test is to feed the rule the LARGEST and the SMALLEST real stored values and assert the outputs DIFFER — a rule whose extremes agree is either unit-confused or dead. Fixture-level tests cannot find it, because the fixture and the threshold usually come from the same person's head on the same day. | 2026-09-21 -- IPODhan item 11 (OD-20), two files with the identical defect. `web/lib/services/ipo-scoring-realtime.ts` scored issue size against 1000/500/100 and `web/lib/utils/rating-calculator.ts` against 1000/500/100 -- both CRORE figures -- while `ipos.issue_size` is stored in RUPEES. Measured read-only on ipodhan_staging: 314 of 366 rows hold a rupee-shaped value (>= 1e7), and the SMALLEST genuine row (PIYUSH, Rs0.70 Cr = 7,007,320 rupees) is still 7,007x the top threshold -- so the largest IPO on the site (NSE, Rs26,579.64 Cr) and the smallest scored IDENTICALLY. Both terms were constant across every IPO. The existing unit tests passed throughout because their fixtures were typed in crore (`issueSize: 2500, // Rs2500 Cr`) -- the same confusion as the code, so they agreed with it; the shared `mockIPOData` used 1500, meaning every OTHER test in that file was silently exercising the bottom band. `rating-calculator.ts` had NO test at all, which is why its half stayed 'documented, not fixed' (F-77) while the twin was tracked (F-95). Reached from a live API route (`app/api/ipos/[slug]/rating/route.ts`) and `scripts/calculate-ratings.ts`. A shared `RUPEES_PER_CRORE` constant already existed in `web/lib/utils.ts`, carrying a comment that says 'one calc treated it as crores' -- the class had been SEEN and the constant created, without the two call sites being changed. SECOND OCCURRENCE 2026-09-24 (F-156): web/lib/admin/dynamic-validation-rules.ts validates issueSize against 10 and 100000 CRORE while the column stores rupees; 336 of 337 staging rows trip the high warning. Root: the unit SSOT itself is wrong -- field-manifest.json tags the five OD-67 rupee columns crore -- so every consumer that trusts a label inherits the error. | convert at the READ using the already-shared constant (`value / RUPEES_PER_CRORE`) rather than restating the thresholds in rupees -- OD-20 makes crore the stored unit, so the crore-denominated comparison is the one that survives the coming migration and the rupee-denominated one would have to be undone. Failing tests first, written from REAL stored values rather than fixtures: 3 red on the scoring service (every case returned the maximum 0.75, including a Rs0.70 Cr IPO) and 5 red on the rating calculator (`calculateMarketScore` exported for the purpose, since an untested private function is what let F-77 survive). Then the pre-existing fixtures were corrected to rupees, because leaving them would have re-encoded the defect in the test suite. Both mutants killed by reverting the division (8 failed). ROUND 2 (2026-09-24, Tier A review REVISE): round 1's admin fix converted at the wrong edge and under the wrong key -- `web/lib/admin/amount-unit-edge.ts` looked up `issueSize`, but the dynamic admin page's real record and column keys are snake_case (`GET /api/admin/dynamic/ipos/[id]` snake_cases every key in route.ts; `column.name` from schema-introspector.ts is the Drizzle column's real DB name), so the conversion, the existing crore label and the crore validator never fired on the live page -- round 1's own tests passed only because they fed the fictitious `{issueSize}` shape. The supervisor's correction: since the live admin page has ALWAYS shown and saved raw rupees (staging ADMIN-sourced values are rupee-scale, e.g. 7,329,740,494.00), the conversion was deleted rather than re-keyed -- `amount-unit-edge.ts` and its test are gone, `web/app/admin/dynamic/[table]/[id]/page.tsx` no longer converts on load or save. Instead the label (`field-labels.ts`) and validator (`dynamic-validation-rules.ts`) got a new snake_case `issue_size` entry that is truthful about the real stored unit (label 'Rs (rupees)'; validator thresholds are the same Rs10 crore / Rs1 lakh crore bounds converted to rupees via `RUPEES_PER_CRORE`, never retyped). The old camelCase `issueSize` entries are kept only because an unrelated pre-existing suite (`dynamic-validation-rules.test.ts`) still exercises them in isolation -- they are documented as dead on the live page. `scripts/generate-field-manifest.mjs`'s `unitForField` also had a MINOR gap fixed in the same round: an unrecognised `current_unit` (e.g. a typo) silently fell through to the class default instead of failing loud; it now throws (`manifestUnitFromCurrentUnit`, unit-tested directly). | TWO checks, corrected 2026-09-24 round 2: (1) `scripts/tests/manifest-unit-matches-current-unit.test.mjs` (`node --test`, wired into `.github/workflows/pr-gate.yml`'s 'Manifest unit follows stored unit' step, runs on every PR) -- for every column the amount-columns probe measured a current_unit for, asserts scraper/config/field-manifest.json's `unit` tag matches it, not the column's amount-class default; carries mutation tests proving a wrong tag is caught AND that an unrecognised current_unit throws rather than silently defaulting (round 2 MINOR fix). This is the schema-level companion the entry originally proposed: `docs/design/probes/amount-columns.mjs`'s current_unit field is read by the generator (`scripts/generate-field-manifest.mjs`'s `unitForField`/`manifestUnitFromCurrentUnit`), so a future column whose measured storage disagrees with its class is caught at the SSOT, not at each consumer. (2) `web/tests/unit/lib/admin/issue-size-live-key.test.ts` (part of `npm run test:unit`, runs on every PR; REPLACES round 1's deleted `amount-unit-edge.test.ts`, which drove the fictitious `{issueSize}` shape and never exercised the live page) -- drives `getFieldLabel('ipos', 'issue_size')` and `validateCustomField({ tableName: 'ipos', fieldName: 'issue_size', ... })`, the EXACT call shape DynamicFormGenerator uses, with real staging min/max rupee values, a real string-typed form value, and a direct demonstration that the dead camelCase `issueSize` validator gives the WRONG (silent) verdict on the same real low value -- proving the check discriminates and that round 1's fix was not load-bearing. Both checks now run in CI (grep-verified against `.github/workflows/pr-gate.yml` and `web`'s `test:unit` script), so `guarded` is a measured claim, not a hoped one. Neither check covers the scoring/rating pair from the first occurrence (already fixed and unit-tested per fix_prs); a future occurrence in a NEW consumer of an amount column is caught by check (1) if it reads field-manifest.json's unit, or needs its own instance of check (2)'s pattern otherwise. | guarded |  |
| a tracking issue outlives the code it describes, and is still read as a work order | any backlog item whose body states a measured condition of the codebase — a missing column, a churning row, an absent guard, a count — and which is picked up for work days or weeks later | an issue is filed with an accurate, well-evidenced description of a defect. Work lands afterwards that removes some or all of the described condition, but nothing re-opens the issue's body to say so, because the later work was tracked under its own number and was never framed as closing this one. The issue then reads exactly as it did on the day it was correct, and a later session picks it up and BUILDS THE FIX IT PRESCRIBES. Universal shape: an issue body is a measurement with a timestamp, but it is rendered without one and read as a present-tense fact. The danger is asymmetric and worst for well-written issues — a vague issue forces a re-measurement, while a precise one with file:line evidence invites the reader to trust it and skip straight to building. Two distinct losses: wasted work, and, when the issue's suggested shape contradicts a decision made later, a REGRESSION that undoes the newer reasoning while looking like progress. | 2026-09-20 — issue #759 (S6 churn-stop). Its three premises each held when filed and none held on `refs/remotes/origin/main` when it was picked up as a wave-1 build item. (1) "165 rows go CHECK_FAILED every cycle": a doubling backoff exists at `packages/shared/src/repositories/ipo-field-plan-repository.ts:299-303` (15m -> 30m -> 60m, capped at 6h by `FIELD_PLAN_BACKOFF_MAX_MINUTES`), plus a finite `attempts < FIELD_PLAN_RECLAIM_MAX_ATTEMPTS` bound at :611. (2) "pollutes CHECK_FAILED so real failures are indistinguishable": #785 added persisted `reason_code varchar(32)` and `cause text` columns (`schema.ts:1782-1783`) and `classifyFailure` maps this exact condition to `SOURCE_UNREACHABLE` (`field-plan-walk.ts:1438`). (3) its suggested fix — add a `NO_FETCHER` enum member — is the terminal treatment the walk explicitly rejects at `field-plan-walk.ts:700-711`: "Registering the adapter must be enough to make the field askable again -- retiring it terminally would mean a config gap silently outlived its own fix." Building the prescribed migration would have reinstated the bug the comment was written to prevent. Same session, same shape, three more times: items 8, 18, 22, 34 and 35 were all carried in the completion inventory with verdicts the code refuted (item 8's row repeated a GitHub issue TITLE while that issue's BODY said "No fix needed"; item 34's spec_ref validation was present and running all along), so five of wave 1's six items came back as corrections rather than builds | no code fix — the remedy is a procedure, and it is the one that caught this: verify every premise of an issue or inventory row against an explicit ref BEFORE writing code, and treat the body as a dated measurement rather than a description of now. Recorded as policy in `.claude/rules/staging-is-the-release-gate.md` R5 ("a card saying DONE is a claim; verify the artefact on the explicit ref"), which this case extends in the other direction: a card saying NOT DONE is equally a claim. #759 re-measured and recommended closed as obsolete rather than built | none, and a fully mechanical one is unlikely: no link exists from an issue body to the commits that invalidate it. What is cheap and discriminates: an issue whose body cites `file:line` evidence can have those exact lines re-read on the named ref at pickup time, and a mismatch is the signal. Of wave 1's six items, that check would have flagged five. A weaker but automatable proxy: flag any issue older than N days whose body quotes code, at the moment it is moved into a build queue. Note that the completion inventory itself (`docs/design/pull-model-completion-state.md`) is the same hazard at larger scale — it is a measurement rendered as a status table, which is why its verdict counts must never be snapshot-tested (see `build-plan-board.test.mjs`) | unguarded |  |
| a value passed to a resolver so it can JUDGE is treated by that resolver as a value being ASSERTED | any conflict resolver, merge step, consolidator or reconciler whose input is one flat object, where the caller must supply both (a) the values it is actually claiming and (b) surrounding facts the resolver needs in order to judge those claims | the resolver needs context to do its job — an entity's type to pick validation thresholds, a segment to choose plausibility floors, a status to decide which rules apply. The caller has that context sitting on the stored row, so it reads it from there and puts it in the same payload as the real claims, because the payload is one flat object and there is nowhere else to put it. The resolver cannot tell the two apart: every key it receives is a claim by the caller's source. The context values are usually IDENTICAL to what is stored (they came from there), so nothing visibly changes and no test fails — but provenance is re-stamped to the caller's source, and any convergence logic reads 'both sides now agree' and CLOSES open disagreements about fields the caller never examined. The damage is invisible precisely because the numbers do not move: the audit trail changes while the data does not. The generic test is to ask, of every key in a resolver's input, 'is the caller ASSERTING this, or does the caller merely NEED it?' — and to check whether the resolver has any way to be told which is which. | 2026-09-21 — IPODhan, the offer-document write path, found by tracing the owner's rule 'a new document should only be adjudicated on its own fields'. `filing-persister.ts:1228-1242` builds its payload by reading five identity fields off the EXISTING row (`companyName`, `segment`, `offeringType`, `status`, `listingExchange`), plus `openDate`/`closeDate` fallbacks at 1289-1303. They are needed as context — the consolidator reads `segment` and `offeringType` to choose validation thresholds (an SME issue has different plausibility floors than a mainboard one). But the consolidator iterates `Object.keys(incomingData)` (`data-consolidation-service.ts:1068-1073`) and treats every key as a claim by source `DRHP`. Fields the document genuinely did not supply arrive `undefined` and ARE correctly skipped (guard at 1427-1444, `NO_INCOMING_VALUE`, no conflict row) — so the system already obeys the rule for the ordinary case. The re-asserted five are not undefined, pass the guard, enter full resolution, and on an equal value hit `autoResolveConverged` (1903-1912), closing open conflicts for fields the document never read. MEASURED EXPOSURE, and it is much smaller than the field counts suggest: of 31,755 auto-resolved conflicts on staging, only **23** involve `DRHP` on either side at all, and only **11** are on the re-asserted fields (9 `companyName`, 2 `status`). A first count of 18,583 'suspect' rows was wrong — it counted every auto-resolution on those FIELD NAMES, but the top pairs are website-vs-website (NSE/MONEYCONTROL 3,209 on `openDate`, BSE/CHITTORGARH 2,394 on `companyName`), which are genuine convergence between two real sources and nothing to do with this path. | not yet built; owner approved the approach 2026-09-21. Separate the two roles rather than trying to guess them apart inside the resolver: (1) pass the identity fields as an explicit CONTEXT parameter the resolver may read but never treats as a claim; (2) restrict resolution to the fields the document actually extracted, using the `onlyFields` narrowing that already exists at `data-consolidation-orchestrator.ts:281-286` and which this path simply does not use; (3) refuse `autoResolveConverged` for any field absent from the incoming document — closing a conflict is a claim about evidence, and re-sending a stored value is not evidence. Recorded as owner decision OD-66. | none built. The runtime check that would have caught it: assert that every field a write path resolves is a field that write path's EXTRACTOR actually returned, and alert on the difference — the set subtraction is cheap and the answer should always be empty. A conflict-level variant, narrower and also cheap: never allow an auto-resolution whose two sides are the same underlying source (here both were effectively the stored row, arriving once as `existing` and once as a re-asserted `DRHP` claim). Note what made the exposure measurable at all: `data_conflicts` stores BOTH source names per row, so 'which resolutions involved a document' was one query. A conflict table recording only the winner could not have been decomposed this way, and the 11 real rows would have stayed hidden inside 18,583 innocent ones. | unguarded | §2.4, §3 |
| a write the walk can never make leaves the plan row PENDING and claimed first on every wake, with no state that says so | the pull-model field-plan walk (claimNextDueField + field-plan-walk.ts): any table and field whose fetcher answers SUPPLIED but whose write the consolidated writer refuses for a structural reason (row not keyable, table not on the consolidated write path, write flag off) | the dropped-write branch treats every dropped write as transient: state stays PENDING, attempts and last_attempt_at are untouched, next_due_at stays in the past. A due PENDING row is the claim query's highest priority (pri 0), so every walk takes it first, asks the source again, gets the same answer, drops the same write and puts it back. When the drop is structural (same cause, same outcome, OD-78) this repeats on every wake forever: the value never lands, the row never reaches a state that names the cause, attempts stays 0, and every plan-row count reads it as fresh work. pull_plan_stuck_reclaim does not see it because it reads only NOT_AVAILABLE_YET and CHECK_FAILED; the wake log shows it only as a warn line per row per wake. | 2026-09-25 ~02:35 IST, ipodhan_staging (serving a2833963), read-only: 26 gmp_records.gmp plan rows PENDING with next_due_at in the past (15 OPEN, 7 CLOSED, 3 UPCOMING, 1 LISTED; e.g. veegaland-developers-ltd row 90f555a1, glass-wall-systems-india-ltd, jindal-supreme-india-ltd), attempts 0, last_attempt_at 2026-09-23 ~18:50 UTC, claimed_at NULL, updated_at 2026-09-24 20:50 UTC. They are the ENTIRE overdue-PENDING population on staging (no other table.field). Staging wake log /var/log/ipodhan-scraper-wake-staging.log: 'PASS 3: the write was DROPPED' for gmp_records.gmp skipReason MISSING_ROW_KEY 24 / 50 / 49 times in the 18:00 / 19:00 / 20:00 UTC hours of 2026-09-24 (about 25 per 30-min wake), each preceded by '[DataConsolidation] child row has no natural key — refusing to file it under the singleton sentinel' (data-consolidation-orchestrator.ts:779: row_key '' and gmp_records is not in SINGLETON_ROW_CHILD_TABLES, nor in ChildConsolidationTable). Walk summary 20:51:03 UTC: fieldsWriteSkipped 23, droppedWrites all gmp_records.gmp MISSING_ROW_KEY. Same log, smaller: ipo_details.fresh_issue / ofs_issue dropped as CHILD_TABLE_CONSOLIDATION_DISABLED. Not the #943/#957 slot budget: the rows ARE claimed every wake (elapsedMs 9,453 of a 732,515 ms budget). | #1019 added the detection. PR #1020 (OD-99) fixes the write path for the class: a structural refusal (MISSING_ROW_KEY, CHILD_TABLE_CONSOLIDATION_DISABLED, CONSOLIDATION_DISABLED) settles the row CHECK_FAILED with gap WRITER_CANNOT_ACCEPT, not charged as an attempt (#923), skip reason in cause, re-asked only when the writer-capability gap key changes; a transient drop keeps the PENDING re-queue. Proven red-then-green on ipodhan_test by the OD-99 block of scraper/tests/integration/field-plan-walk-real-writer.integration.test.ts (run by the pr-gate Scraper Document Integration job); the staging proof is owed after deploy | pull_plan_pending_stranded (scripts/audit-detection-floor.mjs, predicate isStrandedPendingRow in scripts/lib/field-plan-slot.mjs): run read-only on ipodhan_staging 2026-09-25 it flags exactly the 25 live-IPO gmp_records.gmp rows and passes everything else (0 other PENDING rows exist on staging, so the pass side is proven by the unit tests: new rows, not-yet-due rows, recently attempted rows, LISTED/WITHDRAWN IPOs, claimed rows) | guarded | §2.4, §2.5.1, §2.3 |
| an E-1 field on the document path throws and fails the whole document | any document of any type (price-band ad, RHP, prospectus, ...), for any IPO status or segment, whose extraction prints any E-1 field (the exchange-owned timetable/status/listing-venue set; the three that live in ipo_details are basisOfAllotmentDate, initiationOfRefundsDate, creditOfSharesDate) | The filing persister (scraper/src/services/filing-persister.ts) writes E-1 fields into ipo_details via trackField, with source always DRHP (scraperSourceForDocType hard-codes it for every doc type this persister handles). The #862 guard in FieldSourcesRepository.trackFieldUpdate correctly refuses an E-1 field on the document path by THROWING — but nothing in filing-persister caught it, and by the time trackField ran, the ipo_details upsert (and the ipos upsert before it) had already executed. The uncaught throw propagated out of persistFilingExtraction, so the whole document was marked FAILED with no receipts recorded for any OTHER field it read — one exchange-owned field sank an otherwise-clean extraction. | Reported as GitHub issue #1016, RCA confirmed from code by a Tier A reviewer 2026-09-25. Measured population on ipodhan_staging 2026-09-25 (read-only via the sanctioned tunnel): 1 real row — Runwal Enterprises Ltd. (runwal-enterprises-ltd, status OPEN), documents.id 94d54d42-d306-450b-9975-c3cc16522189, type PRICE_BAND_AD, extraction_status FAILED, uploaded_at 2026-09-24T14:46:19Z, extraction_error 'persist: E-1 field creditOfSharesDate may not be written from the document path (source=DRHP)... See #862.' | scraper/src/services/filing-persister.ts: E-1 fields are now filtered out of the ipo_details write candidate BEFORE the write and before trackField is called, using the SAME set (FieldSourcesRepository's exported E1_EXCHANGE_STATED_FIELDS/DOCUMENT_PATH_SOURCES, packages/shared/src/repositories/field-sources-repository.ts) the #862 guard throws on — never a hand-list. Each refusal is a structured logger.warn (field, ipoId, documentId, docType, source, reason: 'e1-document-path-refused') and a skipped_failed_check entry; the field's receipt (what the document printed) still persists, matching the treatment of an OCR value that loses precedence. The #862 guard itself is unchanged and still throws for every OTHER caller (proven by a dedicated unit test). Failing test first: scraper/tests/unit/services/filing-persister-e1-document-path-skip.test.ts, driving the REAL FieldSourcesRepository (stub db/redis) as deps.fieldSources so the real guard is what would have thrown pre-fix (verified red-then-green by temporarily disabling the filter). Six pre-existing tests that had encoded the buggy pass-through behaviour (filing-persister.test.ts's DEEPA-oracle PRICE_BAND_AD mapping test, and five in filing-persister-ocr-loses.test.ts using a REAL extractor envelope from a live SteamHouse India price-band ad) were updated to assert the corrected behaviour. | docs/reviews/detection-checks/e1_document_path_extraction_failed.json — SPEC ONLY today (section: notCoveredByThisManifest), naming the query (documents.extraction_error ILIKE '%may not be written from the document path%') and the one pre-existing row found. Not yet promoted into scripts/audit-detection-floor.mjs's 'checks' section — owed, tracked in that entry's owner field. The fix itself is what stops new rows joining the population; this check exists to confirm that going forward and to surface the one pre-existing row for a readmission decision (not made here; no write was performed). | unguarded |  |
| an ISO string bound where the ORM expects a Date, following a raw-SQL rule on an ORM path | every drizzle query that compares or writes a `timestamp()` column through the query builder (eq/gt/gte/lt/lte/between, .set(), .values(), onConflictDoUpdate) in web/ and packages/shared/ | the project's IST rule says to bind `date.toISOString()` (a string), never a Date object, to a naive timestamp column. That is right for raw node-postgres parameters, where a Date is serialised in the process's LOCAL zone. It is wrong on the drizzle path: drizzle's PgTimestamp.mapToDriverValue calls `value.toISOString()` on whatever it receives, so a string throws `TypeError: value.toISOString is not a function`. The error is wrapped (DatabaseError -> NonRetryableError) and, with a logger that drops `cause`, surfaces only as a generic 500. Wider shape: a rule written for one binding layer, applied to a layer with the opposite contract, turns a timezone guard into a crash. | 2026-09-25 -- IPODhan. (1) /api/ipos/[slug]/demand-graph returned HTTP 500 for every IPO with demand data (prod floor e_route_sweep: prasol-chemicals 09-16, lcc-projects 09-17, veegaland-developers 09-20, jindal-supreme 09-23; issue #954). Reproduced on ipodhan_test by the #954 builder: `TypeError: value.toISOString is not a function` at PgTimestamp.mapToDriverValue from the demand-graph repository query; fixed in PR #1069. (2) The #1033 builder's first fix attempt bound `.toISOString()` into drizzle `.set()` and crashed the same way (caught by its real-function round trip before commit; PR #1067 kept Date objects). | #1069 MERGED e555855b (demand-graph query binds a Date; web logger keeps the wrapped cause); #1067 MERGED 45a27884 (corrigendum accept keeps Date objects). Rule wording correction (.claude/rules/ist-timezone.md: 'bind toISOString()' applies to raw pg parameters only; drizzle-mapped timestamp() columns take a Date) was owner-approved 2026-09-25 (Q4) and lands with the detection check in PR #1079. Related: web logger has no redaction (#1073). | scripts/ci/check-drizzle-iso-string.mjs (PR gate `gate` job, .github/workflows/pr-gate.yml), registry entry docs/reviews/detection-checks/iso_string_bound_to_drizzle_timestamp.json. Classifies every schema column by JS property name (default-mode timestamp() = flaggable; timestamp(mode:'string') and date() = exempt; a name declared both ways anywhere is ambiguous and skipped, favouring a false negative over blocking legitimate code), strips sql`` template bodies (the raw-pg path, where .toISOString() stays correct), then flags .toISOString() bound to a drizzle comparison operator (eq/ne/gt/gte/lt/lte/between/inArray/notInArray) or a .set()/.values() field on a flaggable column. Zero live offenders on the current tree (verified 2026-09-25); baseline file scripts/ci/drizzle-iso-string-baseline.json is empty and may only shrink. Self-test scripts/tests/check-drizzle-iso-string.test.mjs is red on a planted gte(ipos.createdAt, d.toISOString()) and green on gte(ipos.createdAt, d) and sql`${d.toISOString()}`. Also flags shape 2: any `sql<Date>` type argument outside comments (raw sql results are never mapped, so the value is text; the real #954 line, ipo-repository.ts:1680 at e555855b^, makes the check exit 1). Known gap: columns are classified by JS property name across the whole schema, not per table, so a bad bind on a column whose name is ALSO declared as date() or mode:'string' on another table passes silently. | guarded |  |
| an abstention recorded as a disagreement | data_conflicts write path; any queue whose rows are meant to carry a human judgement call | a comparison writes a queue row whenever two sources are not equal, without first asking whether both sources actually SUPPLIED a value. One side null is not a disagreement, it is an absence, and it needs a different remedy (fetch it, or record why it is missing) from a real value-vs-value conflict (decide which is right). The same shape covers a source compared against ITSELF across time: the pair is not two sources, so the row cannot be a source conflict, it is churn. Universal shape: a queue that filters on `a <> b` rather than on `a IS NOT NULL AND b IS NOT NULL AND a <> b` will fill with rows nobody can action, and its size stops carrying information. The tell: the queue grows far faster than the number of genuinely ambiguous facts in the domain, and its rows are dominated by one source or one null side. | 2026-09-20 — issue #818, measured read-only on ipodhan_staging. 28,120 open admin-queue rows across 268 IPOs. Of the 14,140 data_conflicts rows: 13,850 (97.9%) had one side null, 133 (0.9%) held identical values, and only 157 (1.1%) were a true value-vs-value disagreement. 12,152 (86%) were CHITTORGARH compared against CHITTORGARH — one source against its own earlier value. 14,134 (99.96%) sat on IPOs already CLOSED or LISTED. IMPORTANT, established the same night: these rows are HISTORICAL RESIDUE, not a live defect. Same-source rows stop at 2026-08-23 and the guard landed 2026-09-03 (a7aa991e, W-79); null-side rows stop at 2026-08-24 and the last null guard landed 2026-09-16 (59f14418); zero rows of either kind exist after their guard dates. 12,731 of the 12,774 same-source rows were written in the single week of 2026-08-17 — one bad run, not a drip. Staging's deployed build 61808af1 CONTAINS a7aa991e (git merge-base --is-ancestor), and the open backlog FELL from 14,252 (2026-09-07 baseline) to 14,140, which a live unguarded writer cannot do | guarded in code before this class was written down: a7aa991e (2026-09-03, refuses source1 === source2 in logConflict and upsertConflict), 642e2838 (2026-08-24) and 59f14418 (2026-09-16) for the null cases (NOTHING_TO_RECORD / NO_INCOMING_VALUE), plus the existingSource !== incomingSource gate on the consolidation service's Case 3 write. The remaining ~14,000 rows are residue needing a ONE-TIME CLEANUP, not a code fix — nothing regenerates them. Separately #762 owns the 13,980 parked ipo_field_plan absence rows, which ARE still growing (~750/day) and are a different mechanism entirely: no re-queue path back to PENDING | none yet, and the gap is specific: the guards stop new rows but nothing asserts the queue's COMPOSITION, so a recurrence would again be visible only as a large count. A check on this class asserts that the share of open rows which are true value-vs-value disagreements on a live IPO dominates, and treats an excess of abstentions or same-source pairs as a write-path defect rather than a backlog. scripts/ops/admin-queue-size.mjs (item 35) reports the open COUNT grouped by IPO, which is exactly the number this class makes uninformative — 28,120 and 157 read identically to it. Note this class has now recurred TWICE in code before being written down (9,921 of 11,493 rows historically, then a recurrence on priceRangeMin NSE-vs-NSE, T-276), which is why a composition check matters more than another write guard | guarded | §2.5, §2.11, §3, §3.4 |
| an admin write path that performs only part of the admin write contract | every admin route that changes a published value (web/app/api/admin/**: update-field, update-field-record, dynamic/[table]/[id], conflicts/resolve and bulk-resolve, corrigendum accept) | an admin save must do four things together: write the value to the table the page reads, record it as an ADMIN value the scraper will not overwrite (protection row), record who and why (audit), and drop the cache key the page is served from. A path that does fewer reports success while the page shows the old value, or shows the new value until the next scraper cycle silently replaces it, or changes it with no trace. Each path was written separately, so each has a different missing step. Wider shape: a multi-step contract implemented once per caller instead of once, so every caller is a new chance to drop a step. | 2026-09-25 -- IPODhan, read on origin/main while specifying admin editing: (1) conflict resolve writes the value only for table ipos (web/lib/services/conflict-resolution.ts:229; F-169); (2) dynamic-admin PATCH writes no protection row and no audit row, cache TODO at line 258 (F-170); (3) legacy update-field drops a literal 'ipo:slug:*' key and never the detail page key (update-field/route.ts:310; F-171) | none yet -- the admin editing build (§9, OD-102) is to route every admin save through one shared write function; no fix PR exists | none. A cheap one: a test that enumerates every route under web/app/api/admin/ that writes a table and asserts it calls the single shared admin-write function, plus an integration test that saves through each path and asserts value written, protection row present, audit row present, detail key gone | unguarded | §2.7, §2.11, §9.1 |
| audit pool timezone/parser skew | scraper/scripts/*.ts (raw `pg` Pool sites) | a repair/backfill script's raw `pg` Pool omits `-c timezone=UTC` / the 1114 UTC parser, so `now()`/`defaultNow()` writes and naive-`timestamp` reads land IST-shifted against every other UTC-pinned pool (the same "Timestamps off by 5h30m" class in the root CLAUDE.md troubleshooting table) | Aug 2026 (GitHub #28, refresh-calendar.ts + packages/shared/db fixed); recurred Sep 2026 on `repair-dates-and-leadmanagers-t299.ts` / `repair-subscription-regressions-t299.ts` (review-gap round, this PR) | this PR (`options: '-c timezone=UTC'` + `configureUtcTimestampParsing()` on both scripts) | not one of the three sanctioned check types (no `SUBSTANCE_CHECKS` key, no `audit-detection-floor.mjs` `record()` id, no `audit-ipo-coverage.mjs` invariant) — guarded only by `scraper/tests/unit/scripts/pool-utc-pin.test.ts`, a source-level scan asserting every `new Pool(` site in `scraper/scripts/**` is within 5 lines of a `timezone=UTC` marker | unguarded |  |
| both slots extracting at once (522) | scraper cron cadence / nginx | both staging cron slots (:00/:30) run extraction simultaneously on 2 vCPUs, starving nginx and producing Cloudflare 522s | walk W-178 | `7300e0aa`, `ea40e3c3`, `6039504a`, `252cd9d0` (cron offset to :15/:45, nice priority, TZ restore) | none in `substance-checks.mjs`/`audit-*.mjs` (an ops/cadence class, not a DB-row check) | unguarded |  |
| card asserts a false fact about the code, builder patches around it | docs/design/build-cards/** consumed by an implementation worker | a build card states a factual claim about this codebase — a function, a feature flag, a file, a heading, a dependency or a count — that is not true, and the worker silently builds around it instead of correcting the card, so the card keeps asserting the false fact to the next reader and the design and the code drift apart with nothing recording that they did | 2026-09-09 to 2026-09-19: 12 of 14 catalogued deviations, including PR #678 (flag `ENABLE_FIELD_PLAN` did not exist), PR #745 (`buildFieldPlanWalkDeps` did not exist) and PR #758 (a `NO_FETCHER` state impossible because `field_plan_state` is a PostgreSQL enum that does not contain it — that one made the card's intent unbuildable and scope was dropped). Of 148 issues filed since 2026-09-09, 12 name a spec or card defect: 6 assumed something that does not exist, 3 were silent, 2 were wrong against the data. | this PR (spec §0.0.4, `.claude/rules/spec-adherence.md`, `docs/design/spec-deviation-guideline.md` class 1) — the class is named and the correction is made mandatory in the same PR as the build | none yet — the PR-template Spec-deviation block (build item 31) is the first mechanism that would surface it, and it is not built; the guideline's enforcement option C (a CI gate that fails a PR whose Spec-deviation block is missing) is deliberately deferred to the first deviation that slips past option B | unguarded | §0.0.4 |
| check blind to a row that was never created | scripts/audit-detection-floor.mjs and any check whose population comes from a JOIN | a detection check defines its population by JOINing the very table whose MISSING row is the defect. It can therefore only grade rows that exist — it measures the AGE or STATE of a record and is structurally incapable of reporting its ABSENCE. The check passes, or reports unrelated offenders, while the real failure is invisible. The tell: the check's FROM clause and the defect's description name the same table, one asking 'how old is this row' and the other 'there is no row'. Universal shape: population-by-JOIN can never detect a missing member of that population; only a LEFT JOIN / NOT EXISTS against the ENTITY table (ipos, documents) can. | 2026-09-19 — `m_blocked_all_age` (audit-detection-floor.mjs:765-783, `FROM document_fetch_state s JOIN ipos i`) reported three IPOs (INJECTO POLYMERS, LCC Projects, STANBIK AGRO) and MISSED the two IPOs about to open with a blank price band (Anand Seamless, Liqvd Digital), because those two had no PRICE_BAND_AD row to age. Both were confirmed inside the check's own filter (`offering_type='IPO'`), so the exclusion was the JOIN, not the WHERE | none yet. Proposed check (#796) inverts the direction: `FROM ipos i ... WHERE NOT EXISTS (SELECT 1 FROM document_fetch_state s WHERE s.ipo_id=i.id AND s.doc_type='PRICE_BAND_AD')`. Verified to discriminate on real staging data: of 20 live IPOs opening within 7 days it flags exactly 2 and passes 18, so a PASS carries information | self-referential — this class is about the checks themselves. Mitigation is a review question, applied when authoring or reviewing any check: 'if the row I am grading had never been created, would this check say anything?' If no, the check needs an entity-anchored NOT EXISTS companion | unguarded |  |
| date-order violation | scraper persist (open/close/allotment/listing dates) | open > close, or allotment before close, or listing before allotment | pre-existing (SUBSTANCE_CHECKS design) | `checkDateOrdering` | `date_ordering` (`scripts/lib/substance-checks.mjs`) | guarded |  |
| degenerate price band | scraper persist (price_range_min/max) | price band min==max or min>max stored on a non-fixed-price issue | pre-existing (SUBSTANCE_CHECKS design) | `checkDegenerateBookbuildingBand` added to substance checks | `degenerate_bookbuilding_band` (`scripts/lib/substance-checks.mjs`) | guarded |  |
| deploy probe port held | scripts/deploy-linux.sh | the deploy's port-probe listener is not fully killed (group signal misses a child outside the leader's process group), blocking the next deploy | walk W-169 | `d8322a00`, `37288341`, `2cc19d43` (kill by PORT, not only by pgid) | none in `substance-checks.mjs`/`audit-*.mjs` (mitigated by deploy-script bash tests, not a data/audit check) | unguarded |  |
| deployed sha not on origin/main | scripts/deploy-linux.sh (pre-flip gate) | production serves a commit that exists only on an unmerged branch while simultaneously missing main's merged fix — the served-sha probe only proves `served == deployed`, never `deployed is on main` (T-264 P2-4: prod served `1a0b76f`, on `origin/fleet/T-261-gmp-subscription-coverage` only, while missing main's `04f45a74`) | T-264 (2026-08); gap named but left open until T-297's discovery-coverage sweep (2026-09-07, #194) | this PR (`assert_deployed_sha_lineage`: `git fetch origin main` + `git merge-base --is-ancestor "$SHA" origin/main` immediately before the atomic flip; `ALLOW_UNMERGED_DEPLOY=1` escape hatch logs loudly + fires a Notifier P2) | not one of the three sanctioned check types (no `SUBSTANCE_CHECKS` key, no `audit-detection-floor.mjs` `record()` id, no `audit-ipo-coverage.mjs` invariant) — this is a deploy-time gate, not a nightly DB-row audit; registered in `docs/reviews/detection-checks.json` under `notCoveredByThisManifest` as `g_deployed_sha_lineage`, guarded by `scripts/tests/deploy-linux.test.sh` case 32 (a-e) | guarded |  |
| document cycle overrun / no calendar gate | document-cycle.ts (`runDocumentCycle`, `primarySourceDiscovery` step) | the document cycle ran on EVERY 30-min pm2 wake with no weekday/holiday gate and two INDEPENDENT budgets (60s discovery + 25min extraction, no shared ceiling) that could together exceed the 30-min wake and the cycle lock's TTL, letting one wake overlap the next; observed 1,210-1,278s cycles and 105-115 network calls per cycle on a Sunday (2026-09-06) | 2026-09-06 (cycle-overrun RCA; cadence decision D-13, owner 2026-09-03) | calendar gate (`document-cycle-calendar-gate.ts`: Sunday/Saturday/NSE-holiday wakes make only UPCOMING/PRE_OPEN/OPEN candidates eligible for network work) + one shared wake budget (`DOCUMENT_CYCLE_WAKE_BUDGET_MS`, default 20min — extraction gets what discovery left, capped; `CYCLE_LOCK_TTL_MS` in index.ts now derived from the same budget + 5min slack instead of a separately-hardcoded number) | `m_cycle_overrun` (`scripts/lib/document-state-checks.mjs`, wired in `scripts/audit-detection-floor.mjs`) | guarded |  |
| documents cache staleness after raw status writes | scraper/scripts/backfill-issue-size-chittorgarh-detail.ts (and any script writing `ipos` directly, bypassing the repository cache-aside layer) | a repaired/backfilled `ipos` row (e.g. `issue_size`) stays stale behind the 15-min `IPO_DETAIL`/`IPO_LIST` Redis TTL because the write went around `IPORepository`'s cache-aside invalidation — an operator had to read a printed "drop cache keys" line and DEL them by hand | Sep 2026 (review-gap round, this PR) | this PR (`dropIpoCacheKeys()` DELs `ipo:slug:<slug>` / `ipo:id:<id>` after every applied write, fail-open to the printed line + WARN if Redis is unset/unreachable); the sibling web admin route fix for the same class ships in a separate PR (`filing-auto-persist-cache-invalidation.test.ts` covers that path) | not one of the three sanctioned check types — guarded by `scraper/tests/unit/scripts/backfill-issue-size-chittorgarh-detail.test.ts`'s `dropIpoCacheKeys` unit test (fake redis asserts both keys are DELed) plus the pre-existing `filing-auto-persist-cache-invalidation.test.ts` for the sibling write path | unguarded |  |
| duplicate survives when shared identifier arrives late | scraper identity matching / merge (OD-34, OD-68) | two rows created for one real offering stay two rows even after both later gain the SAME strong identifier (e.g. CIN), because identity matching only runs when a new record arrives and is matched against existing rows — it does not re-scan already-stored rows when one of them is retroactively enriched with an identifier the other already has. A matched identifier that arrives AFTER row creation never triggers a merge on its own. | 2026-09-23: rays-of-belief-ltd and rays-of-belief-ltd-o, both CIN U85110DL2017PLC322623, both open 2026-09-01, still two separate rows. | none yet | the nightly check `i_same_ipo_two_rows` groups by CIN (#906) and DETECTS the pair, but nothing automatically resolves it — detection exists, resolution does not. | unguarded | §2.3.3.2 |
| exchange defaulting to BOTH for SME | data-consolidation-service.ts / data-consolidation-orchestrator.ts | an SME IPO is stored with `listing_exchanges = [NSE, BSE]` when SME issues list on exactly one exchange | walk W-145 | `violatesSmeSingleExchange` / `collapseSmeExchanges` — write-time invariant in the consolidation service and orchestrator | none in `substance-checks.mjs`/`audit-*.mjs` — the guard runs only on the write path; no audit check independently re-verifies stored SME rows for a two-exchange value | unguarded |  |
| extraction stuck invisible to audit | scripts/audit-detection-floor.mjs (documents/document_fetch_state join) | a required document type (DRHP/RHP/PROSPECTUS) reaches MANUAL_REVIEW, EXTRACT_FAILED, or FAILED behind a HARD_FAILURE marker and stays there with nothing FAIL-level watching it — `m_extract_failed` is WARN-only and keys ONLY off `document_fetch_state.state=EXTRACT_FAILED`, blind to `documents.extraction_status` entirely (`grep -rn MANUAL_REVIEW scripts/` was empty) | round 5 review (#333 follow-up: two ETIMEDOUTs -> 24h floor -> 10 attempts -> MANUAL_REVIEW, unseen) | this PR (`checkExtractionStuck` in `scripts/lib/document-state-checks.mjs`, wired as FAIL-level `m_extraction_stuck`) | `m_extraction_stuck` (`scripts/audit-detection-floor.mjs`, `docs/reviews/detection-checks.json`) | guarded |  |
| extractor memory blow-up | scraper/scripts/*.py (pdfplumber/OCR extraction) | Python extractor OOMs or aborts with a C-level MemoryError that the caller misclassifies as a soft/retryable failure | walk W-137 | `dfa42f7c`..`6c44ec21`..`2a6817a9` (memory ceiling, single-thread BLAS, hard-classify C-level aborts, exit 3 JSON) | none — this is a process-exit-code contract inside `scraper/scripts/*.py`, not a DB-row audit check; nothing in `substance-checks.mjs` or the audit scripts re-verifies extractor exit-code handling | unguarded |  |
| fabricated open/close dates from Moneycontrol | moneycontrol-scraper.ts | open/close dates are ESTIMATED from listing/allotment date (listing-3, listing-10, allotment-2) and written as if scraped; recurred in `chittorgarh-scraper.ts` / `chittorgarh-rights-debt-adapter.ts` (same class, `close = open + 3` when empty) | 2026-09-04 (W-02 class recurrence; walk W-116) | hotfix `c018f4e1` + `210ad865` (estimation removed both places; matrix reordered so exchanges outrank the filing for bidding-window dates) | **none** — `docs/walks/2026-09-02-deepa-pipeline-walk.md` W-119 explicitly says "No substance rule catches a fabricated date ... open, after the walk (detection upgrade for the W-116 class)" | unguarded |  |
| failed child-row writer indistinguishable from a disabled one | scraper/src/services/filing-persister.ts (child-row consolidation fallback paths) -> field_sources | when the child-row consolidator is absent, throws, or reports a skip, the rows are written with NO per-row provenance, leaving only the legacy `trackField(table, 'rows')` row under `row_key = ''` — byte-identical to the trace left by a slot where `ENABLE_CHILD_TABLE_CONSOLIDATION` is simply OFF. `classifyRowKeyCoverage` reads both as NOT-YET-KEYED and reports UNVERIFIABLE, so a broken writer is silently indistinguishable from an unstarted one | Sep 2026 (F-101, found while auditing why the row-key coverage check could never move off UNVERIFIABLE) | this PR (the writer files provenance under the sentinel key `unresolved:<reason>` on every fallback path, naming the cause; the child row is still written) | `scripts/lib/row-key-coverage-checks.mjs` `classifyRowKeyCoverage` — UNCHANGED by this PR: the sentinel is a non-empty `row_key`, so the pair flips into enforcement and every derived key is then missing, which is FAIL. Wired into CI by the new `Row-key coverage classifier self-test (F-101)` step in `.github/workflows/pr-gate.yml`, which runs `scripts/tests/row-key-coverage-unresolved-marker.test.mjs` (the three-state proof: writer off -> UNVERIFIABLE, writer succeeded -> PASS, writer FAILED -> FAIL) and `scripts/tests/row-key-coverage-checks.test.mjs`, neither of which ran anywhere in CI before | guarded |  |
| fresh/OFS split published without reconciling against the stored issue size (F-51) | scraper/src/services/filing-persister.ts (ipo_details.fresh_issue / ofs_issue, ipos.issue_size) | the persister computed offerTotal = statedTotal ?? (fresh + ofs) and wrote both legs the moment a unit was available; a sum compared against itself always agrees, so a digit-wrong fresh leg (the card's Kanohar shape: 60 where the filing says 300) was published beside a correct OFS leg and neither was ever compared with the ipos.issue_size an exchange had already stored on an earlier cycle | design doc data-sourcing-pull-model.md S2.5.4, diagnosed 2026-09-09 (the '6 of 9 live IPOs' population count in that section is UNVERIFIED - no saved query output exists; what is independently confirmed is that no gate existed) | item 13 (this PR): reconcileFreshAndOfs() in filing-persister.ts - both legs withheld together when fresh + OFS misses the stored issue_size (or the document's own stated total) by more than 0.5%, or when the rupee OFS figure and ofs_shares x priceCap disagree | c_fresh_ofs_reconciliation (docs/reviews/detection-checks/c_fresh_ofs_reconciliation.json) - specified, NOT implemented: no record() call in audit-detection-floor.mjs yet, so it is registered under notCoveredByThisManifest. The write path is guarded by scraper/tests/unit/services/filing-persister-fresh-ofs-reconciliation.test.ts (12 tests) | write path guarded; read-side audit check owed, owner UNASSIGNED |  |
| garbled anchor names | anchor-report-parser.ts (OCR text) | anchor investor names published with OCR corruption (e.g. `OSWAL OT] LAL FINVEST N4 LI IITE D`) | walk W-81 | `c17d5d18` — write-time `NAME_QUALITY_FLOOR` 0.3 gate refuses to publish a book with >30% unreadable names | none in `substance-checks.mjs`/`audit-*.mjs` — the fix is a write-time refusal gate in the parser, not an independent audit check on stored `anchor_investors` rows | unguarded |  |
| hard_date_trust_on_update | scraper persister (data-persister.ts, data-consolidation-orchestrator.ts) | open_date/close_date rendered as fact from a single uncorroborated source, written on an UPDATE not just create | 2026-09-07 (#180 F2, T-459) | #377 | g_provenance_lineage (C1 lineage-ceiling gate, scripts/audit-ipo-coverage.mjs) — a hard date with zero field_sources lineage is the same signature this class produces; write-time guard in data-persister.ts and data-consolidation-orchestrator.ts (findByField corroboration check) covered by scraper/tests/unit/services/data-persister-180-sme-fpo-hard-date.test.ts | guarded |  |
| incapable source value accepted | scraper/src/services/data-consolidation-service.ts (consolidateField) + scraper/config/field-manifest.json capability map | a source the field manifest marks `capability.<SRC>.capable === false` for a field still reaches the row, because "not ranked" only loses a PRIORITY contest — an unranked value still wins an EMPTY slot (Case 1 accepts whoever arrives first) and still wins over an untracked stored value. Measured sample: BSE's `ipos.issue_size` ran 41-76% BELOW the printed total offer on 6/6 live mainboard IPOs on 2026-09-16 (#728). Class: all 14 (field, source) pairs the manifest marks capable:false — ipos.issue_size x {BSE,NSE,MONEYCONTROL}, ipo_details.fresh_issue x {NSE,MONEYCONTROL}, ipo_details.ofs_issue x {NSE,MONEYCONTROL}, financial_statements.revenue x {NSE,BSE}, subscriptions.{qib,nii,retail,total}_subscription x DOC, listing_performance.listing_price x DOC. | 2026-09-16 (review round 5 item C, #728); manifest capability map added item 3 slice S0b. Second instance, measured 2026-09-23: rays-of-belief-ltd issue size 749,936,590 = 3,137,810 x 239 (BSE Issue_Size_No_of_shares, which excludes the anchor portion) vs the true 1,249,970,000 = 5,230,000 x 239 (full offer) — confirms the #728 mechanism (BSE's non-anchor share count) on a fresh row. | this PR (item 3 slice S1c): the resolver carries the row's `incapable` map and `consolidateField` refuses an incapable incoming source ABOVE Case 1, the untracked-value rule and the priority decision, recording one `data_conflicts` row with `resolution_reason = 'REJECTED_INCAPABLE_SOURCE'` | not yet one of the three sanctioned check types — the audit check that reads `data_conflicts` for REJECTED_INCAPABLE_SOURCE rows and cross-checks stored values against the manifest capability map is built in item 3 slice S6 (PULL-WRITE-POLICY). Guarded today by scraper/tests/unit/services/data-consolidation-service.policy.test.ts cases (x)-(xv) through the REAL orchestrator, plus scraper/tests/unit/config/field-source-policy.test.ts for the resolver's capability map. | unguarded |  |
| issue_size 0 or negative | scraper persist (issue_size) | `issue_size` stored as 0 or negative on a live IPO row (a null `issue_size` is NOT caught by this check — `checkIssueSize` returns null/no-violation for a null value; the go-forward `issue_size` presence invariant in `scripts/lib/ipo-stage-completeness.mjs` (`issue_size IS NOT NULL AND issue_size > 0`) is what catches a null) | round-6/7 detection RCA (`docs/reviews/round-7-detection-rca.md` P1-3) | round-7 fix (`[FAIL] issue_size > 0` gate) | `issue_size` (`scripts/lib/substance-checks.mjs`) + go-forward presence invariant (`scripts/lib/ipo-stage-completeness.mjs`) | guarded |  |
| issue_size stored as net-offer x price (fresh/exchange-derived) instead of the printed total | scraper/src/config/field-priority-matrix.ts (issueSize source ranking), scraper/src/scrapers/nse-api-client.ts (computeNSEIssueSizeRupees), scraper/src/scrapers/bse-api-scraper.ts (computeBSEIssueSize) | an IPO with an offer-for-sale component stores `issue_size` from NSE/BSE, both of which derive it as (noOfSharesOffered / net offer) x price — excluding the OFS — instead of the total incl. OFS the site defines (schema.ts ~1102); Meesho 3,085 Cr stored vs 5,421 Cr actual, Wakefit 709 vs 1,289, Aequs 521 vs 922 | 2026-09-07 (owner RCA); same root cause as T-434 (filing/source-backed total beats a share-count-derived exchange figure) | T-453 (`issueSize` sources reordered to `[ADMIN, DRHP, CHITTORGARH, NSE, BSE, MONEYCONTROL]` — Chittorgarh's list-API `Total Issue Amount (Incl.Firm reservations)` field is read directly from the source, not derived from shares x price) | `c_issue_size_consistency` lower bound (`scripts/audit-detection-floor.mjs`) — being redefined in T-452 in parallel to catch the too-small-derived-figure shape of this class; not edited by this PR | guarded (redefinition pending T-452) |  |
| listed rotation stall | document-cycle.ts / document-discovery-runner.ts (LISTED-tier candidate rotation) | shape 1: a LISTED IPO inside the 10-day rotation window has `documents` on file but ZERO `document_fetch_state` rows — `MAX(last_attempt_at)` sorts it first forever (NULLS FIRST), so it occupies the front of the LISTED queue every cycle and `enrichListedCandidates`'s `listedCap * 4` bound starves every LISTED row behind it (`listedSkippedUnenriched` stuck at 7). shape 2 (round 2, W-136): rows DO exist but the discovery-budget wall clock trips every cycle before the LISTED tier is reached — `MAX(last_attempt_at)` never advances even though DUE rows remain — a row in a deliberate future `next_retry_at` backoff is excluded, never counted as stalled (ESDS Software, Priority Jewels: staging, 2 rows re-selected every cycle while 7 others waited) | 2026-09-06 (rotation-stall-null-fetch-state; live on prod, 4 IPOs: madhur-knit-crafts, hy-tech-engineers, augmont-enterprises, tempsens-instruments); round 2 2026-09-06 (staging, ESDS Software Solution + Priority Jewels stuck 2+ days) | `runIpo`'s rotation-stamp guard (`document-discovery-runner.ts`) for shape 1 — guarantees a `document_fetch_state` row is written on every visit, including a `plan.skipIpo` with no prior history and an exception thrown mid-fetch, both of which previously left zero rows written. Shape 2: `runDocumentCycle`'s budget-trip loop now reserves up to `listedCap` LISTED slots (mirroring the existing WITHDRAWN/POSTPONED purge reservation) so `runIpo` — and therefore the rotation stamp — still runs for LISTED rows even when the live backlog burns the whole discovery budget every cycle (`listedReserved`/`listedProcessedAfterBudget` in the cycle summary/log) | `listed_rotation_stall` (`scripts/lib/document-state-checks.mjs`, wired in `scripts/audit-detection-floor.mjs`) — two shapes in the same check | guarded |  |
| listing exchange recorded from bidding venue | data-consolidation (ipos.listing_exchanges write path) | a row's `listing_exchanges` includes an exchange that only ran the BIDDING for the issue, not an exchange the issue will actually list on. The exchange that runs bidding for an issue is not necessarily a listing venue for it — mainboard issues sometimes bid through NSE while the offer document and RHP name only BSE as the designated stock exchange. | 2026-09-23, NSE (National Stock Exchange of India Ltd) IPO itself: price band ad says "(To be listed on the main board of BSE)", "BSE LIMITED SHALL BE THE DESIGNATED STOCK EXCHANGE"; RHP p.3 "proposed to be listed on BSE"; Chittorgarh "Listing at: BSE" — yet staging `listing_exchanges` holds ["BSE","NSE"] because NSE ran the bidding (symbol NSE, series EQ). Issue #938. Bears on OD-64. | PARTIAL, branch fix/951-consolidated-save-unclaimed-fields (2026-09-26). The RULE (whether an exchange feed may add a board it only runs bidding for) is PENDING AN OWNER DECISION: spec row 17 ranks NSE > BSE > CG for listing_exchanges (E-1), and F-135 has no OD row; a first attempt that made exchange feeds a listing source only for SME issues was reverted as a spec change. What landed: (1) the set-merge (data-consolidation-service Case 2b) records a widened set under the source that ADDED the member, with the prior source as previous_source -- the NSE IPO's staging row read {source DRHP, value [BSE,NSE], previous '["BSE"]', previous_source DRHP}, crediting DRHP with a board it never named; (2) the filing persister's echo of the stored boards is context under both key spellings (listingExchange / listingExchanges) on the consolidation, persister-union and fallback doors. Existing wrong rows are NOT repaired (data contract). Issue #938 stays open. | none at the table level. Write-time regression tests only: `scraper/tests/unit/services/consolidated-save-claimed-fields-951-938.test.ts` (#938 provenance block) and `filing-echo-listing-exchange-context-938.test.ts`. | unguarded |  |
| measuring a symptom of a fix that is merged but not deployed | any investigation that measures live/staging data to characterise a defect; scripts/ops/merged-not-deployed.mjs and the SessionStart brief that prints its count | a defect is measured on live data, its population and growth rate are established, and a fix is scoped — while the fix already exists on origin/main, unreachable from the latest prod-* tag. Every number measured is real and every conclusion drawn from it is wrong in the same way: the growth rate is the rate at which UNDEPLOYED code fails, not the rate at which a defect grows. The tell is a measurement whose window sits entirely after a fix commit's merge date. Universal shape: measuring production behaviour characterises the DEPLOYED code, never the code on main, and the two are only the same when a deploy has happened since the last merge. A count of merged-not-deployed commits printed as a bare number is read as background noise; resolved to identities it names exactly which defects the measurement cannot be about. | 2026-09-20 — issue #762. Parked ipo_field_plan rows measured at 12,480 (2026-09-18) and 13,980 (2026-09-20), giving ~750/day, and reported as an accumulating leak needing a re-queue mechanism. The re-queue mechanism had been merged the day before: PR #763, commit 2bdcea47, 2026-09-19 01:31 IST, after three review rounds. Verified: `git merge-base --is-ancestor 2bdcea47 origin/main` true, `... 2bdcea47 prod-2026-09-07` FALSE, newest prod tag twelve days older than the fix, both walk flags opt-in and default off. The growth measured was production running pre-fix code. The SessionStart hook had printed `fixed on main, still not on prod: 190` at the top of the same session and it was read as background noise | no code fix — the remedy is a deploy, and the detection already exists. scripts/ops/merged-not-deployed.mjs (T-498) lists every fix/feat commit on origin/main not reachable from the latest prod-* tag, with issue numbers and days-since-merge, and it ran at session start. The gap is consumption, not detection: a bare count of 190 was printed and not resolved to identities, which signal-ownership.md R1 forbids for exactly this reason | scripts/ops/merged-not-deployed.mjs already covers the class and already runs. What is missing is the consuming step: before measuring any live population to characterise a defect, run it with --json and check whether the issue's own number appears in the merged-not-deployed list. If it does, the measurement characterises undeployed code and must say so. A natural upgrade is for the SessionStart brief to name the issue numbers for the oldest merged-not-deployed commits rather than printing a bare count, so the 190 arrives as identities | guarded |  |
| merge gate skips a compared dimension | duplicate-IPO merge eligibility (OD-69, OD-86, OD-94) | any column that distinguishes two offers but is absent from checkMergeEligibility lets a pair that disagrees on it be merged; the gate refuses only on the dimensions it lists, so a pair of different offering types, or with different populated close or listing dates, passed whenever the listed dimensions happened to agree or were absent on one side. | 2026-09-16 (#679): ipodhan_staging cube-highways-trust (IPO, open/close/listing 2026-07-19/07-26/07-29) vs cube-highways-trust-cube-highways-trust-invit (INVITS, 2026-07-22/07-24/08-03) accepted in both directions; measured 2026-09-24: the IPO row is gone on staging, its slug redirects to the INVITS row with reason DUPLICATE_MERGE and no ipo_merge_log entry. | item 12 / #679 (OD-94): offering_type always refused when both populated and differing; close_date and listing_date refused unless the OD-86 relaunch test passes; every compared dimension listed once in checkMergeEligibility's doc comment | scraper/tests/unit/duplicate-ipo-merge.test.ts 'offering type and close/listing dates (#679, OD-94)' (each guard mutation-tested). Gap: scraper/src/scheduler/jobs/duplicate-sweep-job.ts clusters by name only, reports no eligibility verdict, and its dryRun:false path merges by raw SQL without checkMergeEligibility (production calls it with dryRun:true only). | unguarded | §2.3.3.2, §2.3.3.3 |
| one retry key shared by two steps, so one step's failure re-runs the other | scraper/src/index.ts due-step cycle (catch-up cadence keys); any branch where two steps sit behind a single isCatchUpCadenceDue / markCatchUpCadenceRan pair | A cadence key gates a whole branch but is stamped on the AND of several steps' results, so a failure in the cheap step suppresses the stamp and re-runs the EXPENSIVE step on every wake. Introduced and caught in review on 2026-09-11: the aggregator key was gated on `cgOk && fillOk`, so ONE failed row out of 231 in the issue-type fill - a single transient deadlock - left the key un-stamped and re-ran the Chittorgarh SCRAPE plus a full report fetch every 30 minutes, roughly 48 times a day against a third-party source, instead of once a day. The amplification is unbounded in time: it persists until the failing step succeeds. It was also DATE-TRIGGERED - CURRENT_YEAR is read from the clock, so on 1 Jan 2027 the report URL flips to a financial year with few rows, trips the job's own minimum-row floor, aborts, and the hammering runs for weeks with no code change at all. | Sep 2026, PR #569. Introduced by the fix for a DIFFERENT finding in the same review (a failed fill was suppressing its own retry for a day) - the fix traded a one-day suppression for a permanent retry storm. Never reached production: caught in round 2 before merge. | #569 (each step gets its own cadence key and stamps on its own result; the fill is additionally gated by its own isCatchUpCadenceDue so it cannot run more often than its interval) | scraper/tests/unit/index-issue-type-fill-wiring.test.ts - asserts ISSUE_TYPE_FILL_CADENCE_KEY exists, that `if (cgOk && fillOk)` is absent as a STATEMENT, and that the two stamps are separate. Mutation-checked: restoring the coupled key turns it red. NOT a runtime check - the cost of this class is external HTTP traffic, which no in-repo signal observes; the guard is therefore source-level and the real control is the design rule below. | guarded |  |
| one status value means both 'queued' and 'impossible', so a permanently-unprocessable row is indistinguishable from a waiting one | any pipeline that admits work into a queue before checking whether a processor for that work KIND exists — document extraction, job runners, import pipelines, message handlers with a type-dispatched consumer | the writer admits a row and stamps it with the queue's initial status (PENDING / QUEUED / NEW). The consumer dispatches on a TYPE field against a hard-coded set of handlers, and silently skips any type not in that set. A row whose type has no handler is therefore admitted, stamped PENDING, skipped on every pass, and stays PENDING forever — while a row whose type DOES have a handler is PENDING only until the next pass. One status value now covers two populations with opposite futures: 'will be processed shortly' and 'can never be processed'. Every count, dashboard, backlog metric and alert built on that status silently merges them, so the permanent population looks like transient load and the transient population looks chronically stuck. The shape is worse than a plain backlog because it is self-concealing: adding processing capacity, draining the queue faster, or building a second-pass walker moves the first population and leaves the second exactly where it was, which reads as 'the drain did not work'. The generic test is: partition the queue by whether a handler exists for the row's type, and check whether the no-handler partition is 100% in the initial state — if it is, the status is carrying two meanings. | 2026-09-20 — IPODhan `documents`, measured read-only on ipodhan_staging through the sanctioned tunnel while building item 17. 155 documents sit extraction_status='PENDING'. Partitioned by whether the type is in EXTRACTABLE_DOC_TYPES (PRICE_BAND_AD, RHP, DRHP, PROSPECTUS) plus ANCHOR_DOC_TYPE (ANCHOR_ALLOCATION_REPORT, its own extractor and write door): 90 documents on 85 IPOs HAVE an extractor; 65 documents on 43 IPOs have NONE. The no-extractor population is 100% PENDING — 65 of 65, zero COMPLETED, zero FAILED, across every status the table holds — which is the signature of the class rather than a coincidence of load: a type that is sometimes processed would show a mix. It is also still being written: oldest 2026-06-18, NEWEST 2026-09-17 (three days before measurement), so this is an open tap and not a historical backlog, whereas the has-extractor population stopped arriving 2026-06-18. The types are RATIOS_BASIS_ISSUE_PRICE (43), SAMPLE_APPLICATION_FORMS (6), SECURITY_PARAMS_PRE_ANCHOR (6), SECURITY_PARAMS_POST_ANCHOR (3), BIDDING_CENTERS (3), CORRIGENDUM (2), BASIS_OF_ALLOTMENT_AD (1), ADDENDUM (1). The measurement was prompted by item 17, whose build card and issue #717 both frame the backlog as '74 stuck PROSPECTUS documents' — true, and 42% of the PENDING rows are a different problem that item 17's second-pass walker cannot touch, because a second visit to a document with no extractor produces the same nothing as the first. | none yet — registered as a finding while building item 17 (PR #868), which deliberately does NOT address it and says so in its commit body. The remedy is NOT to build eight extractors. It is to stop one value carrying two meanings, cheapest first: (1) at admission, a document whose type has no registered extractor is stamped with a terminal, honest status (NOT_EXTRACTABLE / NO_EXTRACTOR) rather than PENDING — the check is a set membership against the same constant the consumer dispatches on, so the two cannot drift; (2) the existing 65 rows are migrated to that status by a re-runnable, source-backed tool, dry-run by default; (3) only then is 'which of these types is worth an extractor' a separate, sized product question, answerable because the count is finally honest. Ordering matters: building an extractor first would shrink the number and leave the class intact for the ninth type. | none yet. The check that would catch the next member is generic and cheap: for every distinct documents.type, report the share of rows in extraction_status='PENDING' older than N days, and FAIL when a type is 100% PENDING with a non-zero count — that is the no-handler signature, and it fires on the NINTH type the day it first appears rather than after a quarter of accumulation. It must partition by TYPE, never report a single PENDING total, because the single total is precisely what hid this for three months. The same shape generalises to any type-dispatched consumer: enumerate the handler set from the consumer's own dispatch constant (not a hand-kept list, which drifts) and assert every admitted type is in it. | unguarded |  |
| optional dependency never injected by the one builder that constructs it | scraper/src/services/filing-persist-deps.ts (and any single dependency builder whose interface marks a collaborator optional) | a collaborator is declared OPTIONAL on a deps interface (`childRowConsolidator?:`) and the one builder that assembles that interface never sets it, so the feature behind it is wired to nothing: the code type-checks, every call silently takes the degraded fallback branch, and the only evidence is ABSENT DATA downstream (all 8,076 staging `field_sources` rows carrying `row_key = ''`, coverage `UNVERIFIABLE, enforcedPairCount 0`) rather than any error. TypeScript cannot flag a missing optional member, so neither the compiler, nor code review across thirty slices, nor the fallback's own `logger.error` (which the running staging build predated) surfaced it | Sep 2026 (F-101, PR #625) - item 1's `consolidatedUpsertChildRows` shipped across thirty slices and had NEVER executed in staging or production, because `buildFilingPersistDeps` was never updated when the member was added to `FilingPersisterDeps` | PR #625 (inject the consolidator in the builder; change the interface member from optional to REQUIRED so the next omission is a compile error; refuse in the builder - not at scraper startup - when the flag is on and the method is absent) | not one of the three sanctioned check types (no `SUBSTANCE_CHECKS` key, no `audit-detection-floor.mjs` `record()` id, no `audit-ipo-coverage.mjs` invariant) - guarded by `scraper/tests/unit/services/filing-persist-deps-completeness.test.ts`, which enumerates the members of `interface FilingPersisterDeps` through the TypeScript compiler API (`ts.createSourceFile`, never a hand-typed list) and asserts `buildFilingPersistDeps()` leaves none of them undefined, plus `filing-persist-deps-refusal.test.ts` covering the builder's throw | guarded for `FilingPersisterDeps` only; every OTHER deps interface in the repo with an optional member and a single builder is still unguarded, and generalising the enumerating test into a shared helper is the obvious extension - not taken here because it belongs with whoever owns the builder conventions rather than to this fix |  |
| pdf kept past retention | document store retention (OD-32's 7-day rule) | a successfully-extracted offer-document PDF stays on disk past OD-32's stated 7-day retention window, instead of being deleted once extraction has succeeded and the window has elapsed. | 2026-09-23 (#933): an NSE RHP extracted on 2026-09-11 was still present on the staging disk on 2026-09-23 — 12 days past the 7-day window. Cause unmeasured. | none yet | none | unguarded |  |
| registrar address pollution | scraper persist (registrar) | `registrar` field carries an address/contact fragment instead of the registrar name | round-7 detection RCA (P3 batch) | `checkRegistrarQuality` added | `registrar_quality` (`scripts/lib/substance-checks.mjs`) | guarded |  |
| reopen trigger true on every re-plan | override layer on settled plan rows (spec §2.3.5, OD-73, OD-95, #968) | any reopen condition computed from a row's STANDING state (for example 'chosen source is rank 2 or 3 of the incoming order') instead of from a CHANGE between the recorded and the incoming order is true on every plan pass for every row in that standing state, so the row reopens, is re-settled and reopens again each data slot; and a narrowing held only in memory for one pass lets a lower-ranked source overwrite the settled value on the next pass. The same shape recurs at the END of a reopen: a restore that leaves the row's recorded order unchanged makes the same override reopen it again on the next pass (Tier A review of #1005 reproduced reopened=1 on cycles 1, 2 and 3). | 2026-09-24 (#967 second Tier A review, #968): with no override active, the chosenDemoted condition selected 24 of 236 SUPPLIED ipo_field_plan rows on ipodhan_staging (issue_size settled by CHITTORGARH 17, symbol/company_name settled by NSE 7); the same read-only query under OD-95's rule selects 0, and the Swap Test override (ipos.issue_size CHITTORGARH over DOC) selects exactly 2 (lcc-projects-ltd, rentomojo-ltd). | #968 (OD-95): reopen only when the incoming order is an override, its policy_origin differs from the row's, and it ranks a new source above the settling one; the narrowing is stored as ipo_field_plan.reopened_under_policy; expiry before re-supply restores the settled row; round 1: a restore stamps the tried order (one reopen per distinct override), a priority-matrix loss restores and logs OVERRIDE_SOURCE_LOST_TO_PRIORITY, and a better document completed during the reopen takes the row through OD-91 supersession | scraper/tests/unit/services/settled-field-override-reopen.test.ts and scraper/tests/integration/settled-field-override-reopen.integration.test.ts (no-override-reopens-nothing on the staging-shaped rows, narrowing-survives-two-passes, lower-rank-never-overwrites, override-expiry-no-flip-flop; each guard mutation-tested). Gap: no nightly floor check counts rows reopened per pass, so a future reopen path with the same shape is caught only by its own tests. | unguarded | §2.3.5, §3.2 |
| rollout-flag zeroed silently voids live logic | scraper/src/config/feature-flags.ts (env-driven `*_PERCENTAGE` / `ENABLE_*` rollout flags) | a flag that gates a live code path (e.g. `CONSOLIDATION_PERCENTAGE`) sits at 0 (or a `PROD-REQUIRED-TRUE` boolean sits at `false`) in the prod env file — the guard/feature is present, tested and merged, but the value silently turns it off in production with no signal (T-282: `CONSOLIDATION_PERCENTAGE=0` voided the entire consolidation pipeline for an unknown period; presence-only checks like `assert-env-keys.sh`'s `SCRAPER_REQUIRED_KEYS` never caught it) | T-282 (root cause); filed as discovery gap by T-297 D9 (issue #193) | this PR (`assert_rollout_flags_live()` in `scripts/assert-env-keys.sh`, reading `// LIVE-GATE` / `// PROD-REQUIRED-TRUE` markers straight off `scraper/src/config/feature-flags.ts` — no hand-duplicated flag list; prod slot only; `ALLOW_ZERO_FLAGS` escape hatch for a deliberate ramp-down) | not one of the three sanctioned check types (no `SUBSTANCE_CHECKS` key, no `audit-detection-floor.mjs` `record()` id, no `audit-ipo-coverage.mjs` invariant) — this is a deploy-time config gate, not a DB-row audit check; guarded by `scripts/tests/assert-env-keys.test.sh`'s rollout-liveness cases (mutation-proof: reverting `assert_rollout_flags_live()` fails the suite, per the T-285 P3-1 lesson); registered in `docs/reviews/detection-checks.json` under `notCoveredByThisManifest` (deploy-time, not the nightly DB audit) | guarded |  |
| scraper locks left by deploy | scripts/deploy-linux.sh / scraper cycle locks | a deploy that stops the scraper mid-cycle leaves the Redis cycle lock held, starving the next cron cycle | walk W-176 | `7f964415`, `648c2ba8`, `1d966477` (release locks on deploy stop; compare-and-delete on the lock token) | none in `substance-checks.mjs`/`audit-*.mjs` | unguarded |  |
| self-comparing oracle | audit (a_b_live_conflict) | a live-oracle compare using a source the scraper itself ingests (Chittorgarh) proves nothing about a scraper defect, and only 2 fields (dates) were compared so band/lot/issue-size defects were invisible | GitHub #199 (T-297 discovery-coverage meta-analysis), scoped 2026-09-07 | T-472: `a_b_live_conflict` re-pointed at ipowatch.in (non-ingested) and extended to 6 fields (openDate, closeDate, priceRangeMin, priceRangeMax, lotSize, issueSize; rupee fields at 1% tolerance) | `a_b_live_conflict` (`scripts/audit-detection-floor.mjs`, `checkA_B()`) | guarded |  |
| share-count-as-issue-size | scraper persist (issue_size) | `issue_size` column holds a raw share count (~80x too small) instead of rupees, e.g. `4,575,000` shown as the issue size in rupees | Aug 2026 (round 6/7 detection RCA); recurred Sep 2026 on the create path (W-177) | round-7 RCA fix + `w177-detection-rca.md` fix (segment-floor + shares-x-band consistency rule; `SME_ISSUE_SIZE_FLOOR`) | `issue_size_segment_floor` (`scripts/lib/substance-checks.mjs`) + `c_issue_size_consistency` / `c_issue_size_floor` (`scripts/audit-detection-floor.mjs`, `checkC()`) | guarded |  |
| shared config not shipped by the code deploy | scripts/deploy-linux.sh (shared/config symlink) + scripts/ops/deploy-config.sh + any schema-validated config file | a config file inside a release is a SYMLINK to a shared, slot-scoped file that the code deploy deliberately never overwrites (the same pattern as shared/env and shared/certs). A commit that tightens the config SCHEMA and updates the repo's copy of the config in the SAME change therefore ships the new validator against the OLD config, because only a separate manual command replaces the shared file. The loader refuses at process start and the service is dead — silently, because the deploy's own gate checks served sha, migrations and row counts, none of which require the service to start. Universal shape: any config whose validator ships on the code path while its VALUE ships on a different path can be broken by a single commit that touches both, and no CI drift check catches it because the repo copy is self-consistent. | 2026-09-19 — `b5614cc7` (#781) added `comparisonFamily` as a required zod enum in `scraper/src/config/field-manifest-schema.ts` AND to `scraper/config/field-manifest.json`. Staging deployed `b12c9d28` at 07:58 IST; the shared manifest was still the 2026-09-18 03:41Z copy from `9c20b4d0`, which lacked the key. Every one of the 190 fields failed validation; the scraper crashed at start in 4-6s and every 30-min cycle failed (02:33:51Z, 02:45:01Z) until the shared file was deployed at 08:26 IST. Prod unaffected (older code, no shared-config symlink) | immediate recovery: `scripts/ops/deploy-config.sh --slot staging --sha b12c9d28` (verified: CONFIG_SHA updated, 190/190 fields carrying comparisonFamily). Open: #793 for the three mechanisms — (1) validate the DEPLOYED config against the DEPLOYED schema as a deploy gate and fail the deploy if the loader refuses, (2) a same-day consumer for `wake-failed ... exit=1` in the wake log, (3) alert on `shared/config/<slot>/CONFIG_SHA` drifting from the served sha, mirroring the existing deploy-drift-monitor | THREE mechanisms, #793. (1) DEPLOY GATE (primary) - preflight_deployed_config() in scripts/deploy-linux.sh runs scraper/src/scripts/validate-deployed-config.ts BEFORE the atomic pointer flip and before SCRAPER_RESUME_TARGET="new", so a refusal is a genuine no-op: current still points at the last good release and the EXIT trap restores it. It loads the DEPLOYED field manifest through the scrapers OWN loader (loadFieldManifest, the code that crashed), and ONLY that file - the other scraper/config/*.json are not config-deployed and are flag-gated no-ops at runtime, so validating them would make the gate stricter than the process it models. Self-test scripts/tests/deploy-linux-config-preflight.test.sh: case 2 reproduces the incident (190 fields), case 5 is mutation-tested (moving the gate back after the flip turns it red), case 6 pins the file set. (2) LINEAGE CHECK OUTSIDE THE PROCESS - scripts/ops/config-lineage.mjs compares the served sha against shared/config/<slot>/CONFIG_SHA over read-only ssh. It exists BECAUSE the in-cycle copy cannot cover this incident: checkConfigLineage runs from runStep inside main(), and main() (index.ts:1651) is reached only AFTER the startup config validators (1646-1650) that throw during the failure - a detector killed by the failure it detects. CONFIG_SHA=="release" is reported UNKNOWN, never in-sync: deploy-linux.sh writes that marker only in its first-seed branch and no later deploy rewrites it, so a slot seeded once reads "release" forever while its shared manifest stays at day-1 content - the most drift-prone state, not the safest. Self-test scripts/tests/config-lineage.test.mjs. (3) WAKE-LOG CONSUMER - scripts/ops/wake-delta.mjs resolves wake-failed/ceiling-tripped lines to identities with NEW/GONE/SAME diffing and exit 3 on any class with no issue number (signal-ownership R1/R3); 18 such lines sat unread during the outage. The in-cycle checkConfigLineage in deploy-drift-monitor.ts is retained as a SECONDARY signal for the weaker case (config stale but still schema-valid) and is documented as such in its own header. REMAINING GAP: a hand-edit of the shared file between deploys is caught only by mechanism (2), which is operator-run, not yet on a cron. | guarded |  |
| slot done-stamp needs a pass that cannot finish | scraper scheduler (item 7) / data-job slots | a slot-gated job whose 'slot complete' stamp requires every pass to finish stays open when one pass can never finish (a source that keeps failing, or a retry timer that makes the same work due again on every wake); every later wake then re-runs the job, undoing the slot cadence | staging 2026-09-24 00:15-08:21 IST, release 084928e0: 00:00 slot 'incomplete: pass=discovery,listed_deferred' on 16 wakes, never complete; RETRY_MINUTES 30/60 (document-state-machine.ts:465-477) re-dues the same documents every 30-min wake against a 60 s DISCOVERY_MS budget (:774); 00:45 wake blocked 74 vs 6 first-entry log lines (F-151); due-step discovery-stamp instance found in review of #941 (#943) | none yet (#943) | none - the wake log shows it but no audit reads 'incomplete' streaks | unguarded | §2.1 |
| source mapped from a field the payload never carries (sector) | scraper/src/scrapers/nse-api-client.ts, scraper/src/config/field-priority-matrix.ts | `ipos.sector` stayed 0/251 because the matrix listed NSE/BSE/MONEYCONTROL/CHITTORGARH as sources for a field none of them actually writes — NSE read `data.sector`, a key the real payload never carries (verified against every captured fixture); BSE explicitly writes `undefined`; Moneycontrol and Chittorgarh never assign a `sector` key anywhere in their scrapers | round-7 P3-1, issue #242 | T-455 (this PR): removed the phantom `data.sector` read in nse-api-client.ts, narrowed the matrix `sector` sources to `['ADMIN']` (the only source that can currently deliver a value) | `scripts/audit-ipo-coverage.mjs` CORE ipos COLUMN FILL-RATE section already reports `sector` non-null % nightly (report mode, not `--gate`-blocking — a hard threshold would misfire today since no scraper source populates it until a real one is built, tracked separately per issue #242 item 2) | unguarded (dashboard-only; no `--gate` threshold exists for `sector` specifically) |  |
| source record number discarded | scraper/src/scrapers/bse-api-scraper.ts (fetchBSEDetail) and any scraper fetching by a source's own stable record number | a scraper fetches a source record BY that source's own stable identifier (its record number / IPO_NO / internal id), but discards the identifier instead of persisting it, so on every subsequent cycle the same record is re-matched by NAME instead of by its stable id — which is slower, fragile to spelling drift, and loses the one field that would make a relaunch or a postponement recognisable as the same source record. | 2026-09-23: BSE's IPO_NO is the param the scraper's own fetch call uses (`bse-api-scraper.ts:449 fetchBSEDetail`) but `bse_scrip_code` is empty on 0 of 326 rows measured — 296/326 rows have `symbol` filled (196 CHITTORGARH, 43 BSE, 43 NSE, 10 MONEYCONTROL, 2 ADMIN) and none carry BSE's own IPO_NO or ScripCode. | none yet | none | unguarded |  |
| stage gate requires the value the gated work would supply | scraper/src/scheduler/stage-reconciler.ts (deriveLifecycleStage) + scraper/src/services/document-state-machine.ts (STAGE_DOCUMENT_TYPES, dueDocTypesForStage) | a lifecycle stage is entered only when a fact is already present, and the work that would SUPPLY that fact is due only at or after that stage — a deadlock. Every entity that cannot obtain the fact by some other route is stuck at the earlier stage forever, with no error, no retry exhaustion and no blocked row: the gated work is simply never due, so nothing is tracked as missing. The tell is a perfect correlation between having the fact and having the downstream artefacts, with no scatter — a budget, ordering or retry problem produces a spread, a gate produces a clean split. Universal shape: any promotion condition that reads an output of the work it gates. The bug hides when an incidental side channel supplies the fact for most entities, making a systematic gate look like an intermittent minority bug. | 2026-09-19 (staging). `deriveLifecycleStage` returns PRE_OPEN only when `priceRangeMin > 0` (stage-reconciler.ts:161); `STAGE_DOCUMENT_TYPES.UPCOMING = ['DRHP']` while PRICE_BAND_AD first becomes due at PRE_OPEN (document-state-machine.ts:85-102). So: no band -> stage UPCOMING -> PRICE_BAND_AD never due -> the advertisement that carries the band is never fetched -> no band. Measured across all 16 UPCOMING IPOs: 14 of 14 WITH a band had 6 fetch-state rows; 2 of 2 WITHOUT a band (Anand Seamless opening 09-22, Liqvd Digital opening 09-23) had exactly 1 row (DRHP), zero documents, and would have reached their open date with a blank price band. Most IPOs escape only because exchange board data happens to carry the band | AUDIT 2026-09-19 before proposing a fix, which found two holes in the first proposal: (a) only `offering_type='IPO'` ever holds a PRICE_BAND_AD (21 of 21); every non-IPO type stores min=max (TENDER 16/16, RIGHTS 5/5, NCD 3/3, INVITS 3/3, BUYBACK 1/1, REITS 1/1) - a single fixed price in both columns, never a range - so a naive 'no band -> fetch the band ad' rule chases a document that cannot exist. The existing `isFixedPrice` guard excludes them only when a price is PRESENT: with a NULL band it is false, so 24 rows (19 OFS, 4 NCD, 1 RIGHTS) escape it and would hunt a non-existent document - an explicit offering-type guard is required as well. (b) 2 of 16 live UPCOMING IPOs have NO `open_date` (Adroit Industries, Swastika Infra), so a rule keyed only on open_date proximity would silently never fire for them - the same never-fires shape as the bug. (c) N MEASURED 2026-09-19 across MANY IPOs, after a first proposal of N=5 that was INVENTED (reasoned from the SEBI ~2-working-day requirement, zero IPOs behind it) and was correctly challenged by the owner. `documents.filing_date` is 0 of 21 populated so the true FILING-to-open window is still underivable; `field_sources` however holds 620 band-provenance rows across 310 IPOs with timestamps. Three nested populations, each cleaner: ALL 310 IPOs -> median -30 days, min -2085, because 242 of 310 bands were recorded AFTER the IPO opened (historical backfill, not observation - a median from this population describes our scraping history, not IPO behaviour). LIVE-WATCHED (IPO row created before open, gap 0-60d), n=67 -> median 5, p90 7, max 12; still partly confounded, 20 of 67 had the band written the same day we first saw the IPO. BAND WATCHED ARRIVING (IPO known >=2 days before the band row appeared), n=17 -> median 4, p25 1, p90 5.8, max 7; distribution 0d:2, 1d:3, 2d:3, 4d:4, 5d:3, 7d:2. READING: the spread is wide and the LOW end is what matters - 5 of 17 (29%) had their band recorded 0-1 days before open, so a window set at the MEDIAN would promote too late for nearly a third of IPOs. Set N from the upper end (>=7) : being early costs a few NOT_YET_FILED fetch attempts, which the state machine already handles; being late costs a blank price band on a live IPO. CAVEAT: this measures when WE RECORDED the band, not when the issuer FILED it, so it is a LOWER bound on earliness - the true filing is at or before our recording, which pushes N up, never down, and that direction is safe. | none yet - #795. Initial RCA blamed budget starvation (`UPCOMING_RESERVE_SLOTS = 1` vs 2 candidates) and was WRONG: #468's reservation is ON and firing (`upcomingReserved:1`), the two IPOs were visited 23 times in 78 elapsed cycles, and on every one of those visits `dueDocTypesForStage('UPCOMING')` returned `['DRHP']` only, so no PRICE_BAND_AD row could ever be created. Raising the slot count would have changed nothing. FIX RULE (generic): a promotion condition must depend only on facts our own pipeline cannot suppress - never on an output of the work it gates. Applied here: (1) applicability FIRST, reusing `notApplicableTypes()`/`deriveIssueShape()` plus an explicit offering-type guard (see the measurement below - the existing `isFixedPrice` guard does NOT cover a null-band non-IPO row); (2) promote on whichever signal exists - `open_date` within N days when present, an RHP on file, or the issue appearing on an exchange board; (3) never promote on the price band itself; (4) null-safe by construction - an issue with NO available signal stays UPCOMING and is REPORTED as unresolvable rather than silently stalling, which is the difference between a visible gap and the bug this class describes | none. Every existing document check JOINs `document_fetch_state` and can only grade rows that EXIST (see failure class `check-blind-to-a-row-never-created`), and here the row is never created because the type is never due. Proposed `m_upcoming_missing_price_band_tracking` (#796) is entity-anchored and catches it: live IPO opening within 7 days with no PRICE_BAND_AD fetch-state row; verified to flag exactly the 2 and pass the other 18 of 20 | unguarded |  |
| stale dates after relaunch | data-consolidation (ipos.open_date / close_date write path) for a postponed-then-relaunched issue | an IPO that was postponed and later relaunched with new dates keeps the FIRST attempt's open/close dates stored, because the relaunch arrives under a changed source record number (see `source-record-number-discarded`) and is not recognised as an update to the existing row, so the new dates never overwrite the stale ones. | 2026-09-23: dhanwel-hybird-seeds-ltd close date stored as 2026-06-29 on staging (the postponed June attempt), while the live relaunch ran 19-21 Aug 2026 per BSE IPO_NO 7900 and Chittorgarh page 2846. | none yet | none | unguarded | §2.3.3.2 |
| status-restricted aggregator run drops a name discovery hasn't reached yet | scraper/src/base/BaseScraperOrchestrator.ts (S-02 §5 `allowedStatuses` guard) | prod DOES run an unrestricted NSE+BSE discovery job 4x/day (`scraper/src/index.ts:304-305`, 08:30/11:00/14:00/17:30 IST, no `allowedStatuses`) — that path is unaffected. The class is narrower: a name Chittorgarh or Moneycontrol (both restricted to UPCOMING/OPEN on their once-daily cadence) sees before NSE/BSE has listed it — typically an early-announced SME row — was silently skipped and NEVER created by a later cycle either, because there is never an existing row for the guard to match against, so the aggregator's earlier sighting was simply lost until NSE/BSE eventually discovered the same name on its own 4x/day cadence (or never, if NSE/BSE's own board omits it). 8 rows confirmed missing on prod as of 2026-09-07 (advenzymes, infrax-renewable-ltd, karamtara-engineering-ltd, manipal-payment-and-identity-solutions-ltd, rentomojo-ltd, sis, steamhouse-india-ltd, vinod-texworld-ltd) — of these, `sis` and `advenzymes` are rejected upstream BY DESIGN (`data-validation.ts` `NON_IPO_SCRIP_CODE_NAME`/`NON_IPO_WINDOW_TOO_LONG`, ~line 330/393: bare scrip-code names and no-substance long windows), so this fix creates at most 6 of the 8 | 2026-09-03 (staging-vs-prod discovery lag first observed on Karamtara Engineering, issue #351) | this PR (#351 fix — the guard now allows a brand-new row through when the row's OWN validated status, the same status the write path would persist, falls inside `allowedStatuses`; existing out-of-scope rows and new out-of-scope rows both still skip); spec updated `docs/specs/per-ipo-due-step-pipeline.md` §5 (2026-09-07, architect, owner go) | `scripts/audit-reverse-sweep.mjs --gate` (`g_reverse_sweep`, T-461, #187) — external chittorgarh.com dashboard vs our site, per-IPO row-exists + offeringType + detail-page-200 assertions; wired non-fatal into `scripts/vps-data-audit-cron.sh` step 6/6; real run 2026-09-07 against `https://ipodhan.com` FAILed naming Karamtara Engineering, Infrax Renewable, Rentomojo, Manipal Payment & Identity Solutions, Steamhouse India, Vinod Texworld (matching this row's own list) plus Manika Plastech, Century Business Media, Maharaja & Speedex India, Om Galaxy, Raksan Transformers, Panchatv Bharat, Rays of Belief | guarded |  |
| substance predicate fed rows without a column it reads | scripts/audit-ipo-coverage.mjs, scripts/audit-substance-plausibility.mjs (SUBSTANCE_CHECKS row-builder queries) | a `SUBSTANCE_CHECKS` predicate reads `row.<col>` but the audit script's SELECT never selected `<col>`, so the predicate silently sees `undefined` and never flags a real violation (recurred: `audit-ipo-coverage.mjs` omitted `i.segment`; `audit-substance-plausibility.mjs` independently omitted `i.issue_type`, no-opping the two fixed-price-degenerate-band checks) | T-297 (`audit-ipo-coverage.mjs` / segment); Sep 2026 (review-gap round, `audit-substance-plausibility.mjs` / issue_type, this PR) | T-297 fix (added `i.segment`) + this PR (added `i.issue_type`, plus an `evaluated N rows` print) | `scripts/tests/audit-ipo-coverage.test.mjs` and `scripts/tests/audit-substance-plausibility.test.mjs` — both statically extract every `row.<col>` a `SUBSTANCE_CHECKS` predicate reads and assert the script's SELECT selects it, so deleting a column turns the test red without a live DB | guarded |  |
| the web build breaks while the PR gate stays green | any relative import inside packages/shared/src that is `.js`-suffixed (NodeNext-style, so tsc/tsx resolve it to the sibling `.ts` file), once it becomes reachable from a `web/**` file — directly or transitively through another shared module | packages/shared/src/tsconfig.json and web/tsconfig.json both use `moduleResolution: bundler`/tsc's NodeNext-compatible resolution, which silently maps a `.js`-suffixed relative import to its `.ts` source file. Next.js's production build runs its own webpack pass, and webpack's default resolver does NOT do that remap — it looks for a literal `./foo.js` file, does not find one (only `foo.ts` exists), and fails with `Module not found: Can't resolve './foo.js'`. `web/tsc --noEmit`, `eslint`, and every unit/integration test pass cleanly because none of them run webpack; the PR gate (`pr-gate.yml`) runs exactly those three and never runs `next build`. So a PR that adds or deepens an import chain from `web/**` into a `.js`-suffixed shared import merges green, and the break is discovered only when something actually runs `next build` — a staging or prod deploy. | 2026-09-24 21:30 IST staging deploy window, GitHub Actions run 36024250816, deploying main @ 03359ba2. `next build` failed at `../packages/shared/src/utils/duplicate-ipo-merge.ts:12` — `Module not found: Can't resolve './company-identity-fold.js'` — import trace `web/app/api/admin/conflicts/route.ts` -> `web/lib/services/conflict-resolution.ts` -> `packages/shared/src/services/corrigendum-suggestions.ts` -> `packages/shared/src/repositories/ipo-repository.ts` -> `packages/shared/src/utils/duplicate-ipo-merge.ts`. The reachable chain was created by PR #989, which made a `web/lib/services/*` file import a shared SOURCE service directly (rather than only via the compiled `@ipodhan/shared` package). Every merge from #989 onward passed `pr-gate.yml` (lint + type-check + unit tests) with the staging build silently broken underneath. Reproduced on `origin/main` locally 2026-09-24 (`cd web && npm run build` fails with the identical error and trace); fails identically on any of the 4 other `.js`-suffixed relative imports in `packages/shared/src` (`repositories/field-source-overrides-repository.ts` x2 -> `../db/schema.js`, `repositories/ipo-identity.ts` -> `../utils/company-identity-fold.js`) the moment any of them becomes reachable from `web/**`. | web/next.config.mjs webpack config gets `resolve.extensionAlias = { '.js': ['.ts', '.tsx', '.js'] }`, merged into the existing webpack function (Sentry wrapper kept intact) — this makes webpack apply the same .js->.ts remap tsc/tsx already do, so every current and future `.js`-suffixed shared import resolves under `next build` too, at the class level rather than renaming import sites one file at a time. A second, unrelated pre-existing type error surfaced once the build got past module resolution (packages/shared/src/repositories/ipo-source-keys.ts:376, `check.reason` typed `string | undefined` assigned to a `string` field) and was fixed at the call site with a fallback string, since it was previously invisible for the same reason (next build's type-check step never ran to completion). | Guarded by TWO checks, because the fast one alone missed the second half of the incident. (1) scripts/ci/check-shared-js-imports-resolve.mjs, wired into pr-gate.yml's `gate` job as a pure-fs step before `npm ci` (no install/build needed, seconds): finds every `.js`-suffixed relative import in packages/shared/src whose literal target file does not exist and fails the PR unless web/next.config.mjs carries the matching `resolve.extensionAlias` guard. Proven RED on origin/main's next.config.mjs (exit 1, names all 4 offending imports) and GREEN on the fix branch (exit 0). This check structurally cannot catch a type error `next build`'s type-check step only reaches after module resolution succeeds (the actual second failure this incident hit, ipo-source-keys.ts:376) — a resolve check has no way to see a type-check-only failure. (2) The `web-build` job in `pr-gate.yml` (name: "Web production build (next build)"): runs the REAL `cd web && npm run build` — the exact command that failed in staging run 36024250816 — path-filtered (via a git diff on the PR's base/head shas) to only run when `web/**` or `packages/shared/**` changed, so it costs nothing on unrelated PRs. No Sentry/DB/Redis env vars are set; the local proof build succeeded with none set either. First run on PR #1008 (this fix), GitHub run 36029596048: success, 3m23s (16:46:28Z-16:49:51Z), well inside the 15-minute job timeout. This is the only one of the two checks that would have caught BOTH failures the incident actually hit. | guarded |  |
| two copies of one module drift in BOTH directions | any module that exists as two copies — packages/shared/src/repositories/* against web/lib/repositories/*, and any check or helper duplicated across workspaces | a module exists twice and each copy accumulates something the other lacks, so neither is the good one. The known-duplicate framing ('one is live, one is dead — delete the dead one') is then WRONG and dangerous: deleting the 'dead' copy silently drops whatever only it carried. The tell is a fix whose issue number appears in one copy and not the other; the generic tell is that the two files differ by more than whitespace at all. Universal shape: a fix lands in whichever copy the author happened to open, so over time each copy is ahead on different lines, and the drift is invisible because nothing compares them. Distinct from the plain duplicate class, where one copy is simply stale, because here a merge or a delete must be preceded by a DIFF and a decision per difference. | 2026-09-20 — issue #824 / PR #830. `web/lib/repositories/data-conflicts-repository.ts` (629 lines, zero production importers, 14 test importers) lacked the W-79 same-source guard that `packages/shared/src/repositories/data-conflicts-repository.ts` has, and the shared copy lacked an id tiebreaker the web copy had from #358 (`detectedAt` ties reshuffled which conflicts a `limit` cut off). So the guarded copy was missing a fix and the fixed copy was missing the guard. The deletion was scoped as 'remove a dead unguarded duplicate'; had it run without a diff, #358's fix would have been lost with no trace. The fix was ported instead, proven by a regression that went 4 passed/1 failed to 5 passed/5 | #830 deletes the web copy, repoints its 14 test importers at the shared one, and PORTS the #358 tiebreaker into the shared copy rather than dropping it. The general remedy is the diff-before-delete step, not the deletion | none yet. A check on this class is mechanical and cheap: for any pair of files with the same basename under two workspace roots, fail when they differ by more than a configured allowance, so a duplicate cannot quietly diverge. Until that exists, the mitigation is procedural and belongs in every de-duplication brief: DIFF THE TWO COPIES FIRST, and for each difference state whether it is ported, dropped deliberately, or the reason it does not apply. A de-duplication brief that says only 'delete the dead copy' is an incomplete brief | unguarded |  |
| two sections of one spec disagree about the core mechanism, and a third section enforces both | any multi-section design document written over time, where a later decision revises an assumption an earlier section is built on | a design document states a mechanism in section X and a constraint in section Y that makes X impossible, and neither section references the other as a conflict. Each section reads as complete and correct on its own, so a reader who opens either one alone finds nothing wrong -- the contradiction is only visible to someone who reads both AND holds them side by side. The failure mode is not that the work is hard; it is that the work is UNBUILDABLE while looking perfectly specified, so an implementer builds one side, satisfies its section, and produces a permanently failing check. Worst when a THIRD section (a verification check, a gate, an acceptance test) asserts an invariant that only holds if the two agree: then both readings fail the same check, and the failure looks like a bug in the implementation rather than in the spec. Universal shape: a decision log that REPLACES an earlier decision does not rewrite the prose that the earlier decision justified, so the document carries both the new rule and the old rule's consequences. | 2026-09-20 -- issue #853, build item 9 (the re-read loop) in docs/design/data-sourcing-pull-model.md. §3.2 specifies the disagreement-resolution algorithm as `1. re-fetch the winning document's bytes and re-hash them` and makes it mandatory: "a re-read counts only if it produced a receipt with a hash computed from bytes on disk during this cycle. No receipt, no verification." OD-32 (§0.5.1, dated 2026-09-09, an owner decision REPLACING OD-23) deletes the PDF seven days after its last successful extraction, keeps only the extracted text, and names §3 explicitly in its own retention table: "A website disagreement (§3) ... none of them need the PDF back, they need the words" -- then rules the §3.2 shape out in advance: "a design section that asks for it back is a defect, not a feature." The third section closes the trap: §4's `REREAD-RECEIPT` check alarms when `re-reads with a receipt hashed this cycle / re-reads recorded` falls below 1.0, so building to §3.2 (no bytes exist to hash) and building to OD-32 (no bytes are read) BOTH make the check permanently red. Caught before any code was written, while drafting the build card; zero `reread` hits existed in `field-plan-walk.ts`, so nothing had to be undone; SECOND INSTANCE 2026-09-25 -- §2.4's loop writes the first SUPPLIED rank and stops, while OD-58 requires a field to be confirmed by agreement between independent witnesses: a stop-at-first loop never produces a second witness, so OD-58 was unbuildable as written. Measured: 0 witness rows on staging (F-168). Surfaced while specifying admin editing; resolved by OD-103, which amends §2.4; THIRD INSTANCE 2026-09-25 -- §2.4 says a field with an admin protection row is skipped ('do not store a state'), while OD-106, OD-107 and §9.2 item 9 (all written the same day) need the loop to SEE a newer value for that field to raise a suggestion or apply an exchange date. Found by an independent review of §9 before any code; fixed by a §2.4 clarification ('never write', not 'never read'). Also found in the same review: OD-111 offered the SEBI filing number as a binding identifier although OD-89 had removed it -- a decision written without re-reading the later decision that replaced its premise | none yet -- #853 states the fork and recommends Option A (the receipt names the source document's retained `documents.sha256` plus an extractor run id from this cycle, over the stored text), because OD-32 is the later owner-level decision, has a gate enforcing it (D17), and its own text anticipates this section. The general remedy is procedural: when a decision REPLACES an earlier one, the same change re-reads every section the replaced decision justified, and any section that becomes false is edited or marked in that change -- not left for an implementer to discover | none, and a general one is hard: detecting semantic contradiction between prose sections is not mechanical. Two cheap and specific things that WOULD have caught this at write time: (a) OD-32's retention table already names `§3` in its justification column -- a check that every cross-section reference in a decision's justification is re-read when that decision lands would have surfaced it; (b) a §4 check whose invariant can be satisfied by NEITHER of two readings of the spec is itself the signal -- when writing an acceptance check, state which section's behaviour makes it pass, and a check with no such section is a spec defect, not a check. Note the asymmetry that makes this class expensive: the contradiction costs ONE re-read of two sections when caught at build-card time, and costs a full implementation plus a permanently red check when caught after | unguarded | §3.2, §4, §0.5.1, §2.4 |
<!-- END GENERATED TABLE -->

## Summary (as seeded 2026-09-06; updated 2026-09-07)

20 rows: 10 `guarded` (an audit check independently re-verifies the DB), 10 `unguarded` (fixed at a
write path or in ops config, but nothing independently re-checks it — this is exactly the shape
that let share-count-as-issue-size recur in September after its August fix).

## Nightly audit -> GitHub issues (recurrence loop, part 2)

A `guarded` row in the table above only means a check EXISTS and RUNS nightly. It says nothing
about whether a FAIL that check reports actually gets looked at — `docs/reviews/round-7-detection-
rca.md` and the walks above are full of checks that ran clean for weeks while a defect it would
have caught sat unfixed, because a FAIL line in a cron log nobody reads is not a tracked work
item. Part 2 closes that gap: every FAIL or UNVERIFIABLE from `scripts/audit-detection-floor.mjs`
becomes — and stays — a GitHub issue until the check passes again.

**Mechanism.**

1. `scripts/audit-detection-floor.mjs` writes `<STATE_DIR>/findings-latest.json` on every run
   (gate mode or plain report mode): `{ runDate, generatedAt, gate, results, findings }`, where
   `findings[checkId]` is the list of offending rows (`{ rowKey, title, body }`, capped at 200 per
   check) collected during that run. This file is a snapshot, not an append log — it is
   overwritten every run.
2. `scripts/audit-findings-to-issues.mjs` reads that file and, for every check currently FAIL or
   UNVERIFIABLE, syncs ONE GitHub issue titled `[nightly-audit] <checkId>: <name>`:
   - no open issue exists -> **create** it, labeled `nightly-audit` plus either `needs-decision`
     (the check's rows are data already sitting wrong in the DB — a human decides the repair) or
     `pipeline-failure` (the check's rows are a broken piece of machinery — a worker fixes it in
     code). An UNVERIFIABLE check always gets `needs-decision`, because "the audit went blind" is
     never something a worker can code-fix without first finding out why.
   - an open issue exists and the failing row-key set is UNCHANGED since the last run -> do
     nothing (no comment). This mirrors the digest de-duplication `audit-detection-floor.mjs`
     itself already does for Notifier pages (see that file's header comment on the "~72 pages a
     night" incident) — a bot commenting on its own unchanged issue every night is the same noise
     class in a different channel.
   - an open issue exists and the row-key set CHANGED -> comment listing which row keys are new
     and which resolved, so the issue's history reads as a timeline instead of a wall of
     repeated dumps.
   - the check is back to PASS and an open issue exists -> comment `PASS on <date>` and close it.
3. Classification of `needs-decision` vs `pipeline-failure` for a FAIL is currently a static map
   (`DATA_REPAIR_CHECK_IDS` in `audit-findings-to-issues.mjs`) rather than a field in
   `docs/reviews/detection-checks.json`, because only one check (`listed_rotation_stall`) carries
   a `registryRow` today. When a check's `detection-checks.json` entry does carry a `registryRow`,
   its text is included in the issue body so a reader lands on the right registry row without
   guessing.
4. A per-run `--max-issues` cap (default 30) stops one very bad night from opening dozens of
   issues in a single run; anything past the cap is logged, not filed, and picked up on the next
   run.

**Fail-open, by design.** This step runs on a production root cron on the VPS, and its own repo
rule is that a missing tool or a network blip must never turn a healthy audit into a failed one.
`scripts/audit-findings-to-issues.mjs` exits 0 and prints `ISSUES-SKIP: <reason>` whenever `gh` is
missing, `gh auth status` fails, the findings file does not exist yet, or any `gh` call throws —
never a non-zero exit from this step. `scripts/vps-data-audit-cron.sh` additionally wraps the call
in `|| true` as a second, redundant layer of the same guarantee.

**Dry-run switch.** `AUDIT_ISSUES_DRY_RUN=1` (or the script's own `--dry-run` flag) makes every
`gh` command something the script would have run — printed, never executed, exit 0 — for exercising
this on the box the first night before trusting it with real issue creation:

```bash
AUDIT_ISSUES_DRY_RUN=1 node scripts/audit-findings-to-issues.mjs
# or, off the box against a saved findings-latest.json:
node scripts/audit-findings-to-issues.mjs --dry-run /path/to/findings-latest.json
```

**Cron step [4/5] DEFAULTS TO DRY-RUN.** `scripts/vps-data-audit-cron.sh` does not rely on anyone
remembering to set `AUDIT_ISSUES_DRY_RUN=1` — it stays dry-run on every tick until a marker file
exists, so the very first cron run on a fresh box cannot file every currently-FAILing check as a
real issue in one shot. Go live with:

```bash
touch /root/data-audit-ipodhan/state/issues-live
```

Setting `AUDIT_ISSUES_DRY_RUN=1` in the cron's environment still forces dry-run even after the
marker exists — the env override always wins over the marker.

**State.** `<STATE_DIR>/issues-sync-state.json` — `{ [checkId]: { issueNumber, firstSeen,
lastRowKeys, closedAt? } }` — lives next to `findings-latest.json` in the audit's own state dir and
is the only way the script knows "unchanged since last night" vs "this is new". A closed entry is
KEPT, never deleted, precisely so a PASS->FAIL flap re-attaches to the SAME issue instead of
opening a new one every cycle. Losing this file is recoverable, not silent-failure-shaped: the next
run just re-discovers issues (open OR closed — `gh issue list --state all`) by title and treats
every row as new (one extra comment, not a duplicate issue or a reopen, because the title match
still finds the existing issue in whatever state it is in).

**Human-closed issues are never reopened or recreated.** A check that a human closed as "won't fix"
or "accepted legacy" while it still FAILs gets a comment only when the failing row-key set actually
changes since the issue was closed — never a reopen, never a duplicate issue. See `planIssueSync()`
in `scripts/audit-findings-to-issues.mjs` for the exact rule.

**Public-repo caveat.** Issue bodies and comments embed real data from tonight's run — company
names, dates, price/lot values, row keys — pulled straight from the production database. `abhayla/
IPODhan` is a public repo, so this mechanism publishes that data to anyone who can read its Issues
tab. Nothing here is a secret (it already renders on the live site), but it is a step beyond "a
private log file on the VPS" and should be kept in mind before pointing this mechanism at a check
whose row detail is more sensitive than public IPO data.
