{
  "_why": "OD-52. A rule that no build item implements is allowed — the deploy cadence, the honest uncertainties, the language-model constraint are rules about how we WORK, not code. What is not allowed is silence. D19 fails on any rule id that is neither claimed by a card nor listed here with a reason.",
  "_generated_by": "docs/design/apply-rule-ownership.mjs",
  "unclaimed": {
    "R-119": "5.6 — A standing constraint on HOW we work (no language model in the phase-1 pipeline), not a thing any build item constructs. OD-52 allows this, provided it is declared rather than silent.",
    "R-120": "5.6 — A standing constraint on HOW we work (no language model in the phase-1 pipeline), not a thing any build item constructs. OD-52 allows this, provided it is declared rather than silent.",
    "R-121": "5.6 — A standing constraint on HOW we work (no language model in the phase-1 pipeline), not a thing any build item constructs. OD-52 allows this, provided it is declared rather than silent.",
    "R-168": "7.1 — The build sequence describes the items themselves; a rule about the order of the work is not implemented BY one of the items in it.",
    "R-132": "7.1 — The build sequence describes the items themselves; a rule about the order of the work is not implemented BY one of the items in it.",
    "R-133": "7.3 — The honest list of what the author is unsure about. These become rules only once they are answered, and then they land in the section that answers them.",
    "R-134": "7.3 — The honest list of what the author is unsure about. These become rules only once they are answered, and then they land in the section that answers them.",
    "R-135": "7.3 — The honest list of what the author is unsure about. These become rules only once they are answered, and then they land in the section that answers them.",
    "R-136": "7.5 — The deployment and branching policy. It is enforced by the release process and by `.claude/rules/branching-model.md`, not by application code.",
    "R-137": "7.5 — The deployment and branching policy. It is enforced by the release process and by `.claude/rules/branching-model.md`, not by application code.",
    "R-138": "7.5 — The deployment and branching policy. It is enforced by the release process and by `.claude/rules/branching-model.md`, not by application code.",
    "R-139": "7.5 — The deployment and branching policy. It is enforced by the release process and by `.claude/rules/branching-model.md`, not by application code.",
    "R-140": "7.5 — The deployment and branching policy. It is enforced by the release process and by `.claude/rules/branching-model.md`, not by application code.",
    "R-141": "7.5 — The deployment and branching policy. It is enforced by the release process and by `.claude/rules/branching-model.md`, not by application code.",
    "R-189": "A CORRECTION note, not a buildable rule: it records that §5.2's capacity table stated numeric(15,2) while the live schema is numeric(18,2), and that the overflow argument for the crore conversion therefore does not hold (OD-67). There is nothing to build — the decision it records is to NOT do the migration — so no build card claims it.",
    "R-214": "2.3.3.2 identity-research subsection, 2026-09-23. Research evidence (a value table built from findings F-1 to F-3) recorded for an owner decision, not a rule to build — no OD row changes the binding order or matching rules by itself; see docs/design/findings.json F-124..F-135.",
    "R-217": "2.3.3.2 identity-research subsection, 2026-09-23. A per-finding citation list (F-124..F-141) pointing at docs/design/findings.json; none of these findings change OD-34/OD-35/OD-68/OD-69/OD-71 by themselves — each candidate rule change needs its own owner decision, so nothing here is a buildable rule.",
    "R-219": "2.3.3.2, 2026-09-23. The OD-83/OD-84 explanatory paragraph (exchange postponement is the same IPO; an attempt is defined by filing status before open dates exist) — a decision record and its rationale, not a new mechanism for item 12 to build beyond what R-177/R-218 already cover.",
    "R-226": "2.1 job-table notes, 2026-09-24. The F-151 staging measurement (the 00:00 slot never closed; the document pass reported its budget exhausted every wake, cause not yet identified) — an observation citing #943, not a new rule; the fix is tracked in #943 and item 7."
  }
}
