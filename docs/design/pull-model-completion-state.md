# Pull-model completion state

**What this is.** The live inventory of every pull-model build item, and the evidence for each verdict.
The policy that makes this list load-bearing is `.claude/rules/staging-is-the-release-gate.md`: no
production deploy is targeted until every item here is built and proven on staging.

**How to use it.** When an item lands, change its row here in the same PR. When a verdict is disputed,
re-measure - do not edit the verdict to match a card.

**FORMAT CONTRACT - this table is PARSED.** `scripts/ops/build-plan-board.mjs` reads every numbered
row with a strict pattern, and its verdict cell must contain **exactly** `**BUILT**`, `**PARTIAL**`
or `**NOT BUILT**` and nothing else. A qualifier inside the bold (`**PARTIAL - blocked on X**`)
reads fine to a human and silently stops the row matching: the parser then sees fewer items than it
expects and refuses to emit, failing the Detection-Change Gate on the NEXT unrelated PR. Put every
qualifier at the FRONT OF THE EVIDENCE CELL instead, where it reads the same. After editing this file, or `data-sourcing-pull-model.md`, run **`npm run docs:verify`** from the
repo root: FOUR separate generators parse these documents (plan board, rule index, design-
consistency checker, detection registry) and one edit can stale any of them. `docs:verify` runs all
four and ends with the consistency score; it exits non-zero on a real defect (verified). Running
only one of the four is how #872 and #875 both broke CI on an unrelated PR. (Learned the hard way in #872/#874; registered as
`docs/reviews/failure-classes/prose-edit-breaks-a-parser-of-that-prose.json`.)

**Measured 2026-09-21 against `refs/remotes/origin/main` = `748b8527`.** Always the explicit ref: a local
branch named `origin/main` has shadowed the remote twice and made a merged PR read as unmerged.

---

## Why this document exists rather than the cards

Every tracking artefact here has been caught wrong, so the cards cannot be the list:

- **24 of 42 build cards carry a Status line reading `unknown`**, and `check-build-cards.mjs:155` accepts
  that shape as valid. The gate passes while telling nobody anything. This is the single biggest reason
  nobody had an accurate picture.
- The stage-3 ledger showed five merged slices as `queued` for three days (corrected 2026-09-20).
- Item 31's card reads `NOT STARTED` for work the board records as landed in #813.
- `check-dod.mjs`'s D14 item has been red since 2026-09-09 because it hard-codes two open-fork rows that
  were correctly answered that day (#829).
- **Item 35 read `NOT BUILT` while #817 (`feat(ops): item 35 — admin queue open count in the nightly
  report`) merged to main 2026-09-19T18:13:30Z as `fb02ec1d`** - a merged-and-wired item marked not
  built, the same stale-verdict class as items 31/32/33/34.

So a card saying DONE is a claim. Each verdict below was checked against an artefact that exists.

---

## The items

| # | Item | Verdict | Evidence / what is missing |
|---|---|---|---|
| 1 | Child-table consolidated writer | **BUILT** | `data-consolidation-orchestrator.ts:731` `tableName: ChildConsolidationTable`; `:775` `SINGLETON_ROW_CHILD_TABLES`. Residual: #802 - `peer_companies` unwritten since 2026-06-17 |
| 2 | Field manifest + priority config | **BUILT** | `scraper/config/field-manifest.json`, `field-manifest-loader.ts`, `field-manifest-schema.ts`, 4 unit test files. Residual: #739 |
| 3 | Matrix cleanup / one source table (13 slices) | **PARTIAL** | 13 slices landed. **#923 (066d5203, 2026-09-23):** a configuration gap (a plan row whose source has no fetcher registered) no longer burns a real attempt, and reconciliation reopens rows it had wrongly stranded as exhausted. Staging repair owed: dry run measured 6,718 rows reset / 39 held for review — not yet applied on staging. Missing: **S6 churn-stop half** (#759, needs a `field_plan_state` migration); **S4 and S5 staging proofs owed**; **Swap Test never run** (#893); the churn-stop (#759) is also still open; S2-4 recorded FAIL (`n=10`, a card defect corrected in #835) **2026-09-24:** Staging proof: repair-config-gap-plan-attempts --apply on ipodhan_staging reset 77/77 capped rows (the deployed #923 reconciliation had already reopened the rest of the 6,624); a re-run reports 0 to reset. #893 root cause found (an override never re-ranked existing plan rows: the upsert re-ranked only on a manifest version bump) and fixed for non-SUPPLIED rows in #967 (4029595a, staging since the 13:30 IST window); SUPPLIED fields under an override are #968. #759 churn-stop is covered by #957 (F-152: structural gaps are definitive). Still owed: the Swap Test re-run inside a data slot (card procedure updated). **2026-09-24 (night):** #968 fixed (#1005, 05bdc19f, OD-95, migration 0061): an override now reaches a SUPPLIED field once per distinct override via `reopened_under_policy`/a tried marker instead of looping; a matrix-refusal restores and opens an admin-only conflict without overwriting an already-open dispute; supersession is checked on restore. Owed: a Swap Test run inside a live data slot on staging (today's 14:40-15:23 IST Swap Test used a hand-set override before #968/#1005 landed). **2026-09-25 (goal run):** Staging repair re-run: `repair-config-gap-plan-attempts` dry run found 494 to reset / 51 held (all COVERAGE_GAP no-mapping rows on 11 IPOs that the #923 reconcile had reopened, re-asked once and capped again, last touched 2026-09-24 18:49-18:53 IST); `--apply` reset 494 of 494; re-run 0. Swap Test path 2 started inside the procedure: override 83af50f9 (`ipos.issue_size` ranks [CHITTORGARH, DOC], all IPOs) set 2026-09-25 ~02:30 IST on 20 non-SUPPLIED live rows at `registry:2` (e.g. acme-universal-safezone-9-ltd fca5c949, advenzymes 52e4ff15); read after the 08:00 IST slot, then expire and read the revert after the 14:00 slot. |
| 4 | Per-field validation before write | **BUILT** | `field-extraction-validation.ts` (201 lines); `field_extraction_failures` in `schema.ts`. **Caveat: that table holds ZERO rows** - see OD-62 below |
| 5 | `ipo_field_plan` table + generator | **BUILT** | `ipoFieldPlan` in `schema.ts`; `field-plan-generator.ts`; `ipo-field-plan-repository.ts` |
| 6 | The pull walk over the plan | **PARTIAL** | `field-plan-walk.ts` + **4** fetchers registered at `field-plan-walk-deps.ts` (NSE, DOC, BSE, CHITTORGARH — INVESTORGAIN_GMP retired 2026-09-25, OD-100/#1022: gmp_records is job-owned, no longer in the manifest) - re-counted on `refs/remotes/origin/main` 2026-09-21 after PR #867 registered the NSE fetcher, which is what #705 and #759 correctly named as missing (57 manifest fields rank NSE, including the six E-1 fields only the exchange may state; 136 rows answered `NO_FETCHER_REGISTERED` every wake). #705's remaining premise is stale - the walk does have fetchers. INVESTORGAIN_GMP adapter built this slice (`field-plan-walk-investorgain-gmp-fetcher.ts`): answers from the value the existing GMP job (OD-19, every 30 min) already wrote to `gmp_records` — no second network call; core proof read-only against staging for 3 IPOs (see PR body) — staging proof owed (a live walk re-asking `gmp_records.gmp` rows). Known gap inherited, not fixed here: once a field-plan row reaches SUPPLIED it is terminal (`ipo-field-plan-repository.ts`'s SUPPLIED-reopen mechanism was built and reverted the same day, 2026-09-24, for a regression), so this GMP row — a class-W "live figure" per OD-73 — will not re-ask after its first SUPPLIED; that is a system-wide walk property, not specific to this adapter. Still open: #762's 12,480 parked plan rows (fix merged as #763, **not on prod**). F-161 (2026-09-24): the DOC fetcher answered not-available-yet on 984 staging rows / 23 IPOs with an extracted offer document because it matched only the manifest's `documentType` (PRICE_BAND_AD family = PRICE_BAND_AD alone); fixed in the item 6 DOC-family PR (read-only proof: 0 not-available-yet, 99 supplied); stays PARTIAL until a staging walk re-asks those rows and `pull_doc_nay_with_offer_doc` goes GONE **2026-09-24 (night):** Per-document field receipts + supersession built (#992, dffbb466, OD-91, migration 0060, F-164): extraction writes a receipt per printed field; `superseded_by` links a row to the newer document; the DOC fetcher is receipt+value-gated (a field is credited to a document only when its receipted value equals the current column value) via one shared rule module also read by `pull_frozen`. Owed: a new prospectus after tonight's deploy that actually reopens a receipted field (the existing 28 F-164 rows are left untouched by design — "going forward" per OD-91); the 00:15 IST slot walk proofs; follow-ups #993 (ipos-table lineage gap) and #998 (stuck-reopen detection). **2026-09-25 (goal run):** #993 merged (#1010, 29b852ff, OD-96): an `ipos` row written from a filing names its document, and a document writes a field only inside that field's document family; staging repair stamped 156 of 156 rows (43 IPOs), 110 with no candidate and 65 with several left alone. #998 closed as covered by the new floor check `pull_plan_pending_stranded` (#1019; staging has 2 superseded rows, both SUPPLIED). #762 measured on staging: live-IPO plan rows (NOT_AVAILABLE_YET 6,273, CHECK_FAILED 5,067) were all attempted within 24 h, 0 overdue; the 4,389 unclaimed rows are all on LISTED IPOs, the closed-IPO job's population (item 17). New class fixed: a write the writer refuses structurally (`MISSING_ROW_KEY` on `gmp_records`) re-queued the plan row PENDING forever, 26 staging rows (#1020, OD-99: recorded CHECK_FAILED `WRITER_CANNOT_ACCEPT`, not charged). F-161 residue repaired on staging: 259 stale DOC "no document yet" rows (#1024) and 99 rows settled by rank 2 behind a false DOC answer (#1026, #1025) reopened, re-runs 0. PARKED (#1022): the GMP walk proof — whether a live figure that its own job keeps fresh belongs in the walk plan is an owner question. Owed after the 2026-09-25 13:30 IST staging deploy: new `ipos` writes carry a documentId; the 26 gmp rows leave PENDING; the reopened rows re-answered by rank; a new prospectus reopening a receipted field (an event; PARK rule b). **2026-09-25:** owner decision OD-100 (#1022): gmp_records is owned by the GMP job, not the walk; the GMP walk proof is not applicable. |
| 7 | Job scheduler + budgets | **PARTIAL** | Scheduler built (`scheduler/`, `due-step-cycle.ts`). OD-55 force-kill removal merged but **not on prod** (#805). Tiering (O-4) unverified. Staging proof (2026-09-23, release 084928e0): S1 — the 22:20 IST live wake logged "Live-figures job: took scraper:live (independent of scraper:cycle, OD-27)" with ttlMs 240000, never took scraper:cycle, logged "GMP record created successfully" 23 times (17 rows carry a 22:xx source time; F-143), and released scraper:live. S2 — the 22:15 IST non-slot data wake skipped discovery and the document cycle as designed, but the website list scrapers still ran (F-142; slice S2b). S3 — closed-IPO cron lines installed at 25,55 22,23; ENABLE_CLOSED_IPO_JOB not set (off). Staging check (2026-09-24 08:41 IST, FAILED, release 084928e0): the non-slot skip holds (22:15-23:45 wakes skipped the document cycle; the 14:00 slot closed 21:45), but the 00:00 slot never closed - "incomplete: pass=discovery,listed_deferred" on all 16 wakes 00:15-07:45 because the document retry timer makes the same documents due every wake and the 60 s discovery budget runs out (F-151, #943). **S4 opening-day check (OD-31, OD-87, OD-88), PR #949 round 5 — `--job=opening`, prod cron 45 9 * * *, staging 40 9 * * *, under `scraper:cycle` (skip if held). Two list calls only (NSE current issues, BSE board); for each row opening today (IST) it writes identity, status and the open/close dates through `resolveIpoRow` (OD-85 keys) and the field-priority decision; a new row is created only from the NSE list, with its stated segment, through the real `IPORepository.create` (#860 guard untouched); a BSE-only newcomer is logged by name and left to the 14:00 data job (OD-88); every written column gets its `field_sources` row and the step ledger's F6 counts exactly those rows. Proven on ipodhan_test (`opening-day-create.integration.test.ts`, real saved NSE and BSE list payloads) — not proven on staging.** **S5 post-listing price (OD-29, OD-54), PR #972 round 4 — `--job=price`, every 15 minutes 09:15-15:30 IST Mon-Fri (25 reads a day, the last at 15:30) under the §2.1 `live` lock (`scraper:live`, 4-minute TTL, 3-minute run deadline, 300 s wake ceiling); NSE getSymbolData with the cached working series first, then BSE getScripHeaderData by ISIN; writes only `current_price` and its forward-only exchange as-of stamp; a run with no price (no symbol/ISIN, outage, or an explicit no-such-symbol answer from both exchanges) writes nothing and is logged with its cause. Delisting detection (§2.3.x) is split out of S5 by owner decision 2026-09-24 after three Tier A reds on the "exchange failure counted as delisting" class; it is a separate item (#983). Proven on ipodhan_test (`post-listing-price.integration.test.ts`) and against the live endpoints for 3 staging IPOs — not proven on staging.** **2026-09-25:** #957 slot-convergence work PARKED (#943): parked by the owner (01:20 IST): after the a2833963 staging deploy the 00:00 IST data slot did not close on the 00:15 or 00:45 wake (`data job slot 00:00 IST incomplete: pass=discovery`, discovery budget exhausted both times). Issues #943/#957 stay open; no more rounds this run. **2026-09-25 (goal run):** The S5 price and S4 opening-day crons are installed on staging (price 12/27/42/57 past 09-14 plus 15:12 and 15:30, opening 09:40 IST, Mon-Fri) but no `job=price` or `job=opening` wake had ever run before the a2833963 deploy; the first runs are 2026-09-25 09:12 and 09:40 IST. **2026-09-25 (morning, 7f26a70e):** Staging proof: S5 post-listing price — the 09:12 IST run before the open logged "outside market hours ... ZERO network calls"; the 09:27 IST run took `scraper:live` and wrote `ipos.current_price` for 41 listed IPOs on their real series (NSE EQ 28, SM 10, ST 3), read back lcc-projects-ltd 151.49, lalithaa-jewellery-mart-ltd 381.60, dhoot-transmission-ltd 1582.30 at 03:57Z (09:27 IST, no timezone skew); 29 IPOs got no price with the cause logged (no NSE symbol or ISIN stored) and nothing written. Staging proof: S4 opening-day check — the 09:40 IST run fetched only the two exchange lists (NSE 15 rows, BSE 22), bound rows by OD-85 source keys and listed today's openers (bench-mark-infotech-services-ltd, orient-cables-india-ltd, shree-tnb-polymers-ltd, acevector-ltd, himalayan-solar-ltd); all 11 writes `unchanged` (already correct), 0 failures, 7 s. Staging proof: S2b — the 08:45 IST non-slot wake logged "data job not due at this wake — discovery skipped, website scrapers skipped" with 0 website list fetches, while the 08:15 IST slot wake made 12. The remainder is the owner's park, PARKED (#943). |
| 8 | Ratios / basis-for-offer-price extractor | **BUILT** | RATIOS_BASIS_ISSUE_PRICE is deliberately excluded from `EXTRACTABLE_DOC_TYPES`/`AUTO_PERSIST_DOC_TYPES` (`filing-auto-persist.ts:137-162`) - no source parser exists for its content, and BSE has no ratios document source at all (`document-classifier.ts:177`). #716 (filed, then closed as non-bug) confirms this and finds detection already correct: `not_applicable_documents_named` (PR #677, merged 2026-09-16) reports these PENDING rows as not-applicable, not stuck. The real ratio-population signal is `issuer_ratio_yield` (#670), fed by RHP/DRHP/PROSPECTUS extraction. Row was wrong to call this a missing drainer. |
| 9 | The re-read loop | **BUILT** | **SUPERSEDED IN PART by OD-65/OD-66 (2026-09-21) — the spec's re-read-the-bytes loop is explicitly NOT wanted.** Owner: "everything should be done on day one itself, and it should not be repeated for the same IPO... one IPO, one round of document read." That dissolves the §3.2-vs-OD-32 contradiction (#853) rather than adjudicating it: nothing goes back to a purged PDF because nothing goes back at all. The sizing argument is also gone — the loop was scoped from "578 leadManagers conflicts"; decomposed (same-source rows, null-on-one-side, byte-identical) the real count is **1 on staging, 2 on prod**. What OD-66 DOES require is built and merged (#876): a new document is adjudicated on its own fields only, with the identity fields it must carry for slug resolution passed as CONTEXT rather than as claims — the defect there was real, 11 conflict rows auto-closed for fields no document read. **Still open under this item:** a genuinely new corrigendum/filing must still be FETCHED and read when it appears; that discovery path is item 22's, not a re-read loop. **Reading a stored corrigendum is built on branch feat/item9-corrigendum-reader (2026-09-24, OD-90, F-163):** CORRIGENDUM is on the one extractable list; the document cycle reads each stored corrigendum once (text layer, else OCR, marked) and records a SUGGESTION per changed field in the admin conflicts queue (`data_conflicts` + migration 0057: document_id, evidence {quote, page, ocr}, suggestion_key); the existing resolve action accepts it as an ADMIN value or dismisses it, nothing written without the admin. Core proof on ipodhan_test with the real Rays of Belief letter (designated exchange BSE -> NSE, accepted as ADMIN). Staging proof owed (a stored staging corrigendum producing its suggestion row) **2026-09-24 (night):** Corrigendum -> admin suggestion queue built (#989, e43f15ca, OD-90 [SPEC CHANGE to OD-30], migration 0057, F-163): a stored corrigendum is read once per document cycle and produces a SUGGESTION row in the existing admin conflicts queue (data_conflicts, evidence quote/page/OCR) instead of a direct write; every automatic reader was swept to skip suggestion rows. Owed: migration 0057 applying at the 21:30 window; a stored Skyways/Rays corrigendum producing a suggestion row after a document cycle; accepting a suggestion through the admin page needs `ADMIN_PANEL_ENABLED` turned on on staging (unset there today — owner decision pending). **2026-09-25 (night):** Staging proof: a real corrigendum (veegaland-developers-ltd, doc 038eaf50) was read on the a2833963 deploy into 1 admin suggestion (document-level, field `unknown`, evidence page 1, not OCR, quote on NAV), listed by GET /api/admin/conflicts (200, staging admin panel enabled by the owner 2026-09-24). Still owed: an admin ACCEPT on a mapped field (openDate/closeDate/designatedExchange); the Skyways corrigendum (stored NOT_EXTRACTABLE before the fix) is read when the document cycle next visits it. **2026-09-25 (goal run):** The only mapped-field corrigendum on staging (skyways-air-services-ltd, 3b77bc3b) had been stamped NOT_EXTRACTABLE before #989 and nothing re-admitted it, and its IPO (LISTED 2026-09-01) is outside the 10-day live window the document cycle visits. Fixed at class level (#1017, 692974bb, OD-98): a document stranded NOT_EXTRACTABLE whose type is now extractable is re-admitted by `repair-readmit-stranded-documents.ts` (staging: 1 row, re-run 0), and the extraction pass takes stored never-read documents outside the live window, 3 per cycle, oldest first; floor check `d_stranded_readmit`. Owed: the Skyways corrigendum read on the new code (91st of 95 in the queue) and an admin ACCEPT of one suggestion through the staging admin page. **2026-09-25 (morning, 7f26a70e):** Staging proof: the stored Skyways corrigendum (3b77bc3b, a scanned newspaper notice, re-admitted by #1017 and reached by the extraction-only pass after #1031) was read and produced admin suggestions, one on the mapped field `ipos.closeDate` ("Corrigendum, page 1 (OCR, confidence 0.7828)", the exchange value shown beside it per E-1). An admin accepted it through https://staging.ipodhan.com/admin/conflicts (Resolve → the corrigendum side → Resolve Conflict): `POST /api/admin/conflicts` 200; conflict e7e7b920 resolved `ADMIN` / `CORRIGENDUM_ACCEPTED`; `field_sources` closeDate now `source=ADMIN`, lineage `{method: ADMIN_CORRIGENDUM_ACCEPT, conflictId: e7e7b920, documentId: 3b77bc3b}`, previous source NSE. (The same write stored `updated_at` 5h30m early: timezone defect #1033, filed.) |
| 10 | Verification checks of section 4 | **BUILT** | 8 `audit-*` scripts exist. **14 of the 21 paper checks accounted for 2026-09-20** (12 merged in #859, more in #863; `design_traceability` was already gating every PR and merely mislabelled as paper) — they found four live defects on their first runs (#858 retired listing prices, #860 null segments, #861 a check that never reports, #862 79 E-1 writes from DRHP). **All four now have class-level fixes**: #862 guarded at the `trackFieldUpdate` choke point (merged `685db95f`), #858 at the plan generator (merged `956bd761`), #860 at `IPORepository.create` (#866), #861 in #863. Existing bad ROWS are not repaired by those guards and are named in each issue. **6 of the remaining 8 are BLOCKED on mechanisms that do not exist** (measured, see #778): `pull_frozen` needs supersession (zero callers, no `superseded_by` column); the three `reread_*` need item 9; `pull_noblank` needs a current-value read across 190 columns; `pull_noop_suppression` needs its counters persisted. Real deliverable is 13-15 checks, not 21. **2026-09-24 (night):** `pull_noblank` built (#988, 5f5dcf9a): field_sources.previous_value compared against the current column. `pull_noop_suppression` retired as a duplicate of the already-built `pull_noop` (same SQL/ratio/window; a weaker env-flag gate). `reread_latency`/`reread_receipt`/`reread_verdict` were already retired by OD-65 and are not owed. Remaining paper check: `zip_member_rows`, deferred with item 22. Owed: the nightly detection floor run on the new checks. **2026-09-25 (goal run):** `zip_member_rows` built (#1023, 47f2c91e): the central directory of each stored exchange zip is read by HTTP Range and its typed members compared with the rows written. Staging proof: `audit-detection-floor.mjs` run against ipodhan_staging (BASE_URL https://staging.ipodhan.com) from origin/main 47f2c91e, 82 checks, every new check ran and named identities: `zip_member_rows` PASS (24 of 24 zips read, 4 member rows expected, 3 present and 1 deduplicated; 153 zips not yet re-read since item 22 are UNVERIFIABLE by rule), `pull_noblank` PASS (0 of 39), `pull_frozen` PASS, `t_source_local_time_shift` PASS (0; it counted 273 before the #999 repair), `d_stranded_readmit` PASS (0 after the repair), `pull_plan_pending_stranded` FAIL 25 by name (the gmp_records.gmp loop, fixed by #1020, awaiting deploy), `d_ipos_doc_lineage_document_id` FAIL 1 (srit-india-ltd.companyDescription, staging still on pre-#1010 code). The nightly floor itself runs only against production, which lacks migrations 0057-0062, so the staging run is the proof. |
| 11 | Crore conversion (OD-20) | **BUILT** | **The data conversion is WITHDRAWN — the capacity argument that justified it is false.** Measured 2026-09-21: all five rupee columns are `numeric(18,2)`, not the `numeric(15,2)` the spec's SS5.2 table stated. Saudi Aramco (Rs 2,50,000 cr, largest IPO ever priced anywhere) uses **0.025%** of the ceiling; Hyundai India 0.0028%; our largest stored row (NSE, Rs 26,579.64 cr) 0.0027%. The owner's test — "if the current field holds the world's largest IPO there is no need to change it" — is met with a factor of 4,000 to spare, so no migration, no repair tool, and #854's source-availability blocker is moot. What DID land: F-95 and F-77 (PR #871), the two readers comparing crore thresholds against the rupee column — NSE and a Rs 0.70 cr IPO scored identically before the fix. Storage stays rupees; display converts at the edge (`formatIssueSizeCrores`). Unit-tag work landed 2026-09-24 (F-156, 2nd occurrence of `threshold-in-one-unit-column-in-another`): the manifest generator (`scripts/generate-field-manifest.mjs`) now derives `unit` from the amount-columns probe's measured `current_unit` rather than the column's amount class, correcting all 5 OD-67 rupee columns (`ipos.issue_size`, `ipo_details.fresh_issue`/`ofs_issue`, `ipo_valuation.mcap_at_floor`/`mcap_at_cap`, all now tagged `rupee`) plus the 7 `financial_statements` columns (now tagged the new `per_row` unit) in `scraper/config/field-manifest.json`. The admin `issue_size` input (the only one of the 5 with a live editable UI — grep found none for fresh_issue/ofs_issue/mcap_*) does NOT convert. ROUND 2 (2026-09-24, Tier A REVISE): round 1's edge conversion (`web/lib/admin/amount-unit-edge.ts`) was keyed `issueSize`, but the dynamic admin page reads/writes/validates by the real snake_case key `issue_size` (GET /api/admin/dynamic/ipos/[id] snake_cases every key; column.name is the Drizzle column's real DB name) — so the conversion, and the pre-existing crore label/validator, never fired on the live page; round 1's own tests only proved a fictitious shape. The conversion was deleted (the live admin page has always shown and saved raw rupees, and real ADMIN-sourced staging values are rupee-scale) and the label/validator got a truthful `issue_size` entry instead: `field-labels.ts` shows `₹ (rupees)`, `dynamic-validation-rules.ts` validates the same ₹10 crore / ₹1 lakh crore bounds converted to rupees via `RUPEES_PER_CRORE`. Detection: `scripts/tests/manifest-unit-matches-current-unit.test.mjs` (+ a new mutation test proving `unitForField` throws on an unrecognised `current_unit` rather than silently defaulting) and `web/tests/unit/lib/admin/issue-size-live-key.test.ts` (replaces round 1's deleted test, drives the real `issue_size` key end to end, and shows the dead camelCase validator gives the wrong verdict on the same real value), both in `pr-gate.yml`/`test:unit`; failure-classes registry status `guarded`. Verdict held at PARTIAL pending the staging proof read (supervisor) **2026-09-24 (night):** Admin-page proof for the crore/unit work is blocked — the admin panel is disabled on staging (`ADMIN_PANEL_ENABLED` unset; every `withAdminAuth` route answers 401 on both staging and prod), a finding surfaced while reviewing item 9's admin-queue PR. Owner decision on enabling it is pending. **2026-09-25 (night):** Staging proof: headless Chrome on https://staging.ipodhan.com/admin/dynamic/ipos/aa819c0e (armee-infotech-ltd, issue_size 3000000000.00) shows the Issue Size field labelled `₹ (rupees)`, and typing 500000 shows `Issue size below ₹10 crore is unusually small for IPOs.`; non-GET admin requests were blocked in the run, 0 attempted, nothing written. The staging admin panel was enabled by the owner for this (ADMIN_PANEL_ENABLED=true, ADMIN_AUTH_TOKEN equal to ADMIN_API_TOKEN, staging only). |
| 12 | Name normaliser + duplicate detection | **BUILT** | Built and on `refs/remotes/origin/main`: `company-identity-fold.ts`, `company-name-normalizer.ts`, `company-name-similarity.ts`, `ipo-identity.ts` (OD-34 order). **OD-68/OD-69 landed 2026-09-23 (#910, 7db61b0c):** page-status suffixes and page-title text are stripped before matching (one implementation, `packages/shared/src/utils/identity-decoration.ts`, with a parity-tested JS copy for the audit); a name match binds only with the same segment and a matching known price band, an exact name within 180 days binds a postponement (OD-35); a WITHDRAWN row never binds a refiling (OD-71); a same-name live row with a differing known date or band is HELD for review (audit_logs + nightly `i_identity_held`), with an admin override; `mergeDuplicateInto` refuses differing CIN/ISIN/symbol/open date; the look-alike pairs (Himalayan Solar / Himalaya Nutravedics, Technocraft / Technocrats) are pinned as different. Nightly sweep for duplicates by identifier (#906). **#925 (ecc49140, 2026-09-23):** CIN is now the FIRST binding identifier in the resolver (OD-34 step 1, OD-69) — the resolver never joins two rows on a differing CIN. **Missing:** the SEBI draft number (OD-34's second identifier) is not yet a binding step — measured 2026-09-23: the only per-offering SEBI number present in our documents is the observation-letter number, filled in 10 of 12 RHP/Prospectus rows and `[●]` (unfilled) in 6 of 7 DRHPs; **resolved by OD-89 (2026-09-24): step 2 is dropped (SEBI has no per-offering draft number, F-125); binding is CIN, then source record keys (OD-85), then symbol, then name. No column or migration.**. The live duplicate `rays-of-belief-ltd` / `rays-of-belief-ltd-o` is not merged yet (staging first, prod with the release, OD-68). Residual: #679 **2026-09-24:** Staging proof: #945 source keys: backfill-ipo-source-keys --apply inserted 297 keys on ipodhan_staging (0 collisions, re-run 0), and the 13:50 IST wake on f5e213e8 logged [OD-85] bound by source key for 11 real IPOs (varmora-granito, armee-infotech, swastika-infra, elevate-campuses, adroit-industries, a-one-steels, moneyview, german-green-steel, runwal, acevector, orient-cables). Staging proof: the Rays of Belief duplicate merged on staging (keep rays-of-belief-ltd, drop rays-of-belief-ltd-o; same CIN U85110DL2017PLC322623; issue size 1,250,000,000 from Chittorgarh page 2787; slug redirect written; merge-tool VERIFY false FAIL fixed in #976). OD-89 dropped the SEBI step. Still owed: #679. **2026-09-24:** decorated-slug rename: tool built (`scraper/scripts/repair-decorated-slugs.ts`), staging apply owed by the supervisor. **2026-09-24 (night):** #679 merged (#1001, 97b7f4b4, OD-94): merge gate refuses a pair with differing offering_type always, and differing close_date/listing_date unless OD-86 names a relaunch. Staging proof: `repair-decorated-slugs.ts` --apply wrote 5 renames (0 collisions, re-run 0, redirects held for all 5); the merge tool now runs on ipodhan_staging and refuses a real non-duplicate pair (shree-balaji-mala-textiles-ltd vs shree-tnb-polymers-ltd) naming the reason "open date differs (2026-07-22 vs 2026-09-25, 65 days)"; no real offering-type-mismatched pair exists on staging today, covered instead by 51 unit tests plus a real-value run of the Cube Highways values through checkMergeEligibility. Follow-ups filed, not blocking: #1002 (26 pre-merge-log staging merges from 09-15/16 have no merge log; Cube Highways IPO->InvIT among them is wrong) and #1003 (sweep the raw-SQL merge path + 3 old one-off scripts around the gate). |
| 13 | OFS + fresh-issue extraction | **BUILT** | `filing-persister.ts:364-440` - F-51 reconciliation, tolerance constant |
| 14 | BSE share count to rupees | **BUILT** | **Existing rows only.** Re-measured 2026-09-21 on staging; #728's headline is CLOSED for new writes. BSE is `capable: false` for `ipos.issue_size` on `origin/main` (ranks `DOC -> CHITTORGARH`), and all 6 IPOs in #728's table now hold the EXACT printed total from CHITTORGARH (6/6: Hero Motors 10,000,000,000; SS Retail 5,007,500,000; NSE 265,796,400,000; Jindal Supreme 1,248,800,000; Manika Plastech 1,255,000,000; Sonaselection 1,415,700,000). A SECOND defect found and fixed in PR #870: `computeBSEIssueSize` returned the sentinel 0 on missing inputs and it was persisted as a value - ALL 20 zero-valued `issue_size` rows are BSE-sourced, no other source ever wrote one. Remaining: 17 non-zero BSE-sourced rows carry the wrong-quantity value and 20 hold 0; 19 of those 20 have no completed document to re-derive from, so the repair needs the same source decision blocking item 11. Collision with item 11 on `ipos.issue_size` still stands - a unit conversion and a correctness repair must not be in flight together. **2026-09-24 (detection + tool, not yet applied):** measured on staging via a new nightly check (`c_issue_size_noncapable_source`, `docs/reviews/detection-checks/c_issue_size_noncapable_source.json`) that reads capability directly from `field-manifest.json` rather than hard-coding BSE — 40 of 334 IPO rows currently source `issueSize` from a non-capable source (BSE, NSE, or MONEYCONTROL); of those, 15 are BSE-sourced with a non-NULL value (8 LISTED, 7 CLOSED; 6 of the 7 CLOSED rows carry a NULL `segment`, 2 of those 6 also carry a NULL `price_range_max`). `backfill-issue-size-chittorgarh-detail.ts --recheck-above-floor` now CONSIDERS NULL-segment/NULL-price-cap rows (`isIssueSizeCandidate`, unit-tested) — verified on staging: 14 of the 15 BSE rows are considered (the 15th, `light-of-life-trust`, has a NULL `issue_size` itself, correctly excluded as "nothing to recheck"); 7 of the 14 (the known-segment SME/MAINBOARD ones) matched a Chittorgarh detail page and all 7 read OK (within 40% of stored — dhanwel 267.3M vs 270M, horizon-reclaim 542.7M vs 540M, kwick-forensic 507.744M vs 510M, mopshop 272.6M vs 270M, paluck 330.048M vs 330M, complete-sports 749.25M vs 750M, rays-of-belief 1.25B vs 1.25B exact); the other 7 (kwality-walls-india-ltd + 6 NULL-segment CLOSED rows) are considered but UNMATCHED to a Chittorgarh detail URL this run (Chittorgarh's report-118 discovery map did not carry their company names this fetch — a pre-existing discovery-matching gap, not something this change controls). **Round 2 (2026-09-24, Tier B review finding):** the recheck's "OK" (within-40%) branch never wrote, so a non-capable-provenance row whose value happened to be close to CHITTORGARH's kept its non-capable source forever, and `c_issue_size_noncapable_source` would have failed on it indefinitely even after a full staging apply. Fixed: `decideIssueSizeRepair` now reads the row's current provenance capability (`checkIssueSizeSourceCapability`, the same manifest-driven predicate the audit check uses) and repairs value+provenance under `--apply --overwrite-above-floor` regardless of divergence when the current source is non-capable — an identical-value row gets a provenance-only repair, stated as such in its output line. Proved on a staging dry run (no `--apply`): of the 8 rows still within-band under non-capable provenance, all 8 now print FLAG (was silently OK) and, given `--overwrite-above-floor`, WRITE. The 7 old CLOSED rows with no Chittorgarh match at all (no capable-source figure found) are unaffected by this round — they print `SKIP: no capable source — unresolved` and stay untouched, same as before; their repair still needs the discovery-matching fix noted above. Verdict stays PARTIAL: the tool is now correct for both above- and below-band non-capable rows, but nothing has been applied yet — the supervisor applies on staging next, then the check must read clean. **2026-09-24:** Verification first: nightly check c_issue_size_noncapable_source (#971) found 40 of 334 IPO rows sourcing issue size from a non-capable source (15 BSE, 25 NSE/MONEYCONTROL). Staging proof: backfill-issue-size-chittorgarh-detail --recheck-above-floor --overwrite-above-floor --apply wrote Chittorgarh value and provenance to 30 rows (divergence 0 to 1.4%), and the check then read 8 of 333. The 8 are old CLOSED IPOs with no capable source (#979, deferred as IPO data tuning). BUILT on 2026-09-24: the class is closed at the write path (BSE non-capable), guarded nightly, and repaired on staging; the 8-row residue has no capable source and is deferred data tuning (#979), not build work. |
| 15 | Revive `valueActuallyChanged` | **BUILT** | 7 occurrences in `data-consolidation-service.ts`; noop-write-suppression test |
| 16 | Retire Moneycontrol | **BUILT** | `scraper/src/index.ts:797-804`, with the freshness-SLO consequence recorded |
| 17 | Closed-IPO job (OD-22) | **BUILT** | **#919 (257421cf, 2026-09-23) rebuilt the mechanism: the job now PLANS then WALKS** (OD-76) — it no longer records DONE from a zero-work pass. DONE requires stored plan rows greater than zero AND every row settled (OD-79); the failure cause is `FIELDS_PENDING` when rows remain unsettled (OD-80, migration 0052); a picked event is re-picked rather than abandoned (OD-81); plan and walk share one fingerprint so they cannot drift apart (OD-82). Built and present on `refs/remotes/origin/main`: the job, `isClosedIpoJobDue` (22:00 IST boundary), `closed_ipo_resourcing` + migrations 0050/0052. **Still PARTIAL:** `ENABLE_CLOSED_IPO_JOB` stays OFF by default — the job's own spec-stated dependencies (items 6, 7, 10) are not all built; the new plan-then-walk behaviour has no staging proof yet (owed); the 10 wrong DONE rows recorded on staging by the OLD mechanism (#717) had their repair already applied on staging as of 2026-09-23 (measured 2026-09-24: 10/10 rows carry a `repair-717:` marker, outcome PARTIAL/EXTRACTOR_MISSING) — the earlier "await repair" wording here was stale. Follow-up: #932. Registry: `done-recorded-when-worker-found-nothing`. **2026-09-24 (night):** Still owed: `ENABLE_CLOSED_IPO_JOB` turned on, and a real 22:00 IST night run after items 6, 7 and 10 land (per §6.1's stated dependencies). **2026-09-25 (morning, 8897788f):** Staging proof: `ENABLE_CLOSED_IPO_JOB=true` on staging (backup `scraper.env.bak-20260925-closedipo`) and the job run by hand at 09:00 IST (owner speed-up plan): the F-31 snapshot was written first (1,634 field_sources rows), 10 IPOs picked newest-closed first (himalaya-nutravedics-india-ltd, fx-multitech-ltd, vivekanand-cotspin-ltd, robokidz-eduventures-ltd, axiom-gas-engineering-ltd, spectraa-technology-solutions-ltd, national-stock-exchange-of-india-ltd, varmora-granito-ltd, anand-seamless-ltd, kheria-autocomp-ltd), every outcome PARTIAL with a cause class (FIELDS_PENDING 7, SOURCE_UNREACHABLE 3), 0 DONE (no zero-work DONE), 8 fields written; `closed_ipo_resourcing` rows recorded. The walk asks only due rows (§6.1 "exactly the pull walk", OD-80/81). |
| 18 | Document retention (OD-32) | **BUILT** | `documentPages` table exists (`schema.ts:726`); the page-text writer is live in `filing-auto-persist.ts:1191` (PR #560, #628) and stores rows before COMPLETED is set. The purge (`document-cycle.ts`, `document-store.ts`) keys on per-document `extracted_at` + a `textless_count` veto so a COMPLETED document with zero stored pages is never deleted. 46 real unit tests pass (`document-page-text.test.ts`, `document-page-number-base.test.ts`, `document-pages-schema.test.ts`, `purge-requires-stored-text.test.ts`, `document-purge-policy.test.ts`), verified 2026-09-20. **Under question: #933** — an RHP extracted 2026-09-11 was still on the staging disk 2026-09-23, 12 days after its last extraction, against OD-32's 7-day purge window; cause unmeasured. |
| 19 | Merge tool on shared write path | **PARTIAL** | Singular and plural merge tools routed through `IPORepository.mergeDuplicateInto` (#432, #807 piece 1). **Merge log landed (#888) and made complete (#917, 27cd76f6, 2026-09-23):** both IPO rows are locked and snapshotted whole (`to_jsonb`, every live column), both IPOs' field_sources rows are logged, repointed rows are logged by id and rows deleted on a unique conflict are logged whole, with counts taken from the statements (RETURNING), not pre-counted; #900 fixed (one shared watcher no longer aborts or wipes the merge). **Missing:** the `unmerge` command itself - parked by the owner until the identity work (#903) lands; migration 0051 (ipo_merge_log) is not yet on staging or prod. **2026-09-24 (night):** Unmerge command built (#994, b519089d, OD-92, migration 0058): the merge log stores every deleted/cascaded row whole plus superseded key state; `--unmerge` restores only the survivor columns the merge carried (with their provenance), pre-checks unique collisions and refuses, naming them. Staging proof: `--unmerge` on the pre-extension Rays of Belief entry refused, exit 1, wrote nothing, naming 540 count-only rows across 13 tables (gmp_records 119, ipo_field_plan 190, data_conflicts 77, ...) and pointing to `--partial`. Full restore round trip is proven only on ipodhan_test (5/5, including GMP restore); a merge made after 0058 has not yet been unmerged on staging. **2026-09-25 (goal run):** No merge exists on staging after migration 0058 (`ipo_merge_log` holds one row, 97ede65b, made before 0058), and the duplicate checks find 0 groups across 390 staging rows (`i_same_ipo_two_rows`, `i_company_two_live_rows`). The post-0058 unmerge proof waits for a real duplicate; it is PARKED after 2026-09-28 (PARK rule b) if none appears. PARKED (#1032): the staging unmerge of a merge made after migration 0058 waits for a real duplicate (0 duplicate groups across 390 staging rows on 2026-09-25); parked by the owner's speed-up plan. |
| 20 | Design-traceability CI check | **BUILT** | `scripts/ci/check-design-traceability.mjs` + its test |
| 21 | The read side (OD-39/40/41) | **PARTIAL** | `FieldProvenanceLine.tsx` + test exist. Missing: `chosenConfirmedAt` column; **the staleness threshold is no longer needed (OD-72, 2026-09-23: the time-based marker is retired; the line states source and read date, and a missed live-refresh slot alerts the admin)**; touched-slugs tracker does not survive a restart **2026-09-24 (night):** Built (#997, ee1eff2b, OD-93, migration 0059, F-165): `chosen_confirmed_at` backfilled, missed-slot admin alert (one per IPO per IST day plus an end-of-day digest, checked against every unclaimed slot), Redis-persisted touched slugs surviving a restart. Staging proof: 236/236 eligible SUPPLIED rows backfilled; `https://staging.ipodhan.com/ipos/armee-infotech-ltd` (200) renders "From the offer document, read 19 Sep 2026" and a live "as at 6:20 PM, 24 Sep" line, 0 stale "last confirmed" markers. Owed: the missed-slot admin alert firing on a real missed slot in market hours. **2026-09-25 (goal run):** The missed-slot admin alert is read in the 2026-09-25 market hours. |
| 22 | Document handling + download limits | **BUILT** | Row was stale: OD-37's other three limits are ALSO built with dedicated tests, not just the streaming cap — `resolveHostVerdict` (DNS-rebind fix, `company-host-source.ts:474`), the registrar host allow-list (`company-host-source.ts:756`), and cause-carrying refusal logging (`document-discovery-runner.ts:783-1036`), each tagged "item 22, OD-37" in the code and covered by `company-host-source-registrar-allowlist.test.ts` + `document-discovery-runner-download.test.ts`. `ENABLE_DOWNLOAD_STREAMING_CAP` (`feature-flags.ts:445`) is flag-gated code-complete; only its real-network staging proof is owed (#806) — an ops/deploy step, not a build. OD-36's multi-part rule is now built on this branch (feat/item22-zip-members, 2026-09-24, F-154): every non-GID member of an exchange zip typed as a type other than the main document's is stored as its own document (`storeZipMemberDocuments`, `zip-member-documents.ts`; url `<zip>#member=<member path>`, `part_number` = zip position; sha256 dedupe against the DB; the supplied type marked FOUND; existing zips repaired by `repair-zip-member-documents.ts`), the GID and unclassified members are logged by name and not stored, guarded in CI by `zip-member-documents.test.ts`; its staging proof (a re-fetched RHP_HTEL-shape zip writing its CORRIGENDUM rows) is owed, and a stored CORRIGENDUM is now read into admin-reviewed suggestions by item 9's step (OD-90, branch feat/item9-corrigendum-reader) instead of being admitted `NOT_EXTRACTABLE`; ADDENDUM still is. **Password-protected PDFs (OD-36's blank-password-attempt rule) are now BUILT**, at the Python extractor rather than the `pdf-lib` route the card originally planned (F-153 dissolved both blockers recorded 2026-09-10 — see build-cards/item-22 "AS BUILT"): `extract_filing.py`'s `_is_pdf_password_error()` catches `pdfplumber`'s wrapped `PDFPasswordIncorrect` on the one blank attempt, returns a terminal `extraction_status: "PDF_PASSWORD_PROTECTED"` envelope with the cause recorded, and `filing-auto-persist.ts` writes it straight to `MANUAL_REVIEW` on the first attempt — never the ordinary FAILED/backoff path, so #959's clock never retries it. Proven on a real fixture (a 1-page extract of the real encrypted filing behind F-153, re-encrypted with a genuine user password; the owner-password variant is the paired negative fixture) — `scraper/scripts/test_extract_filing_password_protected.py` (mutation-tested) + `scraper/tests/unit/services/filing-auto-persist.test.ts` ("OD-36" case). **Still missing:** the OCR-page-loses-a-disagreement rule in the field-priority path, and a CORRIGENDUM extractor (F-158: stored corrigenda are admitted NOT_EXTRACTABLE, so OD-30 cannot act yet). **2026-09-25 (night):** Staging proof: the 00:45 IST wake on a2833963 logged `Stored-zip member expansion pass (item 22)` selected 3, expanded 3, refused 0, unresolved 0, membersStored 0. The OCR-loses-a-disagreement rule is built in draft PR #1012 (OD-96, F-167, migration 0062) and has NOT had its Tier A review; it is not merged. **2026-09-25 (goal run):** Staging proof: #806 on the real network — the real `defaultFetcher` with `ENABLE_DOWNLOAD_STREAMING_CAP` on, against the real NSE archive DRHP of papadmalji-agro-foods-ltd (37,624,875 bytes): a 2 MiB cap aborts in 331 ms and a 30 MiB cap aborts (status 0), a 36 MiB and a 100 MiB cap return 200 with all 37,624,875 bytes; staging (flag on since 2026-09-19) has streamed 146 real documents, the largest 37.6 MB, with 0 too_large. Staging proof: zip members — the stored-zip pass logged `membersStored` 1 and then 2: steamhouse-india-ltd's RATIOS zip wrote its PRICE_BAND_AD member ("SIL_Price Band Ad_Jansatta.pdf") and runwal-enterprises-ltd's zip its two PRICE_BAND_AD members, each as its own `#member=` document row. The OCR rule merged (#1012, ab9fbbcb, OD-97, F-167, migration 0062): an OCR-only value loses a disagreement only to a text value from a document of the same or better rank; its staging proof is owed after the 13:30 IST deploy. The "CORRIGENDUM extractor" remainder is superseded by OD-90 (#989): stored corrigenda are read into admin suggestions. #1016 fixed (#1021): a document that prints an E-1 date no longer fails its whole save. **2026-09-25 (morning, 7f26a70e):** Staging proof: OCR mark (#1012) — skyways-air-services-ltd's scanned RHP completed on the new code and wrote 28 receipts, 24 marked OCR and 4 TEXT, each with its real per-value confidence (e.g. `ipo_details.allocationPct` OCR 0.7559, `bidWindows` OCR 0.7658), not the old flat 100. The same document prints E-1 dates and still completed (the #1016 fix, #1021, holding on a real document). |
| 24 | Stage-gate deadlock | **BUILT** | PR #798 merged; #795 closed |
| 30 | Canonical IPO type table | **BUILT** | `docs/design/ipo-type-population.json` / `.md` |
| 31 | PR Spec-deviation block | **BUILT** | Landed as #813. **Card still reads NOT STARTED - card is stale** |
| 32 | Status line on every card + gate | **PARTIAL** | Gate built (`check-build-cards.mjs:142-156`). The 20 cards reading the `unknown` stopgap non-reason (PR #814) were each resolved against `refs/remotes/origin/main` + `gh pr view`: 10 are DONE (items 1,2,4,5,8,13,15,16,18,20 — artefact present, PR confirmed MERGED), 10 remain `unknown` because their OWN item is measurably PARTIAL in this document (items 6,7,9,10,11,12,14,19,21,22), each now with a real reason instead of the non-reason. The gate now REFUSES any `unknown` card not on a shrink-only `UNKNOWN_ALLOWED` list (`check-build-cards.mjs`), pinned by `scripts/tests/check-build-cards-status.test.mjs`, so no new card can pass with `unknown`. Not BUILT: the gate still accepts `unknown` for those 10 cards — it does not yet refuse the shape outright — so verdict stays PARTIAL until those 10 items themselves resolve to DONE or NOT STARTED. PARKED (#1027): the final step (make the gate refuse `unknown` outright) needs an owner decision on the Status line shape for an item that ends PARTIAL with parked pieces; 6 cards still read `unknown` (items 6, 7, 9, 19, 21, 22); item 10's card was resolved to DONE in the same change. |
| 33 | Repoint `check-dod.mjs` into CI | **BUILT** | Row was stale on BOTH halves, re-measured 2026-09-22 on `refs/remotes/origin/main`. Root refusal built (`check-dod.mjs:13-18`), and **the CI wiring exists too** — `docs-gate.yml:85` runs `node docs/design/check-dod.mjs` on every docs PR, and has since the ENOENT fix; `grep -rn "check-dod" .github/workflows/` finds it. It runs `continue-on-error: true` by design, stated in the workflow: this DoD asserts the state of the 2026-09-09 design DELTA, not a live invariant, so an item drifting true-to-false as work legitimately continues must not redden unrelated docs PRs. #829 (the one item that could never be true again — it asserted two fork ids whose ABSENCE is the success condition of the process it measured) is FIXED and CLOSED by PR #883: it now delegates to `check-design-consistency.mjs`, which owns D14, and `scripts/tests/check-dod-d14-mechanism.test.mjs` (wired into docs-gate, 8 tests, 0.45s) guards the class. The walker's fork item now reports MET (end-to-end proof in #883, `DOD_E2E=1`, 6 pass 0 fail); the walker's OWN total is not asserted here because it spawns the mutation harness and any concurrent read of `docs/design/` perturbs it. **Residuals, named not hidden:** (a) `scripts/tests/check-dod-root-resolution.test.mjs` runs in no workflow — three of its four cases drive a full `check-dod` run that spawns the mutation harness, one had not finished after 15 minutes, and two such runs mutate the same tracked files concurrently; the reason is recorded beside the docs-gate step; (b) the card's own Known-gaps review — whether each remaining DoD item is still the right question — is the owner's and stays open |
| 34 | `spec_ref` on every failure class | **BUILT** | `validateSpecRefs()` in `scripts/build-detection-registry.mjs` (refs `origin/main` @ 485f5285) requires the key on every entry (throws if missing), requires an array, and validates each ref against the spec's real heading list, refreshed per run; `docs/reviews/failure-classes/*.json` all 45 entries carry `spec_ref` (currently `[]`); `scripts/tests/build-detection-registry.test.mjs` has 5 cases proving the DoD (missing key refused, bad section refused, non-empty ref renders in the GENERATED table, empty ref renders cleanly, `--check` catches the drift); `node scripts/tests/build-detection-registry.test.mjs` = 10/10 pass. **Card `item-34-findings-spec-ref.md` still reads NOT STARTED - card is stale, same class as item 31** |
| 35 | Admin queue open count in nightly report | **BUILT** | #817 (`feat(ops): item 35 — admin queue open count in the nightly report`) merged to `main` as `fb02ec1d` 2026-09-19T18:13:30Z. `scripts/ops/admin-queue-size.mjs` (`adminQueueSize`, `formatAdminQueueBlock`) is imported and called at `scripts/audit-detection-floor.mjs:82,1937-1938` (`console.log('\n' + formatAdminQueueBlock(queue))`), and `audit-detection-floor.mjs --gate` is invoked by the nightly cron `scripts/vps-data-audit-cron.sh:149` (step 3/5) - built AND wired, not merely present. **Caveat, not a build gap:** the printed block reports raw `conflicts`/`absences` counts per IPO, not the actionable-vs-abstention split (measured 2026-09-19: of 14,140 open `data_conflicts` rows, 13,850 are one-sided abstentions, 133 are identical values, 157 are true value-vs-value disagreements) - #818's 28,120 headline number is still unsplit where it is read. Tracked as a residual, not re-opening the item: **#818** |

**Totals: 17 built, 12 partial, 0 not built, of 29 items.** (2026-09-21: item 17 NOT BUILT -> BUILT; items 11 and 9 NOT BUILT -> PARTIAL, both because an
owner decision REMOVED work rather than because work was done — OD-67 withdrew the crore
conversion, OD-65 withdrew the re-read loop. Nothing now reads NOT BUILT. Derived per ROW by reading each row's verdict cell and
matching its leading `**BUILT` / `**PARTIAL` / `**NOT BUILT`, never by counting word occurrences
across the document: a naive `grep -c BUILT` matches inside NOT BUILT, and a row's own prose
contains all three words. An earlier session summary said 8/11/5 - that was wrong.)

### Not on the numbered list, and blocking

| item | verdict | why it matters |
|---|---|---|
| **OD-62 reason codes** | **NOT BUILT** | `field_extraction_failures` holds **zero rows** while 12,701 plan rows hold no value. Prerequisite of OD-63/S9 (#787 says so explicitly) |
| **OD-63 / S9 admin data-quality queue** | **NOT BUILT** | Blocked on OD-62. 881-line UI page. Note: no human has ever resolved a conflict - 31,706 resolutions, all `resolved_by = SYSTEM` |

---

## Dependency order

**Spec-stated** (the spec's own Depends-on column): 1<-none, 2<-none, 3<-2, 4<-2, 5<-1,2,3, 6<-5,15,
7<-(scheduler none; tiering 6), 8<-none, 9<-6, 10<-6,9, 11<-10, 12<-none, 13<-2, 14<-13, 15<-none,
16<-none, 17<-6,7,10, 18<-none, 19<-1, 20<-2, 21<-1,6, 22<-none.

**Remaining work, in order:**

1. **Unblocked now, can run in parallel:** item 22, item 18 (page-text writer),
   item 14 (#728), item 19 (second routing, #807), item 3's S6 churn-stop (#759) plus
   the Swap Test and the S4/S5 proofs, item 35's actionable-split residual (#818).
   Item 8 needs no build: RATIOS documents are excluded from extraction by design (#716).
2. **Then** item 6 completion - #705 and the #762 re-queue path. *Inferred, not spec-stated:* item 9
   cannot run over parked rows.
3. **Then** item 9 - the re-read loop, section 2.5.1 triggers 3-7. Spec-stated dep on 6.
4. **Then** item 10 - the 21 unbuilt checks. Spec-stated dep on 6 and 9.
5. **Then** item 17 - closed-IPO job. Spec-stated dep on 6, 7, 10.
6. **Then** item 11 - crore conversion. Spec-stated dep on 10; its own release.
7. *Inferred:* OD-62 reason codes must precede OD-63/S9.

Item 21's staleness threshold needs an **owner decision**, not a build.

---

## Size and tier

- **Small / Tier C:** items 32-residual, 33 CI wiring, item 35's actionable-split residual (#818), the
  S4/S5 proof runs, the Swap Test.
- **Medium / Tier B:** item 10's 21 checks, item 18 page-text writer, the #762 re-queue
  path, item 21's remaining halves.
- **Medium-large / Tier A:** item 22, item 9, item 17, item 19's second routing, S6 churn-stop, item 14's
  real fix, OD-62 reason codes.
- **Large / Tier A, own release:** item 11 (crore conversion), OD-63/S9 (admin queue).

---

## Source disagreements found while measuring

Recorded because each is a finding, not noise:

1. Item 31's card says `NOT STARTED`; the board says it landed as #813. **Card stale.**
2. Item 6: four fetchers are wired in code, but #705 is open saying the walk has none. *Unverified which
   is stale* - the file's own comment says the registry used to return an EMPTY registry.
3. 24 of 42 cards carry an `unknown` Status, accepted by the gate **by design**.
4. Item 19: the spec reads it as open; #807 says it is done and the spec text is stale - while a second
   raw-SQL tool survives.
5. The stage-3 ledger's S2 row records `S2-4 FAIL: n=10` inside a row marked `landed`.
6. Item 14: the card says it is mostly already built and the spec sizes it Tier C, while #728 shows the
   conversion is 41-76% wrong on every live IPO.
7. `check-dod.mjs` D14 asserts O-14/O-15 are open forks; they were answered 2026-09-09 (#829).
8. Items 32 and 33 read `NOT STARTED` while their primary artefacts exist on main. **Cards understate.**
9. **Item 35 read `NOT BUILT` while #817 merged and wired it into the nightly cron the day before
   (2026-09-19). Same stale-verdict class as items 31-34: this table lagged a merge by one day.**

**Unverified:** the section 9 S1-S9 slice table referenced in some session notes is not in
`data-sourcing-pull-model.md` on `origin/main` (zero `S9` hits). The document holding it was not located,
so OD-56's stage-change re-open and the S5/S6 mapping in those notes are unconfirmed against a source.
