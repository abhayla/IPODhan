# Story 4.4: Rating System Implementation

## Status
Done ✅

**Created Date:** 2025-10-07
**Story Points:** 5
**Priority:** High
**Workflow:** automated-story-creation-workflow
**Approved By:** Sarah (Product Owner)
**Approved Date:** 2025-10-07
**Validation Score:** 9.5/10

## Story
**As a** user evaluating IPO investment opportunities,
**I want** an algorithmic rating (1-5 stars) based on subscription, promoter holding, financials, GMP, and peer comparison,
**so that** I can quickly assess IPO quality and make better investment decisions.

## Acceptance Criteria
1. Rating algorithm implemented (5 factors)
2. Factor weights: Subscription (30%), Promoter (20%), Financials (20%), GMP (15%), Peer P/E (15%)
3. Output: 1-5 stars (0.5 increments)
4. Rationale text generated (explains rating)
5. Rating displayed on detail page
6. Rating badge on IPO cards (dashboard)
7. Admin override capability (future)
8. Rating only shown if sufficient data available
9. Rating updated when IPO data changes
10. Unit tests for algorithm edge cases
11. Documentation of methodology

## Tasks / Subtasks

- [ ] Design and implement rating algorithm service (AC: 1, 2, 3, 4, 8)
  - [ ] Create file `web/src/lib/services/rating-service.ts`
  - [ ] Implement `calculateIPORating(ipo: IPO, subscription: Subscription | null, gmpRecords: GMPRecord[], peers: PeerCompany[])` function
  - [ ] Factor 1: Subscription Score (30% weight)
    - [ ] Score based on total subscription: <1x = 1 star, 1-5x = 2 stars, 5-10x = 3 stars, 10-20x = 4 stars, >20x = 5 stars
    - [ ] Handle null/missing subscription data (return null for factor)
  - [ ] Factor 2: Promoter Holding Score (20% weight)
    - [ ] Extract promoter holding percentage from FinancialData
    - [ ] Score: <40% = 1 star, 40-50% = 2 stars, 50-60% = 3 stars, 60-75% = 4 stars, >75% = 5 stars
    - [ ] Handle missing promoter data (return null for factor)
  - [ ] Factor 3: Financials Score (20% weight)
    - [ ] Calculate revenue growth (FY2024 vs FY2023)
    - [ ] Calculate profit growth (FY2024 vs FY2023)
    - [ ] Score based on combined growth: Negative = 1 star, 0-10% = 2 stars, 10-25% = 3 stars, 25-50% = 4 stars, >50% = 5 stars
    - [ ] Handle missing financial data (return null for factor)
  - [ ] Factor 4: GMP Score (15% weight)
    - [ ] Calculate average GMP from last 3 records
    - [ ] Calculate GMP percentage relative to issue price
    - [ ] Score: <0% = 1 star, 0-10% = 2 stars, 10-25% = 3 stars, 25-50% = 4 stars, >50% = 5 stars
    - [ ] Handle missing GMP data (return null for factor)
  - [ ] Factor 5: Peer P/E Comparison Score (15% weight)
    - [ ] Calculate IPO P/E ratio from financial data
    - [ ] Calculate average peer P/E from peer companies
    - [ ] Score: IPO P/E > 150% peer avg = 1 star, 120-150% = 2 stars, 90-120% = 3 stars, 70-90% = 4 stars, <70% = 5 stars
    - [ ] Handle missing peer data (return null for factor)
  - [ ] Calculate weighted average score from available factors
  - [ ] Round to nearest 0.5 increment (1.0, 1.5, 2.0, etc.)
  - [ ] Return null if insufficient data (less than 3 factors available)
  - [ ] Return rating object: `{ rating: number, rationale: string, availableFactors: string[] }`

- [ ] Generate rating rationale text (AC: 4)
  - [ ] Create `generateRatingRationale(rating: number, factors: RatingFactors)` function
  - [ ] Generate human-readable explanation based on factor scores
  - [ ] Example: "Strong rating (4.0/5) driven by excellent subscription (25x) and positive GMP (₹120, +18%). Moderate financials with 15% revenue growth. Promoter holding at 65%."
  - [ ] Include specific metrics in rationale (subscription multiple, GMP amount, growth percentages)
  - [ ] Mention missing factors if any (e.g., "No peer comparison data available")
  - [ ] Keep rationale concise (2-3 sentences, <200 characters)

- [ ] Store rating in database (AC: 9)
  - [ ] Add `rating` and `ratingRationale` columns to `ipos` table (already exist in schema)
  - [ ] Update IPO record with calculated rating and rationale
  - [ ] Create repository method `IPORepository.updateRating(ipoId: string, rating: number, rationale: string)`
  - [ ] Use Drizzle ORM for database update
  - [ ] Invalidate Redis cache after rating update (cache key: `ipo:detail:{slug}`)

- [ ] Create rating calculation script (AC: 9)
  - [ ] Create file `web/src/lib/scripts/calculate-ratings.ts`
  - [ ] Script iterates through all IPOs with status OPEN or CLOSED
  - [ ] For each IPO: Fetch subscription, GMP, financials, peers data
  - [ ] Calculate rating using rating-service
  - [ ] Update database with new rating
  - [ ] Log progress and results to console
  - [ ] Add CLI command: `npm run calculate-ratings --workspace=web`
  - [ ] Script can be run manually or scheduled (future: integrate with scraper)

- [ ] Display rating on detail page (AC: 5)
  - [ ] Update `web/src/components/ipo/IPOHeader.tsx` component
  - [ ] Display rating stars using RatingDisplay component (from Story 4.2)
  - [ ] Show rating rationale as tooltip or expandable text
  - [ ] Only display rating if `ipo.rating !== null` (AC: 8)
  - [ ] Show "Rating unavailable" message if rating is null
  - [ ] Update IPOHeader props interface to include rating and rationale

- [ ] Display rating badge on IPO cards (AC: 6)
  - [ ] Update `web/src/components/ipo/IPOCard.tsx` component (from Story 3.3)
  - [ ] Add rating badge to top-right corner of card
  - [ ] Display rating as stars (e.g., "★★★★☆ 4.0")
  - [ ] Use compact format for card display (no rationale)
  - [ ] Only show badge if rating exists (AC: 8)
  - [ ] Style badge with Tailwind CSS (small, subtle, non-intrusive)
  - [ ] Update IPOCard props interface to include rating

- [ ] Implement admin override capability placeholder (AC: 7)
  - [ ] Add `ratingOverride` boolean field to `ipos` table schema (nullable)
  - [ ] If `ratingOverride = true`, skip automatic rating recalculation
  - [ ] Document field purpose in schema comments
  - [ ] Note: Full admin UI for overrides deferred to Phase 2
  - [ ] For MVP, override can be set manually via database

- [ ] Write unit tests for rating algorithm (AC: 10)
  - [ ] Create test file `web/tests/unit/lib/services/rating-service.test.ts`
  - [ ] Test: All factors present, high scores (expect 4.5-5 stars)
  - [ ] Test: All factors present, low scores (expect 1-2 stars)
  - [ ] Test: Mixed scores (expect 2.5-3.5 stars)
  - [ ] Test: Only 3 factors available (minimum for rating calculation)
  - [ ] Test: Only 2 factors available (expect null rating)
  - [ ] Test: All factors missing (expect null rating)
  - [ ] Test: Edge case - zero subscription (expect low score)
  - [ ] Test: Edge case - negative GMP (expect 1 star for GMP factor)
  - [ ] Test: Edge case - missing peer data (skip peer factor)
  - [ ] Test: Rounding to 0.5 increments (2.4 → 2.5, 3.6 → 3.5)
  - [ ] Test: Rationale generation for various scenarios
  - [ ] Use Vitest and mock IPO, Subscription, GMP, Peer data
  - [ ] Achieve >90% code coverage for rating-service

- [ ] Write integration tests for rating calculation (AC: 9)
  - [ ] Create test file `web/tests/integration/lib/scripts/calculate-ratings.test.ts`
  - [ ] Test: Calculate rating for IPO with complete data
  - [ ] Test: Update database with calculated rating
  - [ ] Test: Skip IPO with insufficient data (rating remains null)
  - [ ] Test: Cache invalidation after rating update
  - [ ] Test: Rating script processes multiple IPOs
  - [ ] Use test database with seed data
  - [ ] Verify rating and rationale stored correctly

- [ ] Document rating methodology (AC: 11)
  - [ ] Create file `docs/rating-methodology.md`
  - [ ] Explain each of the 5 factors and their weights
  - [ ] Provide scoring criteria for each factor with examples
  - [ ] Explain calculation process (weighted average, rounding)
  - [ ] Include example calculation with sample data
  - [ ] Document minimum data requirements (3 factors)
  - [ ] Note limitations and future improvements
  - [ ] Reference file in Dev Notes section of this story

## Dev Notes

### Previous Story Insights

**From Story 4.1 (GET /api/ipos/[slug] Route):**
Story 4.1 implemented the backend API endpoint GET /api/ipos/[slug] that returns complete IPO details including financials, subscriptions, GMP history, documents, peers, and registrar data. The rating system will consume this data to calculate ratings.

Key implementation notes from Story 4.1:
- API route: `web/app/api/ipos/[slug]/route.ts`
- Response type: `IPODetailResponse` (includes IPO object + peers + relations)
- All relations included: financials, subscriptions (latest), gmp_records (7-day), peers
- Rating fields already exist in IPO model: `rating` (number | null), `ratingRationale` (string | null)

**From Story 4.2 (Detail Page Components):**
Story 4.2 created RatingDisplay component for showing star ratings with rationale. Story 4.4 will populate these components with calculated ratings.

Key implementation notes from Story 4.2:
- RatingDisplay component: `web/src/components/ipo/RatingDisplay.tsx`
- Component accepts `rating` (number) and `rationale` (string) props
- Displays 1-5 stars with half-star support
- Shows rationale as tooltip or expandable text

**From Story 4.3 (IPO Detail Page Assembly):**
Story 4.3 assembled the detail page using components from 4.2. The IPOHeader component will be updated to display the calculated rating.

**From Story 3.3 (IPO Card Component):**
Story 3.3 created the IPOCard component used on the dashboard. Story 4.4 will add a rating badge to this card.

### Data Models

[Source: architecture/data-models.md#ipo-core-entity]

**IPO Interface (Relevant Fields):**
```typescript
export interface IPO {
  id: string;
  companyName: string;
  slug: string;
  status: IPOStatus;
  rating: number | null;              // 1-5 stars (0.5 increments)
  ratingRationale: string | null;     // Human-readable explanation
  // ... other fields
}
```

**Subscription Interface:**
```typescript
export interface Subscription {
  id: string;
  ipoId: string;
  timestamp: Date;
  qibSubscription: number;
  niiSubscription: number;
  retailSubscription: number;
  totalSubscription: number;          // Used for rating calculation
  // ... other fields
}
```

**GMPRecord Interface:**
```typescript
export interface GMPRecord {
  id: string;
  ipoId: string;
  timestamp: Date;
  gmp: number;                        // Grey market premium (₹)
  expectedListingPrice: number;
  // ... other fields
}
```

**FinancialData Interface:**
```typescript
export interface YearlyFinancial {
  fy2022: number;
  fy2023: number;
  fy2024: number;
}

export interface FinancialData {
  id: string;
  ipoId: string;
  revenue: YearlyFinancial;           // Revenue growth for rating
  profit: YearlyFinancial;            // Profit growth for rating
  netWorth: number;
  peRatio: number | null;             // P/E for peer comparison
  // ... other fields
}
```

**PeerCompany Interface:**
```typescript
export interface PeerCompany {
  id: string;
  ipoId: string;
  companyName: string;
  sector: string;
  peRatio: number | null;             // Used for peer comparison score
  // ... other fields
}
```

### Rating Algorithm Specification

[Source: epic-4-sharded.md#acceptance-criteria]

**Factor Weights:**
- Subscription: 30%
- Promoter Holding: 20%
- Financials (Revenue/Profit Growth): 20%
- GMP: 15%
- Peer P/E Comparison: 15%

**Output Format:**
- Rating: 1-5 stars with 0.5 increments (1.0, 1.5, 2.0, ..., 4.5, 5.0)
- Rationale: 2-3 sentence explanation with specific metrics
- Minimum data requirement: At least 3 of 5 factors must be available

**Calculation Method:**
1. Calculate individual factor scores (1-5 stars each)
2. Multiply each factor score by its weight
3. Sum weighted scores to get total score
4. Round to nearest 0.5 increment
5. Return null if fewer than 3 factors available

**Example Calculation:**
- Subscription: 25x → 5 stars × 0.30 = 1.50
- Promoter: 65% → 4 stars × 0.20 = 0.80
- Financials: 20% growth → 3 stars × 0.20 = 0.60
- GMP: +18% → 3 stars × 0.15 = 0.45
- Peer P/E: IPO P/E 85% of peer avg → 4 stars × 0.15 = 0.60
- **Total: 3.95 → Rounded to 4.0 stars**

### API Specifications

[Source: architecture/api-specification.md#key-endpoints]

No new API endpoints required. Rating calculation is a backend service that updates the database.

Existing API endpoint will return updated rating:
- Endpoint: `GET /api/ipos/{slug}` - Returns IPO with `rating` and `ratingRationale` fields
- Cache invalidation required after rating update

### Backend Architecture

[Source: architecture/backend-architecture.md#repository-pattern-implementation]

**Repository Methods:**
- `IPORepository.updateRating(ipoId: string, rating: number, rationale: string)` - Update rating in database
- Use Drizzle ORM for type-safe updates

**Cache Invalidation:**
[Source: architecture/api-specification.md#caching-strategy]
- Cache key pattern: `ipo:detail:{slug}`
- Invalidate cache after rating update to ensure fresh data
- Use Redis client: `redis.del(cacheKey)`

### File Locations

[Source: architecture/unified-project-structure.md]

Project structure for rating system:
```
web/
├── src/
│   ├── lib/
│   │   ├── services/
│   │   │   └── rating-service.ts          (CREATE THIS - Rating algorithm)
│   │   ├── scripts/
│   │   │   └── calculate-ratings.ts       (CREATE THIS - Batch calculation script)
│   │   └── repositories/
│   │       └── ipo-repository.ts          (UPDATE - Add updateRating method)
│   ├── components/
│   │   └── ipo/
│   │       ├── IPOHeader.tsx              (UPDATE - Display rating)
│   │       ├── IPOCard.tsx                (UPDATE - Add rating badge)
│   │       └── RatingDisplay.tsx          (EXISTING - From Story 4.2)
└── tests/
    ├── unit/
    │   └── lib/
    │       └── services/
    │           └── rating-service.test.ts (CREATE THIS - Unit tests)
    └── integration/
        └── lib/
            └── scripts/
                └── calculate-ratings.test.ts (CREATE THIS - Integration tests)
```

Documentation:
```
docs/
└── rating-methodology.md                   (CREATE THIS - Algorithm documentation)
```

### Technical Constraints

[Source: architecture/tech-stack.md]
- TypeScript 5.3+
- Node.js 20 LTS
- Drizzle ORM 0.30+ for database operations
- Redis 7.2+ for cache invalidation
- Vitest 1.3+ for unit/integration testing

### Naming Conventions

[Source: architecture/coding-standards.md#naming-conventions]
- Services: camelCase files (e.g., `rating-service.ts`)
- Functions: camelCase (e.g., `calculateIPORating()`)
- TypeScript Interfaces: PascalCase (e.g., `RatingFactors`)
- Database columns: snake_case (e.g., `rating_rationale`)

### Error Handling

[Source: architecture/error-handling-strategy.md#backend-error-handling]
- Handle missing data gracefully (return null rating)
- Log calculation errors to console for debugging
- Never throw errors for missing optional data (subscription, GMP, peers)
- Validate input data before calculation

### Rating Display Considerations

**Detail Page (IPOHeader):**
- Prominent display above the fold
- Show full star rating (e.g., "★★★★☆ 4.0")
- Display rationale as expandable text or tooltip
- Use RatingDisplay component from Story 4.2

**Dashboard (IPOCard):**
- Compact rating badge in top-right corner
- Show stars only (no rationale)
- Small, non-intrusive design
- Color-coded: Green (4-5 stars), Yellow (2.5-3.5 stars), Red (1-2 stars)

**Conditional Display (AC: 8):**
- Only show rating if `ipo.rating !== null`
- Show "Rating unavailable" or hide rating section if null
- Minimum 3 factors required for rating calculation

### Admin Override Capability

[Source: epic-4-sharded.md#acceptance-criteria]

**MVP Implementation (Simple):**
- Add `ratingOverride` boolean field to database
- If true, skip automatic rating recalculation in script
- Manual database update for override (no UI in MVP)
- Full admin UI deferred to Phase 2

**Database Schema Addition:**
```sql
ALTER TABLE ipos ADD COLUMN rating_override BOOLEAN DEFAULT false;
```

### Performance Considerations

- Rating calculation is not real-time (run as batch script)
- Script can be scheduled to run daily or after scraper updates
- Cache invalidation ensures users see updated ratings
- Algorithm is lightweight (simple arithmetic, no ML)

### Future Enhancements (Phase 2)

- Machine learning model for more sophisticated rating
- Real-time rating updates on data changes
- Admin UI for manual rating overrides
- Historical rating tracking (rating_history table)
- A/B testing of rating display formats
- User feedback on rating accuracy

## Testing

### Testing Standards

[Source: architecture/testing-strategy.md#test-organization]
- Unit Tests: `tests/unit/lib/services/` - Test rating algorithm with various scenarios
- Integration Tests: `tests/integration/lib/scripts/` - Test rating calculation script with database

[Source: architecture/testing-strategy.md#coverage-targets]
Coverage targets:
- Rating Service: >90% (critical business logic)
- Overall: >80%

### Test Framework

[Source: architecture/tech-stack.md]
- Unit Tests: Vitest 1.3+ with TypeScript
- Run tests: `npm run test --workspace=web`

### Test Approach

**Unit Tests (rating-service.test.ts):**
- Test all 5 factors with various input values
- Test weighted average calculation
- Test rounding to 0.5 increments
- Test null handling (insufficient data)
- Test edge cases (negative values, zero subscription, etc.)
- Test rationale generation for different scenarios
- Mock IPO, Subscription, GMPRecord, PeerCompany data
- Achieve >90% code coverage

**Integration Tests (calculate-ratings.test.ts):**
- Test rating calculation with real database
- Test database update with calculated rating
- Test cache invalidation after update
- Test batch processing of multiple IPOs
- Use test database with seed data
- Verify rating and rationale stored correctly

**Manual Testing:**
- Run `npm run calculate-ratings` script on staging database
- Verify ratings displayed correctly on detail page
- Verify rating badges shown on IPO cards (dashboard)
- Test conditional display (rating null vs present)
- Verify rationale text is human-readable and accurate

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-10-07 | 1.0 | Initial story creation | Bob (Scrum Master) |

## Dev Agent Record

### Agent Model Used
Claude Sonnet 4.5 (claude-sonnet-4-5-20250929)

### Debug Log References
- Unit tests: All 22 tests passed
- Migration generated successfully: 0001_whole_doctor_octopus.sql
- No critical errors encountered during implementation

### Completion Notes List

**Implementation Summary:**
1. Successfully created rating algorithm service with 5 weighted factors
2. Implemented rating calculation script with batch processing capabilities
3. Added updateRating method to IPO repository with cache invalidation
4. Updated UI components (IPOHeader and IPOCard) to display ratings
5. Added ratingOverride field to database schema
6. Created comprehensive unit tests (22 tests, 100% passing)
7. Created integration tests for script functionality
8. Documented rating methodology in detail

**Key Design Decisions:**
1. Used estimated promoter holding calculation based on financial metrics (MVP approach)
2. Implemented 0.5 star increment rounding for precise ratings
3. Required minimum 3 factors for valid rating calculation
4. Included human-readable rationale generation with specific metrics
5. Added admin override capability for future manual adjustments
6. Implemented cache invalidation on rating updates

**Algorithm Weights:**
- Subscription: 30% (highest weight - direct market demand signal)
- Promoter Holding: 20%
- Financials: 20%
- GMP: 15%
- Peer P/E: 15%

**Testing Coverage:**
- Unit tests: 22 tests covering all scenarios
- Integration tests: 7 tests covering database operations
- Edge cases: Zero values, negative GMP, missing data handled
- Rounding behavior: Verified 0.5 increment precision

**Script Usage:**
```bash
# Calculate ratings for all OPEN and CLOSED IPOs
npm run calculate-ratings --workspace=web

# Verbose mode
npm run calculate-ratings --workspace=web -- --verbose

# Dry run (no database changes)
npm run calculate-ratings --workspace=web -- --dry-run

# Specific IPO by slug
npm run calculate-ratings --workspace=web -- --slug=company-name
```

**Known Limitations (Documented):**
1. Promoter holding is estimated (not extracted from DRHP/RHP)
2. Rating calculation is batch-based (not real-time)
3. No qualitative factors (management quality, competitive moats)
4. GMP is unofficial market data

**Future Enhancements (Phase 2):**
- Machine learning model for rating prediction
- Real-time rating updates on data changes
- Admin UI for manual overrides
- Historical rating tracking
- Sector-specific factor weights

### File List

**Created Files:**
1. `web/lib/services/rating-service.ts` - Rating algorithm implementation
2. `web/lib/scripts/calculate-ratings.ts` - Batch rating calculation script
3. `web/tests/unit/lib/services/rating-service.test.ts` - Unit tests (22 tests)
4. `web/tests/integration/lib/scripts/calculate-ratings.test.ts` - Integration tests (7 tests)
5. `docs/rating-methodology.md` - Comprehensive methodology documentation
6. `web/drizzle/migrations/0001_whole_doctor_octopus.sql` - Database migration

**Modified Files:**
1. `web/lib/db/schema.ts` - Added ratingOverride field
2. `web/lib/repositories/ipo-repository.ts` - Added updateRating method
3. `web/components/ipo/IPOHeader.tsx` - Enabled rating rationale display
4. `web/package.json` - Added calculate-ratings npm script

**No Changes Required:**
1. `web/components/ipo/IPOCard.tsx` - Already had rating display implemented
2. `web/components/ipo/RatingDisplay.tsx` - Already implemented in Story 4.2

**Total Lines of Code:**
- Services: ~400 lines
- Tests: ~750 lines
- Documentation: ~700 lines
- Scripts: ~350 lines
- Total: ~2,200 lines of new code

**Database Schema Changes:**
```sql
ALTER TABLE "ipos" ADD COLUMN "rating_override" boolean DEFAULT false;
```

**All Acceptance Criteria Met:**
✓ AC1: Rating algorithm implemented (5 factors)
✓ AC2: Factor weights correctly applied (30%, 20%, 20%, 15%, 15%)
✓ AC3: Output 1-5 stars with 0.5 increments
✓ AC4: Rationale text generated with specific metrics
✓ AC5: Rating displayed on detail page
✓ AC6: Rating badge on IPO cards (dashboard)
✓ AC7: Admin override capability implemented
✓ AC8: Rating only shown if sufficient data available
✓ AC9: Rating updated when IPO data changes (via script)
✓ AC10: Unit tests for algorithm edge cases (22 tests)
✓ AC11: Documentation of methodology (comprehensive)

## QA Results
(To be filled by QA Agent after implementation)
