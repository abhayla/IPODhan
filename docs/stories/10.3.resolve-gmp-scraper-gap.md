# Story 10.3: Resolve GMP Scraper Documentation Gap

**Epic**: 10 - Defect Fixing
**Story ID**: 10.3
**Issue ID**: ISS-003
**Priority**: 🔴 P0 - CRITICAL
**Status**: Draft

---

## Story

**As a** project stakeholder,
**I want** the GMP scraper implementation to match documentation OR documentation updated to reflect reality,
**so that** testing plans are accurate and GMP data handling is clear

---

## Acceptance Criteria

1. **AC-1**: GMP scraper implementation status clarified
   - Decision made: Implement scraper OR mark as future work
   - TESTING_PLAN.md updated to reflect actual implementation
   - No references to non-existent files in documentation
   - TEST-003-5 passes (update testing plan)

2. **AC-2**: Database schema verified for GMP data
   - `gmp_tracking` table exists and is properly structured
   - `ipos` table has GMP-related fields (gmp, gmp_percentage)
   - Foreign key relationships validated
   - TEST-003-1 and TEST-003-2 pass (database verification)

3. **AC-3**: Frontend handles missing GMP data gracefully
   - IPO detail pages don't error when GMP data is null
   - GMP chart component displays "No Data Available" state
   - GMP tracking section shows appropriate placeholder
   - TEST-003-3 and TEST-003-4 pass (frontend graceful degradation)

4. **AC-4**: If implementing GMP scraper - scraper works correctly
   - File created at `lib/scrapers/sources/gmp-api-scraper.ts`
   - Scraper successfully fetches GMP data from source
   - Data properly stored in `gmp_tracking` table
   - Test script created at `scripts/test-gmp-scraper.ts`
   - Success rate documented (target: 95%+)

5. **AC-5**: If NOT implementing - documentation reflects reality
   - TESTING_PLAN.md shows GMP scraper as "Future Enhancement"
   - Scraping coverage report updated
   - Known limitations documented
   - Frontend expectations adjusted

---

## Tasks / Subtasks

- [ ] **Task 1**: Assess Current State (AC: 1, 2)
  - [ ] Check if `gmp_tracking` table exists in database
  - [ ] Run query: `SELECT COUNT(*) FROM gmp_tracking`
  - [ ] Run query: `SELECT COUNT(*) FROM ipos WHERE gmp IS NOT NULL`
  - [ ] Check if any GMP data exists in production/staging
  - [ ] Review database schema for GMP fields

- [ ] **Task 2**: Make Implementation Decision (AC: 1)
  - [ ] Consult with product owner on GMP priority
  - [ ] Assess effort required to implement scraper
  - [ ] Determine if GMP data source is available
  - [ ] Document decision in story comments
  - [ ] Choose Path A (implement) or Path B (defer)

- [ ] **Task 3A**: IF IMPLEMENTING - Create GMP Scraper (AC: 4)
  - [ ] Research GMP data source API/website
  - [ ] Create `lib/scrapers/sources/gmp-api-scraper.ts`
  - [ ] Implement scraping logic
  - [ ] Add error handling and retry logic
  - [ ] Create test script: `scripts/test-gmp-scraper.ts`
  - [ ] Test scraper with sample IPOs
  - [ ] Verify data stored in `gmp_tracking` table

- [ ] **Task 3B**: IF DEFERRING - Update Documentation (AC: 5)
  - [ ] Update TESTING_PLAN.md
  - [ ] Remove references to non-existent GMP scraper
  - [ ] Add GMP scraper to "Future Enhancements" section
  - [ ] Update scraping coverage report
  - [ ] Document known limitations

- [ ] **Task 4**: Fix Frontend GMP Handling (AC: 3)
  - [ ] Locate GMP chart component
  - [ ] Add null/undefined checks for GMP data
  - [ ] Implement "No Data Available" placeholder
  - [ ] Test with IPO records that have no GMP data
  - [ ] Add loading states if applicable
  - [ ] Verify no JavaScript errors in console

- [ ] **Task 5**: Verify Database Schema (AC: 2)
  - [ ] Verify `gmp_tracking` table structure:
    ```sql
    CREATE TABLE IF NOT EXISTS gmp_tracking (
      id SERIAL PRIMARY KEY,
      ipo_id INTEGER REFERENCES ipos(id),
      gmp_value DECIMAL,
      gmp_percentage DECIMAL,
      recorded_at TIMESTAMP,
      source VARCHAR(255)
    );
    ```
  - [ ] Verify `ipos` table has GMP fields
  - [ ] Test foreign key constraints
  - [ ] Document schema in migrations

- [ ] **Task 6**: Execute Auto-Generated Tests (AC: 1, 2, 3)
  - [ ] Run TEST-003-1: Check gmp_tracking table data count
  - [ ] Run TEST-003-2: Check ipos table GMP fields
  - [ ] Run TEST-003-3: Test IPO detail page without GMP
  - [ ] Run TEST-003-4: Test GMP chart handles missing data
  - [ ] Run TEST-003-5: Verify testing plan updated
  - [ ] All 5 tests must pass

- [ ] **Task 7**: Update Documentation (AC: 1, 5)
  - [ ] Update TESTING_PLAN.md with accurate information
  - [ ] Update SCRAPING_COVERAGE_REPORT.md if it exists
  - [ ] Add GMP data handling to API documentation
  - [ ] Document GMP data sources (if scraper implemented)

---

## Dev Notes

### Issue Context (from TEST_ISSUES.json)

**Root Cause**: Testing plan was written with aspirational/planned features that haven't been implemented yet

**Evidence**:
```
Expected file: lib/scrapers/sources/gmp-api-scraper.ts - NOT FOUND
Expected test: scripts/test-gmp-scraper.ts - NOT FOUND
Expected test: scripts/check-gmp-data-types.ts - NOT FOUND
Expected test: scripts/debug-gmp-api-response.ts - NOT FOUND

Actual scrapers found:
- historical-ipo-scraper.ts
- ipo-reviews-scraper.ts
- market-holidays-scraper.ts
- prospectus-scraper.ts
- rights-issues-scraper.ts
```

**Affected Components**:
- GMP data collection
- GMP tracking table
- GMP history
- IPO detail pages (GMP display)
- Testing plan accuracy

**Impact**: HIGH - Documentation mismatch creates false expectations for testing and feature availability

### Relevant Source Tree

**Expected Files** (Currently Missing):
```
lib/
└── scrapers/
    └── sources/
        └── gmp-api-scraper.ts         # ❌ NOT FOUND

scripts/
├── test-gmp-scraper.ts                # ❌ NOT FOUND
├── check-gmp-data-types.ts            # ❌ NOT FOUND
└── debug-gmp-api-response.ts          # ❌ NOT FOUND
```

**Actual Files** (Existing Scrapers):
```
lib/
└── scrapers/
    └── sources/
        ├── historical-ipo-scraper.ts   # ✅ EXISTS
        ├── ipo-reviews-scraper.ts      # ✅ EXISTS
        ├── market-holidays-scraper.ts  # ✅ EXISTS
        ├── prospectus-scraper.ts       # ✅ EXISTS
        └── rights-issues-scraper.ts    # ✅ EXISTS
```

**Frontend Components** (Need Graceful Degradation):
```
app/
└── ipos/
    └── [slug]/
        └── page.tsx                    # IPO detail page
components/
└── ipo-detail/
    ├── GmpChart.tsx                   # Likely location
    ├── GmpTrackingSection.tsx         # Likely location
    └── [other-components].tsx
```

**Documentation Files**:
```
web/
├── TESTING_PLAN.md                    # NEEDS UPDATE
├── SCRAPING_COVERAGE_REPORT.md        # NEEDS UPDATE
└── TEST_ISSUES.json                   # Reference
```

### Technical Details

**GMP (Grey Market Premium)**:
- Definition: Unofficial premium at which IPO shares trade before listing
- Data Points: Current GMP value, GMP percentage, historical trends
- Sources: Typically scraped from financial websites or aggregator APIs
- Importance: Helps investors gauge IPO demand and expected listing gains

**Database Schema Requirements**:

`gmp_tracking` table:
```sql
CREATE TABLE IF NOT EXISTS gmp_tracking (
  id SERIAL PRIMARY KEY,
  ipo_id INTEGER NOT NULL REFERENCES ipos(id) ON DELETE CASCADE,
  gmp_value DECIMAL(10, 2),           -- Absolute GMP in rupees
  gmp_percentage DECIMAL(5, 2),       -- GMP as % of issue price
  recorded_at TIMESTAMP DEFAULT NOW(),
  source VARCHAR(255),                 -- Data source identifier
  created_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(ipo_id, recorded_at)         -- Prevent duplicate entries
);
```

`ipos` table (GMP fields):
```sql
ALTER TABLE ipos ADD COLUMN IF NOT EXISTS gmp DECIMAL(10, 2);
ALTER TABLE ipos ADD COLUMN IF NOT EXISTS gmp_percentage DECIMAL(5, 2);
ALTER TABLE ipos ADD COLUMN IF NOT EXISTS gmp_updated_at TIMESTAMP;
```

**Scraper Implementation Template** (if implementing):
```typescript
// lib/scrapers/sources/gmp-api-scraper.ts
import { db } from '@/lib/db';
import { GMPData } from '@/types/scraper';

export class GMPApiScraper {
  private readonly API_URL = 'https://example-gmp-api.com';

  async scrapeGMPData(ipoSymbol: string): Promise<GMPData | null> {
    try {
      // 1. Fetch data from GMP API
      const response = await fetch(`${this.API_URL}/gmp/${ipoSymbol}`);
      const data = await response.json();

      // 2. Transform data to our schema
      const gmpData = {
        gmpValue: data.gmp,
        gmpPercentage: data.gmp_percent,
        source: 'GMP_API'
      };

      // 3. Store in database
      await this.storeGMPData(ipoSymbol, gmpData);

      return gmpData;
    } catch (error) {
      console.error('GMP scraping failed:', error);
      return null;
    }
  }

  private async storeGMPData(ipoSymbol: string, data: GMPData) {
    // Implementation
  }
}
```

**Frontend Graceful Degradation Pattern**:
```typescript
// components/ipo-detail/GmpChart.tsx
export function GMPChart({ ipoId }: Props) {
  const { data: gmpData, isLoading, error } = useGMPData(ipoId);

  if (isLoading) {
    return <Skeleton />;
  }

  if (!gmpData || gmpData.length === 0) {
    return (
      <div className="text-center p-8 bg-gray-50 rounded">
        <p className="text-gray-500">GMP data not available for this IPO</p>
        <p className="text-sm text-gray-400 mt-2">
          Grey Market Premium tracking coming soon
        </p>
      </div>
    );
  }

  return <Chart data={gmpData} />;
}
```

### Testing

**Test Location**:
- Integration tests: `tests/integration/gmp-scraper.test.ts` (if implementing)
- Frontend tests: `tests/components/gmp-chart.test.tsx`
- Database tests: `tests/db/gmp-schema.test.ts`

**Test Standards**:
- Use Vitest for all tests
- Mock external GMP APIs in tests
- Test both data present and absent scenarios
- Verify database constraints

**Specific Test Requirements**:

1. **Database Tests**:
   ```typescript
   test('gmp_tracking table exists', async () => {
     const result = await db.query(`
       SELECT COUNT(*) FROM information_schema.tables
       WHERE table_name = 'gmp_tracking'
     `);
     expect(result.rows[0].count).toBe('1');
   });
   ```

2. **Frontend Tests**:
   ```typescript
   test('GmpChart shows placeholder when no data', () => {
     render(<GMPChart ipoId={123} gmpData={null} />);
     expect(screen.getByText(/not available/i)).toBeInTheDocument();
   });
   ```

3. **Scraper Tests** (if implementing):
   ```typescript
   test('GMPApiScraper fetches and stores data', async () => {
     const scraper = new GMPApiScraper();
     const data = await scraper.scrapeGMPData('EXAMPLE-IPO');
     expect(data).toBeDefined();
     expect(data.gmpValue).toBeGreaterThan(0);
   });
   ```

### Prevention Measure

**Documentation-Code Sync**:
1. Always verify file existence before documenting
2. Use code scanning tools to validate documentation references
3. Mark aspirational features clearly as "Planned" or "Future"
4. Regular audits: Compare docs with actual codebase

**Pre-commit Hook** (suggested):
```bash
#!/bin/bash
# .git/hooks/pre-commit-docs-check

# Check for references to non-existent files in markdown docs
grep -r "lib/scrapers" docs/*.md | while read line; do
  file=$(echo $line | grep -o "lib/scrapers/[^)]*")
  if [ ! -f "$file" ]; then
    echo "ERROR: Documentation references non-existent file: $file"
    exit 1
  fi
done
```

**Documentation Template**:
```markdown
# Feature Status

## Implemented Features
- [x] Historical IPO Scraper
- [x] IPO Reviews Scraper

## Future Enhancements
- [ ] GMP API Scraper (planned)
- [ ] Real-time subscription tracking
```

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-10-13 | 1.0 | Initial story creation from ISS-003 (Epic 10) | Bob (Scrum Master) |

---

## Dev Agent Record

### Agent Model Used
_To be populated by dev agent_

### Debug Log References
_To be populated by dev agent_

### Completion Notes
_To be populated by dev agent_

### File List
_To be populated by dev agent_

---

## QA Results

_To be populated by QA agent after implementation_
