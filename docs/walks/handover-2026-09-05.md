# Handover: IPODhan session 4 (resume from 2026-09-04 17:45 IST, session 3 ended at 88% context)

Read first, in this order: (1) `C:\Users\itsab\.claude\projects\D--Abhay-Ventures-IPODhan\memory\deepa-walk-state-2026-09-02.md` (last entry = current position); (2) the last 25 lines of `docs/walks/2026-09-02-deepa-pipeline-walk.md`; (3) `docs/walks/2026-09-02-deepa-walk-status.md` tail; (4) the four owner rules added 2026-09-04 in `~/.claude/CLAUDE.md`: "One deployment window per day" (Rules 1-8), "Worktree lifecycle", "Context budget". Tunnel keeper: `powershell -File scripts/dev-db-tunnel.ps1 -Status` (start with `-Detach` if down).

## State
- Production: 8cd287d5 (five deploys on 2026-09-04; NO deploy today by default, D-16). Flags on: DOCUMENT_STATE_MACHINE, STAGE_RECONCILER, FILING_AUTO_PERSIST, DUE_STEP_SCHEDULER. SME auto-persist gate D-15 still closed (`ENABLE_SME_FILING_AUTO_PERSIST` exists on the release branch, default off).
- Integration branch `release/2026-09-05` in worktree `D:\Abhay\Ventures\IPODhan-rel0905` (2d40e5b2) = main + D-15 flag + W-127 + W-133 + W-124 + W-121 + W-111/112. Full local suites were green on 07d15e82 (before W-111): shared 242, scraper 2,695, web 2,420, pytest 76, tsc 90, lint ok. Not pushed.
- Round-2 workers may have committed locally in `IPODhan-w124r2` (branch fix/w124-round2: complete LISTED rows leave candidates, enrichment bounded to listedCap x 4) and `IPODhan-w133r2` (fix/w133-round2: PARTIAL surfaces in the ledger, agreement check None when not evaluated, stale log shape). Check `git -C <worktree> log --oneline -2`; if committed: reproduce their gates, merge into release/2026-09-05, remove the worktrees with `~/.claude/tools/wt-rm.ps1 -Discard`.
- W-111/112 Tier A holes to fix BEFORE any deploy (D-16 Rule 5): pin transitive deps (onnxruntime, opencv-python, numpy) via a constraints file + an import smoke check in `setup_python_venv` before the flip; slot the venv (`shared/venv/$SLOT`); rm -rf a failed partial venv; drop PYTHON_BIN on the `prev` resume path or alert loudly; remove the inert --upgrade-strategy.
- D-18 first cut: deprecated agents deleted (8d706ddc); plugins disabled per project in `.claude/settings.local.json`; STILL TO DO: path-scope 15 of the 21 `# Scope: global` rules with `paths:` frontmatter (keep global: claude-behavior, decision-authority, security-baseline, git-collaboration, supervisor-verification, engineering-roles), Sonnet task, Tier C; trim both CLAUDE.md files under ~15 KB.
- Owner still to do by hand: disable the claude.ai connectors (Wati, Zoho x4, Gmail, Calendar, Drive, Figma, Excalidraw) for this project; approve the deletion of the stale 3.1 GB `/root/t239-gate-build` on the VPS (W-134) and the DEPLOY_KEEP_RELEASES=3/2 runner env change.

## Sequence for the next session (in order, no deploy without the Rule 6 brief)
1. Sweep worktrees (`wt-sweep.ps1 -Repo D:\Abhay\Ventures\IPODhan`), fold the two round-2 branches into release/2026-09-05, fix the W-111 holes (Sonnet, worktree off the release branch, Tier A re-review), then ONE full local pass on the release branch (FULL_SUITE_BUDGET override is justified once).
2. Push release/2026-09-05 once, open ONE PR, let the gate run once, merge to main so staging soaks for a full scraper day (read `pm2 logs ipodhan-scraper-staging` for the proof lines: Due-step cycle, document cycle summary with listedCap/listedDeferred/listedComplete, no ZERO-YIELD, the venv versions line in the deploy log).
3. Write the Rule 6 pre-deploy brief for the owner: done (with proofs), cost (deploys, review rounds, CI runs), pending (W-113, W-114, W-115, W-118, W-119, W-120, W-123, W-125, W-127b wording, W-81 anchor names, W-110, W-105-107, W-77, W-78, W-56, W-97, W-99), DEPLOY/DEFER recommendation, what to complete first. Recommend the evening window only if the staging day is clean.
4. After the deploy (owner-approved, evening window): verify (served sha, scraper alive, venv versions), then the D-15 flip is one env line `ENABLE_SME_FILING_AUTO_PERSIST=true` in prod scraper.env with the owner present; prove Qualiance (or the next SME) through the automatic path; check DEEPA lists 8 Sep (I5/I6).
5. Then the path-scoping task (D-18) and the remaining W rows, batched for a later window.

Rules in force: D-16 (one deploy/day, evening, complete + proven, brief first, no open issue rides along), D-17 (worktrees created via wt-new.ps1, removed same session via wt-rm.ps1, sweep at start/end), D-18 (lean briefs: no full suites in workers, logs not transcripts, ~300-word reports; fresh session per phase), D-12 (bus bookkeeping suspended; ledger only), D-15 (SME gate), Rule 5 routing (Sonnet implements, Opus reviews Tier A, Fable never edits task code), Budget line on every Agent brief.
