# Handover: IPODhan session 5 (resume on 2026-09-05 morning; session 4 ended late on 2026-09-04)

Read first, in this order: (1) `C:\Users\itsab\.claude\projects\D--Abhay-Ventures-IPODhan\memory\deepa-walk-state-2026-09-02.md` (last entry); (2) the last 30 lines of `docs/walks/2026-09-02-deepa-pipeline-walk.md`; (3) the owner rules in `~/.claude/CLAUDE.md`: "One deployment window per day" (Rules 1-9, Rule 9 = disk hygiene), "Worktree lifecycle", "Context budget". Tunnel keeper: `powershell -File scripts/dev-db-tunnel.ps1 -Status`.

## State
- Production: **8cd287d5** (no deploy on 2026-09-04 after 15:00; owner: deploy deferred to the 2026-09-05 evening window, 21:00-23:30 IST, after the Rule 6 brief and the owner's word).
- main = **48e29402** (PR #288 = W-136 on top of 902fa36b) (PR #286 = D-15 flagged SME door + W-111/112 venv + W-121/124/127/133; PR #287 = W-135 enrichment starvation + W-134 disk hygiene). Staging serves 48e29402 since 23:02 IST (902fa36b 21:28-23:02); staging env mirrors prod's four pipeline flags + PRIMARY_SOURCE_DISCOVERY + `ENABLE_SME_FILING_AUTO_PERSIST=true` (backup `scraper.env.bak-20260904-w111soak`); staging API-fallback cadence key stamped in Redis DB 1.
- Soak on staging: cycles 19:30-22:00 all "Scraper completed successfully", extractionFailed 0, no venv/PYTHON_BIN error, no ZERO-YIELD; BLOCKED_ALL lines = first-transition batches (19 on cycle 1, then 0-2). W-135 counters still `listedEnriched 8 / listedSkippedUnenriched 9 / listedComplete 0` on the first 902fa36b cycle (expected until the top rows enter backoff); read the overnight trend. SME auto-persist NOT yet seen on staging (no SME candidate came due).
- VPS: disk 55% (was 74%); prod releases 3, staging 2; hygiene cron installed `17 4 * * 0 /var/www/ipodhan/current-staging/scripts/vps-disk-hygiene.sh` (first run Sunday 2026-09-06 04:17 IST; switch the path to `current/` after the prod deploy). Orphaned deploy-probe next-server (port 3999, 14 days, prod DB) killed 21:35.
- W-136 MERGED (PR #288, main 48e29402; two Opus Tier A rounds + minors; Linux proof 48/48). During the staging deploy of 48e29402 the new cleanup fired for real (a listener survived the group TERM, `fuser -k` cleared it, port free, flip correct): W-136b MINOR for the brief = consider a longer wait before the port check. Worktree removed.
- Local main is ahead of origin by two docs-only ledger commits (23:00-23:05); they go up with the next bundle push.

## Sequence for session 5
1. Sweep worktrees; read the staging night (every cycle since 22:00): completion, extractionFailed, W-135 counter trend, any SME auto-persist line, no new error classes; note prod's 08:00 API-fallback `rateLimitRemaining`.
2. Confirm W-136 on staging overnight (deploy log line above; hygiene `--dry-run` from `/var/www/ipodhan/current-staging/scripts/vps-disk-hygiene.sh` shows the orphan-sweep step).
3. Write the Rule 6 brief (done with proofs, cost, pending list incl. W-113/114/115/118/119/120/123/125/127b, W-81, W-110, W-105-107, W-77, W-78, W-56, W-97, W-99, D-18 path-scoping; DEPLOY/DEFER; complete-first). Recommend DEPLOY only if the staging day is clean (W-136 is merged and soaking since 23:02).
4. With the owner's word, in the window: `gh workflow run deploy-linux.yml` slot=prod on main; verify served sha, scraper alive, venv versions line, hygiene cron path switched to `current/`; then the D-15 flip = one env line `ENABLE_SME_FILING_AUTO_PERSIST=true` in prod scraper.env with the owner present; prove on the next SME IPO; DEEPA lists 8 Sep (I5/I6).
5. Then D-18 rule path-scoping (Sonnet, Tier C) and the remaining W rows, batched.

Rules in force: D-16 (one deploy/day, brief first, no open issue rides along), D-17 (worktrees via wt-new/wt-rm, removed same session), D-18 (lean briefs, ~300-word reports, no full suites in workers; full suites on this laptop: scraper BY DIRECTORY, no `--maxWorkers` flag), D-12 (bus bookkeeping suspended; ledger only), Rule 5 routing (Sonnet implements, Opus reviews Tier A, Fable never edits task code), Budget line on every Agent brief. Lessons from 2026-09-04 evening: a process check must never match its own command line or ancestors; `exec -a` renames only real binaries; group-kill only what you started with setsid; a test suite may run on a real host only after its isolation is proven by grep.
