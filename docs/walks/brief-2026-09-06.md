# Pre-deploy brief, 2026-09-06 (Rule 6), drafted 2026-09-06 19:28 IST

**Candidate:** `release/prod-2026-09-06` = f9b67d0a (code 6039504a; cut 13:35 IST after a full local pass). Prod now: d38b72aa (2026-09-05 21:10). Rollback: `gh workflow run deploy-linux.yml --ref release/prod-2026-09-06 -f slot=prod -f ref=d38b72aa` (ancestor, same branch).

## (a) What users will see change, and what is fixed
- Company pages stop showing a share count as the issue size (the Shanti Inorganics / Ashutosh Fibre bug); the guard now refuses that value on every write path and the nightly audit names any such row. The two live rows are corrected after the deploy (approved).
- Cloudflare 522 errors at :00/:30 stop on prod too: extractors run at low CPU priority and staging no longer extracts in the same minute (already proven on staging; the Kanohar API URL is back to 200 without a purge).
- Listing exchange: no more "NSE and BSE" for every IPO; SME issues collapse to their single board (unit-proven; live collapse of one closed IPO still pending a fresh listing record).
- Documents: corrigenda for listed companies, fair rotation cap, fixed-price labelling, an ipo_details row for every persisted filing (coverage now measurable; the audit counter warns until 09-09, then hard).
- Anchor investors: the automatic door reads real NSE Emerge letters (Shanti parsed live on staging); failing letters no longer starve prospectus extraction; repeated identical failures go to manual review.
- Live IPO date conflicts resolved by exchange agreement (Kanohar flipped on staging by itself); refused conflict writes are logged.
- Deploy hygiene: probe port freed by a direct listener kill (proven), scraper locks released at deploy (proven), staging cron :15/:45 (proven).
- Tooling: the prod page sweep runs for real again; hook timeouts explicit; workers cannot stash in worktrees.

## (b) Cost today
Prod deploys: 0 (this would be the one). PRs merged since last night: 21 (#304-#324). Hosted gate runs: about 30. Staging deploys: about 12 (superseded ones auto-cancelled). Worker rounds: about 35 Sonnet, 7 Opus reviews; roughly 6 M worker tokens today.

## (c) Pending, and what stays as-is live tonight
- Not in this release (merged after the cut, tomorrow's bundle): DRHP never emits a price band; dead scheduler removed; the detection-change PR gate + failure-class registry; one extractor per box; anchor subtotal handling; listed-rotation stall fix (not yet proven: round 2 in progress); migration journal repair + index (in review).
- Live defects that remain until tomorrow: a DRHP can still emit a band on prod (held by validation, so no user-visible effect); seven listed companies still skipped by the document rotation; the nightly audit still red on 65 legacy rows (owner data repair).
- Unproven on staging tonight (all fail safe): SME single-board collapse, issue-size guard firing live, SME cover headline, unenriched counter.

## (d) Recommendation: DEPLOY
The release fixes a live correctness bug and the half-hourly 522s; it soaked on staging for one full cycle at its own sha and its code has run under every later staging sha all afternoon with 30 clean cycles and 0 extraction failures on prod's side today.

## (e) After the deploy (approved): served sha, scraper alive, venv line, port 3999 free; add DSN_ASSERT_REDIS_DB=0 (prod) / =1 (staging) to the env files; repair the two rows through reset-document + re-consolidation, manual RHP-cover figure only if empty; NO SME flip tonight.
