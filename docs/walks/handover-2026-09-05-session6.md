# Handover: IPODhan session 6 (resume 2026-09-05 afternoon/evening; session 5 ended at 87% context, 15:30 IST)

Read first: (1) `C:\Users\itsab\.claude\projects\D--Abhay-Ventures-IPODhan\memory\deepa-walk-state-2026-09-02.md` (last two entries); (2) the last 40 lines of `docs/walks/2026-09-02-deepa-pipeline-walk.md`; (3) `~/.claude/CLAUDE.md` sections "One deployment window per day" (Rules 1-9) and "Production hosts are not test benches; no WSL/Docker on the laptop" (2026-09-05). Owner working rules from today: explain what / problem / approach / cost in every message; no ad-hoc runs on the VPS; never install WSL/Docker; Sonnet implements, Opus reviews Tier A, Fable never edits task code; Budget + model on every Agent brief; worktrees only via `~/.claude/tools/wt-new.ps1` / `wt-rm.ps1`; scraper vitest by directory (no `--maxWorkers`), web suite alone (the laptop kills parallel suites under ~3 GB free).

## State at handover
- **Production: 8cd287d5** (deployed 2026-09-04 15:00). Prod `scraper.env` has `ENABLE_FILING_AUTO_PERSIST=false` since 10:25 IST (outage stopgap; backup `scraper.env.bak-20260905-w137`). No kernel OOM or pm2 daemon restart since 08:38 IST.
- **Deploy candidate: main d38b72aa**, on staging since 15:00 IST (staging env: all pipeline flags on incl. `ENABLE_SME_FILING_AUTO_PERSIST=true`, PRIMARY_SOURCE_DISCOVERY true; backup `.bak-20260904-w111soak`). Contents: the 2026-09-04 bundles (D-15 flagged SME door, W-111/112 venv per slot, W-121/124/127/133/135, W-134 disk hygiene + weekly cron, W-136 probe process group) + W-137 (extractor memory: PR #290) + W-138 (NUL rupee glyph: PR #291).
- **Owner decision pending: DEPLOY tonight 21:00-23:30 IST (recommended) or DEFER.** Brief issued 15:05 IST (in the ledger / previous session's last message).
- **Proof set:** W-137 on the VPS (1.26 GB peak vs 4.7 GB; clean exit 3 at cap 60); type matrix (16 MB DRHP 1,542 MB; scanned RHP 1,245 MB; scanned ad 1,885 MB; ratios ad 1,951 MB; SME RHP 97 MB; Lumino anchor = pre-existing empty, W-139); SME automatic path proven on staging (Qualiance RHP, 3 financial rows); local full passes green on every bundle; 2 hosted gate runs today.
- **Worktrees:** none besides `D:\Abhay\Ventures\IPODhan` (main). Branches merged to main except `chore/d18-rule-path-scoping` (13af53c0; parked, merge AFTER the prod deploy). Local main is ahead of origin by docs-only ledger commits (push with the next bundle).
- **Open rows:** W-136b (probe cleanup backstop fires on every deploy; lengthen the wait), W-139 (anchor OCR heuristic; Lumino has 0 anchors), D-18b (quote older rules' `paths:` values; `*`-leading values are YAML aliases), D-18c (six skills mention `globs:` in prose), plus the older backlog W-113/114/115/118/119/120/123/125/127b, W-81, W-110, W-105-107, W-77, W-78, W-56, W-97, W-99. NOT tested under the new code: SME anchor letter, fixed-price SME issue, corrigendum/addendum docs, listed-IPO rotation across many cycles, probe cleanup under a failed deploy.

## Sequence for session 6
1. Read the staging cycles since 15:00 (`pm2 logs ipodhan-scraper-staging` on the VPS, READ ONLY): each cycle completes, extractionFailed 0 after W-138, no OOM (`dmesg -T | grep -i "out of memory"` must still show 08:38), no `Stopping app:notifier` after 08:38 in `/root/.pm2/pm2.log`.
2. With the owner's word, in the window: `gh workflow run deploy-linux.yml` slot=prod on main d38b72aa (self-hosted runner). Verify: served sha, scraper alive, venv versions line, probe port 3999 free, prod releases 3.
3. Restore `ENABLE_FILING_AUTO_PERSIST=true` in `/var/www/ipodhan/shared/env/prod/scraper.env` (from the backup); watch the next :00 cycle extract Pranav Constructions' RHP under the ceiling (`extractionFailed 0`, peak not visible; no OOM). Switch the hygiene cron path from `current-staging` to `current` (`crontab -e` as root; line in `docs/ops/vps-disk-hygiene.md`).
4. D-15 flip = one env line `ENABLE_SME_FILING_AUTO_PERSIST=true` in prod scraper.env, only with the owner present; prove on the next SME IPO. DEEPA lists 8 Sep (I5/I6).
5. Then merge `chore/d18-rule-path-scoping` (one PR; pr-gate skips docs-only), W-136b + W-139 + D-18b/c batched, then the older backlog.
