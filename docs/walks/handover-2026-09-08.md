# Handover — 2026-09-08 05:2x IST (session 9 -> session 10)

Read first: `docs/walks/brief-2026-09-08.md` (morning brief), `docs/reviews/rca-2026-09-07-missed-live-defects.md`, rule `.claude/rules/signal-ownership.md`, recipes `docs/ops/prod-ops-recipes.md`. Ledger of everything: `docs/walks/2026-09-02-deepa-pipeline-walk.md` (append dated lines). Bus: `D:\Abhay\GetWorkDone` (T-ids via `allocate-tid.py --slug --sid`, registry via `lesson.py`, writes via `bus-write.ps1` only). Memory: `session-9-state-2026-09-07.md`, `prod-deploy-2026-09-07.md`, `feedback-signal-ownership.md`.

## State
- Prod: f0c66b6b (tag prod-2026-09-07, deployed 2026-09-07 22:31 IST), healthy. Staging: main tip (51a0a269 at 05:15). Rollback ref f9b67d0a.
- Tools on main: `node scripts/ops/failure-delta.mjs --slot prod|staging` (tick identities; `--track <class>=<#issue>`; `--file-issues`), `scripts/ops/floor-delta.mjs <today> <yesterday>` (nightly diff; VPS files under /root/data-audit-ipodhan/state/run-<date>.log and, from the first night after the box pulls #407, state/floor/<date>.txt), `scripts/ops/merged-not-deployed.mjs --brief`, `scripts/ops/deploy-and-watch.sh <date> <sha>` (run in the background), merge helper pattern: never `--delete-branch` in a merge command; confirm MERGED before bookkeeping.
- Tracked failure identities: prod (8) and staging (3) all carry issue numbers (#402 Rentomojo persist, #403 Steamhouse unit, #347 anchor refusals, #396 spawn-timeout-hard, #409 Kanohar).
- Open PRs from today: none. Cut B branch `release/prod-2026-09-07b` at 60a7bb46 exists on origin (26 commits behind main).
- Worktrees: none. Stashes: 10 pre-existing (owner call). Laptop: keep <= 2 builders; background waiters get killed under memory pressure — use crons for timed reads.

## Rules that bit today (do not repeat)
- Every nightly/audit read names the file and its run date (#401 was a false alarm from an old file).
- A worker's gate claim is a claim: reproduce every gate yourself (write-ratchet PASS was claimed, FAIL real).
- Never kill by a pattern that appears anywhere in your own command line; kill by PID from a previous call.
- `cd A && cmd`, never `cd A || cd B; cmd`; `git status --short` before every commit; commit only the intended paths.
- Deploy timer 30 min early; deploy via `deploy-and-watch.sh` in the background.
