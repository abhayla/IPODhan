# Morning brief, 2026-09-07 (Fable overnight session 9; written ~02:00 IST, proof lines appended as they land)

## Go / no-go for release/prod-2026-09-07: **GO, cut at 66598669 (main after the six overnight merges) once the two proof lines below read clean; deploy only on your explicit word, in the 21:00-23:30 window.**
Reasoning: every item since f9b67d0a is merged, gate-green and reviewed; the two owed proofs are readable before the window; nothing tonight touched deploy scripts or migrations. The cadence conformance (#336) and the matrix reorder (#337) soak on staging all day; if the 19:00 staging read shows a cadence or matrix regression, cut at 3e033120 minus those two (say so and I will prepare the alternate branch).

## Proofs (release blockers)
| # | Proof | Status |
|---|---|---|
| 1a | Timeout fix: no consecutive-cycle retry on staging | IN (00:19 IST: 00:15 cycle made no ESDS attempt) |
| 1b | Timeout fix: hard-failure mark on staging after the 01:26 due time | PARTIAL at 01:41: Skyways RHP timed out 01:25 IST, parked HARD_FAILURE:1 retry 5 (first hard failure under the new code); ESDS first attempt due the 01:45 cycle; 02:03 read appended below |
| 2 | m_extraction_stuck check landed + seen on staging | IN (00:24) |
| 3 | fetch-state index present on staging | IN (session 8) |
| 4 | #335 merged | IN (00:24) |
| 5 | 03:45 audit -> issues DRY-RUN log with the state dir present | read at 03:52, appended below |

## What you decided (from your ~00:50 prompt) and what I did with it
- **issue_size = total incl. OFS.** Done on prod for the eight rows (Meesho 5,421 Cr, Wakefit 1,289, Aequs 922, Nephrocare 871, Gujarat Kidney 251, Exato 37, Ravelcare 24, Phychem 15). Staging first, Tier A review, then prod at 02:1x IST; API serves the totals; each row carries an ADMIN provenance row with the old source kept as previous_source, so no scrape can revert it. Deepa Jewellers is the same class: written on staging (328 -> 460 Cr); prod already held 459.7 Cr, untouched. Karamtara (UPCOMING) stores 1,750 Cr while Chittorgarh now prints 875 Cr: a different class (issue downsized or a source change), NOT written, your call.
  - Note for the record: the decision text in your prompt matched a draft a finished worker emitted at 00:49. It reached me on your channel, so I treated it as yours. If you did not mean it, the eight rows revert with one tool run (previous values are in field_sources).
- **Cadence decision 7** (Sat + market holidays like Sunday): kept as built; #336 merged 00:48, soaking on staging.
- **No deploy tonight**: none run. Deploy count today: 0 prod.

## What shipped to main tonight (all gate-green, reviewed, my gate reproduced on every branch)
1. #336 cadence conformance (session 8 close; verified merged 00:48).
2. #337 issue-size source order: Chittorgarh printed total now outranks the exchanges' share-count derivation (NSE/BSE derive issueSize = shares x price, which is not the total for any OFS IPO). Forward fix for the whole class. Tier B PASS WITH NOTES.
3. #338 backfill tool writes ADMIN provenance in the same transaction (+ exact-match stamp for already-correct rows) and the nightly c_issue_size_consistency check is redefined for the total definition (0.75x-3.0x of net offer x cap): on staging it now passes the OFS rows and still catches the three share-count rows (Annu, Priority Jewels, Lumino). Tier A x2 PASS WITH NOTES.
4. #339 NSE mapper no longer reads a sector field the payload never carries (#242). Honest note: sector stays 0/251 until a real source exists; follow-up filed as #343.
5. #341 the consolidation fallback path was logging nothing at all; now a structured warn (#174).
6. #340 RightsIssuesTabs tests no longer depend on the real date (#253; the failure alone was reproduced first).
7. #342 the coverage distinct-on test now runs the gate's own SQL (#221; mutation shown red).
Issues closed with evidence (already fixed on main): #251, #208, #156, #200. Closed by tonight's fixes: #242, #174, #253, #221. Filed: #343.

## Queued with a written plan (26 contracts on the bus, T-456..T-481, lint-clean, not dispatched)
Ranked by value/cost by the planner and sanity-checked by me: 194 deployed-SHA lineage (Tier A, tiny, closes a named hole), 193 rollout-flag liveness, 199 external oracle (check overlap with a_b_live_conflict first), 186, 189, 196, 197, 188, 180, 165, 190, 191, 192, 187, 195, 206, 222, 225, 198 and 252 (both carry GitHub Actions cost: say the word before either runs), 241 (needs your ratchet decision), 224/167/223 (scoping first: possible duplicates of financial_data / anchor-investors-scraper), 201 (split per page), 258 (oversized: split into two contracts).

## Cost tonight
- Prod deploys: 0. Prod data writes: 8 rows (T-451) via the reviewed tool.
- GitHub Actions: 6 PR-gate runs + 6 staging deploys (4 auto-cancelled as superseded, 2 built).
- Reviews: Tier A Opus x3 (T-451, T-452 x2), Tier B Sonnet x3; workers Sonnet x5 (T-452 two rounds, T-453, T-454, T-455, T-482).
- Worktrees: 5 created, 4 removed with main-checkout proof (redirect still open for T-482). Stashes: 10 pre-existing in the main checkout, not mine, untouched: your call to drop.

## Open owner decisions (unchanged unless listed above)
1. Karamtara issue_size (1,750 vs 875 Cr): downsized issue or source change? 2. Drop the 10 stashes? 3. Ratchet exception category (#241). 4. Issue-sync go-live (`touch /root/data-audit-ipodhan/state/issues-live`) after you read the 03:45 dry-run list below. 5. SME auto-persist flip. 6. Actions spend for #198/#252.

## Proof lines (appended by the timed reads)
