# Contract: easy wins, round 2 (non-data, small, known cause)

**Executor:** /goal (new owner-present session)   ·   **Created:** 2026-09-25
**Mechanics:** read `docs/contracts/2026-09-25-finish-remaining-items.md` and follow its §0 (one goal session at a time,
worktrees via `wt-new.ps1`/`wt-rm.ps1`, `git show origin/main:` reads, and merges ONLY as
`node scripts/ops/merge-if-current.mjs <PR> > /dev/null 2>&1 && gh pr merge <PR> --squash`), its owner-delegation block, its
verification rules, and `~/.claude/rules/run-discipline.md`. Production is never touched.

**Mission.** Close every open issue that is small, has a known cause, and needs no data change. Do not start anything that
needs deep analysis.

**Owner decisions for this contract (2026-09-25, three questions answered in the planning session):**
- **Q1 pool.** Sweep ALL open issues, not only the deferred ones. The 3 `parked` issues (#943, #1032, #1034) are excluded.
- **Q2 non-data.** In scope: code, tests, CI, tooling and docs only. No production write. No change to any value a reader sees
  on the site. Staging reads and writes only to prove a change.
- **Q3 deliverable.** This contract. The owner starts the run.
- **Size rule (carried from the previous contract).** A piece is in scope only when all of these hold:
  - the cause is already known from the issue;
  - the fix fits one PR of about 300 changed lines or fewer;
  - it needs no DB migration;
  - it needs no production action.
  If a piece grows beyond that, or fails 2 review rounds, label the issue `deferred`, add a one-line comment saying why, and
  move on.

**Screening source:** `docs/contracts/.run/issue-sweep-2026-09-25.tsv` classifies all 202 open issues on 2026-09-25: 17 EASY,
5 LIKELY-FIXED, 3 parked, 177 NOT-EASY. 71 of the NOT-EASY rows are category `other`. Those were never really read, and a
second pass is classifying them (see Step 4).

**The /goal line (type it in the new window; never the bare path):**

```
/goal Close the easy-wins work in docs/contracts/2026-09-25-easy-wins-2.md. Complete ONLY when the final line of docs/contracts/.run/easy-wins-2-PROGRESS.md is "EASY-WINS-2 DONE" and every issue named in the contract's Step 1 to Step 4 tables is either closed (a merged fix, or verified already fixed with evidence in a comment) or open with the `deferred` label and a one-line reason comment. Reading or complying with the contract is not completion.
```

**Progress log:** `docs/contracts/.run/easy-wins-2-PROGRESS.md`. Stamp every line from `date` in IST. Git Bash does NOT know
`TZ=Asia/Kolkata`: it silently gives UTC. Use `TZ=IST-5:30 date`. The log's last line is `EASY-WINS-2 DONE`, and the line
before it summarises CLOSED / DEFERRED.

---

## Step 1: verify and close what is already fixed (cheap, no code)
For each issue:
- Confirm the fix on `refs/remotes/origin/main`: a commit sha, or the code line from `git show`.
- Comment with the evidence, then close.
- If it is NOT actually fixed, move it to Step 2 when it's small, or label it `deferred`.

| Issue | Evidence the sweep found |
|---|---|
| #35 | "CI broken repo-wide" (2026-06-12, a billing block). PR Gate has run green many times since, e.g. on #1080 today. |
| #51, #56 | Web unit-test debt and the SmartDefaults OOM, fixed by PR #53 (`f9caeb5e`). |
| #493, #497 | `__dirname` in ESM. Both files on main use `fileURLToPath(import.meta.url)`. |
| #645 | `apply-rule-ownership --apply` deletes hand-written prose. Probably the same bug as #905, closed by #1061 (`1e65f1b1`). Confirm with #1061's test before closing. |

## Step 2: small fixes, in this order (each rechecked against the size rule first)
Each PR carries:
- a failing test first;
- `Class:` and `Proof:` lines in the brief;
- the literal `No detection change: <reason>` line, or a real detection change.

| # | What (one line) | Note |
|---|---|---|
| #504 | `ci.yml` "Run E2E tests" step has no `DATABASE_URL` (only `REDIS_URL`) | one-line env fix; same shape as the fixed integration step |
| #451 + #461 | a vitest run on a path outside the include pattern, or a `node --test` glob that matches nothing, exits 0 | one PR: fail loudly on 0 test files |
| #694 | `scripts/ci/require-repair-tool-module.mjs:73-77` uses a hand-written comment/string stripper that mis-reads a regex literal and can drop a real `openRepairDb()` call | replace it with a real tokenizer; memory: never hand-roll a lexer |
| #207 | two test-guard gaps (stale-fixture selection; undefined vs `[]` guard) | tests only |
| #572 + #573 + #574 + #575 | four integration tests failing on their own fixtures (FK seed row, drifted expectations, a dropped `category` field, non-UUID ids), then wire them into CI | ONE batch; they share the CI exclusion list |
| #706 | the `decide` job in `deploy-linux.yml` has no `failure() \|\| cancelled()` reporting step; only `deploy` has one (line ~385) | copy the #627 step. **Tier A** (deploy workflow) |
| #665 | `audit:schema-drift` compares columns only; extend it to indexes and constraints | recheck the size rule first; if over, `deferred` |
| #739 | the field-manifest generator's capability reasons come from a hand-written table, not the spec | move the texts into the spec with no wording change; output byte-identical |
| #1082 | 5 MINOR follow-ups to `db-tunnel.sh` from the #1080 Tier A re-check | **Tier A** (hook + a script that kills processes). Do item 3 FIRST: tests must stub `ssh` so a test can never open a real tunnel to prod. |

## Step 3: docs batch (near the end, one PR)
- **#1062:** the one-time `<!-- end generated -->` marker migration on 41 build cards, so `apply-rule-ownership.mjs` stops
  refusing them. Mechanical, Tier C.
- Put the run's ledger lines and any findings in the same batched docs PR.
- Republish the board only if a verdict changed.

## Step 4: second-pass items (read before Step 2 starts)
- The planning session is classifying the 71 unread `other` rows. The result is
  `docs/contracts/.run/issue-sweep-other-2026-09-25.tsv`.
- If it exists when this run starts: take its EASY rows under the size rule, append them to the end of the Step 2 queue in
  that file's order, and close its LIKELY-FIXED rows as in Step 1.
- If it does not exist: skip this step and say so in PROGRESS. Never re-sweep the 71 in this run; that is a separate task.

## Explicitly OUT of scope
- **Parked (owner):** #943 (item 7, fix merged, real-data proof owed), #1032 (item 19), #1034 (item 6).
- **User-level tools (owner call, outside this repo):**
  - #607 (`wt-sweep -Apply` and detached-HEAD worktrees);
  - #613 (non-deterministic `node_modules` junction creation).
  Both live in `~/.claude/tools`. List them in the final summary for the owner; do not change them.
- **Data values and identity (Q2):** every sweep row with category `data-change`, `needs-real-event` or `owner-decision`.
- **Deferred for size or proof in the previous run:** #890, #754, #719 (proof after the 21:30 IST staging window), #715,
  #1033, #1063, #1054, #1051, #1046, #959, #749, #762.
- **Production:** any production deploy or production write. The 6 unauthenticated admin routes wait for the owner's release.

## Rules that matter most here
- **At most 2 builders at once,** never on the same files. Tier B review by default; Tier A for #706 and #1082.
- **Keep generated files out of feature PRs.** Registry aggregates, board pages and rule indexes are regenerated once, in the
  Step 3 docs PR.
- **Run CI's own checks locally before every push.** For a new `scripts/tests/*` file, confirm `pr-gate.yml` names it;
  otherwise CI never runs it.
- **The DB tunnel:** only `bash scripts/ops/db-tunnel.sh start|stop|status`. Never start it as a background task:
  TaskStop cannot kill it. End the run with 0 listeners on 15432.
- **Owner delegation stands:** decide spec-conformant choices, and bring only a SPEC CHANGE to the owner (via
  `owner-questions-<date>.md`, not AskUserQuestion, if the owner is away).
- **Never:** `git stash`, `git checkout --`, `--no-verify`, `rm -rf` on a worktree, or `;` before `gh pr merge`.

## Definition of Done
- [ ] Every Step 1 issue: closed with an evidence comment, or moved.
- [ ] Every Step 2 issue: closed by a merged PR, or open with `deferred` and a one-line reason.
- [ ] Step 3 docs PR merged; the board republished only if a verdict changed.
- [ ] Step 4 done, or skipped with a PROGRESS line saying why.
- [ ] All run worktrees removed via `wt-rm.ps1`; 0 listeners on 15432.
- [ ] PROGRESS ends with a CLOSED / DEFERRED summary (plus the #607/#613 note for the owner), then `EASY-WINS-2 DONE`.
