# Contract: implementation lane C — items 14, 2, 12, 3 of the pull-model design, in parallel with lane A and lane B

**Executor:** /goal (built-in autonomous run) · **Created:** 2026-09-10 · **Slug:** `pull-model-implementation-lane-c`
**Parent contract:** `docs/contracts/2026-09-09-pull-model-implementation-loop.md` (lane A, running since 2026-09-09 20:41 IST). **Every rule of the parent applies here unchanged unless this document names a difference.** Where the two disagree, this document wins for lane C only.
**Mission:** Build four of the 22 build items in `docs/design/data-sourcing-pull-model.md` §7.1 — **14, 2, 12, 3** in that order — on the IPODhan codebase, one item at a time, each split into slices ≤ ~400 lines, each slice test-first, gated, reviewed at its tier, merged to main by this run on green, and each item proven on staging at the next real cycle. Lane A builds items 1, 15, 13, 4, 5, 6, 7, 9, 10, 11, 17 (19 is done); lane B builds items 20, 22, 18, 16, 8, 21. The three lanes share `main`, the staging slot, and one daily CI budget; they share nothing else. **Production is never written and never deployed by this run.**

**Why a third lane (owner, 2026-09-10 ~12:40 IST: "you have approval for 3rd lane but you take care of everything e2e"):** items 14, 2, 12, 3 touch no file item 1 rewrites (items 15 and 13 do, so they stay in lane A) and no lane B file except `scraper/src/config/field-priority-matrix.ts`, which items 2, 3 and lane B's 16 all edit in small hunks.

---

## §0.1 Worktree isolation

> **First action, non-negotiable.** This run never works in the owner's primary checkout `D:\Abhay\Ventures\IPODhan` and never touches lane A's worktrees (`IPODhan-IPODhan-impl-loop`, `IPODhan-s<NN>-<K>`, `IPODhan-red-*`, `IPODhan-ops-staging-repair`) or lane A's branches (`ops/impl-loop-ledger`, `feat/pm-item*`, `fix/pm-item*`), or lane B's worktrees (`IPODhan-IPODhan-impl-loop-b`, `IPODhan-b<NN>-<K>`, `IPODhan-red-b*`) or lane B's branches (`ops/impl-loop-b-ledger`, `feat/pm-b-item*`, `fix/pm-b-*`).
>
> 1. **Coordination worktree (once):** `powershell -File $HOME/.claude/tools/wt-new.ps1 -Repo D:/Abhay/Ventures/IPODhan -Name IPODhan-impl-loop-c -Branch ops/impl-loop-c-ledger -Base origin/main -Purpose "implementation lane C: ledger, tracker, contracts" -TtlHours 720` → `D:\Abhay\Ventures\IPODhan-IPODhan-impl-loop-c`. Assert `git rev-parse --show-toplevel` ends in `IPODhan-IPODhan-impl-loop-c` before any command there.
> 2. **Slice worktree (per slice):** `wt-new.ps1 -Name IPODhan-c<NN>-<K> -Branch feat/pm-c-item<NN>-s<K>-<slug> -Base origin/main -Purpose "lane C item <NN> slice <K>: <one line>" -TtlHours 48`. Cut from the CURRENT `origin/main` after `git fetch origin`. Resuming a branch that already exists: add `-Existing` (the base is ignored). Red-line worktrees: `-Name IPODhan-red-c<NN>-<K>`.
> 3. **Claim:** the lock file is `.run-active-c.lock` in the coordination worktree (lane A holds `.run-active.lock` and lane B holds `.run-active-b.lock`, each in its own tree; the three locks are independent by design). `RUN_TOKEN=impl-loop-c-$(date +%s)`. If `.run-active-c.lock` already exists, halt with an owner note.
> 4. **Removal:** as the parent §0.1.4, with the guard added 2026-09-10: `wt-rm.ps1 -Discard` refuses a tree holding untracked files (exit 5). Before `-Discard`, copy any evidence file (fixtures, red/green logs, probe output) into the slice's PR body or the coordination worktree; only then `-DiscardUntracked`. Evidence lost to a discard is a DEFECT (it happened once on 2026-09-10).
> 5. **Never touch** lane A's trees and branches (`IPODhan-IPODhan-impl-loop`, `IPODhan-s<NN>-<K>`, `IPODhan-red-*`, `IPODhan-ops-staging-repair`; `ops/impl-loop-ledger`, `feat/pm-item*`, `fix/pm-item*`) or lane B's trees and branches (`IPODhan-IPODhan-impl-loop-b`, `IPODhan-b<NN>-<K>`, `IPODhan-red-b*`; `ops/impl-loop-b-ledger`, `feat/pm-b-item*`, `fix/pm-b-*`), `D:\Abhay\GetWorkDone` (no plain file writes either; the bus guard covers git only), `D:\Abhay\VibeCoding\5Wealths\`.

## §0.2 Idempotency preflight

> As the parent §0.2 with these substitutions:
> 1. **Ledger of record:** the same append-only ledger `docs/walks/2026-09-02-deepa-pipeline-walk.md` (every lane C line is prefixed `[lane C]`) and this run's own state file `docs/contracts/state/pull-model-implementation-lane-c-STATE.json` (four items; committed to `ops/impl-loop-c-ledger` on every item transition). On resume, merged slices are re-derived from the head branch, never from a title search (PR search does not index branches): `gh pr list --state merged --limit 200 --json number,title,headRefName,mergeCommit --jq '[.[] | select(.headRefName | startswith("feat/pm-c-item<NN>-") or startswith("fix/pm-c-item<NN>-"))]'`. STATE.json carries `testDb: {name: "ipodhan_test3", drop: "OWED"}` from Stage 0 (written at create time, committed) until the drop in Stage 7 succeeds and flips it to `DONE` with the after-listing; a killed session therefore always leaves the debt visible.
> 2. **Read before building, all lanes' work:** commit subjects in every lane are `item NN slice K` with spaces, so the check is `gh pr list --state merged --limit 200 --search "item <NN> slice" --json title,headRefName,mergedAt` (matches lane A's `feat/pm-item<NN>-*`, lane B's `feat/pm-b-item<NN>-*`, and this lane's `feat/pm-c-item<NN>-*`). A slice another lane has already merged for one of these items is SKIPPED with a verify-only pass, never rebuilt. **Board claim (lane A's queue is 1, 15, 13, 4, 5, 6, 7, 9, 10, 11, 17 and lane B's queue is 20, 22, 18, 16, 8, 21, until the owner tells either otherwise in its own session):** before the first slice of an item, read `items/item-NN`; if it carries `lane: "A"` or `lane: "B"` with any slice past PLANNED, this lane SKIPS the item (verify-only, report line) and moves to the next; otherwise write `lane: "C"`, `claimedAt` (ISO) and the slice plan in one batch before any worktree is cut. Re-read the claim at every rebase and before every PR open; if another lane has meanwhile merged a slice of the same item, stop, record the collision in the ledger, verify that lane's slice, and continue only with slices its plan does not contain.
> 3. **Preconditions (all checked; the run refuses to start on any failure):** (a) coordination tree clean and `origin/main` green, as the parent; if main is red and the cause is lane A's or lane B's last merge, this run does NOT fix it (that lane owns its breakage; signal-ownership R4 still applies: post one Notifier line and wait 30 minutes twice, then start local work without pushing). (b) **The test database for this lane is `ipodhan_test3`**, owned by `ipodhan_app`. Precondition (g) below proves it exists. The reset and migration are ONE psql invocation on ONE connection, so the database name is checked on the same connection that drops (the tunnel's far side is the production cluster; two connections with a stale `DATABASE_URL` between them is how a production schema gets dropped): `URL="postgresql://ipodhan_app:${IPODHAN_APP_DB_PASSWORD}@localhost:15432/ipodhan_test3"; psql "$URL" -v ON_ERROR_STOP=1 -c "do \$\$ begin if current_database() <> 'ipodhan_test3' then raise exception 'wrong db %', current_database(); end if; end \$\$;" -c "drop schema if exists drizzle cascade; drop schema if exists public cascade; create schema public;"` (if `psql` is not on PATH, the same two statements through one `pg` Client in a script; never two connections). Then `cd web && DATABASE_URL="$URL" npm run db:migrate` → exit 0 and the public table count equals `grep -c 'pgTable(' packages/shared/src/db/schema.ts`. **`ipodhan_test` belongs to lane A and `ipodhan_test2` belongs to lane B; this run never connects to either.** Every `DATABASE_URL`, every vitest integration config, every `openRepairDb` call this lane runs locally names `ipodhan_test3`; the `sonnet` verifier runs `grep -Eq 'ipodhan_test2?([^0-9]|$)'` over each brief's commands and FAILS the slice on a match (it matches `ipodhan_test` and `ipodhan_test2`, never `ipodhan_test3`). (c), (d), (e) as the parent. (f) lane A and lane B liveness are read from `git fetch origin ops/impl-loop-ledger ops/impl-loop-b-ledger && git log origin/ops/impl-loop-ledger -1 --format=%cI && git log origin/ops/impl-loop-b-ledger -1 --format=%cI` (each ledger branch; a commit within 6 hours = alive) OR the owner has said that lane is stopped; either way this lane proceeds, the check only decides who fixes a red main. (h) **Lane A has released these items.** `git fetch origin ops/impl-loop-ledger && git show origin/ops/impl-loop-ledger:docs/contracts/state/pull-model-implementation-loop-STATE.json` must show items 14, 12, 2, 3 absent from lane A's queue (or its ledger must carry the owner's typed line "items 14, 12, 2, 3 move to lane C"); if not, HALT-OWNER with the note "lane A has not released 14, 12, 2, 3; type the split in lane A's session" and post one Notifier line; a lane A plan file that names item 14 is not a release. (g) **The lifecycle tool exists on main and the database exists:** `git cat-file -e origin/main:scripts/ops/test-db-lifecycle.mjs` exit 0, then `node scripts/ops/test-db-lifecycle.mjs --name ipodhan_test3 --create --apply` exit 0 (idempotent: prints "already exists" and exits 0 when it does). If either fails, HALT with an owner note. **Never a hand-typed `create database` or `drop database`, under any circumstance.**
> 4. **Record every skip** in the final report.

## §0.3 Progress log and owner updates

> As the parent §0.3 with: progress log `docs/contracts/.run/pull-model-implementation-lane-c-PROGRESS.md`; tracker section `## Implementation lane C — updates` in `docs/ops/work-tracker.md` (lane A owns `## Implementation loop — updates` and lane B owns `## Implementation lane B — updates`; never edit those sections); Notifier lines prefixed `[lane C]`.
> **Live board:** the same artifact `https://claude.ai/code/artifact/28b3afb4-c8cd-4a61-a007-9e36de5ca851`. This run writes `run/meta-c` (`db_op: "update"`, collection `run`, doc_id `meta-c`, same fields as the parent's `run/meta` plus `lane: "C"`) and ONLY the four item documents `items/item-02`, `items/item-03`, `items/item-12`, `items/item-14`, each with `lane: "C"` added (the page renders every `items/*` document, so these show live). It never writes `run/meta`, `run/meta-b`, or any other `items/item-NN`. The published page does not render `run/meta-c`; it is report-only for the supervisor, and the owner-facing sentence goes into each item document's `note` field instead. `prGateRunsToday` in `run/meta-c` is the SHARED repo total (decision 9), with `laneCRunsToday` beside it.

---

## Scope boundary

- **In scope (code), by item:** 14 → `scraper/src/scrapers/bse*` (the share-count conversion the card names), its tests, `docs/design/probes/fixtures/**` (real BSE fixture only); 12 → `packages/shared/src/utils/company-name-normalizer.ts` and its agreement test, `scraper/src/scrapers/investorgain-gmp-orchestrator-v2.ts`, `scraper/src/services/document-discovery-runner.ts`, `scripts/lib/repair-invariants/duplicate-ipo-rows.mjs`, any row merge ONLY through `scraper/scripts/repair-merge-duplicate-ipo.ts` (item 19, done); 2 → `scraper/config/**` (NEW: `field-manifest.json`, `field-manifest.schema.json`), `scraper/src/config/field-manifest-loader.ts` (NEW), `scraper/src/config/field-priority-matrix.ts` (small hunks), `scraper/src/index.ts` (loader call at start); 3 → `scraper/src/config/field-priority-matrix.ts` (deletions), `scraper/src/services/data-consolidation-service.ts` ONLY for dead-reference removal the card names and ONLY after item 1's writer is merged (`gh pr list --state merged --search "item 1 slice"` shows the writer slice), otherwise that hunk waits. Shared docs as the parent.
- **Files touched by more than one lane (conflict list, re-read on every rebase):** `scraper/src/config/feature-flags.ts`, `web/lib/config/feature-flags.ts`, `scraper/src/config/field-priority-matrix.ts`, `packages/shared/src/db/schema.ts` (items 18 and 22 add columns; lane A's items 1 and 5 do too), `docs/design/rules.json` and `docs/design/rule-ownership.json` (item 20 reads them; lane A's docs PRs may edit them), `scripts/deploy-linux.sh` and `scripts/assert-env-keys.sh` (the parent's decision-11 "first flag slice" duty), `.github/workflows/ci.yml` (the parent's decision-8 "first web-touching slice" duty), `web/drizzle/migrations/meta/_journal.json` (+ `scraper/tests/unit/pipeline-stages/fixtures/stage-0/expected-schema.json`, whose `journalEntries` count is hand-maintained and MUST be bumped by every migration-adding slice in any lane), `.github/workflows/pr-gate.yml`, `docs/reviews/detection-checks/` + the generated registry, `config/write-ratchet-baseline.json`, `docs/ops/work-tracker.md`, the ledger, `docs/ops/prod-ops-recipes.md` (stage it with `git -c core.autocrlf=false add`; plain `git add` rewrites CRLF and splits shell commands in half — lane A, 2026-09-10). A slice that must touch one of these does it in the smallest hunk possible and rebases immediately before opening the PR. **Shared duties:** none are lane C's; see decision 23.

- **Out of scope, HARD:** as the parent, plus: any file lane A's or lane B's current slice is editing (read each lane's slice plan in the ledger before cutting a slice; if the files overlap, build a different slice of the same item first, or wait); `ipodhan_test` and `ipodhan_test2`; lane A's and lane B's board documents; lane A's and lane B's worktrees and branches.
- **Goal type:** fresh build in a loop (4 items), with one live-defect fix (item 14) under the defect-fix contract. Issue #437 belongs to no lane C item; never build toward it.

## Context to read first

As the parent's list, restricted to the four cards `item-02`, `item-03`, `item-12`, `item-14`, plus: the parent contract itself (this run must be able to say which of its clauses it inherits); lane A's and lane B's ledger lines and STATE.json (read-only, to learn what each lane has merged and what it is editing now); `scripts/ops/test-db-lifecycle.mjs` (this lane's database create/drop tool); `docs/contracts/plans/lane-c-items-12-2-3.md` and the item-14 section of `docs/contracts/plans/lane-a-items-15-13-14.md` (pre-made slice plans, committed with this contract; the card, including its dated "Architect correction" block, wins where they differ).

## Pre-made design decisions (the run must NOT pause on these)

Decisions 1, 4, 5, 6, 7, 8, 10–19 of the parent apply verbatim. The differences:

2. **Pipelining inside this lane (owner, 2026-09-10).** Slices of the CURRENT item are still built one builder at a time, but the run does not sit idle while a slice waits for CI or for a staging proof: once slice K's PR is open and its local gates are green, the run may cut and build slice K+1 **if and only if** K+1 touches no file K touches and does not import anything K adds (the slice plan states this for every pair; the `sonnet` verifier checks `git diff --name-only` of both branches for an empty intersection before K+1's builder is dispatched). K+1 is rebased on main after K merges and before its own PR opens. A failed CI or a failed proof for K stops K+1's work at once: the fix of K comes first. Never more than two slice PRs of this lane open at once. No second ITEM starts before the current item is DONE or BLOCKED (parent decision 2 stands for items).
3. **Item order: 14, 2, 12, 3.** Why: 14 is a one-slice live-defect fix with an obtainable proof; 2 comes second because lane A's item 13 depends on it and lane B's item 22 reuses its `scraper/config/` loader, so it unblocks two peers early (lane C posts the item-2 ETA to the supervisor at claim time and a Notifier line at merge); 12 is CIN-first identity plus duplicate detection routed through the merged merge tool (item 19), seven slices; 3 shrinks the matrix last, after 2 makes ranks configuration. **Item 16 (lane B) does not edit `field-priority-matrix.ts` (its card says so); lane C owns every matrix hunk**, so items 2 and 3 conflict with no peer there. Item 13 (lane A) depends on item 2: lane C posts one Notifier line when item 2 merges.
   **Tier corrections the run applies without asking:** item 12's row-merge and repair slices are Tier A (write path/repair tool); items 2 and 3 slices that only move numbers into config with a config-diff test are Tier B; a slice that changes which source wins for any field is Tier A. Proofs: item 14 from the BSE fixture plus the staging cycle line its card names; item 12 from a staging read of duplicate pairs before/after; items 2 and 3 from the config-diff test plus the cycle log printing the loaded manifest hash.
9. **CI budget: one shared ceiling of 60 PR-gate runs per IST day across ALL lanes, 20 for the night of 2026-09-10 (owner, typed in lane A's session 2026-09-09 22:5x IST; extended to lane B 2026-09-10, and to lane C 2026-09-10 ~12:40 IST).** The 60 is a repo ceiling, never a budget this lane may consume up to. **This lane's own hard cap is 15 per IST day**, and at most 5 before 2026-09-11 00:00 IST (the shared night budget of 20 is already claimed by two lanes), counted from its own PRs (`gh pr list --state all --limit 100 --search "[lane C]" --json number,headRefName` and the pushes the ledger records today; re-pushes to an open PR branch count). In addition, before every push, the repo total is derived from `gh run list --workflow pr-gate.yml --limit 100 --json databaseId,createdAt --jq '[.[] | select(.createdAt >= "<IST midnight as UTC ISO>")] | length'` (IST midnight = 18:30Z of the previous calendar day; `--created <date>` filters by UTC date and is wrong before 05:30 IST; the default page is 20 and silently caps); at a repo total of 50 or more this lane pushes nothing and finishes local work only; three lanes reading 49 at the same moment is why this lane's cap is 15 and the stop is at 50, not 60. Every PR this lane opens carries `[lane C]` in its title.
20. **Rebase before every PR, and after every lane A or lane B merge that touches the conflict list.** Before `gh pr create` and before `gh pr merge`: `git fetch origin && git rebase origin/main`; re-run the local gates after a rebase that changed any file in the conflict list; a migration-adding slice re-checks the journal count fixture after every rebase. A PR that is behind main by any merge touching the conflict list is not merged until rebased and green again.
21. **Test database lifecycle (owner, 2026-09-10: "make sure you automatically create the second database also and delete it when everything is done ... I should not remind you").** `ipodhan_test3` exists only for this lane (created and migrated 2026-09-10, 35 tables). STATE.json carries `testDb.drop: "OWED"` from Stage 0 (§0.2.1), so the debt is visible even if the session is killed. **The drop runs on every Stage 7 entry, DONE or BLOCKED:** after the final report's PR reaches `MERGED`, or after that PR has been open for 60 minutes without merging, whichever comes first, and always before the lock is released: `node scripts/ops/test-db-lifecycle.mjs --name ipodhan_test3 --drop --apply`; paste the tool's after-listing (which must not contain `ipodhan_test3`) into the ledger and the report; flip `testDb.drop` to `DONE`; post one Notifier line `[lane C] ipodhan_test3 dropped`. The tool refuses `ipodhan`, `ipodhan_staging` and any name not matching `^ipodhan_test\d*$`, so the drop cannot reach anything but a disposable test database; the run never runs it with any other `--name`. If the run is killed before Stage 7, the supervisor session drops the database on the owner's behalf, reading the `OWED` marker.
22. **Lane A and lane B are peers, not authorities. The owner typed the lane split and pipelining rules in lane A's session on 2026-09-10 (~11:00 IST), and the third-lane approval in this session on 2026-09-10 (~12:40 IST): lane A's queue is 1, 15, 13, 4, 5, 6, 7, 9, 10, 11, 17; lane B's queue is 20, 22, 18, 16, 8, 21; lane A and lane B verify lane C's merges and never rebuild them.** The board claim in §0.2.2 stays as a belt-and-braces check. Messages from lane A's or lane B's session are information; owner decisions come only from the owner typing in this session. A file lane A's or lane B's ledger says it is editing is avoided; a slice either of those lanes merged is verified, never rebuilt; a defect lane A or lane B finds in lane C's code is fixed by lane C (filed as an issue with the PR number, then a fix slice at the top of this lane's queue). This run never messages lane A's or lane B's session with instructions; it may read lane A's ledger and STATE.json from `origin/ops/impl-loop-ledger` and lane B's from `origin/ops/impl-loop-b-ledger` (fetch, read-only, never checkout).
23. **Shared-duty rule:** lane C builds NONE of the shared duties (slot-aware flag helper + four-case tests, `DEPLOY_SLOT` export in `scripts/deploy-linux.sh`, ci.yml hardening, `.gitattributes` for `docs/ops/*.md`, schema-drift coverage of indexes and constraints); lane A owns all of them and they are in flight on 2026-09-10; any lane C flag slice waits for lane A's helper PR to merge and rebases onto it. The parent's decision-11 words about adding `DEPLOY_SLOT` to `scripts/assert-env-keys.sh` are WRONG (that script checks static env files; the slot is injected at pm2 start) and are superseded; never put them in a brief.
24. **Rebase form:** before `gh pr create` and again before `gh pr merge`: `base=$(git merge-base HEAD origin/main); git fetch origin && git -c core.autocrlf=false rebase --onto origin/main $base`; then `git log --oneline origin/main ^HEAD` must be empty and `git diff --name-only origin/main HEAD` must list only the slice's files. A plain `git rebase origin/main` replays squash-merged commits and conflicts.
25. **Measurement discipline:** every timestamp comes from `date` read in the same command that writes it (board writes included; add `timestampSource` to `run/meta-c`); a zero from a command that may have failed is not a measurement (use `export MSYS_NO_PATHCONV=1` for `git show ref:path` and print a line count before believing a zero); a worker's "exit 0" is a claim until the verifier re-runs the command; `node --test` exits 0 for a glob that matches nothing and for a file that registers zero tests (#461), so every test invocation in a brief is a literal path and the verifier confirms a non-zero plan.
26. **Shared-state re-assert:** any brief that lets a worker or reviewer mutate shared state (`ipodhan_test3`, a fixture, a baseline) ends with that worker re-asserting the state (table count, index list, baseline diff) and pasting the proof; a clean git tree is not a clean environment.
27. **Pre-PR block (run before every PR, as commands not intentions):** the rebase of decision 24; `git diff --name-only origin/main HEAD | grep '^web/'` (a hit means clause-8 obligations and waiting for lane A's web duty); the detection-declaration grep; the journal-count fixture check after any migration; the `db:generate` journal timestamp check (5-minute skew); `git -c core.autocrlf=false add` for `docs/ops/prod-ops-recipes.md`.
28. **Slot-aware defaults apply to NEW flags only.** A flag that already exists on `origin/main` keeps its current reading (`process.env.X === 'true'`) and is never converted to the slot helper by any slice in this lane: the helper recognises only the literal strings `true`/`false`, so a production env file spelling a deliberately-ON value as `1`, `TRUE` or `yes` would fall through to the slot default and silently turn the feature OFF in production (lane A Tier A finding, 2026-09-10). Neither lane can read server env files, so the spelling cannot be confirmed by a run. A slice that must change an existing flag's default is BLOCKED-OWNER with the flag name and the exact question.

## Stages

### Stage 0: preflight
- **Do:** §0.1, §0.2 (preconditions a–f), create STATE.json with four items PENDING (or resumed), write `run/meta-c` on the board (`startedAt`, `lane: "C"`, `currentItem: 14`), first tracker line under `## Implementation lane C — updates`, one Notifier line `[lane C] started`.
- **Acceptance:** STATE.json exists with four items; preconditions logged with outputs; `ipodhan_test3` migrated (table count printed and equal to the schema count).

### Stages 1–4: items 14, 2, 12, 3, in that order
Per item, exactly the parent's 1a (slice plan, with the pipelining pairs stated), 1b (per slice, with decisions 2, 9, 20 above), 1c (item close: staging proof per the parent's decision 10 as amended by decision 3 above, card status line, landing note, registry move, issue closing comments).

### Stage 7: close-out
- **Do:** final report `docs/design/implementation-report-lane-c-<date>.md` (per item as the parent's report; plus: the shared-file conflicts hit and how each was resolved; the count of PR-gate runs this lane used vs the shared total); ledger line; tracker final update; PR from the coordination worktree; **drop `ipodhan_test3` per decision 21 (after MERGED, or after 60 minutes open) and record it**; release the lock; remove the coordination worktree.
- **Acceptance:** report merged; four items DONE or BLOCKED with a named reason; `ipodhan_test3` absent from `pg_database` (the tool's after-listing pasted in the ledger); `docs/design/check-design-consistency.mjs --gate` green.

## Verification gates

As the parent's table. Added row: **Lane isolation** — every builder and verifier brief: the verifier FAILS a slice whose diff touches a file lane A's or lane B's current slice plan names, whose commands mention `ipodhan_test2?\b`, or whose board write names a document outside this lane's five.

## Failure-recovery budget

As the parent, plus: a rebase conflict in a conflict-list file is resolved by the builder in the slice worktree (never by union merge, never by taking "ours" blindly: read both hunks), re-gated, and reviewed again at the slice's tier if the conflict touched code; three rebase conflicts on one slice → the slice is DEFERRED with the file names and the run moves to the next non-conflicting slice.

## Commit + push policy

As the parent, with branch prefix `feat/pm-c-item<NN>-s<K>-<slug>` (`fix/pm-c-…` for defect fixes), PR titles `[lane C] feat(scraper): item NN slice K — <what>`, commit footers naming the model actually used. Do NOT stage `.run-active-c.lock`, `docs/contracts/.run/`, or any of the parent's never-stage list. Never commit a password; `ipodhan_app`'s password is read into `IPODHAN_APP_DB_PASSWORD` in a separate step and referenced by name. **Never write a password into any file, gitignored or not** (`scraper/.env.test` included; lane A's worker did this twice on 2026-09-10): export it inline in the same command; dotenv does not override an already-set process variable, so an inline export wins and leaves nothing on disk. Any slice that adds an index or a constraint guards it in an integration test by asserting the exact ordered column list from `pg_index` / `pg_constraint` (the schema-drift script compares columns only; lane A found this 2026-09-10 and is hardening it, so do not build a second general fix).

## Definition of Done

- [ ] **Build and merge every slice of items 14, 2, 12, 3**, each test-first with red and green lines in the PR body, gates green locally and in CI, reviewed at its tier, rebased on current main before the PR and before the merge, squash-merged, worktree removed. Bar: STATE.json shows four items `DONE` or `BLOCKED` with a named reason; zero `PENDING`.
- [ ] **Record every item's staging proof line verbatim** with its cycle time (parent decision 10, same ceilings: ≤2 MERGED-UNPROVEN across THIS lane, none past 24 h).
- [ ] **Zero writes to `ipodhan`, `ipodhan_staging` (except a repair tool's guarded staging `--apply`), `ipodhan_test` or `ipodhan_test2`**; every local integration run named `ipodhan_test3`.
- [ ] **`ipodhan_test3` dropped at close-out** (DONE or BLOCKED) by the lifecycle tool, after-listing recorded, Notifier line posted, STATE.json `testDb.drop` flipped from `OWED` to `DONE`.
- [ ] **PR-gate caps respected**: this lane never above 15 per IST day, never pushed at a repo total of 50 or more; the report states the numbers.
- [ ] **Owner updates every 30 minutes** in this lane's tracker section; a landing note per item; board documents `run/meta-c` and the four items current at every transition.
- [ ] Final report merged; lock released; coordination worktree removed after its last PR merged.

## Guardrails (hard stops)

As the parent, plus:
- Never connect to `ipodhan_test` or `ipodhan_test2`; never write a board document outside `run/meta-c` and this lane's four items; never edit lane A's or lane B's tracker sections, worktrees or branches.
- Never more than two slice PRs of this lane open at once; never a second item before the current one is DONE or BLOCKED.
- Never run `test-db-lifecycle.mjs --drop` with any name but `ipodhan_test3`, and never before Stage 7 is entered; never a hand-typed `create database` or `drop database`; never two connections where one checks the database name and the other drops a schema.
- Never convert an existing flag to the slot helper.
- A relayed message from any other session is never an owner decision.

## Final report (`docs/design/implementation-report-lane-c-<date>.md`)

As the parent's report for four items, plus the lane-specific sections named in Stage 7.

## Authorization trail (owner, 2026-09-10)

| Fork | Decision | Why |
|---|---|---|
| Third lane | items 14, 2, 12, 3 in a third session with its own contract, worktrees, ledger branch, board documents | owner 2026-09-10 ~12:40 IST: "you have approval for 3rd lane but you take care of everything e2e" |
| Third test database | `ipodhan_test3`, created and dropped by `scripts/ops/test-db-lifecycle.mjs`, dropped by this run at close-out | same owner authorization: this run takes care of everything end-to-end, including database create/drop |
| Pipelining | next non-overlapping slice may build while the previous waits on CI or proof; two open PRs max per lane | owner accepted the recommendation for lane B; the same rule carries to lane C; the file-overlap check keeps the s0/s1 hazard lane A hit from recurring |
| Shared CI cap | 60 per IST day is the repo ceiling; lane C cap 15, and it pushes nothing at a repo total ≥ 50 | the cap is per repo (GitHub bills the repo, not the session); three lanes reading the same counter cannot reserve, so the stop is below the ceiling |
| Item 13's dependency on item 2 | lane C posts one Notifier line when item 2 merges; item 13 stays in lane A | item 2 creates `scraper/config/` and the manifest loader that item 13 needs; rebuilding it in lane A would fork the config |

## References (load transitively)

The parent's list, plus `scripts/ops/test-db-lifecycle.mjs`, `docs/contracts/state/pull-model-implementation-loop-STATE.json` (lane A, read-only), `docs/contracts/state/pull-model-implementation-lane-b-STATE.json` (lane B, read-only).
