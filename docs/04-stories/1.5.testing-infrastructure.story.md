# Story 1.5: Testing Infrastructure

## Status
Done

## Story
**As a** developer,
**I want** Vitest and Playwright configured with sample tests,
**so that** I can write and run unit and E2E tests for the application.

## Acceptance Criteria
1. Vitest installed and configured
2. @testing-library/react installed
3. At least 1 unit test passes
4. Playwright installed and configured
5. At least 1 E2E test passes
6. Coverage reporting works
7. Test scripts in package.json work

## Tasks / Subtasks
- [x] Install Vitest and @vitest/ui (AC: 1)
- [x] Install React Testing Library (@testing-library/react, @testing-library/jest-dom, @testing-library/user-event) (AC: 2)
- [x] Configure Vitest: Create vitest.config.ts (AC: 1)
- [x] Create test setup file (AC: 1)
- [x] Add test scripts to package.json (AC: 7)
  - [x] `test:unit` - Vitest
  - [x] `test:coverage` - Vitest with coverage
  - [x] `test:e2e` - Playwright
- [x] Write first unit test (sample component test) (AC: 3)
- [x] Verify unit test passes (AC: 3)
- [x] Install Playwright (AC: 4)
- [x] Initialize Playwright browsers (AC: 4)
- [x] Configure Playwright: Create playwright.config.ts (AC: 4)
- [x] Write first E2E test (homepage loads) (AC: 5)
- [x] Verify E2E test passes (AC: 5)
- [x] Configure coverage reporting (AC: 6)

## Dev Notes

### Vitest Configuration
- Location: `web/vitest.config.ts`
- Test environment: jsdom
- Coverage provider: v8
- Path aliases: `@/` → `./`

### Playwright Configuration
- Location: `web/playwright.config.ts`
- Browsers: chromium, firefox, webkit
- Web server: auto-start on port 3000
- Parallel workers: 6

### Test Structure
```
web/tests/
├── unit/
│   └── sample.test.tsx (Button component tests)
└── e2e/
    └── homepage.spec.ts (Homepage validation)
```

### Dependencies Installed
```json
{
  "vitest": "3.2.4",
  "@vitest/ui": "3.2.4",
  "@vitest/coverage-v8": "3.2.4",
  "@testing-library/react": "16.3.0",
  "@testing-library/jest-dom": "6.9.1",
  "@testing-library/user-event": "14.6.1",
  "@playwright/test": "1.55.1"
}
```

### Test Scripts
```json
{
  "test:unit": "vitest",
  "test:coverage": "vitest --coverage",
  "test:e2e": "playwright test"
}
```

### Testing Standards
- Unit test location: `web/tests/unit/`
- E2E test location: `web/tests/e2e/`
- Test file naming: `*.test.tsx` (unit), `*.spec.ts` (E2E)
- Coverage baseline: 5.5% (expected for infrastructure sprint)

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-10-05 | 1.0 | Story completed and validated | Devin (Dev Agent) |

## Dev Agent Record

### Agent Model Used
Claude Code (Sonnet 4.5)

### Completion Notes
- All testing frameworks configured successfully
- 3 unit tests passing (Button component)
- 9 E2E tests passing (Homepage across 3 browsers)
- Issue fixed: E2E test timeout due to multiple dev servers on port 3000
- Coverage reporting operational
- All test scripts working

### File List
- `web/vitest.config.ts` (created)
- `web/vitest.setup.ts` (created)
- `web/playwright.config.ts` (created)
- `web/tests/unit/sample.test.tsx` (created)
- `web/tests/e2e/homepage.spec.ts` (created)
- `web/package.json` (modified - test dependencies and scripts added)

## QA Results
**Status:** ✅ PASSED
**QA Date:** 2025-10-05
**Report:** `docs/06-qa-reports/sprint-reports/story-1.5-qa-report.md`

**Summary:**
- All 7 acceptance criteria met
- 12/12 tests passing (100% pass rate)
- High severity issue fixed (port conflict for E2E tests)
- Coverage reporting operational
- Production ready
