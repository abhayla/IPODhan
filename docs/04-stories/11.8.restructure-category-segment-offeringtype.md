# Story 11.8: Restructure Category Field into Segment + Offering Type

## Metadata

- **Story ID**: 11.8
- **Epic**: Epic 11 - Feature Enhancements & Data Quality Improvements
- **Priority**: P0 - CRITICAL (Breaking Change)
- **Story Points**: 13
- **Created**: 2025-10-19
- **Status**: Ready
- **Approved By**: Sarah (Product Owner)
- **Approved Date**: 2025-10-19
- **Readiness Score**: 9.2/10 (EXCELLENT)
- **Workflow**: automated-story-creation-workflow v2.0
- **SM Agent**: Bob (Scrum Master)
- **PO Agent**: Sarah (Product Owner)
- **Validation Iterations**: 0 (Approved on first review)
- **Workflow Version**: 2.0
- **Source**: Planning document `docs/01-planning/segment-offeringType-change.md`

---

## Status

**Ready**

---

## Story

**As a** platform developer and data architect,
**I want** to restructure the single `category` field into two separate fields (`segment` and `offeringType`),
**so that** the data model accurately represents the distinction between exchange segment (MAINBOARD/SME) and offering type (IPO/RIGHTS/TENDER/etc.), eliminating duplicate IPO listings and enabling proper filtering of corporate actions like TENDER offers and BUYBACK transactions.

---

## Business Problem

**Current Issue:**
Searching for "3i Infotech" on the dashboard shows **two IPO cards** for the same company:
1. "3i Infotech Limited" (Symbol: `3IINFOLTDR`) - NSE, 1-day IPO, no price
2. "3I INFOTECH LTD" (Symbol: `3IINFOTECHLTD`) - BSE, 20-day IPO, ₹17 price

**Root Cause:**
The current `category` field mixes two distinct concepts:
```typescript
category: 'MAINBOARD' | 'SME' | 'RIGHTS' | 'NCD' | 'FPO'
```

This conflates:
- **Exchange Segment** (where it's listed): MAINBOARD, SME
- **Offering Type** (what it is): IPO, FPO, RIGHTS, NCD, TENDER, BUYBACK, etc.

The "TDR" suffix in `3IINFOLTDR` indicates this is a **TENDER offer** (likely buyback/delisting), not an IPO. Both entries are categorized as "MAINBOARD" with no way to distinguish them.

**Expected Outcome:**
- **Before**: "3i Infotech" shows 2 cards (IPO + TENDER)
- **After**: Default view shows only IPO card, TENDER hidden unless explicitly filtered

---

## Acceptance Criteria

### 1. **AC1: Database Schema Migration**
- New PostgreSQL enums created: `segment` (MAINBOARD, SME) and `offering_type` (15 values)
- Old `ipo_category` enum and `category` column removed
- New columns `segment` and `offering_type` added as NOT NULL
- Data migration: Existing records migrated from `category` to new fields
- TENDER offers detected from symbol suffix (e.g., `%TDR`, `%TENDER`)
- Indexes created: `idx_ipos_segment`, `idx_ipos_offering_type`, `idx_ipos_segment_offering_type`
- Migration file: `web/drizzle/migrations/0015_restructure_category.sql`
- All 313 existing IPOs migrate successfully with zero data loss

### 2. **AC2: TypeScript Code Updates**
- Schema file (`packages/shared/src/db/schema.ts`) updated with new enums and fields
- Repository (`ipo-repository.ts`) updated to filter by both `segment` and `offeringType`
- API routes (`app/api/ipos/route.ts`) accept both query parameters with Zod validation
- Services updated to pass both fields when querying IPOs
- API client (`lib/api-client.ts`) updated with new parameter types
- Cache keys updated to include both fields for proper invalidation
- Type safety maintained: All TypeScript code compiles with zero errors

### 3. **AC3: Scraper Updates**
- Offering type detection utility created: `scraper/src/utils/detect-offering-type.ts`
- NSE scraper detects offering type from symbol suffix (TDR, BUYBACK, DELISTING patterns)
- BSE scraper detects offering type from BSE data
- Chittorgarh scraper sets appropriate segment and offering type for historical data
- Segment detection logic: MAINBOARD vs SME based on exchange metadata
- All scrapers successfully persist IPOs with new fields

### 4. **AC4: UI Component Updates**
- Dashboard filters support both segment and offering type dropdowns
- IPO cards display both segment and offering type badges
- Default dashboard filter: Show only IPO and FPO offerings (hide TENDER, BUYBACK, DELISTING by default)
- URL parameters updated to use `segment` and `offeringType`
- No regressions: All existing UI functionality works with new fields

### 5. **AC5: Test Coverage**
- All test fixtures updated to use `segment` + `offeringType` instead of `category`
- Repository tests cover new filtering combinations
- API tests validate new query parameter schemas
- Unit tests verify offering type detection logic
- Integration tests confirm end-to-end functionality
- Test suite passes: 100% success rate (zero failing tests)

---

## Tasks / Subtasks

### Phase 1: Database Schema Changes

#### Task 1: Update Drizzle Schema (AC: 1)
- [ ] **Subtask 1.1**: Remove `ipoCategoryEnum` from `packages/shared/src/db/schema.ts`
  - Delete enum definition: `export const ipoCategoryEnum = pgEnum('ipo_category', [...])`
  - Remove from ipos table definition
  - Document reason: "Conflates exchange segment with offering type"
- [ ] **Subtask 1.2**: Add `segmentEnum` to schema
  - Create enum: `export const segmentEnum = pgEnum('segment', ['MAINBOARD', 'SME'])`
  - Add to ipos table: `segment: segmentEnum('segment').notNull()`
  - Location: After other enums, before ipos table definition
- [ ] **Subtask 1.3**: Add `offeringTypeEnum` to schema
  - Create enum with 15 values: IPO, FPO, RIGHTS, OFS, IPP, QIP, PREFERENTIAL, NCD, BONDS, INVITS, REITS, BUYBACK, DELISTING, TENDER
  - Add to ipos table: `offeringType: offeringTypeEnum('offering_type').notNull()`
  - Document each type with inline comments
- [ ] **Subtask 1.4**: Rebuild shared package
  - Run: `cd packages/shared && npm run build`
  - Verify: No TypeScript compilation errors
  - Confirm: Type exports updated in dist/

#### Task 2: Generate Database Migration (AC: 1)
- [ ] **Subtask 2.1**: Generate migration file
  - Run: `cd web && npm run db:generate`
  - Name: "restructure_category_to_segment_offering_type"
  - Review generated SQL for correctness
- [ ] **Subtask 2.2**: Enhance generated migration with data migration logic
  - Add SQL for data migration (see planning document section "Phase 1: Schema Changes")
  - Map MAINBOARD → segment: MAINBOARD, offeringType: IPO
  - Map SME → segment: SME, offeringType: IPO
  - Map RIGHTS → segment: MAINBOARD, offeringType: RIGHTS
  - Map NCD → segment: MAINBOARD, offeringType: NCD
  - Map FPO → segment: MAINBOARD, offeringType: FPO
  - Detect TENDER from symbol: WHERE symbol LIKE '%TDR' OR symbol LIKE '%TENDER'
- [ ] **Subtask 2.3**: Add indexes to migration
  - `CREATE INDEX idx_ipos_segment ON ipos(segment);`
  - `CREATE INDEX idx_ipos_offering_type ON ipos(offering_type);`
  - `CREATE INDEX idx_ipos_segment_offering_type ON ipos(segment, offering_type);`
- [ ] **Subtask 2.4**: Add rollback instructions in migration comments
  - Document how to revert if issues occur
  - Include backup restoration procedure

#### Task 3: Apply and Verify Migration (AC: 1)
- [ ] **Subtask 3.1**: Backup database before migration
  - Run: `pg_dump -U postgres -d ipodhan > backup_pre_category_restructure.sql`
  - Verify backup file size > 0
  - Store backup securely with timestamp
- [ ] **Subtask 3.2**: Apply migration to local database
  - Run: `cd web && npm run db:migrate`
  - Monitor output for errors
  - Verify: "Migration applied successfully"
- [ ] **Subtask 3.3**: Verify data migration with SQL queries
  - Query: `SELECT segment, offering_type, COUNT(*) FROM ipos GROUP BY segment, offering_type;`
  - Expected: MAINBOARD + IPO (majority), SME + IPO, MAINBOARD + RIGHTS, MAINBOARD + NCD, MAINBOARD + TENDER
  - Verify: Total count matches pre-migration count (313 IPOs)
- [ ] **Subtask 3.4**: Verify indexes created
  - Query: `SELECT indexname FROM pg_indexes WHERE tablename = 'ipos';`
  - Confirm: Three new indexes present
- [ ] **Subtask 3.5**: Visual verification with Drizzle Studio
  - Run: `npm run db:studio`
  - Open ipos table
  - Verify: `segment` and `offeringType` columns visible and populated

---

### Phase 2: Update TypeScript Code

#### Task 4: Update Repositories (AC: 2)
- [ ] **Subtask 4.1**: Update IPORepository filters
  - File: `web/lib/repositories/ipo-repository.ts`
  - Replace `category` parameter with `segment` and `offeringType` in `findAll()` method
  - Support array filters: `segment?: string | string[]`, `offeringType?: string | string[]`
  - Use `inArray()` for array filters, `eq()` for single values
  - Update type annotations to use new enums from schema
- [ ] **Subtask 4.2**: Update cache keys
  - File: `web/lib/cache/cache-keys.ts`
  - Update `getIPOListKey()` to include both segment and offeringType
  - Pattern: `ipo:list:segment={segment}:offeringType={offeringType}:status={status}`
  - Update `getIPOBySlugKey()` if needed (likely unchanged)
- [ ] **Subtask 4.3**: Update repository queries
  - Update all `where` clauses replacing `category` with `segment` and `offeringType`
  - Update method signatures: `findAll(filters: { segment?: string | string[], offeringType?: string | string[], ... })`
  - Ensure backward compatibility: Handle missing parameters gracefully
- [ ] **Subtask 4.4**: Update repository tests
  - File: `web/tests/unit/repositories/ipo-repository.test.ts`
  - Replace all `category` references with `segment` and `offeringType`
  - Add tests for combined filtering: segment=MAINBOARD + offeringType=IPO
  - Add tests for array filters: offeringType=['IPO', 'FPO']

#### Task 5: Update API Routes (AC: 2)
- [ ] **Subtask 5.1**: Update Zod validation schemas
  - File: `web/app/api/ipos/route.ts`
  - Remove: `const IPOCategorySchema = z.enum(['MAINBOARD', 'SME', 'RIGHTS', 'NCD', 'FPO'])`
  - Add: `const SegmentSchema = z.enum(['MAINBOARD', 'SME'])`
  - Add: `const OfferingTypeSchema = z.enum(['IPO', 'FPO', 'RIGHTS', 'OFS', 'IPP', 'QIP', 'PREFERENTIAL', 'NCD', 'BONDS', 'INVITS', 'REITS', 'BUYBACK', 'DELISTING', 'TENDER'])`
  - Update QueryParamsSchema: Replace `category` with `segment` and `offeringType`
- [ ] **Subtask 5.2**: Update query parameter parsing
  - Extract `segment` from searchParams
  - Extract `offeringType` from searchParams
  - Support array format: `offeringType=IPO,FPO` → `['IPO', 'FPO']`
  - Remove `category` extraction
- [ ] **Subtask 5.3**: Update API route tests
  - File: `web/tests/integration/api/ipos.test.ts`
  - Test: `GET /api/ipos?segment=MAINBOARD` returns MAINBOARD IPOs
  - Test: `GET /api/ipos?offeringType=IPO` returns only IPOs (no TENDER)
  - Test: `GET /api/ipos?segment=MAINBOARD&offeringType=IPO` returns filtered correctly
  - Test: `GET /api/ipos?offeringType=IPO,FPO` supports array filters

#### Task 6: Update Services (AC: 2)
- [ ] **Subtask 6.1**: Update mainboard-landing-service.ts
  - Replace: `category: 'MAINBOARD'` with `segment: 'MAINBOARD', offeringType: 'IPO'`
  - Verify: Service returns only MAINBOARD IPOs (no TENDER offers)
- [ ] **Subtask 6.2**: Update sme-landing-service.ts
  - Replace: `category: 'SME'` with `segment: 'SME', offeringType: 'IPO'`
  - Verify: Service returns only SME IPOs
- [ ] **Subtask 6.3**: Update other services
  - Check: ofs-service.ts, rights-service.ts, any other services using category
  - Update: Replace `category` with appropriate `segment` + `offeringType` combination
- [ ] **Subtask 6.4**: Update service tests
  - File: `web/tests/unit/services/*.test.ts`
  - Update all mock data to use new fields
  - Verify service queries work correctly with new filters

#### Task 7: Update API Client (AC: 2)
- [ ] **Subtask 7.1**: Update GetIPOsParams interface
  - File: `web/lib/api-client.ts`
  - Remove: `category?: 'MAINBOARD' | 'SME' | 'RIGHTS' | 'NCD' | 'OFS'`
  - Add: `segment?: 'MAINBOARD' | 'SME'`
  - Add: `offeringType?: 'IPO' | 'FPO' | 'RIGHTS' | 'OFS' | 'TENDER' | ...` (all 15 types)
  - Support array types: `offeringType?: string | string[]`
- [ ] **Subtask 7.2**: Update getIPOs function
  - Remove: `if (params.category) searchParams.append('category', params.category);`
  - Add: `if (params.segment) searchParams.append('segment', params.segment);`
  - Add: `if (params.offeringType) { ... }` with array support
  - Handle array serialization: `['IPO', 'FPO']` → `"IPO,FPO"`
- [ ] **Subtask 7.3**: Update client tests
  - File: `web/tests/unit/lib/api-client.test.ts`
  - Test URL generation with new parameters
  - Verify array parameters serialize correctly

---

### Phase 3: Update Scrapers

#### Task 8: Create Offering Type Detection Utility (AC: 3)
- [ ] **Subtask 8.1**: Create detection utility file
  - File: `scraper/src/utils/detect-offering-type.ts`
  - Export function: `detectOfferingTypeFromSymbol(symbol: string): string`
  - Export function: `detectSegmentFromExchange(exchanges: string[]): string`
- [ ] **Subtask 8.2**: Implement symbol-based detection
  - If symbol ends with 'TDR' or contains 'TENDER' → return 'TENDER'
  - If symbol contains 'BUYBACK' → return 'BUYBACK'
  - If symbol contains 'DELISTING' → return 'DELISTING'
  - Default → return 'IPO'
  - Add comprehensive test cases covering edge cases
- [ ] **Subtask 8.3**: Implement segment detection
  - Check exchange array for 'SME' platform → return 'SME'
  - Default → return 'MAINBOARD'
  - Consider BSE platform codes: MAIN vs SME
- [ ] **Subtask 8.4**: Write unit tests for detection logic
  - File: `scraper/tests/unit/utils/detect-offering-type.test.ts`
  - Test: Symbol '3IINFOLTDR' → 'TENDER'
  - Test: Symbol 'ACMELTD' → 'IPO'
  - Test: Symbol 'COMPANYNAME-BUYBACK' → 'BUYBACK'
  - Test: Exchange ['NSE', 'BSE-MAIN'] → 'MAINBOARD'
  - Test: Exchange ['BSE-SME'] → 'SME'

#### Task 9: Update NSE Scraper (AC: 3)
- [ ] **Subtask 9.1**: Import detection utilities
  - File: `scraper/src/scrapers/nse-scraper.ts`
  - Import: `detectOfferingTypeFromSymbol`, `detectSegmentFromExchange`
- [ ] **Subtask 9.2**: Update IPO data object construction
  - Replace: `category: 'MAINBOARD'` (or similar hardcoded value)
  - Add: `segment: detectSegmentFromExchange(listingExchanges)`
  - Add: `offeringType: detectOfferingTypeFromSymbol(symbol)`
- [ ] **Subtask 9.3**: Test NSE scraper with new fields
  - Run: `cd scraper && npm run start:nse`
  - Verify: Scraped IPOs have both segment and offeringType
  - Check logs: No database insertion errors
- [ ] **Subtask 9.4**: Update NSE scraper tests
  - File: `scraper/tests/integration/nse-scraper.test.ts`
  - Update mock data to include segment and offeringType
  - Verify scraper output has new fields

#### Task 10: Update BSE Scraper (AC: 3)
- [ ] **Subtask 10.1**: Update BSE detail scraper
  - File: `scraper/src/scrapers/bse-detail-scraper.ts`
  - Import detection utilities
  - Add segment and offeringType to extracted data
  - Detect segment from BSE platform metadata
- [ ] **Subtask 10.2**: Update BSE listing scraper
  - File: `scraper/src/scrapers/bse-scraper.ts`
  - Ensure listing scraper passes platform data for segment detection
  - Apply detection utilities during data transformation
- [ ] **Subtask 10.3**: Test BSE scraper with new fields
  - Run: `cd scraper && npm run start:bse`
  - Verify: All BSE IPO types (MAINBOARD, SME, RIGHTS, NCD) have correct fields
  - Special verification: Rights Issues have offeringType='RIGHTS'
- [ ] **Subtask 10.4**: Update BSE scraper tests
  - File: `scraper/tests/integration/bse-scraper.test.ts`
  - Update test fixtures for all IPO types
  - Verify Rights Issues and Debt Issues set offeringType correctly

#### Task 11: Update Chittorgarh Scraper (AC: 3)
- [ ] **Subtask 11.1**: Update historical data scraper
  - File: `scraper/src/scrapers/chittorgarh-historical-scraper.ts`
  - Set segment based on historical IPO metadata
  - Set offeringType based on IPO type classification
  - Default: segment='MAINBOARD', offeringType='IPO' for historical data
- [ ] **Subtask 11.2**: Update Chittorgarh Rights/NCD scrapers
  - Files: `chittorgarh-rights-scraper.ts`, `chittorgarh-ncd-scraper.ts`
  - Set appropriate offeringType: 'RIGHTS' or 'NCD'
  - Set segment based on exchange metadata
- [ ] **Subtask 11.3**: Test Chittorgarh scrapers
  - Run individual scrapers: `npm run start:chittorgarh`
  - Verify: Historical IPOs have correct segment and offeringType
  - Check: No data corruption in existing records

---

### Phase 4: Update UI Components

#### Task 12: Update Dashboard Filters (AC: 4)
- [ ] **Subtask 12.1**: Update DashboardFilters component
  - File: `web/components/dashboard/DashboardFilters.tsx`
  - Remove: Category dropdown (single select)
  - Add: Segment dropdown (MAINBOARD | SME)
  - Add: Offering Type multi-select (IPO, FPO, RIGHTS, OFS, TENDER, etc.)
  - Update state management: Replace `category` state with `segment` and `offeringType`
- [ ] **Subtask 12.2**: Update filter change handlers
  - Add: `handleSegmentChange(segment: string)`
  - Add: `handleOfferingTypeChange(types: string[])`
  - Remove: `handleCategoryChange(category: string)`
  - Update URL params: Use `searchParams.set('segment', ...)` and `searchParams.set('offeringType', ...)`
- [ ] **Subtask 12.3**: Set default offering type filter
  - Default: `offeringType=['IPO', 'FPO']` (hide TENDER, BUYBACK, DELISTING by default)
  - Allow users to clear filter to see all types
  - Add "Show All Types" checkbox
- [ ] **Subtask 12.4**: Update filter UI/UX
  - Use shadcn/ui Select component for segment
  - Use shadcn/ui Multi-Select or Checkbox group for offering types
  - Add labels: "Exchange Segment" and "Offering Type"
  - Add tooltips explaining each offering type

#### Task 13: Update IPO Cards (AC: 4)
- [ ] **Subtask 13.1**: Update IPOCard component
  - File: `web/components/dashboard/IPOCard.tsx`
  - Replace: `<Badge>{ipo.category}</Badge>`
  - Add: `<Badge>{ipo.segment}</Badge>`
  - Add: `<Badge variant="secondary">{ipo.offeringType}</Badge>`
  - Update badge styling: Different colors for segment vs offeringType
- [ ] **Subtask 13.2**: Update card TypeScript types
  - Update IPOCardProps interface to use segment + offeringType
  - Remove category field from types
  - Ensure type safety throughout component
- [ ] **Subtask 13.3**: Update card tests
  - File: `web/components/dashboard/__tests__/IPOCard.test.tsx`
  - Update mock data to use new fields
  - Test: Segment badge renders correctly
  - Test: Offering type badge renders correctly

#### Task 14: Update Dashboard Pages (AC: 4)
- [ ] **Subtask 14.1**: Update main dashboard page
  - File: `web/app/dashboard/page.tsx`
  - Extract segment and offeringType from searchParams
  - Default offeringType to ['IPO', 'FPO'] if not provided
  - Pass both parameters to API client
- [ ] **Subtask 14.2**: Update mainboard IPOs page
  - File: `web/app/mainboard-ipos/page.tsx`
  - Set: `segment='MAINBOARD', offeringType='IPO'` (or user-selected)
  - Update page metadata and titles
- [ ] **Subtask 14.3**: Update SME IPOs page
  - File: `web/app/sme-ipos/page.tsx`
  - Set: `segment='SME', offeringType='IPO'`
  - Update page metadata and titles
- [ ] **Subtask 14.4**: Update URL parameter handling
  - Support array format: `?offeringType=IPO,FPO`
  - Parse comma-separated values into array
  - Maintain filter state across page navigation

#### Task 15: Update Static Pages (AC: 4)
- [ ] **Subtask 15.1**: Update IPO detail page
  - File: `web/app/ipos/[slug]/page.tsx`
  - Display both segment and offering type in IPO details section
  - Update breadcrumb: Show segment and offering type
- [ ] **Subtask 15.2**: Update landing pages
  - Files: Various landing page components
  - Update copy to reflect new terminology
  - Ensure landing pages use correct segment + offeringType filters
- [ ] **Subtask 15.3**: Update footer and navigation
  - Update link filters to use new parameters
  - Example: "MAINBOARD IPOs" link → `?segment=MAINBOARD&offeringType=IPO`

---

### Phase 5: Update Tests

#### Task 16: Update Test Fixtures (AC: 5)
- [ ] **Subtask 16.1**: Update mockIPO helper function
  - File: `web/lib/db/types.ts` (or test utilities)
  - Replace: `category: 'MAINBOARD'` in default mock
  - Add: `segment: 'MAINBOARD', offeringType: 'IPO'` in default mock
  - Support parameterization: Allow tests to specify custom values
- [ ] **Subtask 16.2**: Update all test fixtures in web/tests/
  - Search for all instances of `category:` in test files
  - Replace with `segment:` and `offeringType:`
  - Verify: Each test uses appropriate values (not just blindly replacing)
- [ ] **Subtask 16.3**: Update test fixtures in scraper/tests/
  - Search scraper test files for category references
  - Update mock scraped data to include new fields
  - Ensure scraper tests validate new fields

#### Task 17: Update Repository Tests (AC: 5)
- [ ] **Subtask 17.1**: Update IPORepository test suite
  - File: `web/tests/unit/repositories/ipo-repository.test.ts`
  - Add test: Filter by segment only
  - Add test: Filter by offeringType only
  - Add test: Filter by both segment and offeringType
  - Add test: Filter by multiple offeringTypes (array)
  - Add test: Default behavior (no filters)
- [ ] **Subtask 17.2**: Update SubscriptionRepository tests
  - Update related IPO data in subscription tests
  - Verify: Foreign key relationships still work
- [ ] **Subtask 17.3**: Run repository test suite
  - Run: `npm run test:unit -- repositories`
  - Verify: 100% pass rate
  - Fix any failing tests

#### Task 18: Update API Tests (AC: 5)
- [ ] **Subtask 18.1**: Update API route tests
  - File: `web/tests/integration/api/ipos.test.ts`
  - Test new query parameter validation (segment, offeringType)
  - Test invalid values return 400 Bad Request
  - Test array parameters work correctly
- [ ] **Subtask 18.2**: Update API client tests
  - Test API client correctly constructs URLs with new parameters
  - Test array serialization
- [ ] **Subtask 18.3**: Run API test suite
  - Run: `npm run test:integration`
  - Verify: All API tests pass

#### Task 19: Update E2E Tests (AC: 5)
- [ ] **Subtask 19.1**: Update E2E filter tests
  - File: `web/tests/e2e/dashboard-filters.spec.ts`
  - Test: Segment filter dropdown works
  - Test: Offering type multi-select works
  - Test: Default filter hides TENDER offers
  - Test: URL parameters update correctly
- [ ] **Subtask 19.2**: Update E2E IPO detail tests
  - Verify: IPO detail page displays segment and offeringType badges
  - Test: Breadcrumb navigation reflects new fields
- [ ] **Subtask 19.3**: Run full E2E test suite
  - Run: `npm run test:e2e`
  - Verify: All E2E tests pass
  - Fix any regressions

#### Task 20: Run Full Test Suite (AC: 5)
- [ ] **Subtask 20.1**: Run all unit tests
  - Run: `npm run test:unit`
  - Expected: 100% pass rate
  - Fix any failing tests
- [ ] **Subtask 20.2**: Run all integration tests
  - Run: `npm run test:integration`
  - Expected: 100% pass rate
  - Fix any failing tests
- [ ] **Subtask 20.3**: Run all E2E tests
  - Run: `npm run test:e2e`
  - Expected: 100% pass rate
  - Fix any failing tests
- [ ] **Subtask 20.4**: Generate test coverage report
  - Run: `npm run test:coverage`
  - Verify: Coverage remains above 80% threshold
  - Document any coverage gaps

---

### Phase 6: Documentation and Deployment

#### Task 21: Update Documentation (Not in AC, but required)
- [ ] **Subtask 21.1**: Update data models documentation
  - File: `docs/02-architecture/data-models.md`
  - Replace category with segment + offeringType
  - Update TypeScript interface examples
- [ ] **Subtask 21.2**: Update API documentation
  - File: `docs/02-architecture/rest-api-spec.md` (if exists)
  - Document new query parameters
  - Deprecate old category parameter
- [ ] **Subtask 21.3**: Update README and developer guides
  - Document breaking change in CHANGELOG
  - Update migration guide for developers
  - Add examples of new filter usage
- [ ] **Subtask 21.4**: Update planning document status
  - File: `docs/01-planning/segment-offeringType-change.md`
  - Set status to "IMPLEMENTED"
  - Add implementation date and commit hash

#### Task 22: Staging Deployment and Validation (Not in AC, but required)
- [ ] **Subtask 22.1**: Deploy to staging environment
  - Run database migration on staging
  - Deploy application code to staging
  - Verify: Application starts without errors
- [ ] **Subtask 22.2**: Smoke test on staging
  - Test: Dashboard loads and displays IPOs
  - Test: Filters work correctly (segment + offeringType)
  - Test: IPO detail pages display new badges
  - Test: Search for "3i Infotech" shows only 1 card (IPO, not TENDER)
- [ ] **Subtask 22.3**: Verify data integrity on staging
  - Query database: Verify all IPOs have segment and offeringType
  - Verify: No null values in new fields
  - Verify: TENDER offers correctly identified
- [ ] **Subtask 22.4**: Performance testing
  - Verify: Page load times remain <2s
  - Verify: API response times remain <500ms
  - Verify: Cache invalidation works correctly

---

## Dev Notes

### Context from Planning Document

This story implements a **CRITICAL P0 breaking change** based on `docs/01-planning/segment-offeringType-change.md`. The planning document provides comprehensive implementation details across 5 phases.

**Key Technical Details:**

1. **Database Schema (Phase 1)**:
   - Migration file pattern: `web/drizzle/migrations/0015_restructure_category.sql`
   - Data migration SQL provided in planning document (lines 100-148)
   - Special handling for TENDER detection: `symbol LIKE '%TDR'`

2. **TypeScript Code (Phase 2)**:
   - Schema location: `packages/shared/src/db/schema.ts` (single source of truth)
   - Repository pattern: `web/lib/repositories/ipo-repository.ts` extends BaseRepository
   - Cache keys: `web/lib/cache/cache-keys.ts` must include both fields

3. **Scrapers (Phase 3)**:
   - Detection utility: New file `scraper/src/utils/detect-offering-type.ts`
   - NSE scraper: `scraper/src/scrapers/nse-scraper.ts`
   - BSE scraper: `scraper/src/scrapers/bse-detail-scraper.ts`

4. **UI Components (Phase 4)**:
   - Dashboard filters: `web/components/dashboard/DashboardFilters.tsx`
   - IPO cards: `web/components/dashboard/IPOCard.tsx`
   - Default filter: Show only IPO and FPO (hide TENDER by default)

5. **Testing (Phase 5)**:
   - Update all fixtures: Replace `category` with `segment` + `offeringType`
   - Repository tests: Test combined filtering
   - E2E tests: Verify "3i Infotech" duplicate issue resolved

### Database Schema Architecture (CRITICAL)

**Source:** `CLAUDE.md` and `docs/16-database/SCHEMA_MANAGEMENT.md`

**⚠️ Single Source of Truth:**
- All database schema is defined in `packages/shared/src/db/schema.ts`
- This file contains 13 tables, all enums, and Drizzle relations
- **DO NOT modify** schema outside of the shared package
- Schema is re-exported through `web/lib/db/index.ts` for compatibility

**Schema Management Workflow (MUST FOLLOW):**
1. Edit schema: `packages/shared/src/db/schema.ts`
2. Rebuild shared: `cd packages/shared && npm run build`
3. Generate migration: `cd web && npm run db:generate`
4. Review SQL: Check generated migration file
5. Apply migration: `npm run db:migrate`
6. Verify: `npm run db:studio` or `npm run verify:seed`

**Import Pattern:**
```typescript
// ✅ Correct: Import from @/lib/db (re-exports from shared)
import { ipos, segmentEnum, offeringTypeEnum } from '@/lib/db';

// ✅ Correct: Direct import from shared (for repositories)
import * as schema from '@ipodhan/shared/db/schema';

// ❌ Wrong: This file doesn't exist anymore
import { ipos } from '@/lib/db/schema';
```

**Migration Best Practices:**
- Always backup database before migration
- Test migration on local database first
- Use IF NOT EXISTS for safety
- Include rollback comments
- Document reason for change
- Commit both schema and migration files together

### Repository Pattern with Caching

**Source:** `CLAUDE.md` section "Repository Pattern with Caching"

All repositories extend `BaseRepository` which implements the **cache-aside pattern**:

```typescript
// BaseRepository provides:
// - getFromCache<T>(cacheKey, dbQuery, ttl) - Cache-first retrieval
// - setCache<T>(cacheKey, data, ttl) - Cache population
// - deleteCache(key | keys[]) - Cache invalidation
// - deleteCachePattern(pattern) - Pattern-based invalidation

export class IPORepository extends BaseRepository {
  constructor(
    protected db: NodePgDatabase<typeof schema>,
    protected redis: Redis
  ) {
    super(db, redis);
  }

  async findBySlug(slug: string): Promise<IPO | null> {
    const cacheKey = getIPOBySlugKey(slug);
    return this.getFromCache(cacheKey, async () => {
      // Database query here
    }, CacheTTL.IPO_DETAIL); // 15 minutes
  }
}
```

**Repository Type Requirements:**
- All repository constructors MUST use `NodePgDatabase<typeof schema>` where `schema` is imported from `@ipodhan/shared/db/schema`
- This ensures type compatibility with the shared schema

**Cache Key Updates Required:**
- Current: `ipo:list:category=${category}:status=${status}`
- New: `ipo:list:segment=${segment}:offeringType=${offeringType}:status=${status}`
- Location: `web/lib/cache/cache-keys.ts`

### API Route Patterns

**Source:** `CLAUDE.md` section "API Route Patterns"

**Standard Response Format:**
```typescript
// Success
return NextResponse.json({
  success: true,
  data: results,
  meta: {
    page: 1,
    limit: 20,
    total: 100,
    hasNext: true
  }
}, { status: 200 });

// Error
return NextResponse.json({
  error: 'Error message',
  details: 'Additional context'
}, { status: 400 });
```

**Validation with Zod:**
```typescript
const SegmentSchema = z.enum(['MAINBOARD', 'SME']);
const OfferingTypeSchema = z.enum(['IPO', 'FPO', 'RIGHTS', 'OFS', ...]);

const QueryParamsSchema = z.object({
  segment: z.union([SegmentSchema, z.array(SegmentSchema)]).optional(),
  offeringType: z.union([OfferingTypeSchema, z.array(OfferingTypeSchema)]).optional(),
  // ... other params
});
```

### Offering Type Enum Values

**Source:** Planning document lines 39-67

15 offering types categorized by purpose:

**Public Equity Offerings:**
- IPO - Initial Public Offering
- FPO - Follow-on Public Offering
- RIGHTS - Rights Issue
- OFS - Offer for Sale

**Private/Institutional Placements:**
- IPP - Institutional Placement Program
- QIP - Qualified Institutional Placement
- PREFERENTIAL - Preferential Allotment

**Debt Instruments:**
- NCD - Non-Convertible Debentures
- BONDS - Corporate Bonds

**Investment Trusts:**
- INVITS - Infrastructure Investment Trusts
- REITS - Real Estate Investment Trusts

**Corporate Actions:**
- BUYBACK - Share Buyback
- DELISTING - Delisting from Exchange
- TENDER - Tender Offer

### Data Migration Logic

**Source:** Planning document lines 116-134

```sql
UPDATE ipos SET
  segment = CASE
    WHEN category = 'MAINBOARD' THEN 'MAINBOARD'::segment
    WHEN category = 'SME' THEN 'SME'::segment
    WHEN category IN ('RIGHTS', 'NCD', 'FPO') THEN 'MAINBOARD'::segment
    ELSE 'MAINBOARD'::segment
  END,
  offering_type = CASE
    WHEN category = 'MAINBOARD' THEN 'IPO'::offering_type
    WHEN category = 'SME' THEN 'IPO'::offering_type
    WHEN category = 'RIGHTS' THEN 'RIGHTS'::offering_type
    WHEN category = 'NCD' THEN 'NCD'::offering_type
    WHEN category = 'FPO' THEN 'FPO'::offering_type
    ELSE 'IPO'::offering_type
  END;

-- Detect TENDER offers from symbol suffix
UPDATE ipos SET offering_type = 'TENDER'::offering_type
WHERE symbol LIKE '%TDR' OR symbol LIKE '%TENDER';
```

### Scraper Detection Logic

**Source:** Planning document lines 267-296

```typescript
export function detectOfferingTypeFromSymbol(symbol: string): string {
  if (!symbol) return 'IPO';

  const symbolUpper = symbol.toUpperCase();

  // Detect from suffix
  if (symbolUpper.endsWith('TDR') || symbolUpper.includes('TENDER')) {
    return 'TENDER';
  }

  if (symbolUpper.includes('BUYBACK')) {
    return 'BUYBACK';
  }

  if (symbolUpper.includes('DELISTING')) {
    return 'DELISTING';
  }

  // Default to IPO
  return 'IPO';
}

export function detectSegmentFromExchange(
  listingExchanges: string[]
): string {
  // Logic to determine MAINBOARD vs SME
  // For now, default to MAINBOARD unless explicitly SME
  return 'MAINBOARD';
}
```

### UI Default Filtering

**Source:** Planning document lines 343-351

**Critical UX Requirement:**
Default dashboard must filter to show only IPO and FPO offerings, hiding TENDER, BUYBACK, and DELISTING by default.

```typescript
const response = await apiClient.getIPOs({
  status,
  segment,
  offeringType: offeringType || ['IPO', 'FPO'], // Default filter
  // ...
});
```

**User can:**
- Select "Show All Types" to see TENDER/BUYBACK
- Explicitly filter for specific offering types
- Clear filter to see everything

### Breaking Change Impact Summary

**Source:** Planning document lines 394-416

⚠️ **This is a breaking change** that affects:

1. **Database Schema**
   - Column rename: `category` → `segment` + `offeringType`
   - Enum changes: Old enum deleted, two new enums added

2. **API Contracts**
   - Query parameter change: `category` → `segment` + `offeringType`
   - Response field changes in IPO objects

3. **UI Components**
   - Filter dropdowns completely redesigned
   - Badge displays updated
   - URL parameters changed

4. **Scraper Output**
   - Must set both `segment` and `offeringType` fields
   - Detection logic required

5. **Test Fixtures**
   - All mock data needs updating across 100+ test files

### Expected Outcomes

**Before (Current State):**
Searching for "3i":
- Shows 2 cards: "3i Infotech Limited" (TENDER) and "3I INFOTECH LTD" (IPO)
- Both categorized as "MAINBOARD"
- No way to filter out TENDER offers

**After (Fixed State):**
Searching for "3i":
- Shows 1 card: "3I INFOTECH LTD" (segment=MAINBOARD, offeringType=IPO)
- "3i Infotech Limited" (segment=MAINBOARD, offeringType=TENDER) is hidden by default
- Users can explicitly filter for TENDER offers if needed

### Deployment Strategy

**Source:** Planning document lines 426-432

**Code First Approach:**
1. **Code First**: Deploy code changes that support new schema
2. **Migration**: Run database migration (apply during deployment)
3. **Verification**: Smoke test on staging before production

**NOT using backward compatibility** - Clean break preferred for:
- Cleaner data model
- Prevents future confusion
- One-time breaking change better than incremental patches
- Aligns with industry standards

### Testing

**Source:** `CLAUDE.md` section "Testing Architecture"

**Test Pyramid:**
- **Unit Tests (70%)**: Fast, isolated tests in `tests/unit/`
  - Components, utilities, services (mocked dependencies)
  - Run in < 10 seconds
- **Integration Tests (20%)**: Real database/Redis in `tests/integration/`
  - API routes, repositories with actual PostgreSQL + Redis
  - Requires test database running
- **E2E Tests (10%)**: Full user flows in `tests/e2e/`
  - Critical journeys across browsers (Chromium, Firefox, Edge)
  - Mobile viewports tested

**Test Commands:**
```bash
# Unit tests
npm run test:unit

# Integration tests (requires PostgreSQL + Redis)
npm run test:integration

# E2E tests
npm run test:e2e

# Coverage report
npm run test:coverage
```

**Test Data Pattern:**
```typescript
// Use mockIPO helper for test fixtures
import { mockIPO } from '@/lib/db/types';

const testIPO = mockIPO({
  id: 'test-id',
  companyName: 'Test Company',
  slug: 'test-company-ipo',
  segment: 'MAINBOARD',
  offeringType: 'IPO',
  status: 'OPEN',
});
```

### File Structure Reference

**Source:** `CLAUDE.md` section "Monorepo Structure"

```
IPODhan/
├── packages/shared/          # SINGLE SOURCE OF TRUTH for DB schema
│   └── src/
│       ├── db/schema.ts      # ⚠️ All database tables, enums, relations
│       └── index.ts
├── web/                      # Next.js application
│   ├── app/                  # App Router pages & API routes
│   ├── lib/                  # Backend logic (repositories, services)
│   │   ├── db/              # Database connection + schema re-exports
│   │   ├── repositories/    # Data access layer with caching
│   │   ├── services/        # Business logic layer
│   │   └── cache/           # Redis client & cache utilities
│   ├── components/          # React components
│   └── drizzle/migrations/  # Database migrations
├── scraper/                 # Data scraping service (separate process)
│   └── src/
│       ├── scrapers/        # NSE, BSE, Moneycontrol, Chittorgarh
│       ├── utils/           # Detection utilities
│       └── scheduler/       # Cron-based scheduler
└── tsconfig.json            # Root with project references
```

### Time Estimates

**Source:** Planning document lines 437-447

**Total Estimated Time**: 19-24 hours (2.5-3 days)

**Breakdown:**
- Schema Changes: 1 hour
- Database Migration: 2 hours (including testing)
- Code Updates: 6-8 hours
- Scraper Updates: 3-4 hours
- UI Updates: 2-3 hours
- Testing: 4-5 hours
- Documentation: 1 hour

### Rollback Plan

**Source:** Planning document lines 433-436

**If issues occur:**
1. Restore database from backup (created in Task 3, Subtask 3.1)
2. Revert code changes (git revert or rollback deployment)
3. Investigate issues in development environment
4. Fix and re-attempt migration

**Backup Location:** `backup_pre_category_restructure.sql` (timestamped)

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-10-19 | 1.2 | Story status set to Ready - Final documentation completed | Workflow v2.0 |
| 2025-10-19 | 1.1 | Story validated by Sarah (Product Owner) - APPROVED (9.2/10) | Sarah (Product Owner) |
| 2025-10-19 | 1.0 | Story created - Initial draft from planning document | Bob (Scrum Master) |

---

## Dev Agent Record

*This section will be populated by the development agent during implementation*

### Agent Model Used

*To be filled by Dev Agent*

### Debug Log References

*To be filled by Dev Agent*

### Completion Notes List

*To be filled by Dev Agent*

### File List

*To be filled by Dev Agent*

---

## QA Results

*To be filled by QA Agent after implementation*
